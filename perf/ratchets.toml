[[ratchet]]
counter = "phx_check.allow_count"
value = 0
direction = "down"

# Thirteen: phx-num's `stop`, the sanctioned panic that carries a contract violation out of the run; the benchmark
# files of phx-num, phx-rand, phx-store, phx-exec, phx-core, phx-ledger, phx-market and phx-pop, whose gungraun
# harness prints, exits and passes setups by value; phx-store's three modules that hold its `unsafe` (the backing's
# system calls, the region's typed views, the storable marker) and phx-exec's one (affinity, thread ids and the
# prefetch hint), each lowering the lint for itself alone.
[[ratchet]]
counter = "phx_check.expect_count"
value = 13
direction = "down"

# Placeholder SHAPEs in the committed data, each naming the system that retires it: the count only falls, except by
# the placeholders a stage brings for later systems. Five: BNK's two loan terms, XB's closed borders (S0.18), and the
# wage and rent points (LAB, HSG, S0.25d) until firms and landlords post their own.
[[ratchet]]
counter = "phx_check.placeholder_count"
value = 5
direction = "down"

# Barriers the day runner crosses on a day no handler runs, read by the build run.
[[ratchet]]
counter = "phx_exec.barriers_per_empty_day"
value = 0
direction = "down"

# The map's bytes on the build run's seed: tiles with their relief and rivers, exposure columns, zone distances and
# deposits; the plan's budget is 80 MB.
[[ratchet]]
counter = "phx_geo.map_bytes"
value = 7498868
direction = "down"

# Stage 7 on the build run, the most on any day: rows of lines due read, run heads read, rows of scanned segments read
# and those among them not due, payments, the fixed point's visits, and the bytes of the day's records and nets.
# They rise with the world as its parties arrive, each rise stated. Restated at S0.28 on the twins build run at a factor
# of 170 (20afd3e4), the higher of the two representations on each: every household and small firm is an agent whose run
# head is read on its lines' due days, so heads, visits and the day's records rise (heads 274 774 to 991 630, visits
# 101 829 to 498 793, bytes 58.6 MB to 197.3 MB); a cell's dues were a row per cell, an agent's a row per agent of
# whole twins, so rows streamed and scanned, rows not due and payments fall. The large firms' banks drawn (F-077) moved
# rows streamed, scanned and payments up by 6, 12 and 6 (092c0edb).
[[ratchet]]
counter = "phx_ledger.rows_streamed"
value = 2806417
direction = "down"

[[ratchet]]
counter = "phx_ledger.run_heads_read"
value = 991630
direction = "down"

[[ratchet]]
counter = "phx_ledger.run_rows_scanned"
value = 2879512
direction = "down"

[[ratchet]]
counter = "phx_ledger.run_rows_not_due"
value = 81796
direction = "down"

[[ratchet]]
counter = "phx_ledger.payments"
value = 2798267
direction = "down"

# The fixed point's visits restated from the twins gate run (aa48779e, two years): payers short of money multiply as
# the world runs, since nothing at Stage 0 decides (F-080), and the last paydays are the heaviest.
[[ratchet]]
counter = "phx_ledger.fixed_point_iterations"
value = 715218
direction = "down"

# The day buffers restated from the twins gate run (aa48779e): counted by their room, with the payments 7a hands 7c,
# the day book's due and fail records and the day's look-ups (ab9e4578); over architecture §13.1's line (F-082).
[[ratchet]]
counter = "phx_ledger.day_buffer_peak_bytes"
value = 888941584
direction = "down"

# The members drawn on cleared lines on the heaviest day, and the agents' day at its most: rows gathered, bookings
# read and drawn again, hits, and agents drawn afresh at 10b; first set at S0.28 from the twins gate run (aa48779e),
# the higher of the two representations on each (the small world 8bb7cd39).
[[ratchet]]
counter = "phx_ledger.losers_drawn"
value = 105362770
direction = "down"

[[ratchet]]
counter = "phx_pop.agents_gathered"
value = 6145
direction = "down"

[[ratchet]]
counter = "phx_pop.bookings_read"
value = 14032
direction = "down"

[[ratchet]]
counter = "phx_pop.redraws"
value = 13988
direction = "down"

[[ratchet]]
counter = "phx_pop.hits"
value = 203
direction = "down"

[[ratchet]]
counter = "phx_pop.agents_booked"
value = 197
direction = "down"

# Kernel micro-benchmarks: callgrind instruction counts, first measured when phx-num was built.
[[ratchet]]
counter = "phx_num.ir_value_of"
value = 320
direction = "down"

[[ratchet]]
counter = "phx_num.ir_accrue"
value = 239
direction = "down"

[[ratchet]]
counter = "phx_num.ir_div_round"
value = 161
direction = "down"

[[ratchet]]
counter = "phx_num.ir_split_total"
value = 85
direction = "down"

# phx-rand's samplers, each from a fresh draw cursor; rejection samplers count the rejections their fixed address meets.
[[ratchet]]
counter = "phx_rand.ir_philox"
value = 164
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_philox_x4"
value = 682
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_open_unit"
value = 208
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_below_u64"
value = 255
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_binomial_inversion"
value = 496
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_binomial_btpe"
value = 577
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_binomial_at_least_one"
value = 486
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_joint_at_least_one"
value = 2613
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_multinomial"
value = 2504
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_alias_draw"
value = 524
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_hypergeometric_hin"
value = 601
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_hypergeometric_hrua"
value = 1664
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_multivariate_hypergeometric"
value = 6853
direction = "down"

# The pick with the tree's room kept by its caller, as a caller keeps it (F-009).
[[ratchet]]
counter = "phx_rand.ir_pick_without_replacement"
value = 5798
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_geometric"
value = 435
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_accept"
value = 223
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_normal"
value = 244
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_gamma"
value = 527
direction = "down"

[[ratchet]]
counter = "phx_rand.ir_beta"
value = 1187
direction = "down"

# phx-store's kernels, each from a fixed setup: a push onto a committed page, an in-place append, compaction of 64
# lists with half the arena dead, an insert into a 10 000-entry block list, and encoding and decoding 4 096 sorted
# words (32 KiB) through delta, bit-packing and zstd, of which zstd's context setup, paid once per column, is about
# a third at this size.
[[ratchet]]
counter = "phx_store.ir_column_push"
value = 59
direction = "down"

[[ratchet]]
counter = "phx_store.ir_arena_append"
value = 124
direction = "down"

[[ratchet]]
counter = "phx_store.ir_arena_compact"
value = 21156
direction = "down"

[[ratchet]]
counter = "phx_store.ir_block_insert"
value = 634
direction = "down"

[[ratchet]]
counter = "phx_store.ir_encode"
value = 1002629
direction = "down"

# Raised by 32 at S0.26f (b00aae4b): the store's writer grew its framed output beside the decoder, the decoding
# itself unchanged.
[[ratchet]]
counter = "phx_store.ir_decode"
value = 732779
direction = "down"

# phx-exec's kernels on the calling thread (a pool's idle workers would add instructions that depend on timing): a
# radix sort of 4 096 random (u64, u32) pairs, a keyed reduction of 8 chunks of 512 pairs over 1 000 keys, and a
# gather of 16 chunks of 2 handlers' 128 intents. The keyed reduction's count is CI's: its allocations cost the
# runner's allocator a few hundred instructions more than the build machine's, and CI is where the ratchet holds.
[[ratchet]]
counter = "phx_exec.ir_radix_sort"
value = 1502472
direction = "down"

[[ratchet]]
counter = "phx_exec.ir_keyed_reduce"
value = 1152657
direction = "down"

[[ratchet]]
counter = "phx_exec.ir_gather"
value = 16567
direction = "down"

# phx-core: 64 days of business-day lookups, one monthly schedule's next due day, and one day's gather of 512 due rows
# of 4 096, each booked again at its next reason. The gather's count is CI's, whose allocator costs its buffers a few
# dozen instructions more than the build machine's.
[[ratchet]]
counter = "phx_core.ir_is_business"
value = 3721
direction = "down"

[[ratchet]]
counter = "phx_core.ir_next_due"
value = 3764
direction = "down"

[[ratchet]]
counter = "phx_core.ir_agenda_gather"
value = 149145
direction = "down"

# phx-ledger: a ten-year annual bond's dues on its fifth coupon date (a fixed coupon and its principal, one due), and
# on the day after, when only the search for a schedule date is paid; the search is most of the count (F-014). The
# settlement never pays that search, since 1b hands its dues the date's index; `due_on` is now that entry with no
# index, two instructions more; four more again at S0.26f (8ae4fcd0), the ledger's code laid out anew around it, the
# function itself unchanged.
[[ratchet]]
counter = "phx_ledger.ir_due_on_coupon"
value = 2606
direction = "down"

[[ratchet]]
counter = "phx_ledger.ir_due_on_between"
value = 2295
direction = "down"

# phx-ledger's stage 7: sixty-four run heads read for whether they are due; the pooled-flow rule over sixteen rows
# with two kinks; and 1b with the whole of stage 7 over sixteen loans due on one day, far over its budget (F-020).
[[ratchet]]
counter = "phx_ledger.ir_run_heads_64"
value = 878
direction = "down"

[[ratchet]]
counter = "phx_ledger.ir_pooled_16_rows"
value = 3400
direction = "down"

# A whole stage 7 over sixteen holders: from S0.19 it records each due that falls, with what became of it, for the
# accounts. CI's runner holds the ratchet at its own measure, which is above the build machine's. Raised at S0.28 to
# CI's 412 020 (e0030d0c): the day's look-ups in the fixed-seed sharded maps and the kept payments (F-020, which made
# the payday four times faster at scale while a sixteen-holder day pays the maps' setup), contracts leaving like with
# like, and the day buffers counted by their room; for the owner's review. Raised at S0.26f to 772 400 on the build
# machine: stage 7's sums folded on the pool a key shard to a worker (the day's records in 64 shards, the nets and
# the parties' income in the kernel map's 256), so a day of sixteen payments pays the shards' setup, about 0.1 ms,
# which cut the full load's settlement from 5.5 s to 2.4 s a business day (F-087); waves past the last payment are
# skipped. For the owner's review.
[[ratchet]]
counter = "phx_ledger.ir_settle_day_16"
value = 772400
direction = "down"

# phx-market's linked call on a money market of the declared size (a thousand lenders with budgets, five thousand
# borrowers, twenty thousand reaches): met cold, and met the next day from the first day's basis when every bid has
# moved; most of the warm count is the prices' ranges, not the pivots (F-023). A new bench's ratchet takes CI's
# measure, since the runner, not the build machine, holds it.
[[ratchet]]
counter = "phx_market.ir_linked_call_cold"
value = 1915726951
direction = "down"

[[ratchet]]
counter = "phx_market.ir_linked_call_warm"
value = 1123989366
direction = "down"