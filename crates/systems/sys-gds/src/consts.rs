/// Ten, the base a decimal place counts powers of.
pub const TEN: f64 = 10.0;
/// Days in a year, over which yearly rates and horizons are read.
pub const DAYS_A_YEAR: f64 = 365.0;
/// A decimal place's base as a count, by which a unit's price exponent makes its least traded quantity.
pub const DECADE: i64 = 10;
/// The scale of a rate a fact holds in millionths.
pub const RATE_SCALE: f64 = 1_000_000.0;
/// The scale of a deposit's grade, held in thousandths.
pub const GRADE_SCALE: f64 = 1000.0;
