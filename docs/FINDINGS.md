# Review findings, as found (2026-09-15) — the working file behind `docs/IMPLEMENTATION.md` Part 4; every id here is positioned there

## From the docs (before reading code)
- WORKLIST header says 8 `done` rows never produced an outcome (13f,13h,13g,13d,9,13a/13b,13e); IMPLEMENTATION Part 0 says 95 MET rows are NEVER REACHED.
- PLAN.md progress says 92.9% steps done but IMPLEMENTATION says 47% of clauses missing/partial/dead. Two completion figures that disagree.
- IMPLEMENTATION.md item numbering does not match WORKLIST numbering (two numbering systems, mapping table in Part 3). Law 4 defect in the docs themselves.
- IMPLEMENTATION Part 1 table references items 10b, 10c, 10d, 10e, 10f, 7b, 9 as "closed and sections gone" — verify in RECORD.
- Housing 15.0: `askForMortgages` guard `if (cell.representation === 'cell') continue;` — no household ever gets a mortgage. Verify in code.
- A-24: household orders silently dropped. Verify.
- E-13 ownUncertainty no caller. Verify.
- 21.8 stale placeholder standsInFor Process in estate/index.ts:598. Verify.
- 21.9 dead reads struckRate, findSession, cdsBookOrders. Verify.
- D-2: OMO desk market order. Verify.
- E-7: negative rate impossible. Verify.
- A-36: treasury fails: []. Verify.

## Doc-level findings (after reading all docs in full)
D1. The engine test suite has NOT been run since IMPLEMENTATION item 2 stage 2b (79 red of 793 at last measurement). Every entry since says "tests written, not run". ~40 commits of engine change unmeasured: items 3–10, 10b–10f, 13.x, 14.0, 11.1–11.4. Nobody knows if the world assembles/runs today.
D2. Three numbering systems for the same work: WORKLIST (13k..17), IMPLEMENTATION (11..24, with old AUDIT numbers inside Part 3), plan/manifest. A finding index maps between them. Law 4 violated by the docs themselves.
D3. PLAN.md progress "92.9% steps done" vs "47% of clauses missing/partial/dead" — two completion figures, the first meaningless (steps of closed items ≠ mechanisms that run).
D4. IMPLEMENTATION Part 0 table is stale vs its own annotations (SBP 4/28 now; HF/PB/PE nonzero) — "regenerate with the check in item 1, never by hand" but the table is hand-annotated.
D5. COVERAGE rot: many rows MISSING that code plainly does (Goods D1–D5 delivery/logistics while Freight is MET; Equity G1–G3 while indices exist; Expectations C1 consumption-reads-own-outlook while households/consume.ts does exactly that; Money A4/B1/B2; Register A2/C5). MET is derived from @spec tags, so MISSING = "nobody cited it", not "absent". Denominator disagreement 1361 vs 1369/1371/1372 across docs.
D6. NEVER REACHED marks (95–98) cite finding B-12 that no longer exists (E-25). check:existence's "built and dead" list rests on stale marks. Insurers listed dead though it now has phases.
D7. Macro state of the world at last measurement (items 5, 10, 11.5, 12a, 13g, 13c): output falls to nothing by ~period 30 (12-15 level); "no household here is paid a wage at all" (item 5 census at 6 periods); nobody borrows (96 loans / 9,006 firms); every listed firm publishes a LOSS; every issuer misses payments in every window so every grade is the worst; no FX pair ever traded (12b-3); no contract book has two sides (13b-10) [A-66 fixed later, unmeasured]; nothing is ever built (12d-1). The model has never produced a functioning economy and this is known.
D8. Performance: ~25–60 s per period at full scale (13c.1, 13c); 9,000 share lines + 9,000 decisions/period (F-2); household cells refine every period and never merge (12c.1-1: 16→844 cells in 45 periods, quadratic cost); every cell holds every listed line and is revalued each period (11.6). A year = ~1 hour; target device is a phone (item 17). Law 18 campaigns done: ~39% off at rig scale only.
D9. Seed states an equilibrium in many places despite Seed B5/E1: opening prices per market (shapes), membersPerCohort placeholder (15M/cohort), crossHoldingShare, openingHoldingShare (now "policy"), opening yield, bank balance sheets drawn, funds' commitments as opening condition, PUBLIC_AT_THE_OPENING 1/12.
D10. Standing contradictions: households "nobody lends to it" (Households C1.d cited) vs spec Households E (borrowing) and Housing C (mortgage) — households E1–E5 all MISSING; mortgage guard `if representation==='cell' continue` disables it for every household.
D11. Central bank rate never moves (B1 rule "arrives with worklist 12,16" — never built); 13l done only in the sense of 4 static rates. So monetary policy is inert: the corridor exists but nothing sets it from inflation/activity. XI-17 parliament sets the target but there is no rule.
D12. Polity 0/32; firm birth 0; household formation 0; population strictly falls. Appendix B 29 (constant population by construction) is violated in the other direction: monotone decline.
D13. Two "sovereign default" clauses (G1–G5) MISSING; treasury `fails: []`; a state that misses 1,129 payments in 7 periods keeps running (12a). So the funding constraint XI-9 exists but its consequence does not.
D14. Item 13b-10/13b.1-9 etc: derivative books had no second side; A-66 fix (item 6 of IMPLEMENTATION) claims to fix all eight classes, not measured.
D15. The audit: four of nine families were tautologies until item 4 (A-10..A-48); crossMarket kernel stub deleted; families were "green" for 13 done rows. So the "audit green" claims in RECORD before item 4 are not evidence.
D16. Record admits tests assert "a world that no longer exists" (names drawn parties, sizes); PLAN §7 says a test never names a party but ~30 files did.

## Code-level findings (verified by reading and by ONE smoke run)
C0. typecheck green, lint green, check:spec/forbids/deaths green. None of these load a world.
C1. THE WORLD DOES NOT ASSEMBLE. `rigWorld()` throws at assembly. Eight successive blockers were found by patching each and rerunning, from four different closed items:
  1. short-term-debt/index.ts:792 `shortTermDebt.line` declared `kind:'shape'` with `standsInFor` → ParamRegister throws XI-14 (item 10b, 2026-09-14). Every world since 10b has failed here. 66 of 102 test files build a rig world; all fail at this line.
  2. small-business/index.ts: smallFirm declared depositClass 'operational' with no `bankChoices` → requireBankChoices throws (item 11.1–11.4).
  3. funds/index.ts:2331,2345 `funds.manager`/`funds.capital` anchor `before:'funds.strike'` but are declared BEFORE it in the same array; addPhase requires the anchor to exist → throws (items 10e.4 / 13.5).
  4. short-term-debt `paper.backstop` cycle 'anchor' after 'markets' lands after cycle-2 siblings (labour.pay etc.) → Money E3 cycle-order throw. addPhase's "after = append after all siblings" + inherited cycle is a design defect.
  5. small-business seed runs before seed.foundation (requires lacks 'seed.foundation') → `bank.a` does not exist.
  6. small-business cells key on `line` but Parties enforces ONE global cell-key schema (cohort) → XI-15 throw. The kernel cannot hold two cell populations with different keys. Item 11.1–11.4 never constructed a single party.
  7. funds seed adds `manager.<bank>` once per pool decl; houses consolidated to one per bank (data.ts:707) → Seed B2 duplicate party throw.
  8. banks/subordinated.ts:311 `requiredOf` throws Missing when no `bank.reservation` this period; banks.raise runs before reservations are published → first raise kills the run (period 2).
  With all eight patched the rig world runs; dies at period 23: banks/staff.ts:148 `asQty(hours)` with hours=1.2 (Law 8 fractional quantity).
  At 6 banks/60 firms the seed throws `share.etf.us already exists` (13j trackers drawn per bank, id per country). The rig works at exactly 3 banks.
C2. Smoke census at rig scale (3 banks, 18 firms, 12 household cells, 146 markets, ~340 sessions/period): cleared sessions per period = 1–2; noDemand ≈ 319–326; noSupply 2–15; noOverlap 9–22. From P7 mostly ZERO cleared. Nothing trades. Wages: labour venue clears? `instruction.settled` 144→824/period mostly deposits/interest/estates. Events per period 889→3168 by P22; parties 44→302 by P20 (household cells split every period — `weight` 16–24 events/period — and never merge). Per-period time 120ms→350ms at rig scale, growing with journal and cell count.
C3. mergeCells/weightEvent(entry/death/promotion) have NO caller in any mechanism (only split, reKey, die). Cells only split → count monotone up. Confirms A-17.
C4. housing/index.ts:571 `if (cell.representation === 'cell') continue;` — no household ever asks for a mortgage. Confirmed. Households profile `fails: []` (households/index.ts:420, lifecycle.ts:252): a household cannot fail on anything; treasury `fails: []` (profiles.ts:167); estate/probate `fails: []`; money-market insurer `fails: []`.
C5. ownUncertainty exported and re-exported, zero callers (E-13 confirmed). struckRate, findSession, cdsBookOrders exported, zero callers (21.9 confirmed). estate/index.ts:608 still carries `standsInFor: { noun: 'Process', planItem: 'docs/IMPLEMENTATION.md item 21' }`.
C6. Policy rate: 5 sites in money-market/index.ts, all READ; the only writer is the param declaration (data.ts). No rule ever moves it. Confirmed D11. E-7 confirmed by the `why` on the JPY rate: the solver refuses a negative price so a negative rate is inexpressible.
C7. OMO: central bank posts `price:'market'` orders sized by `targetShare × issued` — a share-of-issue target is a written quantity path (Appendix B: buyer of last resort / seeded outcome). D-2 confirmed.
C8. Seeded equilibria: 22 reads in seeds/foundation.ts of openingPrice.*, openingYield, crossHoldingShare, cbOpeningShare, membersPerCohort; 13 params kind 'shape'; 18 standsInFor. Opening prices per sub-unit are stated for every good (openingPrices()). Seed B5 (no imported equilibrium) violated at every opening print.
C9. Discipline greps clean: no Math.min/max outside core, no ?? 0, no Date/random/console, no `.kind === '` in mechanisms. Good.
C10. 21 of 102 test files name drawn parties ('firm.N', 'bank.N', 'fund.N', 'hh.'); rig.ts itself hardcodes `partyId('bank.a')` in deepBuyer.
C11. Assembly ordering is fragile by construction: phases anchor by NAME to phases that must already exist, across modules ordered by `requires`; a module cannot anchor before a later module's phase; anchoring AFTER appends behind all siblings and inherits the anchor's cycle, so a later-cycle sibling makes an earlier-cycle insert illegal. Two of the eight blockers are this.
C12. Profile (rig, 20 periods, 3.75s): step 74%; households.decide+willWork 13.5%; audit.run 11.4% (accounts.balanceSheet/sheetOf, flows.check walk every party every period); settlement 10.4%; gather 10.2% + runOne 8.9% (every participantDecl × every market × every party of kind, per period); settleEstates 5.8%; participantView/buildParticipantView 4.8% (30 closures per party per cycle); asked 4.1%; outlookOf 3.8%; reach.produced 1.4%; GC 9%. Journal is append-only, never pruned, fully indexed four ways (byPeriod, byKind, byKindPeriod, bySubject) — memory grows without bound; `view.failedPayments(0)` in stillOwed walks from period 0 per party per period.
C13. Cell key schema: Parties.add → cellKeyFaults enforces the registry's single cell-key dimension list. Any second cell population (small firms §42, small-business pools, cohorts of firms) is impossible without a kernel change. This is a kernel item, not a module item.
C14. `requiredOf` (subordinated) and corporate-bond/index.ts:298 both read `bank.reservation` of the current period — two readers of one fact each with its own scan; and both depend on phase order that assembly does not guarantee.
C15. `fails: ['cash','solvency']` for securitisation vehicles never fires (securitisation/index.ts:940 own admission). Vehicles immortal.
C16. Rig only opens ONE country; abroadWorld opens all; the FX layer (spot-fx, fx-derivatives, external, cross-border) is therefore never exercised by the default rig, consistent with "no FX pair ever traded".

## Pass 2 — full kernel read (2026-09-15, second pass)
### core/num.ts, tick.ts, measure.ts
K1. measure.ts throws `RangeError` (pricedAt, amountOf, ratioOf, acrossMembers, eachMember, over) — not `PhoenixError` with a citation. Error discipline ("contract violations throw PhoenixError with a citation") is violated in the one file every arithmetic site passes through. Fix: `Impossible('Law 8', …)`.
K2. measure.ts: `Cash = Money<'piece'>` erases the CURRENCY: the compiler cannot tell USD from EUR anywhere a kernel function is used; cross-currency addition is caught only at a leg. Every mechanism summing `Cash` across a party's holdings in two monies compiles. The file admits it. Consequence: A-23-class defects (wealth = sum of every money ever paid in) are only prevented where a module names its money as a literal, which none do (currencies are drawn). This is a design gap: the currency must be a runtime tag on the amount or every sum must go through `valuation.inMoney`.
K3. `heldAsMoney` is `as unknown as Cash` — a hard cast; every read of a money balance becomes a value with no check that the instrument's price is one. Acceptable only because money price is the one hard-coded price; note it.
K4. num.ts `atMost/atLeast` accept any string reason — the rule "the third argument names the thing that is not there" is unenforced (a reviewer greps). Consider an eslint rule requiring the literal to start with a fixed vocabulary; else it is a comment.
K5. `invertDecreasing` walks a bracket with 64+64+128 iterations per call; used for yields — per instrument per period (prices/curve). Perf note.
### ledger/settlement.ts, instruction.ts
K6. Asset leg validation: `pricePerUnit >= 0` cited Law 6 "a price cannot be negative" — this is where a negative RATE-quoted level dies (E-7). The kind's `quotedAs` is known at this site (registry.instrumentKind(inst.kind)); the refusal should dispatch on it.
K7. `Unpaid.amount: number`, `ReserveLeg.amount: number`, `FailReason.short: number` — untyped money in the ledger's own records (Law 8 type discipline stops at the kernel's edge).
K8. `sold` map in `apply` keyed `seller/instrument` — two asset legs from one seller in one instrument in one instruction overwrite the basis; `realised` then pairs the second basis with both proceeds legs. Latent (no drafter does it today); record as a guard to write.
K9. The kernel does NOT enforce "a create may only appear in the same instruction as the destroy legs of what it was made from" (instruction.ts docstring says it does; settlement.ts says the goods module's audit does). Stale doc, and a FORBID that holds only by a module's family (Commodities Spot F1).
K10. `precheck` overdraft path journals `reserve.overdraft` BEFORE the instruction is applied and even if a later leg fails the instruction — a public event about a drawing that did not happen. Check: apply() has no failure path after precheck (register throws would abort), so it is only wrong if a register throw follows; low.
K11. Every debit of a non-money holding revalues the drawn lots via `valuation.carryingPerUnit` per lot — per leg cost proportional to lots; fine but lots are never merged (register) so a household cell that buys weekly accumulates hundreds of lots per line. Perf + memory note (P5).
### register/register.ts, instruments.ts, parties/party.ts
R1. Lots NEVER coalesce: every `credit` pushes a new lot; a cell that buys weekly holds hundreds of lots per line; `holdingsOf` SNAPSHOTS (copies every lot array) on every call and is called per party per period by the accounts/ownership/flows families, by `equityDust` (twice per party per period: audit + failedWhy) and by `worthOf`. O(parties × lines × lots) per period, growing without bound. Perf P5.
R2. `sameState` requires identical lots IN THE SAME ORDER with the same `acquired` period. Two cells that have ever traded separately can NEVER merge. So `cells.merge` as designed (XI-15 "identical key and identical state") cannot reverse the refinement `split` produces: the cell count is monotone up BY DESIGN, not by omission. The mechanism 12.5 as previously planned would fire on nothing. This is a spec-level lap in XI-15 as implemented: "a partial event splits the cell" + "merge needs identical state" = unbounded refinement. Remake: see finding R2-fix in Part 3/22a.
R3. `heldTotal(instrument)` re-sums every holder each call; the ownership family asks it for every instrument every period.
R4. `Parties.ofKind` builds a fresh array on every call (filters alive); `all()` spreads the map; both are called inside gather loops (per market × per participant decl).
R5. `Register.credit(basisPerUnit: number)` — the door is untyped while the Lot is `PerPiece`.
### prices/value.ts, price-store.ts
V1. `PriceStore.write` forbids a negative price (`Law 6`) — second site of E-7 beside settlement's asset leg; both must dispatch on the market kind's `quotedAs`.
V2. `equityDust` walks and values every holding of the party; it is computed by the accounts family AND by `failedWhy` every period for every party that can fail (double).
V3. `rateInForce` for a pair with no print throws `Unpriced`: with the rig opening one country this never runs; with four countries the seed writes an opening rate (Seed C4) — a stated price (22a).
### clearing/market.ts
M1. **A primary offer at `reservation: 0` is silently dropped from the book.** `runMarket` pushes the issuer's offer as a sell at `reservation`; `onTheGrid` then `continue`s on `level <= 0`. `banks/subordinated.ts:runRaise` posts `reservation: 0` ("Zero is not a floor — it is the absence of one") → **no bank capital raise can ever clear**; the session reports `noSupply`. Verify other `reservation: 0` posters (grep below).
M2. `onTheGrid` drops a bid whose level rounds to ≤ 0 tick and a size ≤ 0 with NO journal event — a decision disappearing (same class as A-24).
M3. **Cell-to-cell trades are arithmetically near-impossible.** `pairFills` steps on `commonGrain(weight(buyer), weight(seller))` = lcm of the two weights; household cells after splits carry arbitrary weights (e.g. 15,000,001 and 14,999,999 → lcm ≈ 2.2e14 pieces). Any market where both sides are cells (households selling shares to households; small-firm cells (§42) selling goods to household cells — item 11.5; lettings between cells) will fill nothing. `fxTrade` and `assetTrade` cash use the same grain. XI-15's integer-per-member rule + arbitrary weights = no cell-cell market. Structural; must be designed out (cells trade through a named intermediary — a dealer, a merchant, a fund — OR weights are kept on a common grain (a fixed cell size, see R2-fix)).
M4. `MarketResult.price: Option<number>`, `settledVolume: number` — untyped (E-11 neighbourhood).
M5. `auctionResult.cover` divides by `offer.size`; fine (positive by construction) but `ratioOf` throws RangeError not PhoenixError (K1).
### world/context.ts, assemble.ts, revalue.ts, actions.ts, module.ts, reach.ts
W1. `SeedContext` exposes `register.debit`, `register.moneyDelta`, `instruments.adjustIssued` and `prices.write`: a seed writes prints and moves units with no instruction. This is where every stated opening price (C8) enters. Seed B5 is not guarded by the type; the seed context should expose `endowUnits`/`endowMoney` only and NO `prices.write` (22a).
W2. `revalue` walks `register.allHoldings()` TWICE a period (foreign, then marks), each call snapshotting every lot array; per holding of a cleared kind it asks `markPerUnit` + `carryingPerUnit` per lot. O(holdings × lots) twice. Perf P2/P5.
W3. `runCorporateActions` asks EVERY live instrument's profile `due(...)` every period (O(instruments) ≈ 9,000+ at full scale, each `due` doing calendar arithmetic); `accelerate` re-walks all instruments per default. Perf: keep a due-date index written at issuance (a heap by next due period).
W4. actions.ts:286 `Math.abs(still.issued) <= heldTotal.dust` — a tolerance on an integer count (Law 8 says exact); stale from the continuous era. Replace with `=== 0`.
W5. actions.ts `cellPays` FORBIDS a cell paying a cell ("does not arise in this world"). It will the moment §42 cells hold trade-credit rows on each other or households hold a cell-issued claim. Same root as M3 (grain) — the world cannot express cell↔cell obligations. Design decision required (Part 2 item 0b/22a): cells transact only with named parties, enforced by a profile flag, OR a common cell grain.
W6. `module.ts`/`assemble.ts`: single-answer hooks (`outlooks`, `clearingCapacity`, per-kind `creditDecisions`, `bankChoices`, `termsOffered`, `borrowNeeds`, `tradingLimits`, `leverageLimits`, `riskBearing`, `marks`, `resolves`) are refused twice by the world but NOT required except `bankChoices` (requireBankChoices) — a kind whose profile says `overdraft: 'aCreditDecision'` with no decider is only caught at first overdraft (settlement:996 `this.d.creditDecision(issuer.kind)` throws Missing mid-run). Assembly should check every "must have exactly one" at seal (like bank choices).
W7. `reach.all()` copies every row on every read; `world.reach()` folds derived kinds by walking every price history each audit (confirmed earlier). Perf P7.
W8. `ParticipantView.parties.all()` lets any participant enumerate every party and its `bank`, `weight`, `status` — public by Observer A3; fine. But `PartiesReads.resolve` exposes successor chains (public). OK.
### audit/*, journal/published.ts, registry/{registry,params,kinds,profiles}.ts
A1. **The units family contradicts the cell model as built.** `units.ts:118–144` reports "two cells standing for the same seat" for any two live cells of one kind with the same (region, bank, key). A SPLIT produces exactly that (the child carries the parent's key), and splits happen every period (labour matching, lifecycle). So every split adds a permanent violation and the audit total climbs monotonically (30 → 174 in the smoke run). Either the identity is wrong or the split design is; they cannot both stand. This is the audit-side face of R2/M3: XI-15 as implemented (split on every partial event) is inconsistent with Part XII's "every represented party sits in exactly one cell".
A2. **The prices family fires every period for every held line that has never printed** (`prices.ts:27–40`): a private company's shares (`carry: 'mark'`, held, no print) are a `Clearing F2` violation each period although `Valuation.atCost` deliberately carries them at cost (§29 C5). The family reads the KIND's carry, the valuer reads the LINE's history: two answers to one question (Law 4). Fix: the family asks `valuation.atCost`.
A3. Audit cost: per period the nine families make ~5 full passes over `allHoldings()` (accounts via holdingsOf per party, flows, money B3.c, units, names, currency) each snapshotting every lot array, 2 passes over the period's ledger (flows, money), and money C4.c re-walks the period's ledger ONCE PER MONEY INSTRUMENT (banks × currencies); `remember` copies every holding again. Plus crossMarket re-reads every index. Perf P2: keep dirty sets and one grouped pass.
A4. `equityLedgerFamily` filters each party's whole equity ledger from period 0 every period (`equityEntries(p, 0, now)`): O(parties × entries) growing without bound. P3.
A5. `params.ts:namesAnItem` only matches "worklist N"; a `why` that names "item 17.2" (this plan's numbering) passes the guard. Stale regex; extend to `\bitem\s+[0-9]`.
A6. `registry.ts:cohort(id)` is a linear `find` on every call (called per member ageing/per cell per period).
A7. `assemble`: the seal does not verify that every kind whose `moneyIssuer.overdraft === 'aCreditDecision'` has a `creditDecisions` provider, nor that every contract market has a `clearingCapacity` (both claimed "assembly refuses" in comments: kinds.ts:401, module.ts:226). Both are only caught mid-run (settlement:996; market deps). Stale comments AND missing checks.
### registry/{claims,derivatives,credit,universe,nouns,wages,blueprint,environment}.ts, calendar, prices/{curve,index-read,contract-value}, clearing/schedule, register/{agreements,contracts,processes,corporate,guarantees,control}, journal, rng
G1. `curve.ts:readCurve` rebuilds the whole curve (every live instrument of the issuer, a `yieldOf` bisection each, ~200 iterations) on EVERY `view.curve()` call; world.ts caches nothing per period here (check `curve(` in world.ts). Callers: valuation of insurers' liabilities, bond worthTo, treasury. Perf.
G2. `index-read.ts:readIndex` walks `decl.constituents(t, world)` for every period since the last cached one; constituents rules walk every instrument. The kernel cache makes it incremental; the crossMarket family keeps ITS OWN cache and re-walks — by design, but two full walks per index per period. Acceptable, note.
G3. `contracts.ts:open_()` = `all().filter(...)`: copies all rows every call; called by revaluation and zero-sum every period, and by any `mark` of a party via `openOf`. Fine at small scale.
G4. `processes.ts:running(what)` and `dueBy` filter every process every call — estates call `running('estate')` per period; fine.
G5. `agreements.ts`: `owed: number` (untyped money); `paid(id, amount)` accepts fractional amounts (`finite` only) — an agreement can be paid to a non-integer residue (Law 8: what is owed is a count of pieces). Add `asQty`.
G6. `guarantees.ts`: `limit: number | null`, `paid: number` untyped; `called` allows fractional.
G7. `journal.ts`: every event frozen twice (`Object.freeze` on data copy + event) and indexed FOUR ways — memory per event ≈ 5 references + 2 objects; ~3,000 events/period at rig scale, hundreds of thousands at full scale; never pruned. Memory grows with the run. `visibleTo`/`publicTail` scan backwards from the end each call (bounded by `last`).
G8. `prng.ts`: lazy start — good; but `derive` allocates a new object per call and the world derives one PER PARTY PER CYCLE for the view (`ParticipantView.rng`) — see P4.
G9. `claims.ts:scheduleOf` WeakMap cache keyed by terms object — good.
G10. `universe.ts:OBLIGORS` hard-codes kind ids `'treasury','centralBank','vehicle'` as strings — a kind branch in data form; should read `PartyKindProfile.sovereign` (which exists) and a `vehicle` flag on the profile.
G11. `environment.ts:conditionsIn` scans EVERY `environment.state` event ever recorded (`journal.ofKind`) for the current period's per region — O(periods × regions) per call, called per crop line per period. Use `ofKindIn`.
G12. `wages.ts:wageFacing` reads `lastPublic('labour.goingRate')` then indexes by venue — fine.
G13. `calendar.ts` caches starts/ends — good. `schedule()` walks with addMonths per k — fine (cached by claims).
### seeds/foundation.ts (2,911 lines, read in full)
S1. `priced()` (foundation.ts:2146) hard-codes `USD` for EVERY instrument's grid and subdivision: `onQuoteGrid(i.kind, USD, priceOf(USD, i.unit, …))`. A JPY bond's opening price is put on the dollar's grid. It coincides today only because every currency declares `MONEY_PIECES`; a currency literal in a four-currency seed is a defect in waiting (Law 4/15). Read the instrument's own `ccy`.
S2. The seed WRITES PRINTS: every sovereign line (at one flat `seed.openingYield`), every FX pair (at `seed.openingRate` = 1.0 for USD/EUR, USD/GBP, USD/JPY and the crosses — a yen at one dollar), every good in every region (`seed.openingPrice.*`). Seed B5 / Appendix B "no imported equilibrium": the whole opening price vector is stated. The FX rate of 1 is not "asserting less" — it asserts parity for four moneys and every foreign balance sheet is valued on it until a pair trades, which none ever does (0.3).
S3. Undeclared numbers in the seed, outside the parameter register (so outside the "honest measure" the register claims to be): `SEED_STOCK_BASIS = 0.8`, `OPENING_WAGE = 40`, `SEED_PLANT_AGES = [26, 78, 130]`, `CARRIER_COUNT = 6`, `PLACES_PER_COUNTRY = 1`, `HULLS_PER_UNIT_OF_SIZE = 1`, `SEED_PROFILE` weights (5,5,5,3,3,3 / 0,0,1,2,3,4), `MATURITY_MONTHS = [3,6,9,12]`, `MATURITY_DAY = 15`, cohorts `fromAge` 18/65, epoch `2026-01-05`. Each is a SHAPE or a RESOLUTION with no kind, no owner and no `why` in the register; the no-magic-numbers lint evidently exempts `seeds/`. Declare every one (Law 2, XI-14).
S4. `foundationFundingFor` sizes every household's opening deposit as the RESIDUAL of the banks' leverage rule: `deposits = assets × (1 − leverage) − already deposited`, spread per member. The population's money is what makes the banks' books close. Honest as bookkeeping (a deposit is a bank liability) but it means household wealth is a function of `regulation.leverageRatio` and the banks' drawn buffers — a seeded outcome and a hidden coupling: raise the leverage ratio and every household is poorer.
S5. Every household cell opens holding every sovereign line (`line.perMember` for 4 of 6 lines) and — from the equity seed — every listed share. Cells × lines at the opening is the seed's doing (P11/22a.3).
S6. The module list order in `foundationSpec` is load-bearing by comment three times ("the order matters at one anchor", "a module that requires the seed is declared after the seed", "assembly keeps declaration order") — the phase DAG (0a) is what removes this fragility; until then a reorder silently changes the period.
S7. `mapReads()` reads the map's declared params by building its own Map from `mapParams()` — a second reader of declared numbers beside `ParamRegister` (acknowledged in a comment). The registry needs the map before the register exists: a bootstrap order problem, tolerable but should be one register built in two stages (assemble already does that for pieceShift).
S8. `openingLevels()` walks recipes up from ONE wage: 35 "shapes" become arithmetic on one — good; but the one (`OPENING_WAGE`) is undeclared (S3) and the levels are still written as prints (S2).
S9. Small-business draw (`drawSmallBusiness`) is called with `GOODS.filter(g => g.output === 'capacity')` lines and `firmRows.length × SMALL_PER_NAMED` — the sector's size is a multiple of the NAMED firm count (a shape) rather than of the population.
### mechanisms/households/*, mechanisms/expectations
H1. `willWork` (households/index.ts:340) offers a cell's hours ONLY if it has an outlook on `benefit`, and a `benefit` observation exists only after a money leg with a `receipt` other than `wage` reached the cell (expectations.ts:125). A cell that has never received a classified non-wage receipt NEVER offers labour. Whether every cell gets one depends on the treasury's standing mandate reaching every cell every period (check treasury). If it does not, "no household here is paid a wage" (record, item 5) is this line. Structural: participation must not be gated on the existence of a transfer; a cell with no outside option has a reservation of zero, not no answer.
H2. **The ageing mechanism IS the cell explosion.** `lifecycle.ts:age` re-keys `crossing` members out of EVERY working cell EVERY period (share ≈ 1/(47 y × 52 wk) ≈ 1/2444 of ~7.5M members ≈ 3,000 people/period) into a NEW retired cell (`reKey` = split with a new key); `die` splits a NEW estate cell per cell per period (15M × 0.0000385 ≈ 289 people). Neither ever merges into the existing retired/probate structure because `sameState` requires identical lots (R2). So the cell count grows by ≈ (#working cells + #dying cells) per period, forever — the 44 → 302 parties in twenty periods in the smoke run, and the record's 16 → 844. The units family then flags every new cell as "two cells standing for the same seat" (A1). This is not a bug in `age`; it is XI-15's per-member-state design meeting Part XII's one-cell-per-key identity. See DESIGN-1.
H3. `decide` → `savingLines` walks `view.instruments.all()` for every cell every period and `shareOrders` posts a `steps`-rung ladder in EVERY listed line the cell has a print for: cells × lines × steps orders a period, the largest order stream in the world at full scale (9,000 lines). `fundPositions` filters `journal.ofKind('fund.struck')` (every strike ever) per venue per cell per period. `wealthOf`/`atRisk` walk holdings twice. P3/P11.
H4. `homeBid` bids `toAHome` (half) of spare for a dwelling every period a `housing.shortfall` is published — which is every cell every period (0.3) — and the dwelling market never clears (no builder: 12d-1 "nothing is ever built"). Half of every cell's spare is committed to a bid that never fills, every period; the other half is the whole of what reaches paper and shares.
H5. `consumptionIsBought` family tolerance `combineDust(...) + grain / 2` where `grain = weightOf(cell)`: half a piece PER MEMBER — a quantisation band stated as a number of pieces, not derived dust. Law 7 says derive it or it is a band; the honest form is the count of legs × 1 piece (what `toGrain` can round), never `weight/2`.
H6. Household profile: `fails: []`, `borrows: false`; docstring cites "Households C1.d: nobody lends to a household" — the spec's Households E1–E5 (consumer credit) and Housing C (mortgage) are hard-coded away in the profile. The horizon `why` still cites "worklist 9" for pricing risk — stale.
H7. `expectations`: the book is `Record<party, Record<variable, Held>>` — every party × every variable ever observed; `observations()` walks the whole period ledger and journal `revaluation` events (`ofKind`, all periods, filtered by period — G11-class) every period; `publishDispersion` walks the entire book; `expectations.surprise` is recorded per party per variable per period (the largest event stream in the smoke run). P3: `ofKindIn`, and record surprises in the module store rather than the journal (they are private and never read by a participant).
H8. `expectations` B1: an outlook of a PRICE is corrected only from the party's OWN fills; a party that never trades a line keeps a stale expectation for ever while the print moves — `savingLines` falls back to the print only where there is NO outlook. A cell that bought once at 100 and watches the print fall to 50 still bids at 100 − confidence: the spec's A2 (a party observes public prints too) is half-built: prints are "one more thing observed" per the comment at expectations.ts:134 but nothing ever records them as observations.
H9. `households.plan` is journaled PRIVATE with the whole order list per cell per period — hundreds of orders serialised into the journal each period and read back by `ordersFrom` per market; journal memory + parse cost (A-24 class). Keep the plan in the module store and journal a summary.
DESIGN-1 (spec-level, for the owner): XI-15 as implemented — holdings PER MEMBER in whole pieces, partial events SPLIT, merge only on identical state — cannot express (a) ageing/retirement into an existing cell, (b) any cell↔cell trade (M3: lcm grain), (c) a cell paying a cell (W5), (d) a bounded cell count (H2), and contradicts Part XII "one cell per key" (A1). The alternative that keeps every law: a cell holds TOTALS in whole pieces with an integer weight; per-member is a READ (`total / weight`, a number, never a holding); a leg on a cell moves whole pieces of the TOTAL (`splitOnTick` across members is unnecessary because members are identical by construction); a weight event that moves `n` members moves `floor(total × n / weight)` pieces with the remainder staying (no residual: it stays with a named holder); merge requires identical KEY only. Cells then trade with cells, age into the retired cell of their key, and the count is bounded by the key space. Every equity account stays per member as a read. This is a kernel remake (register cell sides, settlement `validateCellSide`, cells.ts, flows family) — item 0b'.

### mechanisms/firms, labour, treasury (read in full)

- F1 `firms/index.ts wagesThisPeriod`/`productiveHours`: filter the WHOLE `journal.ofKind('labour.wages')` per firm per period. O(firms × wage events). Fix: labour module keeps a per-employer per-period tally in its own store (working) and firms read it via a view hook.
- F2 `firms/produce.ts areaUnderUse`, `firms/invest.ts plantOffers`: walk all instruments per firm per period to find land/plant lines. O(firms × instruments). Fix: register indexes holdings by holder already (`holdingsOf`) — read that; never scan the instrument list.
- F3 `firms/invest.ts costOfCapital`: requires a `credit.quoted` event for THIS firm in this or the previous period, else `none` → no `project` → no investment ever. Banks quote only on a borrow need; a firm forms no borrow need until it has a project. CIRCULAR: nothing is ever built. This is the mechanism-level reason the plant stock only ages. Fix: cost of capital is the firm's OWN outlook on its funding rate (XI-4, §46) formed from its last quote OR its bank's posted deposit/lending print OR the sovereign curve print it can see; a firm with no history uses the opening quote the seed writes for its bank (`bank.reservation` print). The `credit.quoted` recency gate is deleted.
- F4 `firms/decide.ts ordersFrom` throws via `asQty` on a fractional order size, while `households/consume.ts` silently drops the same case. Two conventions for one fact (A-24). Fix: one `wholeOrderOf(spend, price)` in `clearing/` that floors to the tick grain, used by both.
- F5 `firms/index.ts` `dealershipShare` param owner is `'model'`; a SHAPE with no death and no mechanism named. Law 2: either PREFERENCE of the firm kind (registry) or delete.
- F6 `labour/matching.ts match`: `Math.floor(ratioOf(...))` — a bound helper outside `core/num.ts`, and the rounding remainder (hours that neither side gets) has no holder. Fix: matching in whole hour-pieces via `Qty`; remainder stays with the household as unsold hours (a recorded `noDemand` for that slice).
- F7 `labour/matching.ts hire`/`separate`: each hire and each separation is a partial event on a cell → `split`. With `lifecycle.ts age/die` and the household `decide` split this is the THIRD source of cell explosion (see DESIGN-1). Under the remake, hire moves floor(total×n/weight) hours; no split.
- F8 `labour/register.ts`: the labour register is a second per-party employment store beside the ledger's `labour.wages` events and the firm's own `staff` map (banks/staff.ts). Three writers of "who works where". Law 4. Fix: one employment register (NOUN, `labour.employment`) with employer, employee cell, hours, wage; wages are instructions that READ it; firms and banks read it through a view hook.
- F9 `labour/index.ts`: wage print `labour.print` is written per venue/region, but `treasury/index.ts wageItFaces` reads the LAST print of ANY venue. Treasury of region A pays region B's wage. Fix: `wageItFaces(region)` reads the print keyed by its own region.
- F10 `treasury/index.ts lastReceipts`: reads the last `treasury.receipts` event of ANY treasury, not the subject's. With two sovereigns (abroadWorld) treasury B budgets on treasury A's receipts. Fix: filter by subject; better, the treasury keeps its own receipts tally as a working store, journaled once.
- F11 `treasury/index.ts linesOf`/`debtService`: walk all instruments per treasury per period. Same fix as F2 (issuer index in the register: `instrumentsIssuedBy(party)`).
- F12 `treasury/index.ts openLine`: coupon `atLeast(y, 0)` — a floor. Law 6. A negative-yield print is a cleared price; a zero-coupon line with price above par is the real instrument. Fix: coupon is set from the last cleared yield WITHOUT the floor; a negative coupon means the line is issued as a zero with price above par (instrument profile decides, not a clamp).
- F13 `treasury/index.ts` transfers: every household cell receives a `transfer` receipt every period (if the treasury account is funded) else `treasury.shortfall` is journaled and NOTHING is paid — the transfer obligation vanishes (no arrears row). Law 5 / "no liability without beneficiaries": an unpaid transfer is a liability of the treasury to the cell. Fix: unpaid transfers become an arrears row (instrument `treasury.arrears.<cell>`) paid before new spending.
- F14 `treasury/index.ts`: tax is a fixed share of `lastReceipts`-scaled wages; there is no tax on profits, interest, capital gains or consumption. Spec §27 names four bases. MISSING (partial sector).
- F15 `firms/decide.ts`: pricing rule marks up unit cost by a PREFERENCE markup with no memory of `noDemand` — a firm whose ladder never fills keeps posting the same price. §46 says the firm's outlook on its own sell price adapts; the outlook exists (`expectations`) but `decide.ts` reads only `cost`. Fix: ask price = own outlook on price (from fills AND from the venue print the firm can see), not markup on cost; markup is the FLOOR only as arithmetic (a firm will not post below cost — that is a REASON, not a bound: it is the firm's reservation).

### mechanisms/goods, capital-programme (read in full)

- GD1 `goods/inventory.ts perish`: `chargePerMember: charge === undefined ? 0 : ...` is a numeric default in a journaled fact (rule: missing is Missing). One `settle` per (holding, good) per period; with N household cells × G goods that is N×G instructions per period for spoilage alone, most of them refused as immaterial (`material(perMember, ...)`) AFTER the shareFor arithmetic. Fix: skip before `shareFor` when `rate === 0` or the holding is below one piece per member; better, spoilage is one destroy leg per (good, cell) batched per period.
- GD2 Goods E2.c ("written down, never up") HOLDS: `world/revalue.ts:358` remarks the lot basis after a write-down, and `goodProfile.carriedAt` compares the print to the remarked basis. Verified.
- GD3 Storage charge (Goods E4.a, Commodities Spot D3): declared "nobody to pay until Goods D / 13c" — `merchants` exists now (13c). Check merchants for a storage cash flow (see MR below).
- GD4 `unitsIdentity` and `plantMoves` families re-walk `ledger.inPeriod` and every instrument's `heldTotal`/`holdersOf` per period. Two families walking the same ledger separately (goods, capital) plus `banks.bookMoves`, plus kernel families: one period-walk of the ledger should feed every family (audit `View` precomputes `legsByInstrument`, `legsByHolder` once).
- CP1 `capital-programme/index.ts purchases` re-walks `ledger.inPeriod(when)` once per capital kind per period. Same fix: one walk keyed by instrument.
- CP2 `commission` reads ALL asset legs of `cause: 'trade'` for the capital good: a MERCHANT or DEALER that bought machines to resell would have them commissioned into plant (A4.c says the buyer's question is "did it buy it"; a dealer buys to sell). With merchants (13c) holding goods inventory this is live: check `merchants` buys of capital goods. Fix: commissioning is the buyer's DECISION recorded as a `capital.ordered` event by the firm's invest phase; `commission` reads that event, not raw trade legs.
- CP3 `weather`: per (holding, plant) per period `conditionsFor` + exp/pow — fine; but `lost` is rounded by `deliverable` so a small holding never loses anything and a large one always does: resolution-dependent (Law 2 RESOLUTION test would fail: refine the grid and small firms start losing plant).
- CP4 `vintage()` opens one instrument AND one market per (kind, region, service week). Every week that any firm commissions opens a new market that nothing but an estate ever posts into. After 100 periods: hundreds of dead markets each walked by every bank's `linesQuoted`/`liquidityTargets`/`coveredLines` and by `publishDealing`. Fix: a vintage is an instrument; its MARKET is opened lazily on the first estate sale (`estate` asks `openMarket`), or one market per (kind, region) with vintage as a lot attribute.
- CP5 No second-hand plant market bids: nothing ever bids for a dead firm's plant except… (check estate/firms — firms `plantOffers` walks instruments; does any firm BID for used plant? grep later: `plantMarketId` buyers).

### mechanisms/banks (12 files, read in full)

- BK1 `index.ts:373 costOfFunds`: `atLeast(capital, 0)` — a floor on the residual. The comment argues "a hole funds nothing", which is true, but the mechanism that should make the case unreachable is RESOLUTION of an insolvent bank (Banks Capital D, XI-3). While resolution exists (capital.ts D6 is cited) an insolvent bank still quotes and lends here for the periods between breach and resolution. Decision: keep the arithmetic (funding = owed + max(capital,0) is a statement about what funds the book) but journal `bank.insolvent` at that site so the audit can see how many periods a hole quoted.
- BK2 `index.ts:1676 publishQuotes`: for EVERY alive party that `mayBorrow()` × EVERY bank: `ctx.participant(b.id)` (view build), `regulationOf` (→ `operatingCostOf` → walk of the bank's holdings), `room()` (→ `exposureTo` walks the bank's holdings; `roomFor` parses journal), `costOfFunds` (→ `owedBy` walk of issuedBy), `quote()` (→ `probabilityOfDefault` filters ALL `credit.default` events). Per period: O(parties × banks × (holdings + defaults)). At 300 cells × 3 banks this is the single largest mechanism cost after households.decide. Fix: (a) per bank per period compute ONCE: view, regulation, costOfFunds, exposure map (by issuer), free room; (b) index `credit.default` by subject in the journal; (c) quote only parties that PUBLISHED a borrow intent (`firms.funding`, `housing.funding`, `prime.wanted`) or hold a line at that bank — a bank does not price 300 names nobody asked about.
- BK3 `index.ts:1738 publishReservations`: `holderReservation(view, decl, reg, funds)` DOES NOT TAKE THE OBLIGOR. `required[obligor]` is identical for every issuer — the bank's reservation in a corporate bond market equals its reservation for the sovereign (`expectedLoss: 0` for all). Corporate Credit E5.b is MISSING and every bond's clearing level is priced off a credit-blind yield. The comment records that wiring the loan PD in caused a doom loop. Fix (worklist 12 was supposed to do it): `holderReservation(view, decl, reg, funds, obligor)` = funds + capitalCharge(weight of THIS obligor) + expectedLoss(obligor) where expectedLoss reads the bank's OWN PD model (C4: one model) × LGD from the bank's OWN realised recoveries on estates it was a creditor of (`estate.distributed` events it was a subject of) — and the sovereign in its own money has PD from missed payments like anyone (Sovereign G1 says it can create money, so its missed payment is a REFUSAL event, not a default; the treasury module must not journal `credit.default` for a domestic-currency sovereign — check treasury).
- BK4 `index.ts:1583 runRequests` reads `firms.funding` and `housing.funding` BY NAME — banks are coupled to two modules' event names. A third borrower module (small-business, merchants, insurers) has to be added here by hand; small-business today gets NO bank credit because it is not in this list (its borrow need goes through `bankChoices`, which it does not declare — blocker #2). Fix: one event kind `credit.request` written by any module through `ctx.request({short, security})` on the view; banks read that one kind.
- BK5 `index.ts:920 bookDraws`: throws `Missing` when the bank cannot cost its funding — which is ALWAYS true in period 0 (`costOfFunds` returns none at period 0). Any overdraft allowed by `overdraft()` in period 0 stops the world. `overdraft()` does not check `costOfFunds` before allowing. Fix: `overdraft()` refuses when `costOfFunds(...).perAnnum` is none (the refusal is the honest answer: a bank that cannot price a drawing does not allow it), and the throw in `bookDraws` becomes unreachable and is deleted.
- BK6 `capital.ts:974` capital = equity + `subordinatedOf(bank)`, while `index.ts:owedBy` sums EVERY `liabilityOfIssuer` instrument issued — including the subordinated line. So `funding = owed + capital` counts subordinated debt TWICE (once as owed, once as capital). Law 4. Fix: `owedBy` excludes instruments whose `ranking().seniority` puts them in the capital layer (seniority 2), or `costOfFunds` uses `capital = equity` only for the blend and the sub coupon is already in `couponsPaid`.
- BK7 `quote.ts:157 lossGivenDefault` unsecured = 1 (recovery ZERO). Appendix B forbids a fixed recovery; the comment calls it "the honest number until the estate distributes". Estates DO distribute now (`estate/`). Fix: the bank's LGD on an unsecured name = 1 − (what it recovered ÷ what it was owed) over the estates it has been a creditor of, within `credit.memory`; with no history it is 1 (nothing observed) — that is a REASON with a history, not a constant.
- BK8 `quote.ts:116 probabilityOfDefault`: `failures / min(period, memory)`. A borrower that exists 2 periods and failed once has PD 0.5; the same borrower after 52 periods with the same single failure has PD 1/52. It is a frequency per PERIOD, then used as expected loss per ANNUM in `quote()` (rate = funds + expectedLoss + …, all per annum). A per-period frequency added to a per-annum rate is two periodicities in one sum (Law 8). Fix: annualise: `pd × periodsPerYear` from the calendar, or express the loss as per-period and scale the rate.
- BK9 `lines.ts:446 publishLines`: `atLeast(wants, 0)` and `atLeast(give, NO_QTY)` — floors. A line whose capital use is ABOVE its appetite has `wants < 0`: the real answer is that the line must SHRINK (stop rolling, sell), and nothing makes it. Under the floor the line just asks for nothing and keeps what it has. Fix: a negative `wants` is published as `mustShed` on the line row; the dealing line's `limitAggregate` reads it (it already reads `room`) and the lending line stops writing new rows until it is inside appetite. Then the floor is dead and is deleted.
- BK10 `lines.ts:541 earnedByLine` walks `ledger.inPeriod(prev)` per bank per period, calling `lineOf` → `creditorOf(holdersOf)` per leg. O(banks × ledger × legs). Fix: one walk per period producing `equityDeltaByParty[bank][line]` keyed by the instrument's owner-line; or the ledger record carries `equity` already — index records by party at write time (`ledger.byParty(period, party)`).
- BK11 `loan.ts`: one-year bullet, no amortisation, no prepayment, `accelerates: false`. Banks Lending F2 names "amortisation, prepayment and write-off"; F2 is marked MET by the family that checks stock = flows, but the flows named do not exist. PARTIAL. Corporate Credit A2/A3 (term structure, covenants) MISSING (comment says 13f).
- BK12 `loan.ts:688 maturity = civil(y+1, m, d)`: 29 Feb → invalid civil date; `civil()` may throw. Fix: `addDays(drawn, 365)` or the calendar's `plusYears`.
- BK13 `treasury.ts:90 liquidityPlan paper = atLeast(above, 0)`: floor, justified as "a short is not a smaller target" — correct: the plan is cash-covered; fine as arithmetic (a target of nothing). Keep.
- BK14 `treasury.ts:414 worthOfMoney`: `atLeast(paid, floor)` — "it would park at the floor rather than lend below it" — this is a REASON (the floor is a real alternative) and is stated as one. Keep. But `worthOfMoney` walks ALL `moneyMarket.print` events per bank per period (O(banks × prints)). Fix: journal index by (kind, subject).
- BK15 `treasury.ts:715 bestRival` walks ALL `bank.depositRate` events ever (not just last period) per bank per class per period: O(banks × classes × all deposit-rate events ever). The keenest board EVER shown, not the one showing now. A rival that paid 8% in period 3 is matched for ever. Bug + cost. Fix: `ofKindIn('bank.depositRate', period − 1)`.
- BK16 `treasury.ts:652 classesSeen` reads the last `deposit.classes` event: O(events of kind) per call via `ofKind`; called once per period. Fine.
- BK17 `treasury.ts:507 dueNext` walks ALL instruments per bank per (fallsDueNext, comesBackNext) per venue per session: `sessionOrders` is called per venue (money market venues = banks × secured/unsecured), so O(banks² × instruments × cashFlows) per period. Fix: `fallsDueNext` reads `instruments.issuedBy(self)` (index exists); `comesBackNext` reads `view.holdings()`.
- BK18 `dealing.ts:1034 coveredLines`: walks ALL instruments per call; called by `covers()` per market per bank (`dealingOrders`) → O(banks × markets × instruments) per period. With CP4's dead plant markets this is the walk that grows without bound. Fix: compute `coveredLines` once per bank per period (cache in a working store keyed by period), and `covers` is a Set lookup.
- BK19 `dealing.ts:960 stateOf` is rebuilt PER MARKET per bank (`dealingOrders` → `stateOf`), each build calling `liquidityTargets` (walks markets twice), `bookValue` (walks targets), `linesQuoted` (walks markets), `capitalOf`, `liquidOf`, `carryRate` (journal reads). O(banks × markets²) per period. Fix: `stateOf` once per bank per period in a working store; the participant hook receives it.
- BK20 `dealing.ts:1221 urgentSale` walks every market per market per bank. Same fix as BK19 (per-bank once).
- BK21 `dealing-quote.ts:153 viewOf` — for a dated claim the desk's view is `priceAtYield(required)` where required is the bank's reservation: identical for every issuer (BK3). So every desk's VIEW of every bond is the same curve-blind, credit-blind number ± its own cost of funds. Outlooks (XI-16) disagree only via cost of funds. This is the structural reason "1–2 of ~340 sessions clear then none" (Part 0 census): all bids are the same price, offers are at edge above, and once inventory targets are met `skew` pushes every desk the same way.
- BK22 `dealing-quote.ts:200 riskOf` returns 0 confidence when no outlook: a desk with no history charges no risk — fine (edge still has carry). `adverseOf` reads `bought`/`sold` outlooks — outlooks on FLOW quantities, journaled per instrument per bank per period by expectations (H7 cost).
- BK23 `dealing-quote.ts:309`: `least(least(room, inBook), inMoney)` and `material(size, 2, limitPerInstrument)`; `offerSize = inventory` (whole free inventory offered every period regardless of target — a desk holding the treasury's liquidity portfolio OFFERS ALL OF IT every session at `offer`; only `skew` protects it. If a buyer lifts the offer the bank sells its liquidity buffer.) Fix: `offerSize = inventory − target` when positive (what it is carrying ABOVE target), plus the target part only at a higher offer (the treasury's reservation).
- BK24 `dealing.ts:1318 primaryBid`: `atLeast(obliged, gap)` = max; semantic. `dealershipShare` read from `auction.announced` — the treasury's `dealershipShare` param owner 'model' (F5).
- BK25 `staff.ts:541 linesCovered` / `processesRun`: `Math.floor(ratioOf(...))` — floor outside core/num (also F6). `payrollSince(view.period)`: a bank that paid wages last period covers lines this period; a bank that has never paid a wage covers ZERO lines → no desk quotes anything until the labour market has cleared a `banking` venue once. In the census the labour venue for `banking`/`dealing` occupation: does the seed give banks staff? (`staff.ts` says a bank that never met a wage runs nothing — combined with the seed not endowing bank employment, NO bank quotes any line in period 0 and 1.) Check seed: `foundation.ts` employment for banks. → S-check.
- BK26 `staff.ts:508 staffOrders`: bids `earned(1) / hours` for hours — a bank whose last period's earnings were ≤ 0 posts NO opening and therefore its staff... are they separated? Labour `separate` on no re-hire? If employment persists without a wage bid, wages are unpaid (one-sided). Check labour: is employment a standing relation or re-matched every period? (labour/matching.ts read earlier: hire/separate per period — employment is re-matched each period; so a bank with one bad period loses its whole desk and all coverage the next period, then quotes nothing, earns nothing, hires nobody: an absorbing state.) This is a MISSING mechanism: a standing employment contract with notice (Labour C3 severance is cited). Same for firms (`firms/index.ts` wage bids from last period's revenue).
- BK27 `prime.ts:801 requirementOn`: `atMost(wide, p.worth)` — arithmetic cap ("a line cannot fall by more than the whole of it") fine. `requirementOn` uses `broker.quantity(p.instrument)` — the BROKER's own holding of the line, not the CLIENT's (`held <= 0 → return p.worth`): a broker that holds none of a line requires the WHOLE of the client's position; one that holds some computes `confidence × broker's own units` — wrong quantity (Law 19 "wrong quantity" warning). Should be the client's units (`p` has worth only; add `units`).
- BK28 `prime.ts:933 runPrime`: `clients = FUND` kind only; hedge funds only if they are FUND kind. `wantedBy` reads `prime.wanted` lastOf (any period — a stale want is honoured for ever).
- BK29 `subordinated.ts:1277 runRaise` `reservation: 0` → `clearing/market.ts onTheGrid` drops `level <= 0` orders silently (M1) → the raise NEVER enters the book → Banks Capital C2 is unreachable: every raise fails, silently, and the record says `bank.raise.offered`. Blocker-class (already in the nine: `requiredOf` throws when no reservation this period — and even when it does not throw the offer is dropped). Fix: `ctx.offer` reservation is `Option<PerPiece>`; `none` means "no level"; the solver's issuer offer with no level is a market order on the sell side.
- BK30 `subordinated.ts:1324 requiredOf` reads `bank.reservation` events of THIS period: `publishReservations` runs in `lending.write` (cycle 0, after corporateActions) but `banks.raise` also runs at cycle 0 after corporateActions and is declared EARLIER in the phases list → `requiredOf` throws Missing at the first raise. Blocker #6 confirmed by ordering. Fix: the coupon of a new sub line is struck from the LAST period's reservations (a coupon is set from what was said, N5.a), `lastOf`, not `ofKindIn(period)`.
- BK31 `index.ts:1060 tradingBookIsCapitalised`: filters `journal.ofKind('bank.dealing')` per bank per period — O(banks × all dealing events ever). Fix: `ofKindIn(kind, period)`.
- BK32 `index.ts:1466 worthToItsLender` mark per loan: `heldTotal` walk + `probabilityOfDefault` (all defaults) per loan per revaluation. With L loans and D defaults: O(L × D). Same journal-index fix.
- BK33 Missing (Banks Funding): deposit CLASSES exist but there is no depositor choice of bank in banks — check `households`/`funds` for `bank.depositRate` readers; a board nobody moves for is a display-only number (Appendix B). Grep later.
- BK34 Missing (Banks Capital D): resolution — `capital.ts` cites D6 (acquirer) but no phase in this module resolves a bank below requirement; search `resolution` in credit-events/estate later.

### mechanisms/money-market (8 files, read in full)

- MM1 `index.ts:277 runSession`: for EVERY bank × EVERY book (4): `ctx.gather(venue)` — the kernel asks every venue participant (all banks: `sessionOrders`, plus funds/insurers) for orders; each `sessionOrders` call rebuilds `positionOf`, `fallsDueNext` (walks all instruments), `comesBackNext`, `pledgeable` (walks holdings), `lenderReservation` (journal parse). Then `windowOffer` → `windowAdvances` walks the borrower's holdings. Cost per period O(banks² × books × (instruments + holdings)). This is the money market's share of `gather/runOne` 19% in the profile. Fix: (a) per-bank per-period `TreasuryState` computed once (gap, fallsDueNext, pledgeable, reservation map) in a working store; `sessionOrders` reads it; (b) `windowAdvances` per borrower once per period, not per book.
- MM2 `index.ts:526 capacityOf` → `lentThisPeriod` walks ALL instruments per struck fill. O(fills × instruments). Fix: the session keeps a per-period `placedBy: Map<lender, Qty>` in its working store, incremented in `writeRow`.
- MM3 `index.ts:529 capacityOf` central bank: `Number.MAX_SAFE_INTEGER` as a Qty — a numeric literal standing for "unlimited" (Appendix B: no unlimited exposure). It is bounded by collateral downstream, but the literal is a lie in the type. Fix: `capacityOf` returns `Option<Qty>`; none for the issuer of the money.
- MM4 `session.ts:1079 fallsDueToIt` walks all instruments per bank per period (called from `publishFunding`). Fix: `instruments.heldRowsBy(lender)` or read `view.holdings()` filtered by `isRow`.
- MM5 `session.ts:1096 netReserveFlow` AND `treasury.ts:351 reserveFlow` (banks module) are the SAME walk over `ledger.inPeriod(period).reserveLegs` filtered by bank — two writers of "what this bank's reserve account did" (Law 4), both O(banks × ledger). Fix: the ledger keeps `reserveFlowByBank[period]` at settlement time (one writer), both read it.
- MM6 `resolution.ts:159,369,643 valueBook/allocate/moveBook` walk ALL instruments three times per resolution — acceptable (rare event) but `issuedBy(bank)` index exists and should be used.
- MM7 `resolution.ts:285`: `winner = taking[0] ?? bids.sort(...)[0]` — when EVERY bank declined, the highest DECLINED bidder is FORCED to take the book. D3.b says an acquirer can decline; the fallback overrides every decline with a forced buyer (Appendix B: no forced buyer). The `noBank` branch is reachable only with zero other banks. Fix: winner = `taking[0]`; if none, `bank.resolution.noBank` and the bank goes to a BRIDGE (the deposit insurer takes the book as a temporary bank: it is a party with an account; give the insurer kind `moneyIssuer` in its currency for this case) — or the purse recapitalises and the bank continues in resolution. Spec Banks Capital D3.b/D5 decides; record the choice.
- MM8 `resolution.ts:571 writeDownRow` returns `scaleQty(moved, Math.round(ratioOf(owed, total)))` — `Math.round` outside core/num and a rounded RATIO applied to a quantity: for a row worth 0.6 per unit the loss is reported as 1 × moved, for 0.4 as 0. The reported `lost` is wrong by up to 100%. Fix: lost = `share` (the Cash it was asked to write down) capped by what was actually wiped × valuedAt.
- MM9 `resolution.ts:346 allocate` `equity` reported is the bank's equity at that moment, not "what equity absorbed" — E3's conservation family (`holdersLost + insurer + purse + equity = hole`) can only hold if `equity` = −(pre-hole equity). Check the family (`bank.resolution.done` reader) — it is `nothingLeftBehind` only; there is NO E3 conservation family. Banks Capital E3 is marked MET by citation only. MISSING.
- MM10 `resolution.ts:1000 reserveOverdraft`: `solvent = equity() > 0`. Solvency read every overdraft request — fine. But `failedBanks` uses `world/failure.ts failedWhy` (cash/solvency triggers): a bank with equity ≤ 0 is refused the window (correct) but resolution runs `before revaluation` in the anchor cycle; the refused payment FAILS at the site in the markets cycle, and the bank is resolved the same period. OK.
- MM11 `deposits.ts:200 payDepositInterest`: one `settle` per (bank, holder) per period: with 300 household cells at 3 banks that is 300 instructions/period at least. Each `shareFor` + `cellSide`. This is the second-largest instruction source after wages. Fix: none possible under "a payment is an instruction" unless cells are batched — under the DESIGN-1 remake a cell is one holder with one total, so one instruction per (bank, cell) is already the minimum; the COUNT of cells is what must fall (H2/F7).
- MM12 `deposits.ts:273 announced`: `journal.forSubject('bank.depositRate', bank)` then takes the last — the index exists (`forSubject`); fine. But `treasury.ts bestRival` (BK15) does NOT use it.
- MM13 `index.ts:934 printed`: `ofKind('moneyMarket.print').filter(period && subject)` — full scan per call. Fix: `ofKindIn` + subject index.
- MM14 `collateral.ts:582 requiredOf` → `published(view, 'required', issuer)`: reads the bank's reservation per issuer — which is credit-blind (BK3), so every lender advances the same haircut against every issuer's paper: B3.b "haircut differs by lender's credit view" is only true through cost of funds. Same root cause as BK3.
- MM15 `index.ts:214 publishClasses` `premium: c.insured ? ... : 0` — a 0 literal as a rate for uninsured classes: fine (it IS zero premium), but it is a numeric default by shape; make it the param read for both with the uninsured premium declared as a 0 policy param? No — 0 is exact. Keep.
- MM16 `data.ts POLICY_RATES` USD 0.02, EUR 0.005, GBP 0.015 — POLICY primitives, owner centralBank. OK. `MM_PARAMS.insuranceLimit = 100` per member "of the money" — 100 dollars?! `denominated: true` → `params.amount(id, currencyUnit)` presumably scales by unit pieces (100 whole dollars = 10,000 cents). A $100 insurance limit per member means practically NO deposit is insured (a household member holds far more) → `couldLeave` ≈ whole base → every bank's liquidity requirement ≈ its whole deposit base → `fundingRoom` ≈ liquid − whole base < 0 → **`room()` binds on 'funding' for every bank every period → NO BANK EVER LENDS.** This is a candidate root cause for "nothing is ever built" alongside F3. Verify: `params.amount` semantics + seed household deposits per member vs 100. → VERIFY in registry/params.ts and seed.
- MM17 Duplicate code: `households/bank.ts:146 board()` and `funds/bank.ts:92 board()` are the same 9 lines (modules cannot import each other, so the shared read belongs in `registry/` beside `wages.ts` — `registry/boards.ts`). Same pattern likely for `chooseBank` logic; check when reading funds/bank.ts and households/bank.ts.
- MM18 Missing (Money Market): term book exists but nothing ROLLS: a row matures via `due` (maturity) and is repaid by the kernel; the borrower's need next period is `fallsDueNext` → it rebids. OK, rolling is emergent. Money Market D (window) exists. Central Bank E (OMO) is in `central-bank-omo`. Missing: B6.a term premium read (averageRate exported, no reader) — display only.

- MM16 (verified): `params.amount(id, unit)` = `units.pieces(unit, value)` = value × MONEY_PIECES → `regulation.depositInsurance.limit = 100` is ONE HUNDRED DOLLARS per member (`ACCREDITED_WEALTH` in the same file is written as `1_000_000 * MONEY_PIECES` — the two conventions differ, and the insurance limit is the one that forgot the multiplier; Law 8 / A-44-class unit bug). Consequence chain: `couldLeave` ≈ the whole deposit base of every bank → `bank.liquidity.couldLeave` ≈ base → `quote.ts fundingRoom = liquid − couldLeave < 0` for any bank whose reserves + eligible paper < its deposits (every bank by construction: a bank funds loans with deposits) → `room().most ≤ 0, binds: 'funding'` for every borrower at every bank from the first period `bank.liquidity` exists (period 1) → NO LOAN IS EVER WRITTEN after period 0, no `credit.quoted` is published, F3's `costOfCapital` is `none`, no firm invests. Also the liquidity plan `wanted = couldLeave × (1 + cushion)` > base → every treasury wants MORE paper than its whole balance sheet → desk targets are unreachable → every desk bids for sovereign paper every period with its whole cash (`primaryBid` gap) → the "1–2 sessions clear then nothing" census. TEST: rig world, period 2, read `bank.liquidity.couldLeave / base` per bank (expect ≈ 1.0) and `credit.declined` events with `binds: 'funding'` (expect = all). FIX: `value: 250_000 * MONEY_PIECES`-style declaration is wrong too (a `denominated` param's `value` is in NAMED units, so `250_000`); the bug is the number, not the convention; and add a units test that every `denominated` money param is within an order of magnitude of the seed's per-member deposit.

### mechanisms/funds (10 files, read in full)

- FD1 `index.ts:1898 eligibleLines` walks ALL instruments per (fund, market) per session: `ordersOf` is called per market per fund; each call walks all instruments calling `eligible` → `view.classify` + `view.worth` (a valuation) per instrument. O(funds × markets × instruments × valuation). Fix: `eligibleLines` once per fund per period (working store keyed by period).
- FD2 `index.ts:1845 holdingsWorth` per (fund, market) per session; same fix.
- FD3 `index.ts:2029 noRequestVanishes` walks ALL `fund.requested` and `fund.redeemed` events EVER, every period: O(periods × requests). Fix: keep the asked/paid tallies in the Book (it already keeps the queue) — one writer — and check `asked = paid + queued` per (fund, holder) incrementally; or `ofKindIn`.
- FD4 `index.ts:1141 payQueue` runs in BOTH `funds.strike` (cycle 0) and `funds.settle` (cycle 2): two payment points per period for one queue; fine, but the `fund.gate` event fires twice per period per gated fund.
- FD5 `index.ts:1419 placeSpareCash` posts the fund's WHOLE spare into EVERY borrower's overnight secured book (banks × 1 order each) — the same money offered N times; the session's `capacityOf` cuts it after the first fill, but every book sees the full size, so `bestAsk`/`cheapestFirst` and the borrower's `asked` are computed against phantom supply. Acceptable (comment says so) but it inflates order counts: O(funds × banks) orders per period.
- FD6 `index.ts:1698 openPool` seeds via a posted market order at `price: 'market'` into the fund venue: fine.
- FD7 `nav.ts:1552 forShares = atLeast(net, 0)`: a floor, argued as limited liability. Correct as arithmetic of a residual claim (`ranking` says so). Keep — but the EQUITY ACCOUNT of the pool then goes negative and the solvency trigger fires: verified reasoning holds.
- FD8 `nav.ts:1520` walks ALL instruments per NAV read to find the fund's own liabilities: `navOf` is called per fund per revaluation AND per `feeAccrued` AND per `markPerUnit` read anywhere (e.g. `netAssetsOf` in manager, `standsBehind`, prime's `positionsOf` valuation of shares held by cells…). O(reads × instruments). Fix: `reads.issuedBy(fund)` (index exists in `instruments.issuedBy`).
- FD9 `manager.ts:201 feeOn` uses `calendar.endOf(period)` while every other per-period year fraction uses `startOf(next)`: two conventions for "this period of a year" (A-24 class). Fix: one `yearFractionOfPeriod(calendar, period)` in `calendar/` used everywhere (banks/dealing-quote.ts periodOfYear, staff.ts, deposits.ts, index.ts costOfFunds, treasury.ts…). There are at least SIX copies of this 4-line computation across modules.
- FD10 `manager.ts:312 noticeToGive`: `earns < cost` → wind up. `cost = costOfAPool` = wage × hoursPerPool — a manager with 3 pools compares EACH pool's fee to the cost of ONE pool. OK. But `netAssetsOf` reads `fund.struck` perShare × shares — a pool of one member's 100 shares earns fee ≈ 0 → every seeded pool is wound up at `patience` periods unless households subscribe enough that fee ≥ wage × hours. With H1/H4 (households never subscribe because `willWork` gate / dwelling bids) every pool in the world is wound up at period `patience`. Check census: `fund.notice` count. → measure.
- FD11 `manager.ts:359 launchToMake`: copies the SMALLEST rival's book and undercuts; `expects: smallest` — a "forecast" of what it will gather with no falsification test (Law 17): the launch record should carry `expects` and a later read compares it to what it gathered (`fund.struck.shares × perShare` at `patience`); the notice mechanism is that killer — state it in the record.
- FD12 `mandate.ts:816 payingThisPeriod`: `period % everyPeriods === 0` — a window placed by PERIOD INDEX not by calendar date (Law 8, "periodicities placed by date"). Fix: window opens when `calendar.startOf(period)` crosses a month/quarter boundary from the mandate's `since` date.
- FD13 `inkind.ts:962 lastMark` reads `prices.latest` — fine. `basketOf` re-walks fund holdings per call; called in `create`/`redeemInKind`/`readListed`/`inKindGaps` (banks) per period per ETF. Cheap enough.
- FD14 `commitment.ts:1360 callCapital`: calls when `cash <= 0` — a closed-end pool calls its ENTIRE undrawn commitment the first period (all at once) since there are no deals (comment admits). Private Equity A2 "called when a deal needs it" is MISSING; `13.5b` buyout not built. The whole commitment lands as cash on day one, then sits (a closed-end fund with no mandate to buy anything — check `ordersOf`: closed pools use `ordersOf` and bid `spare` into eligible lines: fine, it invests like an open fund).
- FD15 `passive.ts:1718 targetUnits`: `baskets = worth / cost`, `target = weight × baskets` — a fund's book value INCLUDES cash; a tracker with cash and no holdings bids for every line at the print with `affordable` cap: OK. But `bookValue` uses `view.quantity` at prints — fine.
- FD16 `bank.ts fundChoosesBank` and `households/bank.ts` — DUPLICATED `board()` (MM17) and the whole switching logic (`stayed`, `foregone > cost`) is the same algorithm in two modules with different switching-cost params. Law 4 (one mechanism for "a depositor moves"): the mechanism belongs in the kernel (`world/deposits.ts` or `registry/switching.ts`) with the PREFERENCE (switching cost) per party kind in the registry; modules declare only the cost.
- FD17 `things.ts thingOrders`: `view.outlook(price)` required; a commodity fund with no outlook never bids (comment: honest). `storageRateIn` — storage market exists (13c). OK.
- FD18 `index.ts:2312 requires: ['households','estate','labour','seed.foundation', ...needs]` — `seed.foundation` is a module id? If the foundation seed is a module (`src/seeds/foundation.ts` as SystemModule) fine; blocker #2 said small-business lacks it. Consistent.
- FD19 Phase anchors: `funds.manager` and `funds.capital` anchor `before: 'funds.strike'` which is declared LATER in the same module's phases list — blocker #3 (addPhase resolves anchors at insertion, in declaration order). Fix in kernel: `World.addPhase` resolves anchors AFTER all phases of a module are collected (two-pass: collect, then topological insert), which also fixes short-term-debt's cycle-2 landing (blocker #4). This is the assembly-DAG item (0b).
- FD20 Missing (Fund Shares): D3 (a saver's choice between deposit and fund) exists only through households' `decide` (H3) which ladders every share line — check households/consume for fund subscription reads of `fund.struck.offered`. Hedge Funds C–E: short selling by a pool (`mayWrite`, borrow via securities-lending) exists in the derivative layer's `tradingLimits`; verify in securities-lending whether a FUND ever posts a borrow need (`borrowNeeds` declared only by banks → a hedge fund can never short: Hedge Funds C1 MISSING though marked).

### mechanisms/equity, control, estate, credit-events (read in full)

- EQ1 `equity/index.ts:165 decide` runs for EVERY row (every firm, ~thousands) every period; each builds `ctx.participant(firm)`, reads `firms.funding`, `decideEquity` (reads `view.equity()` — an equity walk). O(firms × equity walk). Acceptable if `equity()` is O(1) (register keeps the account) — verify register `equityWalk` cost; it walks entries (A4 in earlier findings). Fix: firms that published no `firms.funding` this period are skipped before the view is built.
- EQ2 `equity/index.ts:291 recordDividends`: one `ctx.owes(...)` AGREEMENT per (firm, holder) per declaration. With every listed firm's line spread over up to every household cell, a quarterly declaration writes cells × firms agreements, each later paid by one instruction (`payDividend`) that filters `agreements.ofKind(DIVIDEND_DECLARED)` per action → O(actions × all dividend agreements). The C-2 finding (65% of instructions were dividends) was cut by declaring quarterly, but the agreement scan is quadratic. Fix: `agreements.byDebtorAndKind(firm, DIVIDEND_DECLARED)` index; and a cell's dividend claim is one agreement per cell (already) — under DESIGN-1 cells are fewer.
- EQ3 `equity/index.ts:366` `owed.owed / weightOf(holder)` raw division (not `over`) and `payDividend` `Number(share.total)`, `asQty(total)`: type-erasing casts. Minor.
- EQ4 `equity/index.ts:498 dividendLegs` parses the instruction REASON STRING (`'payout on <line> to <holder>'`) to find the line — a fact recovered from a string (the file's own A-52 comment condemns this elsewhere). Fix: `receipt: { of: 'dividend', line }` on the money leg (the leg already carries `receipt`), read the leg.
- EQ5 `equity/share.ts:1163 worthTo`: value = published annual earnings / required — a PERPETUITY at the holder's required rate with no growth and no retention: a firm that retains earnings (patience) is valued the same as one paying them out. Acceptable as one participant's opinion (Law 3) but every holder uses the SAME formula with different `required` → the book's two sides differ only by cost-of-money (same monoculture as BK21). §46 A3 wants disagreement from HISTORY; here it is from one parameter. Fix: the holder's outlook on the firm's income (adaptive, own memory, from reports it saw — the `research` estimate machinery already does this for banks) feeds `worthTo`, not the last report verbatim.
- EQ6 `equity/index.ts:820 seed`: `Math.round(...)` in engine seed code (outside core/num). `shares = book / OPENING_SHARE` — book at seed = firm's holdings value; OK.
- EQ7 `equity/index.ts:845` seed distributes each line to a ROTATING window of cells with `reached + w > shares → break`: a line with fewer pieces than the first cell's weight is held by NOBODY (`holders.length === 0 → continue`) — the very defect the comment says was removed ("the bound is gone") returns for any firm whose book < one cell's weight pieces at a cent a share (book < weight × $0.01, e.g. a 50,000-member cell needs a $500 book: unlikely but the branch exists). Keep as note.
- EQ8 `control/index.ts:843 equityLines` walks all instruments once per period; `couldBuy` × `controlBidsFor` × lines: O(buyers × lines × (published read + heldTotal)). `heldTotal` walks holders per line per buyer → O(buyers × lines × holders). Fix: precompute `heldTotal` per line once per period (or read `issued`, which for shares = heldTotal by construction).
- EQ9 `control/index.ts:414 runTender`: `ctx.participant(holder)` for EVERY holder of the line — for a widely held line that is every household cell; `tenders()` → `mustSell` (two journal reads) + `wouldTakeFor` (outlook read). Per bid. OK for rare events.
- EQ10 `control/index.ts:538` `asQty(units / weightOf(seller))` — throws if the fill is not a multiple of the cell weight: `clear(...,'proRata')` fills are NOT weight-multiples → a tender with a cell seller THROWS at settlement time (asQty refuses fractional). Blocker-class for any tender involving a household holder (which is every listed line's seed holders). Fix: the tender venue posts cell orders in `commonGrain` multiples and the solver fills on the grid (M3), or `settleTender` rounds down to the cell's grain and records the remainder as unfilled.
- EQ11 `control/index.ts:808` `unit: currencyUnit('USD' as CurrencyCode)` — a hard-coded currency literal in a family (S1-class). Fix: the region's currency.
- EQ12 `control/index.ts:600`: control taken at `held × 2 > inIssue` and combine when `held < inIssue` is false i.e. only at 100% ownership; a tender at `needs = ½ outstanding` never reaches 100% unless every holder tenders → `combine`/`own` almost unreachable; a controlled subsidiary persists (fine: `takeControl`). M&A A4/A5 "balance sheets combine" is reachable only by chance. Note as PARTIAL.
- EQ13 `estate/index.ts:1113,1234 open/claimsOn/close` walk ALL instruments per estate per period (`claimsOn` is called in `distribute` every period per running estate AND in `close`). O(estates × instruments × holders). Fix: `instruments.issuedBy(estate)` (used in `claimsOf` for the family — the mechanism itself doesn't use the index).
- EQ14 `estate/index.ts:1190 offers`: reservation = last print × (left/total): a written PRICE PATH ("no written price path", Appendix B) — the estate's ask declines linearly with time by construction. It is argued as patience; but it is a formula on a print. Alternative that is a REASON: the estate posts at the last print while it has time and at 'market' in its last period (two states, no path), or its reservation is the highest bid it has SEEN refused (adaptive). Record as a design decision to make.
- EQ15 `estate/index.ts:1345` `some({ perMember, weight: holder.weight })` — `holder.weight` may be undefined for named parties; guarded by `representation === 'cell'`. OK.
- EQ16 `estate` participants: the estate posts `offers` into EVERY market it holds the instrument of (`view.quantity(m.instrument)`) — for CP4's per-vintage plant markets this is where a dead firm's plant is sold; nobody BIDS for used plant (grep: no firm participant bids in `plantMarketId` markets — firms' `invest.ts plantOffers`? it OFFERS). → Capital Programme D3 "a dead firm's plant is sold to whoever will have it" has no buyer side: MISSING mechanism (firms bid for second-hand plant when its price per unit of capacity < new). Verify in firms/invest.ts (read earlier: `plantOffers` walks instruments to OFFER surplus plant; no bids).
- EQ17 `credit-events/index.ts:1764 impairments`: walks all instruments × holders every period, records `credit.impaired` per (holder, non-performing instrument) EVERY PERIOD (not once) → journal growth O(periods × impaired holdings). Fix: record on transition (status changed this period) and keep a `credit.impaired` read derived from status.
- EQ18 `credit-events` `credit.default` is recorded for ANY `overdraftRefused` payer — including a TREASURY refused by the central bank in its own money (BK3 concern): the banks' PD model then counts a sovereign refusal as a default. Sovereign G1 says a domestic-currency sovereign does not default; it is a REFUSAL of the no-overdraft rule (Treasury D3). Fix: the event kind for a treasury in its own money is `treasury.shortfall` (already exists, F13), and `credit.default` skips payers whose kind issues/receives its own money's fiscal backstop (registry: `partyKind.sovereignIn: ccy`).

### mechanisms/reporting, research, ratings (read in full)

- RP1 `reporting/report.ts:313 listedLineOf` walks ALL instruments per firm per period (called in `publish` for every FIRM every period, and again in `revise`): O(firms × instruments) per period ×2. Fix: `instruments.issuedBy(firm)`.
- RP2 `reporting/report.ts:375 incomeOf` walks `ledger.inPeriod(p)` for every period of the quarter (13 periods) per reporting firm — and `restate` re-walks the same span EVERY period for EVERY quarter ever reported (`already.includes(label)` → restate → `incomeOf` over 13 periods). O(firms × reported quarters × 13 × ledger). This grows without bound. Fix: `register.equityEntries(firm, from, to)` already gives the entries; the `cause` map is the only reason to walk the ledger — put `cause` on the equity entry at write time (one writer), delete the ledger walk. Restatement check: compare the entries' count/sum since last read (append-only), not a re-walk.
- RP3 `reporting/report.ts:436 cashOf` walks 13 periods of ledger legs per report. Same fix: `ledger.legsByParty(period, party)` index.
- RP4 `reporting/index.ts:66 publish`: `quarterClosedBy(anchorOf(seed, firm), today)` per firm per period — cheap. `guide` → `guidanceOf` → `ctx.participant(firm).outlook(income)` per public firm per period. OK.
- RP5 `research/index.ts:716 coverageOf`: `journal.forSubject(kind, company).filter(bank)` ×3 kinds per (bank, company) per period: O(banks × companies × events-about-company). Fix: index `(kind, subject, bank)` or keep the desk's coverage in the working store (it says it deleted that as a mirror — but the JOURNAL read cost is the price of Law 19 here; the fix is the index, not a mirror).
- RP6 `research/index.ts:837 wagePrinted` walks ALL `labour.print` events per bank per period and takes the LAST matching region (not the latest period — insertion order; OK) — O(banks × prints). Fix: `lastPublicAbout('labour.print', venue)`; and this duplicates `registry/wages.ts wageFacing` (a third copy of "what an hour costs here": staff.ts wageFacing, manager.ts wageFacing (registry), research wagePrinted). Law 4.
- RP7 `research/index.ts:793 pay`: analysts are paid to the household cells that BANK at the bank — a payment with no employment relation, no hours matched in the labour venue, split equally across every member of every cell at that bank: Reporting D2 says "coverage costs it real money paid to a named payee", and this is money sprayed over depositors. It is a one-sided-in-substance flow (nobody worked). Fix: research analysts are the bank's `analysis`-trade staff hired in the labour venue (manager.ts already uses `INVESTING='analysis'`); the cost is the wage bill of hours = names × hoursPerName, matched like any employer; `pay()` is deleted.
- RP8 `research/index.ts:916 moved`: `Number.EPSILON * (|now|+|said|)` — a hand-rolled tolerance beside `dustOf`; use `dustOf(2, …)` (Law 7 one derivation).
- RP9 `ratings/index.ts:1494 assessAll`: assessors × subjects(all treasuries, banks, named firms) × (`ctx.blind(subject)` view build + `assess` (failedPayments walk, earned(window) ledger walk, owedIn walk) + `rateTheLines` × issuedBy) per period. With 3 assessors × thousands of firms this is O(assessors × firms × ledger-window). Fix: `assess` reads cached per-party per-period reads (`earned(window)` is the same read `staffOrders` and `firms` make — one `PartyReads` cache per period in the kernel view).
- RP10 `ratings/index.ts:1447 published`: `journal.forSubject('rating.action', subject).filter(assessor)` per (assessor, subject, line) per period: O(assessors² × subjects × actions). Fix: the working store keeps `lastPublished[assessor][subject]` (it is NOT a mirror if the event is written from it in the same function — one writer; the comment's Law 19 argument cuts the other way: the journal is the copy for readers).
- RP11 `ratings/assess.ts:1937,1941,1975`: `atLeast(period − window, 0)`, `atLeast(missed, 0)`, `atMost(atLeast(moved,0), len−1)` — three floors/caps in 40 lines. The first is arithmetic (no negative period); the second is a no-op (missed is a count ≥ 0 by construction: delete); the third is a clamp on an index into a finite scale — acceptable as "the scale ends" but express as `GRADES.at(...)` with the ends stated, not min/max.
- RP12 `ratings` `firstBoundary`/`boundaryStep` declared `kind: 'shape'` with a death ("when a grade can be measured against defaults, E3") but NO `standsInFor` → they are SHAPES, not placeholders (blocker #1 class: `check:params` may refuse shapes without death? short-term-debt's `kind:'shape'` WITH `standsInFor` was refused — the rule is a shape cannot have standsInFor; so these are compliant shapes). Note: shape count must FALL (Law 2) — the E3 killer (grade-vs-default calibration) is the mechanism; item.
- RP13 `ratings/index.ts:1611 collectFees`: fee = equity × rate per period per rating — an issuer with negative equity pays nothing (`worth <= 0 → continue`) — fine. Each rated party pays 3 fees per period → 3 × subjects instructions per period. Ratings A5 is per period; real-world annual. Cost: with thousands of firms rated by 3 assessors, ~3×firms instructions per period for fees alone. Fix: fee is annual, collected on the anniversary of the first rating (calendar-placed, Law 8).
- RP14 Ratings C1 consumers: `riskWeights()` per grade are declared (policy) but `banks/capital.ts riskWeightOf` does NOT read them (it uses `regulation.riskWeight.loan` and `sovereignWeight` only). Ratings C2 "a capital charge per grade" is MISSING in the consumer; the params are display-only (Appendix B: no display-only number). Fix: `riskWeightOf` reads the grade published on the instrument (`rating.action` last for the line) and the per-grade weight; unrated → `regulation.riskWeight.loan`.
- RP15 `ratings/bank.ts assessorChoosesBank`: a THIRD copy of the bank-choice mechanism (FD16). Same fix.

### mechanisms/external, spot-fx, derivative-layer (read in full)

- EX1 `external/index.ts:67 externalOf` walks the whole period ledger PER REGION, and is called once in `publishExternal` and AGAIN in the `accounts` family per region: O(2 × regions × ledger legs) per period. Fix: one walk producing all regions' accounts; family reads the published event (Law 19) and re-derives only the zero-sum from the same walk result passed in.
- EX2 `external/index.ts:199` `dust = (legs+2) × Number.EPSILON × magnitude` hand-rolled beside `dustOf` — Law 7 has one derivation (`dustOf`). Same as RP8.
- EX3 Cross-Border: only E (accounts) is built; A–D (tariffs, quotas, the customs mechanism, transport across borders — freight exists) — verify COVERAGE: Cross-Border A–D MISSING?
- FX1 `spot-fx/participants.ts needOrders`: `view.owedIn(foreign)` — a walk over own liabilities per (party, pair) per period for TREASURY, FIRM, HOUSEHOLD kinds: with C currencies there are C(C−1)/2 pairs and every household cell is asked in every pair → O(cells × pairs × liabilities). Fix: `owedIn` per party once per period (cached in the view); and a party with no foreign-currency liability or balance (the common case) is excluded by a cheap precheck (`holdsAnyForeignMoney || owesAnyForeign`).
- FX2 `spot-fx/participants.ts dealerOrders`: `positionIn(view, ccy, home) = view.cash(ccy)` — "its position is what it holds of moneys not its own" — the desk's inventory in a pair is its whole foreign balance, which is also every foreign deposit its customers' payments left it holding; OK by the comment's argument. `skew = edge × held/room` unbounded either way — fine (Law 6).
- FX3 `spot-fx/arbitrage.ts arbitrage`: `Math.abs(gap) <= cost` — `Math.abs` outside core; `size = equity × inventoryLimit / ab.price` — sized on EQUITY per triangle per period without checking cash held: `arbitrageOrders` posts `price: 'market'` for `size` in all three books — a desk with no B money cannot deliver leg 2 → settlement fails for that leg after leg 1 settled (three separate sessions, not atomic) → the desk ends up with an open position it did not decide on. Spot FX C2.a "three trades at once" requires atomicity across three books which the kernel does not offer. Fix: size = min(equity-based, cash in A, cash in B at rates, cash in C at rates); and record the round trip as a `Process` (item 21 noun) so a leg that fails aborts the rest.
- FX4 `spot-fx/index.ts:431` `[TREASURY, FIRM, HOUSEHOLD]` — a LIST OF PARTY KINDS in a module (the comment argues these are kernel kinds). Law 15 says no mechanism branches on entity type; a participant registration per kind is the sanctioned form, but a fund/estate/insurer with a foreign obligation is silently NOT in the FX book (comment admits: "is its own module's participant" — none of those modules register one). Fix: the FX need is a kernel hook (`ParticipantView.owedIn` exists): register `needOrders` for EVERY party kind the registry has (`registry.partyKinds`), not a list.
- DL1 `derivative-layer/index.ts:277 marginCalls`: for every open contract × both sides × `marginPairsOf(side)` (walks `contracts.openOf(side)`) → O(contracts × contracts_per_party); then `requirement()` walks `contracts.between(poster, holder)` and computes `initialMargin` + `valueTo` (a mark) per row: O(contracts × rows-between) marks per period. Fix: iterate PAIRS once (`contracts.pairs()`), not contracts×sides; cache `valueTo` per (contract, period) in the contracts store (a mark is one read per period).
- DL2 `derivative-layer/index.ts:405 trueUpFunds`: `coverOne` computes `requirement` for every member (each a walk + marks) and `trueUpFund` computes it AGAIN for the same members, then `fundShareOf` again for the observer. Three derivations of one number per period. Fix: `requirement` cached per (poster, holder, ccy, period).
- DL3 `derivative-layer/index.ts:430 resolveContracts`: `journal.ofKind('margin.call').filter(period)` full scan (fix: `ofKindIn`).
- DL4 `derivative-layer/index.ts:548 runWaterfall(..., atLeast(claim, 0))` — a floor; semantically "no hole" (claim ≤ 0 → loss 0). Express as `claim > 0 ? claim : 0` at the call site — it is the same thing; keep but note the rule.
- DL5 `derivative-layer/margin.ts:1332 requirement`: `owing = net < 0 ? |net| : 0` — variation margin is one-way per pair (correct). `initialMargin` for a row whose `initialMargin` is none contributes 0 — `asCash(0, ...)` where G2 says such a row was never admitted; fine.
- DL6 `derivative-layer/margin.ts:1499 releasePledges`: `lien.reason.includes(String(ccy))` — a fact recovered from a STRING (A-52 class): `secures: 'margin with <holder> in <ccy>'` then parsed by substring. Fix: lien carries structured `secures: {kind:'margin', holder, ccy}`.
- DL7 `derivative-layer/house.ts:1011 coverOne`: `Math.sqrt(horizon)` — a square-root-of-time scaling is a MODEL assumption (Brownian) wearing a policy's clothes; C3.b says the fund covers the largest member over the close-out horizon — the honest read is the largest member's requirement times the horizon's realised move width (the house's own surprise history, §46 B3), not √T. Note as a SHAPE with a killer (compare fund adequacy to realised close-out losses).
- DL8 `derivative-layer/house.ts:1175`: house capital line of the waterfall is "already fallen" — `rounds.push({line:'houseCapital'})` with NO instruction; correct by the comment (residual), fine.
- DL9 `derivative-layer/index.ts:863 participants: tradesContracts.map(...)` default `[BANK, FIRM]` — funds/insurers must be passed by the assembling world; FD20's hedge-fund shorting depends on this list being extended in `world` assembly. Check foundation/rig: does it pass FUND? (grep `derivativeLayer(` in seeds/test.)
- DL10 `capacity().admits`: builds `ctx.participant(party)` and `capacityOf` (walks holdings) PER ADMISSION DECISION (per fill attempt) — the E4 measurement recorded 31,640 admission decisions in one run. O(fills × holdings). Fix: capacity per (party, ccy, period) memoised in the layer's working store, decremented by admitted margin within the session.
- DL11 `derivative-layer/index.ts:918 seed`: house banks at the first money-issuing party of the region that is not itself — i.e. the FIRST BANK in party order; a house's own bank failing takes the house's margin cash with it (resolution moves it; fine). But the house is an ordinary depositor with `depositClass: null` → its cash is uninsured and NOT in `couldLeave` (classOf undefined → skipped) → a bank holding a clearing house's margin has liabilities that "cannot run" by construction: a bank whose largest depositor is the house has an artificially low liquidity requirement. Fix: `depositClass: 'wholesale'`.

### mechanisms/cds, irs (read in full)

- CD1 `cds/index.ts:101 openBooks`: walks ALL instruments every period, and per candidate reference walks `holdersOf(i.id)`; opens `tenors` (3: 1y,3y,5y) books per reference that has ANY cleared liability held by anyone — i.e. every firm with a bond, every bank with a sub line, every treasury. Books are never closed. Each book is a market walked by every bank's `linesQuoted`/`coveredLines` (BK18/19) and asked in `gather`. Cost grows as O(references × tenors) markets per period, never falling. Fix: open on the first REASON (a party's `cdsOrders` would post) rather than on existence; close a book with no open contract and no print in N periods (Clearing A1: a market nobody is in is not a market) — closing is a kernel operation (`closeMarket`) that does not exist yet (item).
- CD2 `cds/index.ts:171 rollSeries`: `ctx.period % every` — period-index periodicity (Law 8; FD12 class). Fix: calendar-placed roll dates (March/September 20th).
- CD3 `cds/index.ts:188` `ccy = currencies.keys().next().value` — the series is written in the FIRST currency of the registry regardless of the names' currencies; a euro name's face is summed with dollar faces as weights (`i.issued` in different moneys added: "two currencies never added"). Fix: one series per currency, names filtered by `i.ccy`.
- CD4 `cds/index.ts:228 publishedGradeOf`: walks ALL `rating.action` events per reference at every roll — and `recoveryIsKnown` walks `credit.default`, `estate.opened`, `estate.closed` in full per contract per period (`settleEvents` per open contract; `settleSeriesNames` per name per index contract). O(contracts × all defaults ever). Fix: journal `lastOf(kind, subject)` (exists: `journal.lastOf`) — `recoveryIsKnown` uses `ofKind().some/filter` where `lastOf` exists three lines away.
- CD5 `cds/index.ts:247 settleSeriesNames` walks ALL `cds.index.settled` events per period to build `paid`: O(periods × settlements). Fix: the settlement is a fact about the contract; hold `settledNames` on the contract's own terms via `restate`, or an index by contract.
- CD6 `cds/participants.ts:551 levelFor`: `belowPar / tenorYears` — the cash bond's discount below par spread evenly over the CDS tenor with no discounting and IGNORING the bond's own maturity and coupon: a 10y bond at 90 anchors a 1y CDS at 10%/yr, a 1y CDS on the same name whose bond matures in 1y at 90 anchors at 10% too — right for one, absurd for the other. Also `belowPar <= 0 → undefined`: a bond above par (any coupon above the sovereign yield) gives NO anchor at all → the party falls back to its outlook → in a never-printed book: nothing → no orders. The anchor should be the bond's YIELD over the sovereign curve at the same tenor (`basisFor` in measures.ts already computes exactly `cashSpread`); Law 4: one derivation.
- CD7 `cds/participants.ts:740 sizeOf`: `own / spread` — "what a year of this spread on its own capital would carry": notional = capital ÷ spread → a bank with 1bn capital and a 100bp spread offers 100bn notional per book, both sides, in EVERY CDS book it has no exposure in. With N references × 3 tenors that is 3N books × 100bn of two-way quotes per bank per period. The `admits` capacity gate then refuses most of it (DL10's 31,640 refusals). Fix: a naked position is sized by the capital the regulator's weight consumes (`riskWeightSold` × notional × capitalRatio ≤ appetite) — the number the B3 comment says it reads and does not.
- CD8 `cds/participants.ts:643 counterpartyTerm`: `1 − held/(held+own)` — the discount for facing a full counterparty uses the party's OWN cover held, not the counterparty's book ("how much of THIS name that counterparty has already written, which is public" per the comment — not what the code reads). Comment/code mismatch (Law 16 stale comment).
- CD9 `cds/contract.ts:965 markOf`: mark = notional × (spread now − struck) × years left — undiscounted, linear in years: a 5y CDS 100bp richer is worth 5% of notional flat (real ≈ risky annuity ≈ 4.4%). Acceptable as a stated approximation? Law 3: mark derived from price — fine; but the same undiscounted annuity is in `irs/contract.ts markOf` and in `initialMargin`: three copies of "years left × rate move × notional" (Law 4) → one `annuityOf(rateMove, notional, yearsLeft)` in `registry/derivatives.ts`.
- CD10 `cds/contract.ts:946 payoff`: recovery = `reads.mark(obligation)` at settlement: the defaulted bond's last MARK — which after the estate closes is the write-off price 0 (estate `close` moves claims at `pricePerUnit 0`) unless the estate SOLD the bond into a market; a bond nobody bought recovers 0 → protection pays par. D2.a "no fixed recovery" is honoured in form, but in substance recovery is 0 or last-print. OK given XI-8; note.
- CD11 `cds/series.ts:1200 weightShare`, `runningShare`: raw `/` divisions (not `ratioOf`) on `number` weights — type-erased.
- IR1 `irs/index.ts:1623 openBooks`: `journal.ofKind('index.benchmark').some(...)` full scan per currency per period. Fix: `lastOf`.
- IR2 `irs/index.ts:1682 forwardRate`: `Math.pow` compounding annually with `1+rate` — a MODEL formula (discrete annual compounding) in a module; it is a READ for the observer (C2) so acceptable, but it is the parity-formula forward that Appendix B forbids for PRICING; ensure no participant reads it (grep: `forwardRate(` callers).
- IR3 `irs/participants.ts:1766 fixedDebtOf`: walks ALL instruments per (party, book) per session → O(parties × irs books × instruments). Fix: `instruments.issuedBy(self)`. Also it counts EVERY liability as fixed ("A1.d: what makes a liability fixed is that its terms name a rate" — but the code does not check for a rate: a deposit (money) is `liabilityOfIssuer` and would count — for a BANK `fixedDebtOf` = its whole deposit base → every bank wants to RECEIVE fixed on its entire balance sheet. Bug. Fix: read the kind profile for `cashFlows` with a fixed coupon (a bill/bond/loan with `rate` in terms), exclude money.
- IR4 `irs/participants.ts:1820 mine`: level = the overnight FIXING (a per-annum rate) posted as the fixed rate for a 9y swap — the party's "own number" for every tenor is the overnight rate: a flat curve at overnight for all tenors, all parties (monoculture again, BK21/EQ5). §46: the outlook should be the party's expectation of the AVERAGE fixing over the tenor (its own outlook on `index.benchmark`), which the expectations module could form if it observed benchmark prints (H8: outlooks only form from own fills — a party never trades the overnight book directly unless it is a bank).
- IR5 `irs/contract.ts:2055 legs`: `(at − c.opened) % fixedEvery === 0` — period-index periodicity (Law 8); `accrualFor` uses `startOf(at − every + 1)` which for the first payment reaches BEFORE `opened` (accrues on periods before the contract existed). Fix: calendar-placed payment dates from the trade date (`addMonths`), accrual from the later of last payment and trade date.
- IR6 `irs/measures.ts:2163 swapSpread`: `minus(swap.price (PerPiece), asPerPiece(yield))` — a book PRICE (money per piece, i.e. rate×100 cents?) minus a YIELD ratio cast as PerPiece: the E-11 finding the comments keep citing ("a book cannot say which of the two its level is") is a real unit bug here: `priceOf(ccy, unit, perNamedUnit(rate))` converts the rate to money-per-piece on the way IN (participants.ts:1821) and `asRatio(now.value.price)` reads it back as a rate on the way OUT (contract.ts:2003) — the two are only equal if `priceOf` is the identity for `NOTIONAL` (perUnit 1) in cents... `2% → 0.02 → priceOf → 2 cents per unit?` Then `asRatio(2)` = 200%. VERIFY with a test: post at fixing 0.02, read `markOf` after one print. Blocker-class for IRS/CDS marks if wrong.
- IR7 IRS B2 (pension matching) declared PARTIAL, insurers exist (14.0) — check insurers/allocate for IRS use.

- IR6 (verified against `registry.ts:384 priceOf`): `priceOf(ccy, NOTIONAL, 0.02)` = 0.02 × 100 (cents per currency unit) / 1 (notional perUnit) = **2** money-pieces per unit. The book prints 2. `markOf` then reads `asRatio(now.value.price)` = 2 = **200 % per annum** and multiplies notional by it. Every rate-quoted contract kind (irs, cds, cds.index, xccy) posts rate×100 and reads it back as a rate: MARKS, INITIAL MARGIN and CLOSE-OUTS on these classes are off by the currency subdivision (×100). Contract-level bug, E-10/E-11 named in comments but never fixed. FIX (one place): `ContractReads.print` for a `quotedAs: 'rate'` kind returns `rateOf(price) = price × subdivision(unit) / subdivision(ccy)`, and `struckAt` is stored as the RATE by the market when `quotedAs === 'rate'` (`clearing/market.ts` at the print), so no module ever sees a rate as a level. Test: post at fixing 0.02, read `markOf` after one print = 0 (struck = now).

### mechanisms/fx-derivatives, options, bond-futures (read in full)

- FXD1 `fx-derivatives/index.ts:138 openBooks`: `anyone = ctx.participant(ctx.parties.all()[0]?.id ?? house)` — builds a participant VIEW of an arbitrary party (the first in the list) to read a PUBLIC event (`index.benchmark`) — wrong reader (Observer A4 in reverse) and a view build per pair per tenor per period. Fix: `ctx.journal.lastOf('index.benchmark', subject)`.
- FXD2 `fx-derivatives/index.ts:151` xccy terms freeze `baseRate`/`quoteRate` from the overnight fixing AT BOOK OPENING for the life of the BOOK (not of a contract): every contract struck in that book for 3 years pays interest at the opening period's overnight rate. C1.a says each leg pays its own money's rate — floating. Fix: legs read the fixing at payment time (as IRS does).
- FXD3 `fx-derivatives/participants.ts:275 carryOf`: `Math.pow(1+r, years)` compounding — covered-interest-parity FORWARD as the party's reservation. Appendix B: "no parity-formula forward" — the module argues it is a reservation not a price; but EVERY bank's reservation is the same CIP number (monoculture) and the quotes are ±1 tick around it: the forward IS CIP by construction with a 2-tick spread. Since B2 says banks take the arbitrage while it pays, the honest form is: reservation = the bank's OWN funding cost in each money (`bank.costOfFunds.alsoIn` — `ownBasis` already reads it for xccy!) not the overnight benchmark. Law 4: `carryOf` and `ownBasis` are two derivations of "what one money costs me against the other"; use the second for both.
- FXD4 `fx-derivatives/participants.ts:336`: hedger posts at `mine` (its carry) for `positionIn(base) − covered` where `positionIn` = `owedIn(base)` = liabilities due in base less cash held — it hedges the WHOLE liability stock every period at every tenor book (1y, 3y): the same exposure is posted into both tenor books (one party, one position per BOOK but the same need twice across books). Fix: the need is allocated to the tenor matching the liability's maturity (`owedIn` by date bucket), not repeated.
- FXD5 `fx-derivatives/contract.ts:765` `(at − started) % payEvery` — period-index periodicity (FD12/IR5); `accrual` from `at − payEvery + 1` reaches before `started` on the first payment.
- FXD6 `fx-derivatives/contract.ts:627,723` `market: '' as never` / `'' as InstrumentId` — an EMPTY id cast to a branded type for `underlying` — the kernel's "underlying must exist" check (G2) is bypassed by an empty string. Fix: `underlying` returns the spot pair's market id (it exists: `fxMarketOf`).
- OP1 `options/index.ts:1382 openBooks`: `atLeast(opened, 0)` floor; `nextCycle(period, life)` period-index ladder (Law 8 again: expiry should be the third Friday of a calendar month).
- OP2 `options/index.ts:1202 worthToIt`: `distance + width` where width = confidence × √years — the party's own dispersion; OK as a REASON. But `worth > 0 ? worth : nothing` and then `if (mine <= 0) return []` — a party whose expected level is far below the strike has NO view at all (not "worth zero"): fine per comment.
- OP3 `options/index.ts:1235 optionOrders`: `covered` loop walks `view.contracts.mine()` per (party, book); books = lines × 2 rights × ladder; `reasons` restricts to lines the party has an outlook on. OK. But `room = own / price` with `own = standsBehind()` (whole equity) → a party writes options up to its ENTIRE equity per book, in every book (same as CD7). Fix: risk-bearing capacity is shared across books (a per-party per-period budget decremented in `admits` — DL10).
- OP4 `options` `Math.sqrt` in three places; `intrinsic` returns `nothing` (0) for out-of-the-money (a decision, fine).
- OP5 `optionKind.initialMargin`: `Math.sqrt(left / horizon)` — √T scaling (DL7 shape).
- BF1 `bond-futures/index.ts:1731 bondCarryOf`: `journal.ofKind('index.benchmark').filter(...).at(-1)` full scan per (deliverable, read) — called in `netBasis` per book measure and in `futureOrders`? (no: futureOrders uses outlook). Fix: `lastOf`.
- BF2 `bond-futures/index.ts:1838 futureOrders`: `want = −held/contractSize` — EVERY holder of a sovereign line (every bank's liquidity portfolio, every household cell holding sovereign paper from the seed) wants to be SHORT the future by its whole holding, every period, at every expiry: I2 says "a holder OVER ITS OWN TARGET shorts" — the code shorts the whole holding regardless of target. So the future book has massive one-sided supply from every cell; a cell posting `over(held, contractSize)` contracts — with cells holding per-member quantities the division yields fractions → `deliverable()` rounds; fine. Fix: want = −(held − target)/contractSize where target is the party's own liquidity target (banks publish `bank.lines`/targets; households have none → a household hedges nothing).
- BF3 `bond-futures/index.ts:1955 deliver`: the long pays `price × face` where price = deliverable's latest print — at delivery the future's struck price is NOT used: the contract is closed (`act: 'close'`) with its mark settled by the layer? No — `legs: []` and `expires: () => false` means the layer never pays the mark; delivery settles at the CASH price, so the futures P&L (struck vs cash at expiry) is realised only through variation margin flows (`requirement` netting) which are RETURNED on close (`returnMargin`). Net: a long that bought at 98 and takes delivery at 100 pays 100 and had margin moved… the margin is returned → the long GAINS NOTHING from the price move. The future's economic P&L is lost at delivery (D11.a violated: value at close-out = mark; here 0). Fix: delivery legs price the bond at the STRUCK future price (`moneyLevel(c.struckAt)`), which is what a delivery IS (invoice price = futures settlement price), and the mark to that point was margin.
- BF4 `bond-futures/index.ts:2035 matching`: finds the house's other row by (deliverable, expiry, struckAt, notional) — two members with identical fills are indistinguishable → wrong pairing possible; fills at the same uniform price are COMMON (uniform-price sessions). Fix: the layer records the pair id at the strike (`c.pairedWith`).
- BF5 Sovereign I3 basis trade: `futureOrders` never posts a REPO borrow need for the cash leg — I3 "financed" is only true if the party independently repos the bond. The "largest single source of real demand in a secured market" claim in the header is not built (no `borrowNeeds`/repo posting from this module). PARTIAL.

### mechanisms/commodity-futures, index-futures, indices, securities-lending, securitisation
- CF1 commodity-futures/index.ts `futureOrders`: a holder of a deliverable shorts its WHOLE holding every period (BF2 class); no hedging rule, no fraction, no inventory-need read.
- CF2 commodity-futures: the (series × good) market list grows every period; no series ever expires from the market table (CP4 class, unbounded growth of markets phase).
- CF3 commodity-futures `carryFromWorld.lastPublic` uses `journal.ofKind(kind).at(-1)` — full scan of all events of the kind every read; must be `lastPublic` / indexed.
- CF4 commodity-futures delivery at spot price and margin returned → futures P&L never realised (BF3 class; both bond and commodity futures share the defect and the fix: settle (spot − struck) × notional through the house, then deliver at struck).
- CF5 commodity-futures `matching` pairs by (deliverable, expiry, struckAt, notional) → two contracts with any struck difference never net; must key by (deliverable, expiry) and net by notional with variation settled (BF4 class).
- IF1 index-futures/index.ts: the hedger shorts its ENTIRE share book (units × price of every listed line) every period; no exposure rule, no partial hedge; identical to CF1/BF2.
- IF2 index-futures: series list unbounded per period (CF2 class).
- IX1 indices/index.ts `goodsBoughtIn`: re-walks the WHOLE ledger on EVERY `view.index` read (called by every hedger, every research analyst, every futures pricer); must be one per-period walk stored in `working` state keyed by period.
- IX2 indices/baskets.ts `sizeSegmentOf`: sorts every listed line by capitalisation on every call, uncached; called per party per period; cache per period.
- IX3 indices/benchmark.ts `benchmark(ccy, secured)`: walks ALL `moneyMarket.print` events ever (ofKind) per (ccy, secured) read; must be `ofKindIn(kind, period)` on the last printing period or a journal lastOf index.
- IX4 indices `listed()`: walks all instruments per rule per period; one pass per period building all baskets.
- IX5 indices A-level: index level is recomputed from constituents every read; check no store of a level (FORBID "no stored index level") — currently honoured, but the recompute is the IX1/IX2 cost; fix the cost by caching per period, not by storing a level.
- SL1 securities-lending/index.ts `lendable`: builds a ParticipantView per holder per line per period (O(holders × lines) view constructions, each an allocation with closures).
- SL2 securities-lending `receivedOn`: walks the period ledger per loan and matches `reason.includes(instrument)` — string parsing of a reason (A-52); must read the instruction's typed legs / instrument field.
- SL3 securities-lending `lienFor` matches `reason.includes` — same A-52 class; liens must be looked up by lien id stored on the loan row.
- SL4 securities-lending: cells cannot lend (only named parties) so the lendable pool is almost empty; short-selling by funds (Hedge Funds C1) is starved by this, not by demand.
- SL5 securities-lending `due` keyed by period index: a loan is due at `openedAt + tenor` in periods, so the period counter is a hidden calendar (Law 8); date from the calendar.
- SZ1 securitisation/index.ts `moneyOf(bank)`: first registry currency for every bank → non-first-currency banks never securitise (CD3 class); read the bank's home currency from its profile.
- SZ2 securitisation `interestCollected`: walks the WHOLE ledger per deal per period; O(deals × ledger). Must read the period slice (`ledger.inPeriod`) once and bucket by deal.
- SZ3 securitisation `noteBids`: builds a ParticipantView per other bank per deal per period.
- SZ4 securitisation `windUp`: gating on `cease` event presence via ofKind scan; the deal state should carry its wind-up flag.
- SZ5 securitisation: pool loans are named rows (correct, XI-11) but the servicer fee is a fixed fraction declared as SHAPE with no death (placeholder without a named mechanism).

### mechanisms/short-term-debt, small-business, trade-credit
- ST1 short-term-debt/index.ts:307 `costOfBorrowing`: an issuer issues ONLY if it has a `credit.quoted` this period or appears in some `bank.reservation.required[issuer]` this period. With BK3 (reservations carry no obligor → `required` is one number for every name) and BK30 (reservations published late) this is either always the same number for every issuer or absent. Banks are never quoted credit → a BANK never issues paper: A3 ("the state, a bank, a firm issue the same instrument") is dead for banks by construction. Fix: an issuer's alternative is its OWN last borrowing cost (its live loan rows' rates, or its own last cleared paper yield), read off its register/journal, not a fresh quote.
- ST2 short-term-debt/index.ts:555 `grantBackstops`: EVERY alive firm and bank is granted a committed line every period regardless of whether it ever issues paper, and `ctx.participant(p.id).equity()` builds a ParticipantView + full valuation per party per period just to size it. B4 says an ISSUER keeps a backstop. Grant on first `paper.offered`, size once, re-size on the lender's decision (17.2).
- ST3 short-term-debt/index.ts:639 `drawBackstops` — A DRAW IS A GIFT. Money moves bank→issuer, `terms.drawn` rises, but the agreement's `owed` stays 0, no interest accrues on the drawn amount, no repayment is ever scheduled, and nothing ever reduces `drawn`. The bank has lost the money and holds no claim in the estate (Law 5: one economic side; Appendix B: no liability without…). Fix: a draw creates/increases a loan row (the banks module's loan instrument) at the line's agreed rate with a maturity; the agreement's `drawn` is a READ of that row (Law 19), never a second store.
- ST4 short-term-debt phases: `paper.backstop` is anchored `after: markets`, but maturities are collected in `corporateActions` which runs BEFORE markets. So the maturing paper has already been presented and FAILED (recorded as a missed payment → `defaultOn` → `accelerates: true` → the whole balance sheet is accelerated) before the backstop is drawn to meet it. B3.b is inverted: the run always ends in default first and the draw arrives after the default. Fix: draw in `corporateActions` BEFORE the kernel presents dues (anchor `{ before: 'corporateActions' }` or a pre-dues hook), and only against the shortfall at that moment.
- ST5 short-term-debt/index.ts:397 `buys`: per (market, buyer): `view.equity()` (full valuation of all holdings), `exposureTo` (walk all holdings), `onDeposit` (walk all banks). With P paper lines × B cash investors = P×B full valuations per period. Fix: a per-(party,period) cache of equity and of exposure-by-issuer on the view (the same cache serves banks, funds, insurers); `onDeposit` computed once per (ccy, period).
- ST6 short-term-debt/index.ts:702 audit `units` family: a defaulted line stays `live` with outstanding > 0 forever until the estate winds it up → the E3 violation fires every period for every defaulted line. Not wrong, but the family measures "estate not yet run", not E3. Exclude lines whose issuer is in estate / whose default is recorded.
- ST7 short-term-debt: `SENIOR = 1` and `'ACT/360'` are literals in a mechanism (allowed: 1), fine; `PAPER_DAY_COUNT` should be a term on the paper (terms.dayCount already is) — one writer.
- ST8 short-term-debt C1: BANK as a cash investor prices against the keenest DEPOSIT board of OTHER banks (`onDeposit`) — a bank's alternative to holding paper is the central bank's deposit facility / the overnight market, not a rival's retail board. The buyer participant should read its own kind's alternative (a profile hook), not one reason for every kind.
- SB1 small-business/index.ts: the module has NO phases, NO participants, NO seed holdings — cells exist as parties with a weight and nothing else (no cash, no inventory, no labour, no loans). COVERAGE marks §42 A1/A2/A3/A5/A6 MET on existence alone; A1 says "it sells, employs, borrows and can fail" and none of the four happens. `fails: ['cash','solvency']` can never trigger (no flows). Steps 11.5–11.10 must be real items in IMPLEMENTATION.md with the mechanism each turns on: sales on terms into the goods market (buys inputs from named firms, sells to household cells in its region), employment (a labour venue per cell), a loan row per (bank, cell) sized by the bank's `creditDecisions`, default from cash flow via the kernel's missed payment, promotion at A5's size.
- SB2 small-business/data.ts:331,342 `SMALL_PER_NAMED = 12`, `CELLS_PER_KEY = 4`: numeric literals in a mechanism outside the parameter register (CLAUDE.md: no numeric literals outside core/registry; XI-14: every behaviour-shaping number declared with kind/unit/owner). Declare as params (SHAPE with death, RESOLUTION).
- SB3 small-business/index.ts:279 cell display name is built from the bank's ID string (`at ${r.bank}`), not its display name (Law 9).
- TC1 trade-credit/index.ts:69 `freeRow`: scans n = 1.. until an unused id, per SALE; instruments are never deleted so this is O(invoices ever between the pair) per sale → quadratic over the run. Fix: name the row by (seller, buyer, period, sequence-in-period) with the sequence a per-period working counter, or ask the register for `countIssued(seller,buyer)`.
- TC2 trade-credit/index.ts:197 `letDown` → `overdue(seller)` walks ALL of the seller's holdings per sale; O(sales × holdings) per period. Cache `overdue` per (seller, period) in working state.
- TC3 trade-credit `invoiceKind` has NO `defaultOn` and `due()` presents the maturity only in the period containing the due date. A buyer that cannot pay on the day: the kernel records a failed payment, and then the invoice is NEVER PRESENTED AGAIN (`due` returns [] every later period), no default event is raised, the buyer stays alive and the seller carries the row at cost forever (an asset that will never pay, Goods E2 write-down never triggered, D1 "late" has no reader that changes a value). Fix: (a) `due` returns the maturity in every period from the due date until redeemed (a past-due bill is due every day), (b) `defaultOn` = missed payment by the buyer (a missed trade payment is XI-1's loss event and B3's stop-shipment trigger; whether it accelerates is a policy of the kind — spec Trade Credit D2/D4 says the seller tightens, the estate opens only on insolvency), (c) the seller's write-down of an aged receivable is a REASON the seller has (D1) — an outlook read on its own experience of the buyer, written through the same lot write-down door as inventory.
- TC4 trade-credit `shipsOnTerms`: terms are offered on EVERY firm-to-anyone goods sale unless the buyer is overdue; the buyer may be a HOUSEHOLD CELL (a cell issuing an invoice = consumer credit, §Households, not built, and a cell as an ISSUER of a liability instrument is a representation the register was not designed for — verify the kernel refuses). Restrict `termsOffered` to buyers whose kind profile `borrows` on terms (firms, small firms), and make the seller's decision depend on its own cash position (a seller short of cash ships for cash: B2).
- TC5 trade-credit A4 "the tier that lives on trade credit" — small firms neither buy nor sell so no invoice ever names one (SB1).
- TC6 trade-credit: no discount for early payment, no interest on late payment, one convention (A3 30 days) for every trade — acceptable as stated technology; but the days are the SELLER's term and should be a per-seller preference the seller sets (its own cash position), not a world constant (Law 15 data; §46).

### mechanisms/insurers, housing
- IN1 insurers/index.ts:252 `quoteCover`: the BUY side of cover does not exist (comment says "item 14's") → every cover session is `noDemand`, no policy is ever written, no premium ever paid, `claimsSeen` is always over zero cover, `longestPromise` is always undefined → the insurer has no liabilities, no duration, and B2.b (the clause "this exists for") is not exercised in any run. COVERAGE marks Insurers A4/B1/B2/B2.b MET on code that never runs. Fix: a buyer of cover — a firm standing in a physical fact (Insurers B4: a mill that would rather not carry its fire/flood/weather exposure) bids at its own outlook of the loss (Environment events give the fact), a household cell bids for life/pension cover out of its retired-cohort need; and a CLAIM event: an environment event that destroys plant/inventory of a covered party pays the schedule.
- IN2 insurers/index.ts:297 `claimsSeen` reads ONLY the LAST claim (`lastOwn(CLAIM_PAID)`), not claims over the cover written — "what a unit of cover has cost it" is the last claim over ALL cover ever written. Must be a run of claims over the same window as the cover (a §46 outlook on its own claim history).
- IN3 insurers/index.ts:259 `required`: `capital/(capital+surplus)` where capital = `owedIn(ccy)` (a money amount) is called "what its capital costs" — a leverage ratio is not a cost of capital, and `coverPrice` adds a RATIO to a PerPiece via `asPerPiece(requiredOnCapital)` (unit smuggling: a share becomes a level "per unit of cover"). A4.b's required return on capital is the insurer's own cost of equity (XI-4 cost of capital read) times capital held per unit of cover.
- IN4 insurers/index.ts:606 `coverTerms`: every policy promises ONE unit of money at ONE date 52 periods out — the "schedule" is a single zero-coupon; the sector's duration is a constant one year and the pension book (§Insurers A1 "a schedule of stated amounts at stated times", decades) does not exist. Pension liabilities to retired cohorts (a schedule per cohort over its remaining life, read from the households cohort table) are the missing mechanism.
- IN5 insurers/index.ts:582 `sellerOf`: the first sell fill from another party is paired with every buy fill regardless of quantity — with two insurers the second is never the writer; with a fill larger than the seller's, the seller issues cover it did not sell. Pair fills by the solver's own allocation (fills carry counterparties per Clearing D2 or the module must allocate pro rata).
- IN6 insurers/index.ts:537 `policyIdFor(insurer, holder, period)` → one instrument per (insurer, holder, period) with `issue` only if absent, then `settle` asset legs from insurer → holder: the insurer must HOLD the policy units first to deliver them (an `asset` leg from the issuer) — verify the kernel allows an issuer to deliver units it has not been issued (`issued` is set at issue?). If `ctx.issue` issues 0 units and the asset leg moves qty from the issuer, the register throws (no short). Probable runtime throw the first time cover clears.
- IN7 insurers/allocate.ts:705 `doors` + `heldIn`: per insurer per period walks all venues, `journal.lastOf('fund.struck')` per fund, and `instruments.issuedBy(fund)` per door TWICE (feedTheSmallest and meetCalls). Fine at N=insurers small; but `feedTheSmallest` (feed the door held least of) with ONE subscription per period means diversification takes `doors` periods and the allocation is always into the SMALLEST at the fund's LAST struck NAV — a fund that struck once and died is still a door (`v.key.kind==='fund'` with no liveness check).
- IN8 insurers/allocate.ts:762 `investable`: the buffer is "the last claim" and "the last call" — two `lastOf` reads; both should be the insurer's outlook over its own history (§46), not the last observation.
- IN9 insurers/allocate.ts:818 `meetCalls` walks `journal.forSubject(CALLED_KIND, insurer)` — all calls ever — every period to find last period's. `lastOwnSince`/`ofKindIn`.
- IN10 insurers seed: one insurer per region with `[...ctx.parties.all()].find(bank)` — O(parties) per region, trivial; but the insurer has NO opening capital (no holdings seeded) → `view.equity() <= 0` → `quoteCover` returns [] forever even if a buyer existed. The seed must give it capital (a share issue subscribed by household cells, or an opening equity from the estate of the seed, Seed B1).
- IN11 insurers `derive`: policy PV via `curve.priceOf(schedule)` at the sovereign curve — B2 says "a rate read from a market"; sovereign curve is fine; but with IN4 the "duration" is a single point. Also `fairValueThroughIncome: true` + `owes: 'value'` → the insurer's equity moves with the curve; correct, but never exercised (IN1).
- HO1 housing/index.ts:1541 `askForMortgages`: `if (cell.representation === 'cell') continue;` — EVERY household is a cell, so NO household ever asks for a mortgage; the entire mortgage path (askForMortgages → banks read `housing.funding` → loan → `charge` → `foreclose`) is dead code in every run. COVERAGE marks Housing B1/C1/C3/C4 MET. The comment blames "13f" (missed-payment servicing); the honest state is: households do not borrow. Fix: remove the skip once the household budget services debt (Households E3/E4 — item), and until then mark C1/C3/C4 MISSING.
- HO2 housing/index.ts:1460,1643 `String(sec.instrument).startsWith('good.dwelling.')` and :1658 `lien.reason.includes(String(i.id))` — kind detection and lien lookup by STRING PARSING of ids and reasons (A-52 class). The loan's `security` rows should carry the instrument id and the lien id; the lien should carry `secures`.
- HO3 housing/index.ts:1639 `foreclose` walks ALL instruments every period (`instruments.all()`) to find mortgages of dead borrowers; index by kind (register `ofKind(LOAN)`) or by the estate event of the period.
- HO4 housing/index.ts:1589 `charge` runs for every alive party every period: builds a ParticipantView per party (`ctx.participant`) and calls `mortgagesOf` (issuedBy walk) — O(parties) views per period for a path that (HO1) can never have a mortgage. Gate on `housing.funding` having been answered.
- HO5 housing/index.ts:1823 `publishShortfall` + :1267 `ordersOf`: `needs`, `owned`, `taken`, `letOut` each re-walk `view.commitments()` (all the party's agreements) and are called up to 6 times per cell per period; `needs` does `rows.find` per call. Compute once per (cell, period).
- HO6 housing/index.ts:1242 `reservation`: a tenant bids its WHOLE expected income per member over its dwellings-per-member — i.e. 100% of income for rent ("the alternative is nowhere to live"). With `marginalBid` pricing and owners asking at wear (spoilage × price), rent clears at the marginal TENANT's full income whenever dwellings are short, and at wear when long. Between those, no budget: a household paying all its income in rent buys no goods → the goods market loses its demand. B1.a's "reservation" for a tenant is what is left after the consumption basket (Households E1/E2: the budget is one decision), not gross income.
- HO7 housing/index.ts:1345 `letIn` allocation: tenants sorted by bid, owners by ask, walked one dwelling at a time — but the solver already produced pro-rata fills; re-allocating fills by a second rule (best bid housed first) contradicts the `proRata` outcome (two allocation rules for one book; Law 4). Use the fills as the solver struck them.
- HO8 housing/index.ts:1389 `collect`: rent settles per lease every period at the struck rent; a tenant that cannot pay "fails to pay and the tenancy stands" forever — no arrears row, no eviction, no landlord decision (Housing A2/C-something: a landlord ends a tenancy after non-payment). A tenancy whose rent has failed N periods must be ended by the landlord (a REASON: the landlord's own outlook of that tenant), and the arrears are a claim in the tenant's estate.
- HO9 housing: tenancies are struck "once and the tenant still pays it" (A2) — but ALL leases are re-signed every period? `ordersOf` bids only for `short` (needs − owned − taken), so a housed tenant does not re-bid: correct. But a lease never ENDS except by death → rents never re-strike for sitting tenants → a rent print reflects only the marginal newcomers. Housing A3 (a tenancy has a term and is re-let at the end) — missing: a term on the tenancy (`TenancyTerms.until`) and re-letting at expiry.
- HO10 housing/index.ts:1075 `DWELLING_WEEKS` unit at TONNE_PIECES: occupancy counted on the tonne grid; `needs` converts dwellings-per-member × weight into pieces of the DWELLING good unit (`goodUnitOf`) not `DWELLING_WEEKS` — the venue's unit and the orders' unit differ (Law 8 unit-in-the-number). Verify `ctx.post` unit checks; if the venue unit is dwellingWeeks and orders are in dwelling pieces the quantities are on different grids.
- HO11 housing seed opens a venue per region only if the dwelling good exists (`instruments.has(goodId(DWELLING, region))`) — depends on goods seed order; `requires: ['goods','households']` should guarantee it, but a region with no builder has no dwelling line and its households never rent (silently). Report as a seed finding (a region with people and no dwellings).

### mechanisms/land, merchants, freight
- LD1 land/index.ts:197 the treasury sells ALL its unbuilt ground every period at ONE CENT (`price: CENT_TICK`) with `proRata` rationing: a firm bidding 1 cent (the "no opinion" bid at :251) is filled at the treasury's ask under `sellersCompete`? — the market's pricing rule is the market decl's (`rationing: 'proRata'`, pricing default). If the print is the marginal ask, land is printed at 1 cent whenever demand < supply, which it always is (the state holds a continent): LAND IS FREE BY CONSTRUCTION, the module's own stated purpose ("free land, which is the thing this module exists to remove") is not achieved. The state's reservation must be a REASON: its own outlook of what ground fetched last (§46) or the rent it forgoes; and the supply per period is not "everything it holds" but a release decision (planning consent, E-5) — without one, the price cannot rise.
- LD2 land/index.ts:187 the treasury sells only in its own region (`m.instrument !== landId(view.self.region)`) → every region other than the capital has ground held by the state and NEVER SOLD; firms there cannot buy ground; if plant needs ground (capital-programme reads `landPerUnit`) they build anyway (LD3) — the land constraint binds nowhere.
- LD3 land: nothing in capital-programme REFUSES to build on ground the firm does not hold (the header says a firm "buys the ground first" but `groundUnderPlant` is a read after the fact; `short` is what it "has built and not bought"). Land is a post-hoc purchase, not a constraint on building: Capital Programme A2/C1 as implemented is "buy after". Fix: `capital-programme` plant start requires free hectares held (a pledge of land to the plant), else the batch is not started.
- LD4 land/index.ts:319 `endowUnits(treasury, id, total×HECTARES_PER_KM2, 0)` with basis 0: the largest asset in the world carried at cost 0 — stated as deliberate (Seed C4); but then a sale at 1 cent (LD1) prints a price and `carry: 'cost'` means the state's remaining ground stays at 0 while the firm's is at 1 cent. Acceptable per Law 3 only once LD1 is fixed.
- LD5 land: `HECTARE = unitId('km2')` named "hectare" but the id is km2 and perUnit HECTARES_PER_KM2 — the unit's NAME is square kilometres, its piece is a hectare; fine, but the constant's name misleads (Law 16).
- MR1 merchants/index.ts:485 `marketsOf`: walks `view.instruments.all()` per merchant per cycle to list portable goods in its region; O(merchants × instruments). One per-period index of portable goods by region on the goods module (published) or in the registry.
- MR2 merchants/index.ts:412 `dearest` → `expected` per region: per merchant per market per cycle an outlook read + print read for every region; O(merchants × goods × regions). Cache `dearest(subUnit)` per (merchant, period).
- MR3 merchants/index.ts:447 bid = dearest × (1 − margin) with NO freight cost and NO transit time: the merchant bids the far price less its margin; freight is paid later from the same cash (comment says "it cannot pay for the passage and the cargo stays"). A merchant that always over-bids because it ignores freight is systematically wrong; §46 says a party's outlook forms from its own history — the merchant should carry an outlook of the freight rate on that leg (`freight.session` prints `rate` per leg: read `lastPublic` of it) and bid dearest − expected freight − margin.
- MR4 merchants: a merchant SELLS in the far region at basis × (1+margin) as a limit ask; if the far market clears below it the cargo sits (carrying storage cost: commodities storage venue) — fine. But `basisPerUnit` returns on the FIRST holding matching (loop with `return` inside; fine) and includes freight in basis (freight A-68 fix) — good.
- MR5 merchants: `speculative: true` participants; but the merchant's `appetite × cash` per line is spent in EVERY portable line in its region simultaneously (no budget across lines: `appetite` is per line, so with 20 lines it bids 20 × appetite × cash — over-commitment; the wire refuses the later fills (silent partial)). Budget must be one per period across lines (the same defect as households F-class).
- FR1 freight/index.ts:168 `toShip`: for every origin, builds a ParticipantView for EVERY alive party in the region (`ctx.participant(p.id)` including all household cells) and walks all its holdings, each period — O(parties × holdings) view constructions, then `shippers` builds ANOTHER view per shippable per destination (`ctx.participant(s.party).print(there)` :207) — O(shippables × legs) views. The doc says this was 30 s/period before caching by origin; it is still the dominant cost with many cells. Fix: read the register's holders-of index for portable goods in the region (`register.holdersOf(good)`), skip cells (they do not ship), and read the far print once per (good, region) from `ctx.prices.latest`, not through a participant view.
- FR2 freight/index.ts:203 shippers: EVERY holder of a portable good bids to ship ITS WHOLE FREE HOLDING to every region where the print is higher, at the full gap, every period — a farm with grain bids on all N−1 legs simultaneously for the same tonnes (over-commitment; the first leg to load takes them and the rest fail at `cargo`). A producer is not a merchant: it should ship only what it is not selling at home (its own unsold stock after the home session, C1.a), on the ONE dearest leg, with the gap less its outlook of freight.
- FR3 freight/index.ts:279 `load` pairs fills by walking carriers in order — the pro-rata fills of the solver are re-allocated by a second rule (same as HO7); and `cargo` :319 walks ALL of the shipper's holdings per pairing.
- FR4 freight/index.ts:330 `Math.ceil` in a mechanism (CLAUDE.md: no Math.* outside core/num.ts) and :438 `Math.exp/Math.pow` — put `upTick`/`attenuation` in core/num.
- FR5 freight/data.ts:674 `VESSEL_HOLD = 25000`, `VESSEL_WEAR_PER_UNIT_KM`, `usefulLifePeriods: 1300`, `buildLagPeriods: 26`, `standsWind: 5`, `windHardness: 8` — numeric literals in a mechanism data file; the first two are then declared as params (fine), the CapitalKindDecl numbers are registry-style data in a mechanism — declare in the registry (the same rule the other CapitalKindDecls follow) or tag the file as registry data.
- FR6 freight/index.ts:126 `costOf`: carrier's reservation = hull wear per unit-km × km + (wear per hull / hold) × days — crew is priced as HULL WEAR per day ("the crew is paid per DAY" but the arithmetic uses `wear.value` = plant wear per unit, not a wage). There is no wage in the freight cost: carriers are firms with employees (labour module) — the crew cost should be the carrier's own labour cost read from its payroll, not a second copy of wear. Law 4 (one writer of wear) and a missing read.
- FR7 freight/index.ts:433 `sail`: a voyage in a gale "crawls and never stops" — fine; but "takes a share of the hulls caught in it WITH THE CARGO ABOARD on two named books" (header) — no code here destroys hulls or cargo in a storm; verify `voyages.ts`/environment applies losses. If not, Freight B4 loss-at-sea is a claim in a docstring.
- FR8 freight: `session` runs for EVERY ordered pair of regions each period (N² legs) even when nothing is shippable from the origin (guarded by `mine.length === 0`) — fine; but `legs(ctx)` recomputes `legsBetween` (path-finding over the map) EVERY PERIOD (:100, :592). Paths depend only on the map and VESSEL: compute once at assembly (registry) and cache.
- FR9 freight: a shipper's transit good `inTransit(subUnit, from, to)` must pre-exist (`instruments.has(transit)` else skip silently) — who creates transit lines? If the goods seed creates them per (good, from, to) that is N²×G instruments at seed. Verify; if missing for a pair, freight on that leg silently never loads.

### mechanisms/commodities, environment, corporate-bond, sovereign-instruments, sovereign-curve, central-bank-omo
- CO1 commodities/index.ts:100 `schedules`: builds a ParticipantView for EVERY alive party in the region (household cells included) each period and walks ALL holdings twice (`roomFor`, `perUnitWorth`), plus `vintagesHeld` per party. A cell holds no goods that take space (or if it does, it cannot let). Restrict to parties whose kind profile produces/holds storable goods (a registry read: `PRODUCING_KINDS`) and compute both reads in one walk.
- CO2 commodities/index.ts:105 a party with spare space LETS it and never TAKES; a party short TAKES at the worth of its dearest good — a taker's bid is the mark of its DEAREST good per space unit for ALL its shortfall (it pays grain-storage rates for gravel). A bid schedule per good (a step of (worth_g, space_g) orders) is what a real taker posts.
- CO3 commodities/index.ts:200 second allocation walk over `outcome.fills` (HO7/FR3 class); and a taker whose payment fails skips the letter (`at += 1`) — the letter's remaining space is skipped for every later taker too (bug: `at` advances past a letter with room because ONE taker could not pay).
- CO4 commodities: `commodities.leased` event per taker is read by `registry/physical.ts:rentedRoom` — the kernel registry reads a MODULE's event kind by string (the kernel depending on a module's event name; 4.9b inversion). The rented room should be an agreement row (XI-8: a lease is an Agreement) the kernel reads structurally.
- CO5 commodities/data.ts:349-356 `usefulLifePeriods: 1040`, `buildLagPeriods: 4`, `standsWind: 4`, `landPerUnit: 0.15`, `windHardness: 6` — numeric literals in a mechanism (FR5 class).
- CO6 Commodities Spot as a SYSTEM: this module builds STORAGE only. Spot A1/A2 (a spot market per commodity per place with named producers and consumers) is the goods module; B (supply shocks) is environment; C (inventory as state) is goods; D2/D3 here; E (the E4 chain: commodity shock → margins → inflation → policy) has no reader; F (verify) partially. COVERAGE should mark Commodities Spot as spread across goods/environment/commodities with named clauses, and E4 as MISSING (no policy reaction reads a commodity print).
- EN1 environment/state.ts:559 `moveOn`: an AR(1) in log space — correct and bounded by arithmetic; but `1 - climate.persistence` scaling of the fresh draw makes the stationary variance depend on persistence in a non-standard way (variance = swing²/3 × (1−p)²/(1−p²)); harmless but undocumented (Law 16: say why).
- EN2 environment: the weather is PUBLISHED per region per fact — but who CONSUMES it? Freight (`conditionsFor` WIND in `sail`), goods recipes (GROWING via `registry/environment`), households (WARMTH?), insurers (B4 — nothing reads it: IN1). Verify each declared consumer reads it; the `Insurers B4` citation on this module is a claim about a reader that does not exist.
- EN3 environment/data.ts:768 `CLIMATE_CELLS = 3` is a RESOLUTION (its comment says so) but is a literal, not a param (XI-14: resolution tested by invariance needs a name in the register).
- EN4 environment/index.ts:478 `ctx.state<Weather>` keyed store with `written` guard — the "one writer once a period" guard is a symptom of phases being able to run twice (cycle 0 vs anchor); the kernel should guarantee one run per (phase, period).
- CB1 corporate-bond/index.ts:242 `wouldHold` reads `bank.reservation.required[issuer]` — with BK3 (no obligor in the reservation) every issuer's "keenest requirement" is the same number, so the coupon of EVERY corporate bond in the world is the same rate and the covenant test is credit-blind at issue. Same root as BK3/ST1.
- CB2 corporate-bond/index.ts:254 `cheaper = !quoted.some || keenest < quoted.rate`: a firm NOBODY quoted issues in the market whenever any bank has published a reservation — and after MM16 no bank quotes anyone, so EVERY firm with a `shortTerm` gap goes to the bond market every period, opening a new line per maturity (one line per issuer per period since `maturity` = today + 60 months moves every period → a NEW instrument and a NEW market per firm per period: unbounded instrument/market growth, CP4 class). Fix: tap the standing line when one exists within the tenor band (nearest maturity within ±k months), not a new date each period.
- CB3 corporate-bond/index.ts:376 coupon = `keenest` (the holders' required yield) — a bank's reservation is a yield on its OWN book with its own funding; a coupon "roughly par where a book would start" is the sovereign curve at tenor + the keenest spread; here the coupon is the flat required yield (which after IR6/BK-class bugs may be 0 or absurd). Acceptable as SHAPE if the reservation were per-obligor (BK3).
- CB4 corporate-bond/index.ts:454 covenants = the firm's own ratios AT ISSUE ("no worse than today") — every bond has a different covenant and the tightest possible one; a firm that issues at its best quarter breaches on the next ordinary quarter → `covenant.breached` fires for most issuers within a year, and `SOURED` (ST) then shuts them out of paper and (banks) out of loans. Covenants need headroom which is a NEGOTIATION outcome — a PLACEHOLDER with a named mechanism (Corporate Credit B2 headroom as a holder's term in its bid) not a silent tightest-case.
- CB5 corporate-bond/index.ts:535 `testCovenants` walks ALL instruments per period and `alreadySaid` walks `forSubject('covenant.breached', bond)` per bond — O(bonds × breaches). Index live corporate bonds (register ofKind) and keep the last-tested quarter on the instrument's working state.
- CB6 corporate-bond: NO participant BUYS corporate bonds here — buyers are banks (`banks/index.ts` reservations) and funds (mandates). Households never hold corporate paper directly (fine). But with BK21 monoculture and FD-class starvation, corporate issues clear only if a bank bids; `noDemand` is the usual outcome. Corporate Credit C2–C6 (book-building, allocation, syndicate) are marked MET on the kernel's uniform-price auction; there is no syndicate/lead manager (C2.a "an indication" is an order), no allocation rule other than pro-rata. PARTIAL at best.
- CB7 corporate-bond: Corporate Credit D (secondary trading, dealers with inventory in credit), E1–E4 (spread as derived, credit curve), F (ratings interplay), G3–G5 (restructuring, recovery) — not in this module; verify COVERAGE marks. G2 acceleration is a kind flag read by the kernel; G4 (workout) — nothing.
- SI1 sovereign-instruments: profile only; correct as far as it goes. `accelerates: false` and "no estate" — the sovereign default consequence (exclusion from the market, Sovereign G3/G5) has NO mechanism: after a sovereign misses a payment nothing stops it auctioning next period (verify treasury module: does the auction refuse a defaulted issuer? if not, Sovereign G5 MISSING).
- SC1 sovereign-curve/index.ts:914 audit family walks ALL instruments every period and reads two prices per sovereign line: O(instruments) per period for a check that only concerns sovereign lines — `issuedBy(treasury)` index.
- SC2 sovereign-curve: curve built at every read (`ctx.curve` fits from prints) — every `curve.at(years)`/`priceOf(schedule)` call (insurers derive per policy per revaluation, banks, funds) REFITS the curve. Per-(family, period) memo of the fitted curve is a pure cache (no stored level: the fit is from the period's prints and is discarded with the period) — Law 18 layout-only.
- OM1 central-bank-omo/index.ts:1052 `new Set(view.parties.ofKind(TREASURY))` built per market per period per central bank; and the participant is asked about EVERY cleared market in its money (goods, shares, bonds) and returns [] for all but sovereigns: use `markets:` filter to name only sovereign lines.
- OM2 central-bank-omo/index.ts:1057 target = `targetShare × issued` of EACH LINE, with `price: 'market'` on the BUY side: a price-taking buyer of 25% of every sovereign line — this IS demand added to clear (Appendix B "no buyer of last resort"?) — the spec (C1, C3) allows a quantity at market as POLICY; but the target share is a POLICY primitive held constant → the central bank buys 25% of every NEW line at any price the book strikes, forever, funding a quarter of the deficit with created money in the SECONDARY market one period after issue (C1.b honoured in letter, not in substance). Policy must MOVE (the polity/central-bank reaction function, §Central Bank B: a rate decision from its own reading of prices) — until then this is a written policy path (Appendix B).
- OM3 central-bank-omo/index.ts:1085 `remit`: walks the ledger for EVERY period since the last remittance (12 months = 52 periods) and every settled instruction's equity legs — O(52 × ledger-per-period) once a year; acceptable, but the equity delta per party per period should be a ledger index (`ledger.equityDeltaOf(party, period)`) used by reporting too (RP2 class).
- OM4 central-bank-omo/index.ts:1171 `lastRemittance`: `ofKind('centralBank.remittance')` full scan + filter + sort EVERY period for each central bank. `lastOf(kind, cb)`.
- OM5 central-bank-omo/index.ts:1143 `dueThisPeriod` loops `period+1` times advancing months from the epoch every period — O(period) per period (quadratic over the run) for a calendar question the calendar can answer directly (`calendar.isAnniversary(epoch, months, period)`).
- OM6 central-bank: Central Bank A (the policy rate as an ADMINISTERED rate with a real quantity response: deposit facility / lending facility both sides booked), B (the reaction function), D (LOLR with the four classical conditions: solvent, good collateral, penalty rate, open to all) — where are they? The money-market module has a standing facility? `CLAUDE.md` says LOLR has all four conditions; verify which module implements Central Bank A/B/D and whether the policy rate ever CHANGES in a run (if it is a constant param, §Central Bank B is MISSING and "no policy set directly or by path" is violated by a constant path).

### observer/observer.ts, world/world.ts (kernel) — second read
- CORRECTION to earlier perf findings: `World.participantView` (world.ts:1420) memoises ONE view per party per (period, cycle), and `mechanismContext` (:1723) one context per owner per cycle. So "builds a view per X" findings (DL10, FXD1, SL1, SZ3, ST2, HO4, FR1, CO1, IN7) cost a Map lookup after the first build in a cycle, not an allocation. The real cost in each of those is the READS made through the view (holdings walks, `equity()`, `issuedBy` walks, `journal.ofKind` scans), which are NOT memoised. Each of those findings stands with the cost re-attributed to the reads.
- OB1 observer.ts:863,870 `housing`: `journal.ofKind('housing.rent')` and `ofKind('housing.foreclosed')` full scans per region per snapshot; `ofKindIn(kind, period)`.
- OB2 observer.ts:1071 `indicesOf`: `ofKind('fund.launched')` full scan per snapshot; :1152 `statementsOf`: `ofKind('research.estimate')` and `ofKind('research.surprise')` full scans PER COMPANY (O(companies × events)). Index by subject (`forSubject`) or by (kind, period).
- OB3 observer.ts:983 `hedgesOf`: for every alive party × every pair calls `hedgedResidual(view, base, quote)` — with cells included that is O(parties × pairs) contract walks per snapshot; skip parties with no contracts and no foreign balance (a register read) before computing.
- OB4 observer.ts:618 `positions`: every holding in the world valued via `valueOfLots` per snapshot (fine for inspector; a party scope should walk `holdingsOf(party)` not `allHoldings()` then filter).
- OB5 observer.ts:30-41 the OBSERVER imports MECHANISM modules (`mechanisms/firms/data`, `mechanisms/labour/data`, `mechanisms/research/index`, `mechanisms/fx-derivatives/index`) — the kernel-side surface depends on four modules by import (ARCHITECTURE 4.9b: the kernel never imports a module). `consensusOf` and `hedgedResidual` should be `DerivativeClassDecl.measures`-style reads registered by the module; `OCCUPATION_OF`/`OCCUPATIONS` belong in the registry.
- OB6 observer.ts:688 `bookCurves`: derivative curves are grouped by PARSING the market id string (`split('.')`, `endsWith('y')`) — A-52 string parsing; the book's tenor is on its `ContractMarketDecl.contract.terms`, read it.
- OB7 observer.ts:713 `numOf` answers 0 for a missing bank field ("Missing is Missing"; the same defect item 3 fixed for statements): `reserves`, `liquid`, `couldLeave`, `base`, `capital`, `weighted`, `assets` all default to 0 → a bank that published no liquidity shows as one with none.
- OB8 observer: `snapshot` is called per period by the app — every one of the above is per period. The surface should be built incrementally from the period's own events (the journal slice) with the static parts (map, instruments) cached by `instruments.version`.
- WK1 world.ts:1237 `addPhase`: anchors are resolved at INSERTION against the phases already present → a module can only anchor to a phase of a module assembled BEFORE it; the assembly order (`requires` DAG) must therefore be a total order consistent with every anchor, which is why nine assembly blockers exist (FD19). Fix: collect all phase decls first, then resolve anchors by a topological sort over (anchor edges ∪ module order), erroring on a cycle with the cycle named.
- WK2 world.ts:1275 after every insert, the whole list is re-validated for cycle monotonicity: O(phases²) at assembly — trivial, but the check that "cycle ≥ last" means a `before: markets` phase with cycle 0 inserted before a cycle-1 phase is fine while an `after: corporateActions` cycle-2 phase throws if the next phase is cycle 1 — module authors are forced to pick cycles by trial; derive `cycle` from the anchor always (drop the `cycle` field for anchored phases).
- WK3 world.ts:1386 `step`: EVERY phase runs EVERY period (Audit C3) — there is no per-phase "nothing to do" short-circuit, and each phase's `run` typically walks all parties/instruments. That is by design, but the walks are the cost: the kernel should offer indexed reads (see WK6) so a phase with nothing to do costs O(1).
- WK4 world.ts:1526 `lastPublic`: `journal.ofKind(kind).filter(public)` full scan + copy on every call (used by every module reading a public print: MM benchmark, freight, indices, CF3). Must be `journal.lastOfKind(kind)` O(1) from an index the journal keeps (last event per kind).
- WK5 world.ts:2150 `classifyAsset.gradesOn`: `journal.forSubject('rating.action', obligor)` walk per classification; `classify` is called per instrument per mandate check per fund per period. Keep the latest grade per (assessor, obligor) in a journal-side index (or the ratings module publishes it as a read).
- WK6 world.ts:2368 `owedIn`: walks every instrument the party issued and asks the kind's `due()` for two periods; memoised per view (per cycle) — fine; but `due()` for a loan/bond schedule computes the calendar placement each time. A per-(instrument, period) due-cache in the register (invalidated by restate) would serve corporate actions, owedIn, cashDue and the audit at once.
- WK7 world.ts:2079 `curveAt` → `readCurve(... instruments: () => this.instruments.all() ...)` walks ALL instruments to find a family's lines on EVERY curve read (see SC2). `instruments.issuedBy(family.issuer)` exists — use it.
- WK8 world.ts:2749 `reach()`: for every market walks `prices.history(m.instrument)` from the start looking for the first traded print — O(markets × history) EVERY PERIOD (called from `reads()` for the audit). Keep "first traded period" per market in the reach tally when the print is written.
- WK9 world.ts:2782 `reads()`: `journal.inPeriod(period)` and `ledger.inPeriod(period)` filtered twice; trivial. But `moneyStock()` walks all instruments — fine.
- WK10 world.ts:1085 `remit` OM3 aside, `ledger.inPeriod(p)` for 52 periods: the ledger must keep per-period slices (it does: `inPeriod`) — verify it is an index not a filter over the whole ledger (if a filter, every `inPeriod` call is O(ledger) and the reporting module's 13-period walk is O(13 × ledger) per company).
- WK11 world.ts:478 markets phase sorts the market list every period (O(M log M), trivial) and filters by instrument liveness; then `runOne` per market asks EVERY participant decl × its parties → with `decl.markets` undefined for most decls (banks, funds, households?) every party of the kind is asked about every market: O(parties × markets) `orders()` calls per period — this is THE dominant cost of the period loop by construction (the comment at :1413 says 3,000 firms × 260 markets). Every participant decl MUST name its markets (`markets:`), and the kernel should make `markets` mandatory (a decl without it is refused at assembly) so the ask is O(Σ parties' interest lists).
- WK12 world.ts:1265 `market.noView` is recorded for every market with orders and no speculative participant — one event per market per period → journal growth O(markets) per period for a measurement; record once per period as one event with the list.
- WK13 world.ts: `enter` (:1832) sets equity to 0 for an entrant but `parties.add` with a `bank` that may not issue money in the entrant's currency is not checked here (freight seed checks it; `enter` does not) — an entrant banked wrong throws at its first payment, not at entry.
- WK14 world.ts:1447 `blindView` spreads the memoised view into a NEW frozen object per call (`{...open}`) — called per assessor per subject per period (ratings): allocation of ~60 properties per call; memoise the blind view per party per cycle too.
- WK15 world.ts:2308 `index()` cache key = `${instruments.version}:${prices.version}` — any print ANYWHERE invalidates every index read; during the markets phase (where prints happen) every `view.index` read after any market re-walks the whole basket. Key on the basket's constituents' print versions (per-instrument version) or freeze index reads to `indexThrough` (the previous close) which the comment says they already are — if reads during a period return the LAST CLOSE level, the shape key should not include this period's prices at all.
