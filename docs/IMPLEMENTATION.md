# Project Phoenix — the implementation

> **Parts 1–2 define dependency order; Part 4 is the complete executable implementation list.**
> Take the first open Part 4 point whose prerequisites are met. Tick nothing by hand.
> When a point closes, re-mark its row MET in `docs/COVERAGE.md` and regenerate Part 4 in the same
> commit; the generator removes it. A step names the change and the test. *Delete* names the read
> that replaces it.
>
> **NOTHING DONE IS IN THIS FILE.** Done is done: it is removed, and what it produced is the code.
> A plan that carries its own history stops being a list of what is left, and a reader who wants to
> know what the world does reads the world.
>
> **THIS IS WHERE A FINDING LIVES.** A finding sits under the item that should fix it, or is
> INSERTED as its own item at its dependency position — saying where. It leaves this file only by
> being placed, never by being deleted to make the file shorter.
>
> **AND WHAT THIS WORLD DOES NOT MEET IS IN IT — all of it.** Part 4 carries every MISSING and
> PARTIAL **requirement** in `docs/COVERAGE.md`, generated by `npm run plan:gaps`. Every clause owns
> one stable to-do point, ordered under the Part 1 milestone responsible for it. Every system block
> repeats the mandatory specification and production-code review that precedes implementation. An
> absence cannot be found by running the world or by a red test, so if it is not in the list it does
> not happen.

## Fixed weekly clock (implemented)

The kernel now uses `calendar::Week` for every executable timestamp. World stepping, sessions, journals, agreements, schedules, settlement queues, maturities and all mechanisms share that type. The former configurable epoch and days-per-step fields were removed from `RunConfig`; all declared execution horizons and payment frequencies are weekly. The former overnight funding and fixing paths are weekly-funding paths.

Civil Gregorian dates are confined to `calendar.rs`. Boundary callers use `Calendar::week_on_or_after`, which rounds an external date up to the first available weekly tick. Actual/360 and Actual/365 remain financial measurement conventions: they derive elapsed civil days as seven times the distance between weekly boundaries and never schedule execution.

Regression enforcement lives in `phoenix-check`, whose narrow allowlist permits `CivilDate` only in the calendar adapter and rejects the removed executable vocabulary elsewhere in production code.

## Part 0 — The measured state

> `npm run check` refuses a row citing a path that is not in the tree, a PARTIAL that names no
> item, and a table in 0.1 that disagrees with COVERAGE, so the state below cannot go stale in
> silence.

### 0.1 By spec system

`npm run check:existence` over `docs/COVERAGE.md`, which prints this live and holds this file to it
with `--verify`. A MET mark means a module cites the clause; it does not mean a run has confirmed it.

| system | MET | PARTIAL | MISSING | UNMEASURED | total |
|---|---|---|---|---|---|
| Money | 19 | 5 | 13 | 0 | 37 |
| Register | 16 | 3 | 7 | 0 | 26 |
| Clearing | 16 | 5 | 6 | 0 | 27 |
| Audit | 14 | 3 | 6 | 0 | 23 |
| **Seed** | **1** | 1 | **20** | 0 | 22 |
| Currency | 3 | 5 | 17 | 0 | 25 |
| Bond | 8 | 4 | 4 | 0 | 16 |
| **Derivative** | **3** | 5 | **10** | 0 | 18 |
| **Corporate Credit** | **4** | 6 | **52** | 0 | 62 |
| **Sovereign** | **7** | 6 | **38** | 0 | 51 |
| **Short-Term Debt** | **4** | 3 | **12** | 0 | 19 |
| Equity | 10 | 1 | 26 | 0 | 37 |
| **Money Market** | **6** | 0 | **22** | 0 | 28 |
| Spot FX | 12 | 4 | 11 | 0 | 27 |
| **Fund Shares** | **3** | 7 | **16** | 0 | 26 |
| **Securities Lending** | **2** | 1 | **18** | 0 | 21 |
| **Prime Brokerage** | **2** | 1 | **21** | 0 | 24 |
| **Derivative Layer** | **6** | 1 | **25** | 0 | 32 |
| **CDS** | **3** | 0 | **22** | 0 | 25 |
| **IRS** | **2** | 0 | **18** | 0 | 20 |
| **FX Forwards** | **1** | 1 | **19** | 0 | 21 |
| **Commodity Futures** | **2** | 0 | **18** | 0 | 20 |
| **Commodities Spot** | **4** | 3 | **17** | 0 | 24 |
| Indices | 9 | 1 | 12 | 0 | 22 |
| **Banks Lending** | **3** | 2 | **27** | 0 | 32 |
| **Banks Funding** | **3** | 0 | **29** | 0 | 32 |
| **Banks Capital** | **3** | 2 | **18** | 0 | 23 |
| **Dealer Desks** | **2** | 4 | **21** | 0 | 27 |
| **Insurers** | **0** | 1 | **22** | 0 | 23 |
| **Hedge Funds** | **1** | 2 | **21** | 0 | 24 |
| **Private Equity** | **3** | 1 | **21** | 0 | 25 |
| **Treasury** | **5** | 3 | **17** | 0 | 25 |
| **Central Bank** | **7** | 2 | **20** | 0 | 29 |
| **Polity** | **7** | 1 | **24** | 0 | 32 |
| Firm | 9 | 3 | 18 | 0 | 30 |
| **Capital Programme** | **5** | 2 | **18** | 0 | 25 |
| **Firm Birth** | **2** | 2 | **21** | 0 | 25 |
| **M&A** | **1** | 1 | **20** | 0 | 22 |
| **Trade Credit** | **2** | 3 | **17** | 0 | 22 |
| Goods | 12 | 1 | 26 | 0 | 39 |
| **Freight** | **0** | 1 | **19** | 0 | 20 |
| Labour | 7 | 1 | 19 | 0 | 27 |
| **Housing** | **1** | 4 | **21** | 0 | 26 |
| **Households** | **6** | 2 | **25** | 0 | 33 |
| **Small-Business Pools** | **2** | 1 | **25** | 0 | 28 |
| **Cross-Border** | **6** | 1 | **19** | 0 | 26 |
| **Ratings** | **2** | 7 | **14** | 0 | 23 |
| Reporting | 13 | 6 | 19 | 0 | 38 |
| **Observer** | **5** | 3 | **18** | 0 | 26 |
| Expectations | 21 | 0 | 6 | 0 | 27 |
| **Geography** | **0** | 0 | **42** | 0 | 42 |

### 0.2 What the world actually does

`npm run world:runs` prints it, every run: the census, the period cost, and the nouns with no kernel
home. It is not copied here, because a copy is stale the next time the world changes.

**That world is ARBITRARY and is declared arbitrary** — every number in it is drawn from a counter,
nothing in it was cleared or decided, and it is not a seed (5 E1). What it proves is that every module
is REACHED by the period loop, that the phase order holds at scale, and that what a mechanism proposes
goes over the ordinary wire. Item **13** replaces it.

### 0.2a Code-review baseline

The September 2026 review read every tracked source and configuration file, every production and
test module, the full specification, the coverage ledger, this plan and the architecture document.
The review was code-first: specification clauses supplied the mechanism questions and Laws 1–19
plus Appendix B supplied the acceptance rules; prose descriptions were checked against the source,
not used as evidence for it.

| source group | reviewed | result that changes the plan |
|---|---:|---|
| kernel stores, calendar, clearing, wire, sessions and assembly | 22 files | the kernel is substantial; agreement terms and opening inputs are now typed and owner doors are restricted, while several lifecycle transitions still have no production caller |
| production mechanisms | 49 files | a `Mechanism` implementation exists for many systems, but IRS still has none, commodity-futures helpers are not consumed by the wired spot mechanism, and several implementations remain counters or simplified proxies rather than the mechanism their pure helpers describe |
| scale/diagnostic binaries | 8 files | the scale world is an arbitrary reachability and cost harness rather than evidence that the implemented causal paths satisfy the specification |
| documentation and tooling | 14 files | coverage existence is a citation check, not semantic proof; the previous plan retained completed 0q/0r work, stale TypeScript history, false live counts and an arcs-first order that contradicted Part XIII |

The review therefore withdraws two earlier premises: **not every specified system is wired**, and
**an event with a journal reader is not necessarily a completed causal mechanism**. Part 4 remains
the exhaustive clause ledger. Parts 1–2 below are the dependency-safe implementation order derived
from the source review.

### 0.3 Cross-cutting defects found in source

1. At the review baseline, `Agreements` stored every contract as `kind + Vec<f64>` and derivative
   classes had incompatible positional layouts. Agreement terms and opening inputs are now typed;
   unused numeric placeholders were removed rather than promoted into invented legal contracts.
   Periodicity, collateral, clearing status and the obligation/agreement link remain incomplete.
2. Unpriced orders are absent limits and protocols now require a finite opposing level. A market
   order may consume a named resting level but can never set one (Laws 3 and 6).
3. Mortality, risk weighting, central-bank facility access and credit coverage now read declared
   `KindProfile` capabilities. Kind IDs remain only in system/participant eligibility, not in the
   economic rule selected after a party enters a mechanism (Law 15).
4. `systems::declare` contains global Model-owned preferences (`firm.buffer`, dealer limits,
   household cash retention, money-market reservations and others). They are neither dispersed
   party preferences nor decisions from observations. Each must become party state, a lawful policy
   primitive, technology, or be deleted under Law 2 and XI-16.
5. `Parties::add` admits duplicate live cells on the same lattice key; public mutation doors on the
   register, instruments and prices formerly let production code bypass their owners. Issuance,
   debit and claim mutation are now crate-private, while the law checker confines remaining
   construction-facing calls to settlement, session, assembly and opening owners. Duplicate cells
   remain open under item 1.7.
6. The one-book-per-instrument rule is deliberate. `BookDecl` is now authoritative in both
   directions even when market and instrument rows differ; participants and fixings do not
   reconstruct either identity from the other (Law 19).
7. The scale world is not evidence that the economy is connected: no book clears in four periods,
   millions of outlook rows are written, and the audit reports invented holdings and schedule/wire
   mismatches. These findings are not to be normalised away; the arbitrary fixture remains only a
   reachability and cost harness until the final seed.
8. The duplicate `sovereign.default` declaration found at the review baseline has been removed.
   `world-runs` is again usable as a reachability gate, but its arbitrary opening state still cannot
   establish semantic coverage.

## Part 1 — The order

| # | item | dependency reason |
|---|---|---|
| 1 | **Build physical geography and make kernel contracts and market state unambiguous** | every place, route, derivative, cure, delivery, benchmark and audit reads these identities |
| 2 | **Finish sovereign funding and the transacted benchmark** | Part XIII places a sovereign that can fail before anything priced over its curve |
| 3 | **Finish firm, household, labour and goods operating flows** | credit, ratings and loss need real cash flow and named counterparties to assess |
| 4 | **Finish credit rows, loss rights and resolution** | loans, collateral, recovery and failure must exist before risk transfer |
| 5 | **Finish redeemable claims, equity, dealers and cost-of-capital transmission** | forced selling and investment require real claims, inventory limits and cleared prices |
| 6 | **Finish bank funding, capital and resolution as one balance sheet** | deposits, reserves, collateral and capital must reconcile |
| 7 | **Finish currency, cross-border and benchmark curves** | derivatives and foreign funding require named currencies, pairs and transacted references |
| 8 | **Finish reporting, estimates, housing, employment and corporate control** | these consume stable accounting, price and credit layers and supply inputs to the polity |
| 9 | **Finish polity and policy transmission** | votes require cell-level lived outcomes; mandates then change only policy-owned primitives |
| 10 | **Finish derivatives and risk transfer, one typed contract at a time** | CDS, IRS, FX derivatives, commodity futures and securitisation require all earlier underlyings and failure states |
| 11 | **Complete cell life-cycle and physical delivery** | entry/death/promotion/merge and goods in transit must preserve ownership and relationships |
| 12 | **Close and audit the sixteen causal chains** | only now do all endpoints exist; every chain gains a falsification test and one invariant owner |
| 13 | **Generate the calibrated opening world** | seed primitives, histories and reasons only after consuming mechanisms are stable |
| 14 | **Measure, optimise, then build the observer/app** | Laws 11 and 18 put diagnosis and performance after behaviour |

## Part 2 — Milestone gates

The checkboxes below are cross-cutting gates not represented by a single specification clause.
Part 4 owns every clause-level implementation point. A gate may state its acceptance test, but it
must not hide a second behaviour behind “and”, a comma list, or a broad verb such as *complete*.

## 12. Causal chains and audits

**Implementation boundary:** implement invariant contributions in `audit.rs` and make
`tools/phoenix-check/src/main.rs` verify every causal-chain field without duplicating economic
state.

- [ ] 12.1 Implement the CrossMarket audit contribution.
- [ ] 12.2 Implement the ZeroSum audit contribution.
- [ ] 12.3 Implement the Liveness audit contribution.
- [ ] 12.4 Implement the Ownership-family audit contribution over holdings, title and agreement ownership.
- [ ] 12.5 Implement the Flow-family audit contribution over opening stock, settled movement and closing stock.
- [ ] 12.6 Record the producer for each Part XII causal chain.
- [ ] 12.7 Record the legal observation for each Part XII causal chain.
- [ ] 12.8 Record the decision for each Part XII causal chain.
- [ ] 12.9 Record the settled state for each Part XII causal chain.
- [ ] 12.10 Record the consumer for each Part XII causal chain.
- [ ] 12.11 Record the lag for each Part XII causal chain.
- [ ] 12.12 Add one falsification test for each Part XII causal chain.
- [ ] 12.13 Generate the said/read/declaration diagnostic.

## 13. Seed

**Implementation boundary:** construct every opening primitive through `opening.rs` and existing
store doors; draw randomness only from the run seed and run the ordinary audit before period zero.

- [ ] 13.1 Generate parties through `OpeningState`.
- [ ] 13.2 Generate instruments through `OpeningState`.
- [ ] 13.3 Generate holdings through `OpeningState`.
- [ ] 13.4 Generate agreements through `OpeningState`.
- [ ] 13.5 Generate schedules through `OpeningState`.
- [ ] 13.6 Draw all stochastic opening inputs from the single run stream.
- [ ] 13.7 Reconcile opening stocks before period zero.
- [ ] 13.8 Reconcile opening flows before period zero.
- [ ] 13.9 Supply the minimum required lagged histories.
- [ ] 13.10 Reject duplicate opening cell identities.
- [ ] 13.11 Pass the period-zero audit without allowances.
- [ ] 13.12 Record provenance for each calibrated primitive distribution.

## 14. Measurement, performance and delivery

**Implementation boundary:** keep experiments and scale measurements in `src/bin/*_at_scale.rs`;
expose immutable reads through `mechanisms/observer.rs`; create app packages only after runtime
selection.

- [ ] 14.1 Run each Part XII experiment on the calibrated world.
- [ ] 14.2 Hold model shape fixed between scaling rungs.
- [ ] 14.3 Plot runtime against measured work when shape cannot remain fixed.
- [ ] 14.4 Profile only after behavioural gates pass.
- [ ] 14.5 Preserve causal depth while optimising.
- [ ] 14.6 Publish immutable observer snapshots.
- [ ] 14.7 Enforce participant action boundaries at the observer interface.
- [ ] 14.8 Benchmark each candidate deployment runtime on target devices.
- [ ] 14.9 Select the deployment runtime from target-device measurements.
- [ ] 14.10 Build the shared web application shell.
- [ ] 14.11 Build the Android wrapper around the shared application.
- [ ] 14.12 Restore only CI jobs backed by existing application packages.

---

## Part 3 — Source review index

This is the durable disposition of the code-first review. It names every implementation area once;
Part 2 owns execution order and Part 4 owns individual specification requirements. Test-only helpers
were reviewed with their production file and do not count as production wiring.

| source area | files | controlling items |
|---|---|---|
| crate boundary, IDs, nouns, numeric helpers, places, registry | `lib.rs`, `ids.rs`, `nouns.rs`, `num.rs`, `places.rs`, `registry.rs` | 1, 3, 11 |
| tiled geography, territory, sites and physical routes | `geography.rs`, `registry.rs`, `places.rs`, `opening.rs`, `mechanisms/freight.rs` | 1, 3, 13 |
| time, instruments, holdings, prices | `calendar.rs`, `instruments.rs`, `register.rs`, `prices.rs` | 1, 2, 5 |
| clearing, venue protocols and sessions | `clearing.rs`, `protocols.rs`, `session.rs` | 1, 5, 7 |
| settlement and persistent legal/economic stores | `ledger.rs`, `stores.rs`, `parties.rs`, `journal.rs`, `params.rs` | 1, 4, 11 |
| mechanism facade, phases, assembly, opening and system table | `module.rs`, `world.rs`, `assembly.rs`, `opening.rs`, `systems.rs` | 1, 12, 13 |
| audit and observer/reporting surfaces | `audit.rs`, `mechanisms/observer.rs`, `mechanisms/reporting.rs` | 8, 12, 14 |
| sovereign, treasury, money and benchmarks | `mechanisms/{sovereign,treasury,money,benchmarks}.rs` | 2, 6, 7 |
| firms, capital, goods, freight, labour, housing and households | `mechanisms/{firms,capital_programme,goods,freight,employment,housing,households,commodities}.rs` | 3, 5, 11 |
| credit, loss, estate, mortality and short-term finance | `mechanisms/{lending,corporate_credit,short_term_debt,trade_credit,small_business,loss,estate,mortality}.rs` | 4 |
| funds, equity, dealers, control and institutional balance sheets | `mechanisms/{funds,redeemable,equity,dealing,control,bank_capital,bank_funding,money_market,insurers,hedge_funds,private_equity}.rs` | 5, 6, 8 |
| FX and cross-border | `mechanisms/{currency,spot_fx,cross_border,fx_forwards}.rs` | 7, 10 |
| assessment, expectations and polity | `mechanisms/{ratings,second_opinion,expectations,polity,cost_of_capital}.rs` | 5, 8, 9 |
| derivatives and risk transfer | `mechanisms/{derivative_layer,cds,irs,securities_lending,prime_brokerage,securitisation}.rs` | 10 |
| scale binaries | `src/bin/*.rs` | 12–14 |
| calibration and law/coverage tooling | `tools/**` | 12–14 |
| CI and repository configuration | `.github/**`, Cargo/npm/TypeScript/format configuration | 14 |
| architecture, specification, coverage and this plan | `docs/**`, `CLAUDE.md` | all; documentation describes source and never overrides Laws 1–19 |

---

<!-- plan:gaps -->

## Part 4 — Owned implementation backlog

**1119 clauses: 997 MISSING, 122 PARTIAL.** Generated from
`docs/COVERAGE.md` by `npm run plan:gaps`, ordered by the Part 1 milestone that owns each clause.
Every checkbox is one uniquely named to-do point and owns exactly one unmet requirement. Work
top-to-bottom by milestone; within a milestone, satisfy prerequisites before dependent points.
A MISSING clause is a mechanism nobody has written; a PARTIAL
one is a mechanism that exists and does not yet do all the clause says, and its row says what is
short. Neither is a finding — a finding is a defect in something that was built — and neither
waits on a measurement: an absence is not measurable (Audit E1), which is exactly why it has to be
in this executable list rather than in a table somebody reads later.

Re-mark the row in `docs/COVERAGE.md` in the change that meets it, and re-run `npm run plan:gaps`
in the same commit. Nothing here is ticked by hand. A point leaves this list only when its
coverage row becomes MET; therefore no MISSING or PARTIAL clause can be unowned.

### 1. Money — 13 missing, 5 partial

> **Required review before this block:** read the **Money** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 383), then inspect `packages/kernel-rs/src/instruments.rs`, `packages/kernel-rs/src/register.rs`, `packages/kernel-rs/src/ledger.rs`, `packages/kernel-rs/src/mechanisms/money.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 1.MONEY.A2B** — `Money A2.b` MISSING — VERIFICATION 5.3: there is no currency-carrying amount type in packages/kernel-rs. Money is a bare f64 in every leg, store and mechanism, and nothing can refuse an addition across two currencies
- [ ] **TODO 1.MONEY.A4** — `Money A4` MISSING — no read of the money stock exists, and the Money audit family that would check it is one contribution deep (VERIFICATION 1.4)
- [ ] **TODO 1.MONEY.B1B** — `Money B1.b` MISSING — packages/kernel-rs/src/register.rs `held_total` is the read it needs, but nothing compares it with an issuer`s money liability, and there is no issued amount to compare it with (VERIFICATION 7.1)
- [ ] **TODO 1.MONEY.C2C** — `Money C2.c` MISSING — nothing sums the legs of a pass. The Money family has one contribution and it checks that money accounts carry no lots (VERIFICATION 1.4); the issuance exception C4.c exists to measure is unmeasured and unguarded (5.1)
- [ ] **TODO 1.MONEY.C3** — `Money C3` MISSING
- [ ] **TODO 1.MONEY.C4C** — `Money C4.c` MISSING — VERIFICATION 5.1, 1.4: nothing counts issuance legs and nothing reads the money stock`s change
- [ ] **TODO 1.MONEY.D3** — `Money D3` MISSING — VERIFICATION 1.4: the Flows family is NOT BUILT. packages/kernel-rs/src/mechanisms/capital_programme.rs `PlantMoves` does this identity for capital lines only, in the Units family
- [ ] **TODO 1.MONEY.E4** — `Money E4` MISSING — VERIFICATION 7.3: settlement never reads `parties.alive`. A ceased party pays and is paid like any other, and VERIFICATION 7.2 leaves it holding the money to do it with
- [ ] **TODO 1.MONEY.F1** — `Money F1` MISSING
- [ ] **TODO 1.MONEY.F1A** — `Money F1.a` MISSING
- [ ] **TODO 1.MONEY.F2** — `Money F2` MISSING
- [ ] **TODO 1.MONEY.F3** — `Money F3` MISSING — no clearing house residual is read; the ZeroSum family is NOT BUILT (VERIFICATION 1.4)
- [ ] **TODO 1.MONEY.F4** — `Money F4` MISSING — nothing counts money landing on a holder with no account; the Money family is one contribution deep (VERIFICATION 1.4)
- [ ] **TODO 1.MONEY.B3** — `Money B3` PARTIAL — packages/kernel-rs/src/register.rs `money_delta` can carry a negative total, but settlement refuses a payment the payer cannot make, so nothing reaches it through the wire. B3.a`s credit decision by the bank is in packages/kernel-rs/src/mechanisms/bank_funding.rs; what is missing is B3.b, a bank overdrawn at the central bank, and there is no central bank at all (item 1)
- [ ] **TODO 1.MONEY.B3C** — `Money B3.c` PARTIAL — packages/kernel-rs/src/ledger.rs refuses rather than overdrawing (`ShortOfMoney`, `BankCouldNotSettle`), and since item 0k.5a it weighs every leg of an instruction TOGETHER — two legs out of one account used to pass one at a time and take the balance negative in silence. The refusal is recorded on the wire; the LENDING that would make an overdraft priced is still not reached (item 1)
- [ ] **TODO 1.MONEY.C1** — `Money C1` PARTIAL — packages/kernel-rs/src/ledger.rs `Leg::Money` names payer, payee, amount, instrument and a `Receipt` (the reason, which cannot be omitted). VERIFICATION 5.2: its `ccy` is a second copy of the instrument`s currency, never checked against it, and read by one mechanism
- [ ] **TODO 1.MONEY.C4** — `Money C4` PARTIAL — packages/kernel-rs/src/ledger.rs `Leg::Mint` is the one door that creates money — but VERIFICATION 5.1: it is never checked, so the creators are not enumerable and not few
- [ ] **TODO 1.MONEY.E1** — `Money E1` PARTIAL — packages/kernel-rs/src/ledger.rs (a payer that cannot pay does not pay, nothing half-settles, and the outcome is recorded by name), packages/kernel-rs/src/ledger.rs `Queue::gave_up` (the arrear). What is missing is the downstream: no claim is ever filed against an estate (VERIFICATION 4.1), so `owed_to_the_state` has no caller

### 1. Register — 7 missing, 3 partial

> **Required review before this block:** read the **Register** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 532), then inspect `packages/kernel-rs/src/register.rs`, `packages/kernel-rs/src/instruments.rs`, `packages/kernel-rs/src/parties.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 1.REGISTER.B4** — `Register B4` MISSING — packages/kernel-rs/src/instruments.rs carries `matures` as a term, but nothing ceases an instrument and nothing resolves its holdings; default-into-recovery needs the estate claims nobody files (VERIFICATION 4.1)
- [ ] **TODO 1.REGISTER.C5** — `Register C5` MISSING — nothing reads bought against sold over a window; the Flows and Ownership families that would are NOT BUILT or tautological (VERIFICATION 1.3, 1.4)
- [ ] **TODO 1.REGISTER.D3** — `Register D3` MISSING — VERIFICATION 6.1: the kernel`s balance-sheet read `instruments.rs equity` values every holding at its lot BASIS — a price stored on the holding, which is what this clause forbids. Where a mark is taken at all it is three inline copies with a silent fallback to cost (6.2)
- [ ] **TODO 1.REGISTER.E3** — `Register E3` MISSING — VERIFICATION 4.1: no claim is ever filed against an estate, so no loss lands on a holder
- [ ] **TODO 1.REGISTER.E4** — `Register E4` MISSING — packages/kernel-rs/src/mechanisms/equity.rs `ShareEvent` names the four events; nothing issues, buys back, splits or cancels in the running world
- [ ] **TODO 1.REGISTER.F2** — `Register F2` MISSING — VERIFICATION 7.2: `parties.rs cease` flips a flag and nothing else. There is no successor anywhere in packages/kernel-rs, and the dead party keeps its holdings
- [ ] **TODO 1.REGISTER.F3** — `Register F3` MISSING — VERIFICATION 1.4: the Flows family is NOT BUILT, so nothing would notice unexplained drift across a period boundary
- [ ] **TODO 1.REGISTER.B1** — `Register B1` PARTIAL — packages/kernel-rs/src/instruments.rs carries `issued`, moved only through `moves(i, Issuance, units)` and only by settlement, which is where units come into and go out of existence — `Leg::Create`, `Leg::Mint` and `Leg::Destroy`. **Two of B1`s five events**: a buyback, an amortisation and a maturity are the other three and nothing does any of them, which is redemption and is item 0n.6
- [ ] **TODO 1.REGISTER.B3** — `Register B3` PARTIAL — packages/kernel-rs/src/instruments.rs `owed_by` reads the liability from the other side rather than storing it. It is used only by `equity()`, which VERIFICATION 6.1 shows values everything at cost
- [ ] **TODO 1.REGISTER.E2** — `Register E2` PARTIAL — packages/kernel-rs/src/stores.rs `Owing` carries a maturity on the schedule; nothing extinguishes the holding when it is paid (VERIFICATION 7.1: there is no issued amount to reduce)

### 1. Clearing — 6 missing, 5 partial

> **Required review before this block:** read the **Clearing** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 605), then inspect `packages/kernel-rs/src/clearing.rs`, `packages/kernel-rs/src/protocols.rs`, `packages/kernel-rs/src/session.rs`, `packages/kernel-rs/src/prices.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 1.CLEARING.B3** — `Clearing B3` MISSING — packages/kernel-rs/src/mechanisms/dealing.rs is a set of pure functions; no dealer posts into a book in the running world
- [ ] **TODO 1.CLEARING.D4** — `Clearing D4` MISSING — VERIFICATION 6.1, 6.3: the print does not become anybody`s mark. `instruments.rs equity` values holdings at cost and there is no revaluation anywhere in packages/kernel-rs
- [ ] **TODO 1.CLEARING.D5** — `Clearing D5` MISSING — VERIFICATION 8.3: nothing reads bought against sold or cash paid against cash received per clearing, and a book can print with nothing settled
- [ ] **TODO 1.CLEARING.E2** — `Clearing E2` MISSING — nothing in packages/kernel-rs takes one book`s print as another book`s input. With one book of 1,546 printing per period (VERIFICATION 3.2) there is nothing to be an input
- [ ] **TODO 1.CLEARING.E3** — `Clearing E3` MISSING — no dealer posts a schedule, so there is nothing to read a bid-offer off (Clearing B3)
- [ ] **TODO 1.CLEARING.F3** — `Clearing F3` MISSING — a Part XII measurement, and with one book clearing per period (VERIFICATION 3.2) there is nothing to move
- [ ] **TODO 1.CLEARING.A2** — `Clearing A2` PARTIAL — packages/kernel-rs/src/clearing.rs (the solver takes schedules; every venue posts them) and packages/kernel-rs/src/protocols.rs. A2.a is still unmet on the participant side: a sell order may carry `price: None` and is then in the book at every level (item 1.2)
- [ ] **TODO 1.CLEARING.A4** — `Clearing A4` PARTIAL — packages/kernel-rs/src/clearing.rs asserts every BUY names a level. A sell with no level is admitted deliberately (XI-2`s forced seller) and is a price-taker of a level the mechanism has not produced
- [ ] **TODO 1.CLEARING.D3** — `Clearing D3` PARTIAL — packages/kernel-rs/src/session.rs settles each trade as one atomic instruction. VERIFICATION 8.3: the print is written BEFORE any of them is attempted and stands even where every one failed
- [ ] **TODO 1.CLEARING.E4** — `Clearing E4` PARTIAL — packages/kernel-rs/src/prices.rs `Provenance::Carried` is the right shape — a book that ran and did not cross carries its last level and says so. VERIFICATION 8.3: a book whose trades all failed still writes `Provenance::Cleared`
- [ ] **TODO 1.CLEARING.F2** — `Clearing F2` PARTIAL — packages/kernel-rs/src/prices.rs keys a print by (instrument, period) so one period has one level. Nothing forbids a second print into the same period, and there is no rate in force that valuation and settlement are held to (VERIFICATION 8.1)

### 1. Geography — 42 missing, 0 partial

> **Required review before this block:** read the **Geography** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 4382), then inspect `packages/kernel-rs/src/geography.rs`, `packages/kernel-rs/src/registry.rs`, `packages/kernel-rs/src/places.rs`, `packages/kernel-rs/src/opening.rs`, `packages/kernel-rs/src/mechanisms/freight.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 1.GEOGRAPHY.A1** — `Geography A1` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.A2** — `Geography A2` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.A3** — `Geography A3` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.A4** — `Geography A4` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.A5** — `Geography A5` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.B1** — `Geography B1` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.B2** — `Geography B2` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.B3** — `Geography B3` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.B4** — `Geography B4` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.B5** — `Geography B5` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.C1** — `Geography C1` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.C2** — `Geography C2` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.C3** — `Geography C3` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.C4** — `Geography C4` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.C5** — `Geography C5` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.C6** — `Geography C6` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.D1** — `Geography D1` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.D2** — `Geography D2` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.D3** — `Geography D3` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.D4** — `Geography D4` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.D5** — `Geography D5` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.E1** — `Geography E1` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.E2** — `Geography E2` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.E3** — `Geography E3` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.E4** — `Geography E4` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.E5** — `Geography E5` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.F1** — `Geography F1` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.F2** — `Geography F2` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.F3** — `Geography F3` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.F4** — `Geography F4` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.F5** — `Geography F5` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.F6** — `Geography F6` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.G1** — `Geography G1` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.G2** — `Geography G2` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.G3** — `Geography G3` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.G4** — `Geography G4` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.G5** — `Geography G5` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.G6** — `Geography G6` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.H1** — `Geography H1` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.H2** — `Geography H2` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.H3** — `Geography H3` MISSING — Tiled geography is not implemented; owned by item 1
- [ ] **TODO 1.GEOGRAPHY.H4** — `Geography H4` MISSING — Tiled geography is not implemented; owned by item 1

### 2. Sovereign — 38 missing, 6 partial

> **Required review before this block:** read the **Sovereign** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1199), then inspect `packages/kernel-rs/src/mechanisms/sovereign.rs`, `packages/kernel-rs/src/mechanisms/treasury.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 2.SOVEREIGN.A1C** — `Sovereign A1.c` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.A4** — `Sovereign A4` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `Missed` is the definition and is dead code (VERIFICATION 11.1). Nothing makes default a choice
- [ ] **TODO 2.SOVEREIGN.B1** — `Sovereign B1` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.B2** — `Sovereign B2` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.B3** — `Sovereign B3` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.B4** — `Sovereign B4` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.B5** — `Sovereign B5` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.B6** — `Sovereign B6` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.B7** — `Sovereign B7` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.C3** — `Sovereign C3` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.C4** — `Sovereign C4` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.D3** — `Sovereign D3` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.D4** — `Sovereign D4` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.D6** — `Sovereign D6` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.E1** — `Sovereign E1` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.E1A** — `Sovereign E1.a` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.E2** — `Sovereign E2` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.E3** — `Sovereign E3` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.E4** — `Sovereign E4` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.E5** — `Sovereign E5` MISSING — no risk weight is read anywhere against a sovereign holding
- [ ] **TODO 2.SOVEREIGN.F1** — `Sovereign F1` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.F2** — `Sovereign F2` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.F3** — `Sovereign F3` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.F4** — `Sovereign F4` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.F5** — `Sovereign F5` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.G1** — `Sovereign G1` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.G2** — `Sovereign G2` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.G3** — `Sovereign G3` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.G4** — `Sovereign G4` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.G5** — `Sovereign G5` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.H1** — `Sovereign H1` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.H2** — `Sovereign H2` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.H3** — `Sovereign H3` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.H5** — `Sovereign H5` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.I1** — `Sovereign I1` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.I2** — `Sovereign I2` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.I3** — `Sovereign I3` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.I3A** — `Sovereign I3.a` MISSING — VERIFICATION 12.2, 9.1: the `sovereign` system runs `mechanisms/sovereign.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary
- [ ] **TODO 2.SOVEREIGN.A1** — `Sovereign A1` PARTIAL — packages/kernel-rs/src/mechanisms/sovereign.rs `Sovereign` reads what falls due on the treasury`s own schedule. Its outlays are hard-coded 0.0 (`Programme { redemptions, outlays: 0.0, buffer }`), so a fiscal authority with revenue and outlays does not exist
- [ ] **TODO 2.SOVEREIGN.A2** — `Sovereign A2` PARTIAL — packages/kernel-rs/src/mechanisms/sovereign.rs `Programme::to_raise` sizes the need against the buffer, and packages/kernel-rs/src/mechanisms/treasury.rs `Funding` brings paper for it. There is no tenor mix and no plan made ahead of the outlay — a decision missing inside a system that runs (item 2)
- [ ] **TODO 2.SOVEREIGN.C1** — `Sovereign C1` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs `TreasuryIssues` posts a sell sized from its own position. Nothing announces it ahead of the session, so C1.a`s calendar does not exist (item 2)
- [ ] **TODO 2.SOVEREIGN.C5** — `Sovereign C5` PARTIAL — packages/kernel-rs/src/mechanisms/sovereign.rs `handle` returns `ComeBackToTheMarket { still_short }`. VERIFICATION 9.1: `Sovereign` journals the answer and proposes nothing, so the treasury never comes back
- [ ] **TODO 2.SOVEREIGN.C7** — `Sovereign C7` PARTIAL — see C5 — the three ways a shortfall is handled are computed and said. None of them is done (item 2)
- [ ] **TODO 2.SOVEREIGN.D5** — `Sovereign D5` PARTIAL — packages/kernel-rs/src/register.rs `pledge` can encumber a holding. Nothing sets a haircut and nothing pledges sovereign paper: the module that would post it is imported by nothing (item 2)

### 2. Treasury — 17 missing, 3 partial

> **Required review before this block:** read the **Treasury** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2863), then inspect `packages/kernel-rs/src/mechanisms/treasury.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 2.TREASURY.A3** — `Treasury A3` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.B1** — `Treasury B1` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.B2** — `Treasury B2` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.B3** — `Treasury B3` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.C1** — `Treasury C1` MISSING — nothing levies a tax. `treasury.rs Collected` is the read of what named payers remitted and has no caller (VERIFICATION 11.1)
- [ ] **TODO 2.TREASURY.C2** — `Treasury C2` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.C3** — `Treasury C3` MISSING — see C1 — there are no receipts, so there is nothing to be a sum of
- [ ] **TODO 2.TREASURY.D4** — `Treasury D4` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.D5** — `Treasury D5` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.D6** — `Treasury D6` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.E1** — `Treasury E1` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.E3** — `Treasury E3` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.E4** — `Treasury E4` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.F1** — `Treasury F1` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.F2** — `Treasury F2` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.F3** — `Treasury F3` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.F4** — `Treasury F4` MISSING — VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer
- [ ] **TODO 2.TREASURY.B4** — `Treasury B4` PARTIAL — packages/kernel-rs/src/mechanisms/lending.rs `Servicing` pays what falls due, maturing debt included. VERIFICATION 15.1: it pays one holder rather than the holders
- [ ] **TODO 2.TREASURY.D1** — `Treasury D1` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs `TreasuryIssues` sizes its order with `treasury::must_raise(outlays, receipts, own_cash, buffer)` and posts a sell. Receipts are always zero (C1), and `mechanisms/treasury.rs Funding` calls the same function with `0.0` hard-coded for receipts
- [ ] **TODO 2.TREASURY.E2** — `Treasury E2` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs `TreasuryIssues` chooses a size against its own cash position each period. It chooses no timing and no maturity (E1) — a decision missing inside a system that runs (item 2)

### 3. Commodities Spot — 17 missing, 3 partial

> **Required review before this block:** read the **Commodities Spot** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2237), then inspect `packages/kernel-rs/src/mechanisms/commodities.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.COMMODITIES-SPOT.A1** — `Commodities Spot A1` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.A2** — `Commodities Spot A2` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.A3** — `Commodities Spot A3` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.B1** — `Commodities Spot B1` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.B2** — `Commodities Spot B2` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.B3** — `Commodities Spot B3` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.B4** — `Commodities Spot B4` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.C1** — `Commodities Spot C1` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.C2** — `Commodities Spot C2` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.C3** — `Commodities Spot C3` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.C4** — `Commodities Spot C4` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.D4** — `Commodities Spot D4` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.D5** — `Commodities Spot D5` MISSING — VERIFICATION 1.4: the Units family has one contribution, `capital_programme.rs PlantMoves`, and it covers capital lines only. `commodities.rs units_balance` is the identity for goods and has no caller
- [ ] **TODO 3.COMMODITIES-SPOT.E1** — `Commodities Spot E1` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.E2** — `Commodities Spot E2` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.E3** — `Commodities Spot E3` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.E4** — `Commodities Spot E4` MISSING — VERIFICATION 9.1: the `commodities` system runs `mechanisms/commodities.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1)
- [ ] **TODO 3.COMMODITIES-SPOT.A4** — `Commodities Spot A4` PARTIAL — packages/kernel-rs/src/register.rs holds units of a good by named holder at a named place. `commodities.rs supply_at` is the read and has no caller (item 3)
- [ ] **TODO 3.COMMODITIES-SPOT.D1** — `Commodities Spot D1` PARTIAL — packages/kernel-rs/src/session.rs clears a goods book per line. A grade-and-location identity is not the key: a line is one instrument, and packages/kernel-rs/src/places.rs holds where things are separately, and `systems.rs` wires `commodities::Storing`, but its production path does not consume the commodity-futures helpers (item 3)
- [ ] **TODO 3.COMMODITIES-SPOT.D2** — `Commodities Spot D2` PARTIAL — inventory is the register`s own rows and carries across periods. It is not an input to any price: every bid is `last print x k` (VERIFICATION 13.2)

### 3. Firm — 18 missing, 3 partial

> **Required review before this block:** read the **Firm** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3106), then inspect `packages/kernel-rs/src/mechanisms/firms.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.FIRM.A3** — `Firm A3` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.A4** — `Firm A4` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.B4** — `Firm B4` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.B5** — `Firm B5` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.B6** — `Firm B6` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.C1** — `Firm C1` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.C2** — `Firm C2` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.C4** — `Firm C4` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.D2** — `Firm D2` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.D3** — `Firm D3` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.D4** — `Firm D4` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.E1** — `Firm E1` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.E2** — `Firm E2` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.E3** — `Firm E3` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.E4** — `Firm E4` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.E5** — `Firm E5` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.E6** — `Firm E6` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.F4** — `Firm F4` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired (item 3)
- [ ] **TODO 3.FIRM.A2** — `Firm A2` PARTIAL — packages/kernel-rs/src/parties.rs puts it in a region and packages/kernel-rs/src/registry.rs reads its money through the country. It has no sector: nothing says what it buys and sells beyond the lines it holds, and what a firm makes is the seeding's (item 3)
- [ ] **TODO 3.FIRM.B1** — `Firm B1` PARTIAL — packages/kernel-rs/src/session.rs prints revenue where a goods book crosses, and `GoodsSellers` posts from the firm`s own cost. VERIFICATION 3.2: one book of 1,546 crosses per period
- [ ] **TODO 3.FIRM.D5** — `Firm D5` PARTIAL — packages/kernel-rs/src/ledger.rs `Queue::gave_up` records the arrear when the days run out. Nothing follows: no event, no lender loss, no rating action — the loss chain terminates nowhere (item 3)

### 3. Goods — 26 missing, 1 partial

> **Required review before this block:** read the **Goods** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3466), then inspect `packages/kernel-rs/src/mechanisms/goods.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.GOODS.A3** — `Goods A3` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.A4** — `Goods A4` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.B1D** — `Goods B1.d` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.B4** — `Goods B4` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.B5** — `Goods B5` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.C1** — `Goods C1` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.C3** — `Goods C3` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.C4** — `Goods C4` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.C6** — `Goods C6` MISSING — VERIFICATION 8.2: the buyer pays out of its OWN account, not the seller`s money, and nothing is bought from anybody
- [ ] **TODO 3.GOODS.D1** — `Goods D1` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.D2** — `Goods D2` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.D3** — `Goods D3` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.D4** — `Goods D4` MISSING — packages/kernel-rs/src/mechanisms/goods.rs `Consignment::landed_cost` is ex-works plus freight plus duty and has no caller
- [ ] **TODO 3.GOODS.D5** — `Goods D5` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.E2** — `Goods E2` MISSING — packages/kernel-rs/src/mechanisms/goods.rs `carry` implements lower of cost and net realisable value with the broker-dealer exception, exactly as E2/E2.b ask, and it has no caller (VERIFICATION 11.1): of 19 items in the module only `CostFlow`, `Lot`, `take` and `Perishing` are reached
- [ ] **TODO 3.GOODS.E2C** — `Goods E2.c` MISSING — see E2 — the FORBID`s guard is `carry`, which nothing calls, and nothing marks inventory at all (VERIFICATION 6.1)
- [ ] **TODO 3.GOODS.E3** — `Goods E3` MISSING — see E2 — no holding loss is ever an event with a date and an income line
- [ ] **TODO 3.GOODS.F1** — `Goods F1` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.F2** — `Goods F2` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.F3** — `Goods F3` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.F4** — `Goods F4` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.G1** — `Goods G1` MISSING — VERIFICATION 11.1: packages/kernel-rs/src/mechanisms/benchmarks.rs `Index`/`Weighing` are dead code; no producer or consumer price index is built
- [ ] **TODO 3.GOODS.G1C** — `Goods G1.c` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.G2** — `Goods G2` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.G3** — `Goods G3` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.G4** — `Goods G4` MISSING — VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.F5** — `Goods F5` PARTIAL — packages/kernel-rs/src/mechanisms/goods.rs computes cost of goods sold over the lots that left. Nothing books it: no income statement is produced from it, because `Publishes` reads the equity account and not the cost (item 3)

### 3. Freight — 19 missing, 1 partial

> **Required review before this block:** read the **Freight** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3569), then inspect `packages/kernel-rs/src/mechanisms/freight.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.FREIGHT.A1** — `Freight A1` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.A2** — `Freight A2` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.A3** — `Freight A3` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.A4** — `Freight A4` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.B2** — `Freight B2` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.B3** — `Freight B3` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.B4** — `Freight B4` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.C1** — `Freight C1` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.C2** — `Freight C2` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.C3** — `Freight C3` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.D1** — `Freight D1` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.D2** — `Freight D2` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.D3** — `Freight D3` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.D4** — `Freight D4` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.D5** — `Freight D5` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.D6** — `Freight D6` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.E1** — `Freight E1` MISSING — VERIFICATION 12.1: nothing transports anything, so there is no transport to be instantaneous or costless — the FORBID holds by the system being absent
- [ ] **TODO 3.FREIGHT.E2** — `Freight E2` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.E3** — `Freight E3` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.B1** — `Freight B1` PARTIAL — packages/kernel-rs/src/systems.rs wires `LetsItsPlant` as the freight participant and `Carriage` as its mechanism. The former offers plant use and the latter only counts live agreements; neither creates a route, shipment or capacity (item 12)

### 3. Labour — 19 missing, 1 partial

> **Required review before this block:** read the **Labour** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3623), then inspect `packages/kernel-rs/src/mechanisms/employment.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.LABOUR.A3** — `Labour A3` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.B1** — `Labour B1` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.B2** — `Labour B2` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.B3** — `Labour B3` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.B4** — `Labour B4` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.B5** — `Labour B5` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.C1** — `Labour C1` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.C2** — `Labour C2` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.C3** — `Labour C3` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.C4** — `Labour C4` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.C5** — `Labour C5` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.D1** — `Labour D1` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.D2** — `Labour D2` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.D2B** — `Labour D2.b` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.D3** — `Labour D3` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.D4** — `Labour D4` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.D5** — `Labour D5` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.E3** — `Labour E3` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.E4** — `Labour E4` MISSING — VERIFICATION 12.1: `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.E2** — `Labour E2` PARTIAL — packages/kernel-rs/src/mechanisms/goods.rs carries wages into batch unit cost and GoodsSellers combines own price outlook, lot basis and holding cost into its ask; broader labour-price feedback remains incomplete (item 3)

### 3. Housing — 21 missing, 4 partial

> **Required review before this block:** read the **Housing** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3718), then inspect `packages/kernel-rs/src/mechanisms/housing.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.HOUSING.A2** — `Housing A2` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.A3** — `Housing A3` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.A4** — `Housing A4` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.A5** — `Housing A5` MISSING — packages/kernel-rs/src/mechanisms/housing.rs `Housing` reads `dwelling.upkeep` into `let _upkeep` and discards it. Nothing depreciates and nothing is maintained
- [ ] **TODO 3.HOUSING.B2** — `Housing B2` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.B3** — `Housing B3` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.B4** — `Housing B4` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.B4A** — `Housing B4.a` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.B5** — `Housing B5` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.C2** — `Housing C2` MISSING — see C1 — an `Agrees` carries terms and no `owing`, so no interest and no principal is ever payable
- [ ] **TODO 3.HOUSING.C3** — `Housing C3` MISSING — `housing.rs loan_to_value` is the read and is unreached (VERIFICATION 11.1)
- [ ] **TODO 3.HOUSING.C4** — `Housing C4` MISSING — `housing.rs foreclose`/`Foreclosed` are unreached. Nothing defaults, nothing is seized and no foreclosed supply returns to the market
- [ ] **TODO 3.HOUSING.C6** — `Housing C6` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.D1** — `Housing D1` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.D2** — `Housing D2` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.D3** — `Housing D3` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.D4** — `Housing D4` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.D5** — `Housing D5` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.E1** — `Housing E1` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.E3** — `Housing E3` MISSING — VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1)
- [ ] **TODO 3.HOUSING.E4** — `Housing E4` MISSING — VERIFICATION 1.4: nothing reconciles mortgage debt against mortgage assets, and a mortgage is an agreement rather than a claim anybody holds
- [ ] **TODO 3.HOUSING.A1** — `Housing A1` PARTIAL — packages/kernel-rs/src/instruments.rs `Class::Plant` with a unit, held by a named party, and packages/kernel-rs/src/places.rs puts it in a region. A dwelling is not counted in dwellings: it is a plant line, and `housing` is imported by nothing (item 3)
- [ ] **TODO 3.HOUSING.B1** — `Housing B1` PARTIAL — packages/kernel-rs/src/mechanisms/housing.rs `clearing` crosses bids against offers per place and `Housing` runs it. VERIFICATION 9.1: nothing settles — no dwelling moves in the register and no money is paid
- [ ] **TODO 3.HOUSING.C1** — `Housing C1` PARTIAL — packages/kernel-rs/src/mechanisms/housing.rs `Housing` strikes an `agreed::MORTGAGE` naming a lender and a borrower. It carries `[price, deposit_share]`, no term, no rate, no amortisation and NO SCHEDULE — so nothing ever falls due on a mortgage, and `housing` is imported by nothing (item 3)
- [ ] **TODO 3.HOUSING.C5** — `Housing C5` PARTIAL — packages/kernel-rs/src/mechanisms/housing.rs `standard` is reached and `standing::LENDING_STANDARD` carries it, so the standard is a read of the lender`s own measurements rather than a constant. Nothing tightens it in response to anything — a decision missing inside a system that runs (item 3)

### 3. Households — 25 missing, 2 partial

> **Required review before this block:** read the **Households** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3792), then inspect `packages/kernel-rs/src/mechanisms/households.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.HOUSEHOLDS.A1** — `Households A1` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.A2** — `Households A2` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.A2D** — `Households A2.d` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.A2G** — `Households A2.g` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.B2** — `Households B2` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.B3** — `Households B3` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.B3A** — `Households B3.a` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.B4** — `Households B4` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.B5** — `Households B5` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.C2** — `Households C2` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.C3** — `Households C3` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.C4** — `Households C4` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.C5** — `Households C5` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.D2** — `Households D2` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.D4** — `Households D4` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.D5** — `Households D5` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.E1** — `Households E1` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.E2** — `Households E2` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.E3** — `Households E3` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.E4** — `Households E4` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.E5** — `Households E5` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.F1** — `Households F1` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.F2** — `Households F2` MISSING — `households.rs Inherited` is unreached; nothing dissolves and nothing is inherited
- [ ] **TODO 3.HOUSEHOLDS.F3** — `Households F3` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.F4** — `Households F4` MISSING — VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1)
- [ ] **TODO 3.HOUSEHOLDS.A2F** — `Households A2.f` PARTIAL — packages/kernel-rs/src/mechanisms/households.rs `HouseholdBuyers::orders` is evaluated per cell and never at a sector mean. What it evaluates is `last print x 1.2` for every cell alike (VERIFICATION 13.2)
- [ ] **TODO 3.HOUSEHOLDS.C1** — `Households C1` PARTIAL — packages/kernel-rs/src/mechanisms/households.rs `HouseholdBuyers` spends out of its own money and keeps `household.keeps` back. C1.a`s income, C1.b`s wealth and C1.c`s own expectation are not read (VERIFICATION 13.3)

### 3. Expectations — 6 missing, 0 partial

> **Required review before this block:** read the **Expectations** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 4304), then inspect `packages/kernel-rs/src/mechanisms/expectations.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.EXPECTATIONS.A5** — `Expectations A5` MISSING — packages/kernel-rs/src/stores.rs `Outlooks` carries a level and no unit or horizon; `about::` names the subject and not the periodicity
- [ ] **TODO 3.EXPECTATIONS.C6** — `Expectations C6` MISSING — see C1, and Polity: no cell votes
- [ ] **TODO 3.EXPECTATIONS.E1** — `Expectations E1` MISSING — a Part XII measurement; there is no dispersion to read while every reservation is the same constant (VERIFICATION 13.2)
- [ ] **TODO 3.EXPECTATIONS.E2** — `Expectations E2` MISSING — VERIFICATION 13.3: outlooks are formed correctly by `mechanisms/expectations.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing (12.1)
- [ ] **TODO 3.EXPECTATIONS.E3** — `Expectations E3` MISSING — VERIFICATION 13.3: outlooks are formed correctly by `mechanisms/expectations.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing (12.1)
- [ ] **TODO 3.EXPECTATIONS.E4** — `Expectations E4` MISSING — VERIFICATION 13.3: outlooks are formed correctly by `mechanisms/expectations.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing (12.1)

### 4. Bond — 4 missing, 4 partial

> **Required review before this block:** read the **Bond** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 900), then inspect `packages/kernel-rs/src/instruments.rs`, `packages/kernel-rs/src/stores.rs`, `packages/kernel-rs/src/mechanisms/lending.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 4.BOND.N10** — `Bond N10` MISSING — nothing redeems an instrument. `instruments.rs` and `register.rs` have no door that ceases a line or empties its holdings
- [ ] **TODO 4.BOND.N11** — `Bond N11` MISSING — no instrument carries an early-termination regime, and there is no field in which "none" could be stated
- [ ] **TODO 4.BOND.N12** — `Bond N12` MISSING — no instrument carries a definition of default. packages/kernel-rs/src/mechanisms/sovereign.rs `Missed` is the shape and is dead code (VERIFICATION 11.1)
- [ ] **TODO 4.BOND.N13** — `Bond N13` MISSING — packages/kernel-rs/src/mechanisms/estate.rs `Rank` is the ordering, but no instrument states what its holder is entitled to on failure, and no claim is ever filed (VERIFICATION 4.1)
- [ ] **TODO 4.BOND.N2** — `Bond N2` PARTIAL — packages/kernel-rs/src/instruments.rs carries a `unit`, and the register counts units of it. VERIFICATION 7.1: there is no issued PRINCIPAL — no column records the amount owed
- [ ] **TODO 4.BOND.N3** — `Bond N3` PARTIAL — packages/kernel-rs/src/instruments.rs `ccy_of` (one currency per line), and since 0t.5 a schedule row carries the money its amount is in, which `mechanisms/lending.rs` `Servicing` compares with the payer's account rather than inferring from it. What is still not true is that EVERY figure about a line is in that money — there is no currency-carrying amount type (Money A2.b), and the payer that owes a money it does not bank in has no way to buy it (item 4)
- [ ] **TODO 4.BOND.N5** — `Bond N5` PARTIAL — packages/kernel-rs/src/instruments.rs `coupon_of` is a fixed rate or `None` (N5.a and N5.c). N5.b has no representation: there is no margin, no reference-rate field, and nothing fixes a floating coupon — the `benchmarks` module that would print the fixing is imported by nothing (item 4)
- [ ] **TODO 4.BOND.N14** — `Bond N14` PARTIAL — packages/kernel-rs/src/instruments.rs `display` builds issuer + coupon + maturity and the id is never the name. It is called by packages/kernel-rs/src/mechanisms/observer.rs `display_name`, which is itself dead code (VERIFICATION 11.1)

### 4. Corporate Credit — 52 missing, 6 partial

> **Required review before this block:** read the **Corporate Credit** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1030), then inspect `packages/kernel-rs/src/mechanisms/corporate_credit.rs`, `packages/kernel-rs/src/mechanisms/lending.rs`, `packages/kernel-rs/src/mechanisms/loss.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 4.CORPORATE-CREDIT.A2** — `Corporate Credit A2` MISSING — VERIFICATION 12.2: the `corporate_credit` system runs its own `Brings`, a PLACEHOLDER that reads a borrower's receipts as nothing; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable (11.1) — `capital.debt_share` = 0.6 is the capital structure, stated (VERIFICATION 13.4)
- [ ] **TODO 4.CORPORATE-CREDIT.A4** — `Corporate Credit A4` MISSING — no assessment is held by anybody. packages/kernel-rs/src/mechanisms/second_opinion.rs is not imported (12.2) and packages/kernel-rs/src/mechanisms/ratings.rs `Rating` is dead code (11.1)
- [ ] **TODO 4.CORPORATE-CREDIT.B1** — `Corporate Credit B1` MISSING — no instrument carries an early-termination regime (Bond N11)
- [ ] **TODO 4.CORPORATE-CREDIT.B2** — `Corporate Credit B2` MISSING — `corporate_credit.rs Covenant`/`standing`/`waive` implement B2, B2.a and B2.b and are unreachable. VERIFICATION 12.2: the `corporate_credit` system runs its own `Brings`, a PLACEHOLDER that reads a borrower's receipts as nothing; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable (11.1)
- [ ] **TODO 4.CORPORATE-CREDIT.B3** — `Corporate Credit B3` MISSING — no instrument carries a seniority, and no claim is ever filed against an estate (VERIFICATION 4.1)
- [ ] **TODO 4.CORPORATE-CREDIT.B4** — `Corporate Credit B4` MISSING — there is no floating coupon anywhere (Bond N5.b)
- [ ] **TODO 4.CORPORATE-CREDIT.C1** — `Corporate Credit C1` MISSING — `Funding` brings paper directly. No underwriter is appointed and none is paid. `corporate_credit.rs bring`/`Brought` is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.C2** — `Corporate Credit C2` MISSING — no book is built. `corporate_credit.rs Indication` is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.C4** — `Corporate Credit C4` MISSING — there is no walk-away: `Funding` brings the paper unconditionally
- [ ] **TODO 4.CORPORATE-CREDIT.C5** — `Corporate Credit C5` MISSING — nothing allocates a book
- [ ] **TODO 4.CORPORATE-CREDIT.C6** — `Corporate Credit C6` MISSING — no proceeds reach an issuer and no fee reaches an underwriter; `Funding` proposes no instruction (VERIFICATION 9.1)
- [ ] **TODO 4.CORPORATE-CREDIT.C7** — `Corporate Credit C7` MISSING — `corporate_credit.rs` has the whole shape — `Brought`, `Basis`, `Member`, `syndicate`, `fee_gap` — and it is unreachable. VERIFICATION 12.2: the `corporate_credit` system runs its own `Brings`, a PLACEHOLDER that reads a borrower's receipts as nothing; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable (11.1)
- [ ] **TODO 4.CORPORATE-CREDIT.C7B** — `Corporate Credit C7.b` MISSING — see C7; `fee_gap` is the read and is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.C8** — `Corporate Credit C8` MISSING — `corporate_credit.rs tap` is unreachable; `Funding` brings a fresh line every time it is short
- [ ] **TODO 4.CORPORATE-CREDIT.C9** — `Corporate Credit C9` MISSING — `agreed::COMMITMENT` is the shape and `corporate_credit.rs draw` decides it; the module is unreachable and nothing opens a line
- [ ] **TODO 4.CORPORATE-CREDIT.C10** — `Corporate Credit C10` MISSING — `corporate_credit.rs syndicate` is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.C10B** — `Corporate Credit C10.b` MISSING — see C10 — the FORBID is guarded inside `syndicate`, which nothing calls
- [ ] **TODO 4.CORPORATE-CREDIT.C10C** — `Corporate Credit C10.c` MISSING — see C10
- [ ] **TODO 4.CORPORATE-CREDIT.C11** — `Corporate Credit C11` MISSING — `corporate_credit.rs Basis` states the two bases and is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.C11D** — `Corporate Credit C11.d` MISSING — see C11 — a FORBID whose guard is in unreachable code
- [ ] **TODO 4.CORPORATE-CREDIT.C11E** — `Corporate Credit C11.e` MISSING — see C11
- [ ] **TODO 4.CORPORATE-CREDIT.D1** — `Corporate Credit D1` MISSING — `corporate_credit.rs secondary` is the schedule-posting side and is unreachable; no participant posts in a corporate bond book
- [ ] **TODO 4.CORPORATE-CREDIT.D2** — `Corporate Credit D2` MISSING — see D1 — nothing clears
- [ ] **TODO 4.CORPORATE-CREDIT.D3** — `Corporate Credit D3` MISSING — VERIFICATION 12.1: `dealing` is neither wired as a system nor imported by anything
- [ ] **TODO 4.CORPORATE-CREDIT.D4** — `Corporate Credit D4` MISSING — see D3
- [ ] **TODO 4.CORPORATE-CREDIT.D5** — `Corporate Credit D5` MISSING — see D1
- [ ] **TODO 4.CORPORATE-CREDIT.D7** — `Corporate Credit D7` MISSING — `corporate_credit.rs accrued` is unreachable, so nothing adds accrued to what a trade settles (Bond N9.b)
- [ ] **TODO 4.CORPORATE-CREDIT.E2** — `Corporate Credit E2` MISSING — VERIFICATION 1.3: nothing sums held against issued
- [ ] **TODO 4.CORPORATE-CREDIT.E3** — `Corporate Credit E3` MISSING — VERIFICATION 6.1: `instruments.rs equity` values holdings at cost, not at the cleared price
- [ ] **TODO 4.CORPORATE-CREDIT.E4** — `Corporate Credit E4` MISSING — VERIFICATION 6.3: there is no revaluation, so no change in a mark reaches anybody's income. `corporate_credit.rs mark` is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.E5** — `Corporate Credit E5` MISSING — VERIFICATION 13.2: a holder's reservation is a stated multiple of the last print, not its cost of funds, its expected loss and its capital
- [ ] **TODO 4.CORPORATE-CREDIT.E5D** — `Corporate Credit E5.d` MISSING — see E5
- [ ] **TODO 4.CORPORATE-CREDIT.E6** — `Corporate Credit E6` MISSING — `corporate_credit.rs funding_withdrawn` is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.E7** — `Corporate Credit E7` MISSING — nothing charges regulatory capital against a position
- [ ] **TODO 4.CORPORATE-CREDIT.E9** — `Corporate Credit E9` MISSING — VERIFICATION 12.1: `observer` is not imported and packages/kernel-rs/src/mechanisms/observer.rs is dead code
- [ ] **TODO 4.CORPORATE-CREDIT.F1** — `Corporate Credit F1` MISSING — nothing accrues interest to a holder of record
- [ ] **TODO 4.CORPORATE-CREDIT.F4** — `Corporate Credit F4` MISSING — there is no early-termination regime to exercise (B1)
- [ ] **TODO 4.CORPORATE-CREDIT.F5** — `Corporate Credit F5` MISSING — `corporate_credit.rs refinanced_at` is unreachable; nothing retires an old issue with a new one
- [ ] **TODO 4.CORPORATE-CREDIT.F6** — `Corporate Credit F6` MISSING — nothing matures an instrument out of existence (Bond N10)
- [ ] **TODO 4.CORPORATE-CREDIT.G1** — `Corporate Credit G1` MISSING — no covenant exists and no missed payment is published as an event holders can observe
- [ ] **TODO 4.CORPORATE-CREDIT.G2** — `Corporate Credit G2` MISSING — nothing accelerates
- [ ] **TODO 4.CORPORATE-CREDIT.G3** — `Corporate Credit G3` MISSING — VERIFICATION 4.1: no claim is ever filed against an estate
- [ ] **TODO 4.CORPORATE-CREDIT.G4** — `Corporate Credit G4` MISSING — VERIFICATION 4.1, 7.2: nothing realises an estate; a dead party keeps what it held
- [ ] **TODO 4.CORPORATE-CREDIT.G5** — `Corporate Credit G5` MISSING — packages/kernel-rs/src/mechanisms/estate.rs `waterfall` is correct and is never reached (4.1)
- [ ] **TODO 4.CORPORATE-CREDIT.G6** — `Corporate Credit G6` MISSING — see G5
- [ ] **TODO 4.CORPORATE-CREDIT.G7** — `Corporate Credit G7` MISSING — nothing restructures; there are no holders to decide
- [ ] **TODO 4.CORPORATE-CREDIT.G8** — `Corporate Credit G8` MISSING — there is no assessment for a default to move (A4)
- [ ] **TODO 4.CORPORATE-CREDIT.H1** — `Corporate Credit H1` MISSING — VERIFICATION 11.1: packages/kernel-rs/src/mechanisms/benchmarks.rs `Index` and `Constituent` are dead code
- [ ] **TODO 4.CORPORATE-CREDIT.H2** — `Corporate Credit H2` MISSING — a VERIFY, and it needs both sides at once: enough graded names for an assessment to differ across, and enough traded paper for the difference to show in a spread. Every grade this world publishes is the worst one (21.69) and the corporate books are thin (21.58). It is a MEASUREMENT and never a rule; positioned at 23.3 with Part XII's
- [ ] **TODO 4.CORPORATE-CREDIT.H3** — `Corporate Credit H3` MISSING — a VERIFY over two claims on ONE issuer that rank differently, and this world's issuers have one line each. It is a measurement and never a rule; positioned at 23.3, after 17b gives an issuer a junior tranche to be junior to
- [ ] **TODO 4.CORPORATE-CREDIT.H4** — `Corporate Credit H4` MISSING — the cash market clears (D2) and the synthetic is §19's CDS book, which exists and has never traded in a scale model (BUILT AND DEAD, `check:existence`). The BASIS between them is one derivation and it was 18.5's and item 18 closed without it; positioned at 21.137
- [ ] **TODO 4.CORPORATE-CREDIT.H4A** — `Corporate Credit H4.a` MISSING — the same basis, read the other way: neither leg sets the other. It is refused by construction — a derivative on an uncleared price is forbidden and both books clear on their own — and what is not built is the MEASUREMENT of the gap. Positioned at 21.137 with H4
- [ ] **TODO 4.CORPORATE-CREDIT.A1** — `Corporate Credit A1` PARTIAL — packages/kernel-rs/src/parties.rs and packages/kernel-rs/src/instruments.rs give a named issuer with a balance sheet. VERIFICATION 12.2: the `corporate_credit` system runs its own `Brings`, a PLACEHOLDER that reads a borrower's receipts as nothing; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable (11.1)
- [ ] **TODO 4.CORPORATE-CREDIT.A3** — `Corporate Credit A3` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs `Funding` reads what falls due against what the party holds. `corporate_credit.rs coverage` is unreachable, so A3.b's read does not exist (item 4)
- [ ] **TODO 4.CORPORATE-CREDIT.C3** — `Corporate Credit C3` PARTIAL — packages/kernel-rs/src/mechanisms/corporate_credit.rs brings discount claims with no model-authored coupon into a real book; the remaining gap is full underwriter/issuer issue pricing (item 4)
- [ ] **TODO 4.CORPORATE-CREDIT.E8** — `Corporate Credit E8` PARTIAL — packages/kernel-rs/src/register.rs `pledge` encumbers units. Nothing pledges a corporate bond, and no haircut is read: the module that would post it is imported by nothing (item 4)
- [ ] **TODO 4.CORPORATE-CREDIT.F2** — `Corporate Credit F2` PARTIAL — packages/kernel-rs/src/mechanisms/lending.rs `Servicing` pays what fell due on the schedule out of the issuer's account. It pays the SCHEDULE's payee, not whoever the register says holds the paper then (Register A2.a) — a payment that reaches the wrong party is 0k's, at the wire (item 4)
- [ ] **TODO 4.CORPORATE-CREDIT.F3** — `Corporate Credit F3` PARTIAL — see F2 — principal is an `Owing` on the schedule; nothing reduces the holding when it is paid (Register E2), which is the same wire defect as F2 (item 4)

### 4. Short-Term Debt — 12 missing, 3 partial

> **Required review before this block:** read the **Short-Term Debt** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1340), then inspect `packages/kernel-rs/src/mechanisms/short_term_debt.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 4.SHORT-TERM-DEBT.B2** — `Short-Term Debt B2` MISSING — VERIFICATION 12.1, 12.2: `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.B3** — `Short-Term Debt B3` MISSING — VERIFICATION 12.1, 12.2: `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.B4** — `Short-Term Debt B4` MISSING — VERIFICATION 12.1, 12.2: `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.B5** — `Short-Term Debt B5` MISSING — VERIFICATION 12.1, 12.2: `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.C1** — `Short-Term Debt C1` MISSING — VERIFICATION 12.1, 12.2: `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.C2** — `Short-Term Debt C2` MISSING — VERIFICATION 12.1, 12.2: `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.C3** — `Short-Term Debt C3` MISSING — VERIFICATION 12.1, 12.2: `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.C4** — `Short-Term Debt C4` MISSING — VERIFICATION 12.1, 12.2: `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.D1** — `Short-Term Debt D1` MISSING — VERIFICATION 12.1, 12.2: `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.D2** — `Short-Term Debt D2` MISSING — VERIFICATION 12.1, 12.2: `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.D3** — `Short-Term Debt D3` MISSING — VERIFICATION 12.1, 12.2: `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.D4** — `Short-Term Debt D4` MISSING — VERIFICATION 12.1, 12.2: `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.A1** — `Short-Term Debt A1` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs `Funding` brings paper with a tenor and a coupon read from `params`. A1.a`s discount instrument does not exist: every line it brings carries a coupon (item 4)
- [ ] **TODO 4.SHORT-TERM-DEBT.A3** — `Short-Term Debt A3` PARTIAL — the short borrower is the issuer and discount paper is priced by its book rather than a shared coupon; issuer-specific buyer limits remain incomplete (item 4)
- [ ] **TODO 4.SHORT-TERM-DEBT.E3** — `Short-Term Debt E3` PARTIAL — packages/kernel-rs/src/ledger.rs refuses a delivery beyond what is held, so an outstanding cannot go negative. Nothing matures, so a maturity passing without cash moving is what every line does (Bond N10) — the `redeemable` module that would retire a line is imported by nothing (item 4)

### 4. Banks Lending — 27 missing, 2 partial

> **Required review before this block:** read the **Banks Lending** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2368), then inspect `packages/kernel-rs/src/mechanisms/lending.rs`, `packages/kernel-rs/src/mechanisms/corporate_credit.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 4.BANKS-LENDING.A2** — `Banks Lending A2` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.A3** — `Banks Lending A3` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.A3B** — `Banks Lending A3.b` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.A4** — `Banks Lending A4` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.A5** — `Banks Lending A5` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.B1** — `Banks Lending B1` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.B2** — `Banks Lending B2` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.B2D** — `Banks Lending B2.d` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.C1** — `Banks Lending C1` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.C2** — `Banks Lending C2` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.C3** — `Banks Lending C3` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.C3A** — `Banks Lending C3.a` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.D1** — `Banks Lending D1` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.D2** — `Banks Lending D2` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.D2B** — `Banks Lending D2.b` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.D3** — `Banks Lending D3` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.D4** — `Banks Lending D4` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.D5** — `Banks Lending D5` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E1** — `Banks Lending E1` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E2** — `Banks Lending E2` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E3** — `Banks Lending E3` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E4** — `Banks Lending E4` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E5** — `Banks Lending E5` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E5A** — `Banks Lending E5.a` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E6** — `Banks Lending E6` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.F2** — `Banks Lending F2` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.F3** — `Banks Lending F3` MISSING — VERIFICATION 12.1: `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.A1** — `Banks Lending A1` PARTIAL — `agreed::COMMITMENT` is a named lender to a named borrower and packages/kernel-rs/src/mechanisms/lending.rs `Rows` is a book of loan rows. Nothing originates one, and the origination IS the wiring (item 4)
- [ ] **TODO 4.BANKS-LENDING.F1** — `Banks Lending F1` PARTIAL — packages/kernel-rs/src/mechanisms/lending.rs `Rows` is a sum of named rows and never a scalar. It has one caller and nothing writes a row into it (item 4)

### 4. Trade Credit — 17 missing, 3 partial

> **Required review before this block:** read the **Trade Credit** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3394), then inspect `packages/kernel-rs/src/mechanisms/trade_credit.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 4.TRADE-CREDIT.A2** — `Trade Credit A2` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.A3** — `Trade Credit A3` MISSING — `trade_credit.rs Terms`/`Discount`/`implied_rate` are unreached (VERIFICATION 11.1), so there is no discount for paying early and therefore no implicit rate
- [ ] **TODO 4.TRADE-CREDIT.A4** — `Trade Credit A4` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.B1** — `Trade Credit B1` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.B2** — `Trade Credit B2` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.B3** — `Trade Credit B3` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.B4** — `Trade Credit B4` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.B5** — `Trade Credit B5` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.C1** — `Trade Credit C1` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.C2** — `Trade Credit C2` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.C3** — `Trade Credit C3` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.D2** — `Trade Credit D2` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.D3** — `Trade Credit D3` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.D3A** — `Trade Credit D3.a` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.D4** — `Trade Credit D4` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.D5** — `Trade Credit D5` MISSING — VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.E3** — `Trade Credit E3` MISSING — VERIFICATION 7.2: nothing resolves when a debtor ceases — a queued payment owed by a dead party stays queued
- [ ] **TODO 4.TRADE-CREDIT.A1** — `Trade Credit A1` PARTIAL — packages/kernel-rs/src/ledger.rs `Queue::given_time` holds the SAME debt open longer rather than writing a second one — receivable and payable are one row, which is what A1 asks. Nothing extends terms: `TradeCredit` journals and decides nothing (item 4)
- [ ] **TODO 4.TRADE-CREDIT.C4** — `Trade Credit C4` PARTIAL — the identity holds by construction — a queued payment is one obligation seen from two sides — and nothing checks it: the Flows family is NOT BUILT (VERIFICATION 1.4)
- [ ] **TODO 4.TRADE-CREDIT.D1** — `Trade Credit D1` PARTIAL — packages/kernel-rs/src/ledger.rs `Queue` makes lateness a real state with a day. Nothing reads it as a stress on the seller (item 4)

### 4. Small-Business Pools — 25 missing, 1 partial

> **Required review before this block:** read the **Small-Business Pools** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3880), then inspect `packages/kernel-rs/src/mechanisms/small_business.rs`, `packages/kernel-rs/src/mechanisms/securitisation.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A1** — `Small-Business Pools A1` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A2** — `Small-Business Pools A2` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A3** — `Small-Business Pools A3` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A4** — `Small-Business Pools A4` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A5** — `Small-Business Pools A5` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.B1** — `Small-Business Pools B1` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.B2** — `Small-Business Pools B2` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.B3** — `Small-Business Pools B3` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.B4** — `Small-Business Pools B4` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C1** — `Small-Business Pools C1` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C2** — `Small-Business Pools C2` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C3** — `Small-Business Pools C3` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C4** — `Small-Business Pools C4` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C5** — `Small-Business Pools C5` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C6** — `Small-Business Pools C6` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.D1** — `Small-Business Pools D1` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.D2** — `Small-Business Pools D2` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.D3** — `Small-Business Pools D3` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.D4** — `Small-Business Pools D4` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.D4A** — `Small-Business Pools D4.a` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.E1** — `Small-Business Pools E1` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.E2** — `Small-Business Pools E2` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.E3** — `Small-Business Pools E3` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.E4** — `Small-Business Pools E4` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.E6** — `Small-Business Pools E6` MISSING — VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A2A** — `Small-Business Pools A2.a` PARTIAL — the sector is cells rather than one firm. VERIFICATION 13.2: every cell posts the same reservation, so a mean-preserving spread crosses nothing

### 5. Equity — 26 missing, 1 partial

> **Required review before this block:** read the **Equity** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1408), then inspect `packages/kernel-rs/src/mechanisms/equity.rs`, `packages/kernel-rs/src/mechanisms/redeemable.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 5.EQUITY.A5** — `Equity A5` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.A5B** — `Equity A5.b` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.B1** — `Equity B1` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.B2** — `Equity B2` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.B5** — `Equity B5` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.B6** — `Equity B6` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.C2** — `Equity C2` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.C3** — `Equity C3` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.C4** — `Equity C4` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.C5** — `Equity C5` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.C6** — `Equity C6` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.C7** — `Equity C7` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.D2** — `Equity D2` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.D3** — `Equity D3` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.D4** — `Equity D4` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.E1** — `Equity E1` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.E2** — `Equity E2` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.E3** — `Equity E3` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.E4** — `Equity E4` MISSING — VERIFICATION 4.1: nothing wipes a register on insolvency, because no estate is ever opened
- [ ] **TODO 5.EQUITY.F1** — `Equity F1` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.F2** — `Equity F2` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.F3** — `Equity F3` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.F4** — `Equity F4` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.G1** — `Equity G1` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.G2** — `Equity G2` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.G3** — `Equity G3` MISSING — VERIFICATION 12.1: the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.A6** — `Equity A6` PARTIAL — packages/kernel-rs/src/instruments.rs `display` names a share by its issuer. Its one caller, `observer.rs display_name`, is dead code (VERIFICATION 11.1)

### 5. Fund Shares — 16 missing, 7 partial

> **Required review before this block:** read the **Fund Shares** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1642), then inspect `packages/kernel-rs/src/mechanisms/funds.rs`, `packages/kernel-rs/src/mechanisms/redeemable.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 5.FUND-SHARES.B3** — `Fund Shares B3` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.B4** — `Fund Shares B4` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.C2** — `Fund Shares C2` MISSING — `funds.rs Redeems`/`Redeemed` and `redeemable.rs holders_against_the_book` are the redemption and are unreachable (VERIFICATION 11.1). Nothing redeems, so C2.b`s forced-seller channel — "the point" — does not exist
- [ ] **TODO 5.FUND-SHARES.C4** — `Fund Shares C4` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.C5** — `Fund Shares C5` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.D1** — `Fund Shares D1` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.D2** — `Fund Shares D2` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.D3** — `Fund Shares D3` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.D4** — `Fund Shares D4` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.D5** — `Fund Shares D5` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.E1** — `Fund Shares E1` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.E2** — `Fund Shares E2` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.E3** — `Fund Shares E3` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.E4** — `Fund Shares E4` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.F2** — `Fund Shares F2` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.F3** — `Fund Shares F3` MISSING — VERIFICATION 9.1: the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1)
- [ ] **TODO 5.FUND-SHARES.A2** — `Fund Shares A2` PARTIAL — packages/kernel-rs/src/mechanisms/redeemable.rs `Subscribing` counts shares off `agreed::SUBSCRIPTION` terms. They are agreement terms rather than an instrument holders hold, so A2`s "counted in shares" is a number on a relation, and the `redeemable` module that holds the claim is imported by nothing (item 5)
- [ ] **TODO 5.FUND-SHARES.A3** — `Fund Shares A3` PARTIAL — packages/kernel-rs/src/mechanisms/redeemable.rs `Book::nav` computes assets minus liabilities over shares. Nothing checks that a fund`s equity is zero; the Accounts family is NOT BUILT (VERIFICATION 1.4)
- [ ] **TODO 5.FUND-SHARES.A4** — `Fund Shares A4` PARTIAL — `agreed::MANDATE` carries the lowest grade a pool may hold, and packages/kernel-rs/src/mechanisms/funds.rs `FundMandates` reads it. Nothing else constrains what a fund buys, and `funds` is imported by nothing (item 5)
- [ ] **TODO 5.FUND-SHARES.B1** — `Fund Shares B1` PARTIAL — packages/kernel-rs/src/mechanisms/redeemable.rs `Book::nav` is a read every time and is stored nowhere. VERIFICATION 6.2: its asset side is one of the three inline copies of units-times-price with a silent fallback to cost
- [ ] **TODO 5.FUND-SHARES.B2** — `Fund Shares B2` PARTIAL — see B1 — where a line printed the mark is the print; where it did not, the fallback is cost and nothing says so (B2.a) — value is a function and it reaches no balance sheet (item 5)
- [ ] **TODO 5.FUND-SHARES.C3** — `Fund Shares C3` PARTIAL — shares rise on a subscription. Nothing takes them back (C2): `redeemable::Meeting` has no caller (item 5)
- [ ] **TODO 5.FUND-SHARES.G1** — `Fund Shares G1` PARTIAL — packages/kernel-rs/src/mechanisms/redeemable.rs is the shape of a redeemable claim. `Meeting` and `holders_against_the_book` have no caller, so no investor can ask for its money back (item 5)

### 5. Dealer Desks — 21 missing, 4 partial

> **Required review before this block:** read the **Dealer Desks** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2598), then inspect `packages/kernel-rs/src/mechanisms/dealing.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 5.DEALER-DESKS.A4** — `Dealer Desks A4` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.B1** — `Dealer Desks B1` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.B2** — `Dealer Desks B2` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.B3** — `Dealer Desks B3` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.B4** — `Dealer Desks B4` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.C1** — `Dealer Desks C1` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.C3** — `Dealer Desks C3` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.C4** — `Dealer Desks C4` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.C5** — `Dealer Desks C5` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.C5A** — `Dealer Desks C5.a` MISSING — VERIFICATION 13.1: `bid = around - width - skew; ask = around + width - skew` is a stated width applied to a mid, which is what this FORBID names
- [ ] **TODO 5.DEALER-DESKS.C5B** — `Dealer Desks C5.b` MISSING — VERIFICATION 13.1: `dealer.width` = 0.02, declared "share of the mid", is the stated spread table this FORBID names
- [ ] **TODO 5.DEALER-DESKS.D2** — `Dealer Desks D2` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.D3** — `Dealer Desks D3` MISSING — nothing charges a desk rent for its inventory. XI-4 joint three: a desk that carries inventory for free has no reason to shed it
- [ ] **TODO 5.DEALER-DESKS.D4** — `Dealer Desks D4` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.D5** — `Dealer Desks D5` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.E1** — `Dealer Desks E1` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.E2** — `Dealer Desks E2` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.E3** — `Dealer Desks E3` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.E4** — `Dealer Desks E4` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.F2** — `Dealer Desks F2` MISSING — VERIFICATION 12.1: §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.F3** — `Dealer Desks F3` MISSING — a desk has no P&L at all: VERIFICATION 6.1 values its book at cost and 6.3 has nothing revalue it, so what the inventory did is never booked
- [ ] **TODO 5.DEALER-DESKS.A1** — `Dealer Desks A1` PARTIAL — packages/kernel-rs/src/mechanisms/dealing.rs `Dealers` is a named party of `kinds::DEALER` with its own register rows. It has no balance sheet inside a bank (A1) and no funding cost, and `dealing` is imported by nothing (item 5)
- [ ] **TODO 5.DEALER-DESKS.A2** — `Dealer Desks A2` PARTIAL — packages/kernel-rs/src/mechanisms/dealing.rs `Dealers::orders` posts a bid and an ask. VERIFICATION 12.1: no dealer kind is wired as a system, and 13.1: both sides come off a stated width applied to a constant
- [ ] **TODO 5.DEALER-DESKS.C2** — `Dealer Desks C2` PARTIAL — packages/kernel-rs/src/mechanisms/dealing.rs `Dealers::orders` skews on inventory against its limit, which is the one part of §26 C that is a consequence. The level it skews around is a constant (VERIFICATION 13.1)
- [ ] **TODO 5.DEALER-DESKS.D1** — `Dealer Desks D1` PARTIAL — packages/kernel-rs/src/mechanisms/dealing.rs `Dealers::orders` stops quoting at `held.abs() >= limit`. VERIFICATION 13.1: the comparison is units against a money amount

### 5. Capital Programme — 18 missing, 2 partial

> **Required review before this block:** read the **Capital Programme** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3183), then inspect `packages/kernel-rs/src/mechanisms/capital_programme.rs`, `packages/kernel-rs/src/mechanisms/cost_of_capital.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 5.CAPITAL-PROGRAMME.A3** — `Capital Programme A3` MISSING — packages/kernel-rs/src/instruments.rs `worn` is the one schedule A3 asks for and it is unreached (VERIFICATION 11.1). Nothing depreciates: no charge against profit and no reduction in the stock. (The row was also the one MALFORMED row in this file — it had no closing table cell, so no reader and no tool had ever counted it: VERIFICATION 0.5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.A4** — `Capital Programme A4` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.A5** — `Capital Programme A5` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.B2** — `Capital Programme B2` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.B3** — `Capital Programme B3` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.B4** — `Capital Programme B4` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.C1** — `Capital Programme C1` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.C2** — `Capital Programme C2` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.C4** — `Capital Programme C4` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.D1** — `Capital Programme D1` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.D2** — `Capital Programme D2` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.D3** — `Capital Programme D3` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.D4** — `Capital Programme D4` MISSING — VERIFICATION 1.4: capacity, output and utilisation are reconciled nowhere; `goods.rs utilisation` is unreached
- [ ] **TODO 5.CAPITAL-PROGRAMME.E1** — `Capital Programme E1` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.E2** — `Capital Programme E2` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.E3** — `Capital Programme E3` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.E4** — `Capital Programme E4` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.F1** — `Capital Programme F1` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.B1** — `Capital Programme B1` PARTIAL — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` reads the firm`s own quantity and named-price outlooks, names the required plant line, and `Builder` submits a bounded bid only in that book. The programme still needs an explicit financing handoff (item 5)
- [ ] **TODO 5.CAPITAL-PROGRAMME.C3** — `Capital Programme C3` PARTIAL — `invest.takes` is the declared build horizon; the programme now completes its purchase commitment only from settled plant trades, but construction/in-service transition after that purchase remains open (item 5)

### 6. Money Market — 22 missing, 0 partial

> **Required review before this block:** read the **Money Market** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1491), then inspect `packages/kernel-rs/src/mechanisms/money_market.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 6.MONEY-MARKET.A1** — `Money Market A1` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.A2** — `Money Market A2` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.A2B** — `Money Market A2.b` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.A3** — `Money Market A3` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.B1** — `Money Market B1` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.B2** — `Money Market B2` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.B2B** — `Money Market B2.b` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.B3** — `Money Market B3` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.B4** — `Money Market B4` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.B5** — `Money Market B5` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.B6** — `Money Market B6` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.B6A** — `Money Market B6.a` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.C1** — `Money Market C1` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.C3** — `Money Market C3` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.D1** — `Money Market D1` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.D2** — `Money Market D2` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.D5** — `Money Market D5` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.D5B** — `Money Market D5.b` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.D6** — `Money Market D6` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.E1** — `Money Market E1` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.E2** — `Money Market E2` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing
- [ ] **TODO 6.MONEY-MARKET.E3** — `Money Market E3` MISSING — VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing

### 6. Banks Funding — 29 missing, 0 partial

> **Required review before this block:** read the **Banks Funding** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2453), then inspect `packages/kernel-rs/src/mechanisms/bank_funding.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 6.BANKS-FUNDING.A1** — `Banks Funding A1` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.A1D** — `Banks Funding A1.d` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.A2** — `Banks Funding A2` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.A3** — `Banks Funding A3` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.A4** — `Banks Funding A4` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.A5** — `Banks Funding A5` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.B1** — `Banks Funding B1` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.B2** — `Banks Funding B2` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.B3** — `Banks Funding B3` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.C1** — `Banks Funding C1` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.C2** — `Banks Funding C2` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.C3** — `Banks Funding C3` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.C3A** — `Banks Funding C3.a` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.C4** — `Banks Funding C4` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.D1** — `Banks Funding D1` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.D2** — `Banks Funding D2` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.D3** — `Banks Funding D3` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.D4** — `Banks Funding D4` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.D5** — `Banks Funding D5` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.D6** — `Banks Funding D6` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.E1** — `Banks Funding E1` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.E2** — `Banks Funding E2` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.E3** — `Banks Funding E3` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.E3A** — `Banks Funding E3.a` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.E4** — `Banks Funding E4` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.E5** — `Banks Funding E5` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.F1** — `Banks Funding F1` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.F3** — `Banks Funding F3` MISSING — VERIFICATION 1.4: the Accounts family has no contribution and is NOT BUILT every period
- [ ] **TODO 6.BANKS-FUNDING.F4** — `Banks Funding F4` MISSING — VERIFICATION 9.1: `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller (11.1). Deposit classes and run behaviour remain unwired

### 6. Banks Capital — 18 missing, 2 partial

> **Required review before this block:** read the **Banks Capital** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2527), then inspect `packages/kernel-rs/src/mechanisms/bank_capital.rs`, `packages/kernel-rs/src/mechanisms/loss.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 6.BANKS-CAPITAL.A2** — `Banks Capital A2` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.A3** — `Banks Capital A3` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.A4** — `Banks Capital A4` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.B1C** — `Banks Capital B1.c` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.B2** — `Banks Capital B2` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.B3** — `Banks Capital B3` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.B3A** — `Banks Capital B3.a` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.C3** — `Banks Capital C3` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D1** — `Banks Capital D1` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D2** — `Banks Capital D2` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D2A** — `Banks Capital D2.a` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D3** — `Banks Capital D3` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D4** — `Banks Capital D4` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D5** — `Banks Capital D5` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D6** — `Banks Capital D6` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.E1** — `Banks Capital E1` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.E2** — `Banks Capital E2` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.E3** — `Banks Capital E3` MISSING — VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.A1** — `Banks Capital A1` PARTIAL — packages/kernel-rs/src/instruments.rs `equity` is a residual and is never a stored pot. VERIFICATION 6.1: it is computed at cost, so a loss on a mark never reaches it
- [ ] **TODO 6.BANKS-CAPITAL.B1** — `Banks Capital B1` PARTIAL — `bank.min_weighted` = 0.08 and `bank.min_leverage` = 0.03 are declared POLICY primitives owned by the parliament, and `BankCapital` reads them. Nothing weights an asset, so B1.a`s risk weights do not exist — no value reaches a balance sheet to be weighted (item 6)

### 6. Insurers — 22 missing, 1 partial

> **Required review before this block:** read the **Insurers** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2669), then inspect `packages/kernel-rs/src/mechanisms/insurers.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 6.INSURERS.A2** — `Insurers A2` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.A3** — `Insurers A3` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.A4** — `Insurers A4` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.B1** — `Insurers B1` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.B2** — `Insurers B2` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.B2B** — `Insurers B2.b` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.B3** — `Insurers B3` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.B4** — `Insurers B4` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.C1** — `Insurers C1` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.C2** — `Insurers C2` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.C3** — `Insurers C3` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.C4** — `Insurers C4` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.C5** — `Insurers C5` MISSING — was OUT OF SCOPE on the ground that an insurer buys credit through the pools it subscribes to and the pool holds the mandate. VERIFICATION 12.1: the pools and the mandate machinery cited are the deleted TypeScript; nothing in packages/kernel-rs forces a sale on a downgrade (Ratings C1.a)
- [ ] **TODO 6.INSURERS.D1** — `Insurers D1` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.D2** — `Insurers D2` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.D3** — `Insurers D3` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.D4** — `Insurers D4` MISSING — was OUT OF SCOPE on the ground that the gap is hedged in the swap and futures books, "which exist". VERIFICATION 12.2: `irs` runs the generic loan-servicing mechanism and there is no futures book anywhere, so the reason the exemption rested on is not true of this engine
- [ ] **TODO 6.INSURERS.D5** — `Insurers D5` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.E1** — `Insurers E1` MISSING — VERIFICATION 12.1: there is no liability at all, so there is none without beneficiaries — the FORBID has nothing to refuse
- [ ] **TODO 6.INSURERS.E2** — `Insurers E2` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.E3** — `Insurers E3` MISSING — nothing stores a liability value, because nothing holds a liability. `insurers.rs present_value` is the read and has no caller
- [ ] **TODO 6.INSURERS.E4** — `Insurers E4` MISSING — VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.A1** — `Insurers A1` PARTIAL — `kinds::INSURER` is a named party with an account and rows, and packages/kernel-rs/src/mechanisms/insurers.rs `InsurerMatching` puts it in long books. It holds no liability to anybody, and `insurers` is imported by nothing (item 6)

### 7. Currency — 17 missing, 5 partial

> **Required review before this block:** read the **Currency** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 818), then inspect `packages/kernel-rs/src/registry.rs`, `packages/kernel-rs/src/ledger.rs`, `packages/kernel-rs/src/mechanisms/currency.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 7.CURRENCY.A3** — `Currency A3` MISSING — VERIFICATION 5.3: there is no amount type in packages/kernel-rs that carries a currency. An amount is a bare f64 in every leg, store and mechanism, so A3.a`s "a function that takes an amount takes its currency with it" does not hold anywhere
- [ ] **TODO 7.CURRENCY.A4** — `Currency A4` MISSING — VERIFICATION 5.2, 8.1: a leg carries a `ccy` that nothing checks against the instrument it names, and settlement infers where a payment lands from the payee`s bank without reading a currency at all
- [ ] **TODO 7.CURRENCY.A5** — `Currency A5` MISSING — VERIFICATION 2.1: there is no seed, so there is no named set of currencies. packages/kernel-rs/src/registry.rs can hold one; nothing states it
- [ ] **TODO 7.CURRENCY.B4** — `Currency B4` MISSING — VERIFICATION 9.1: `SpotFx` proposes no instruction, so nothing is ever acquired from anybody at a price. packages/kernel-rs/src/mechanisms/currency.rs `short_of`/`MustBuy` is the shape and is dead code (10.1)
- [ ] **TODO 7.CURRENCY.B5** — `Currency B5` MISSING — no bank runs a foreign position; VERIFICATION 9.1
- [ ] **TODO 7.CURRENCY.C1** — `Currency C1` MISSING — VERIFICATION 9.1: `SpotFx` computes a rate and journals it. No FX trade is ever proposed, so no rate is cleared from anything that settled
- [ ] **TODO 7.CURRENCY.C4** — `Currency C4` MISSING — there is no numéraire read anywhere in packages/kernel-rs
- [ ] **TODO 7.CURRENCY.C4A** — `Currency C4.a` MISSING — VERIFICATION 8.1: value does not live in a numéraire, but only because there is no numéraire; the currency position is destroyed by settlement converting at par instead
- [ ] **TODO 7.CURRENCY.C5** — `Currency C5` MISSING — VERIFICATION 8.1: there is no rate in force. Settlement converts at one and nothing values a foreign position at all (6.3)
- [ ] **TODO 7.CURRENCY.D1** — `Currency D1` MISSING — VERIFICATION 6.3: no rate is in force for a period and nothing values against one
- [ ] **TODO 7.CURRENCY.D2** — `Currency D2` MISSING — VERIFICATION 6.3: there is no revaluation anywhere in packages/kernel-rs
- [ ] **TODO 7.CURRENCY.D2B** — `Currency D2.b` MISSING — VERIFICATION 6.3, 8.1: every foreign position is unrevalued, which is what this FORBID calls money created or destroyed silently
- [ ] **TODO 7.CURRENCY.D3** — `Currency D3` MISSING — VERIFICATION 6.3
- [ ] **TODO 7.CURRENCY.D4** — `Currency D4` MISSING — VERIFICATION 6.3, 1.4: nothing revalues and no family checks it
- [ ] **TODO 7.CURRENCY.E1** — `Currency E1` MISSING — VERIFICATION 9.1: no rate moves because somebody traded at it; nobody trades
- [ ] **TODO 7.CURRENCY.E2** — `Currency E2` MISSING
- [ ] **TODO 7.CURRENCY.E4** — `Currency E4` MISSING — VERIFICATION 9.1: there is no flow to move a rate
- [ ] **TODO 7.CURRENCY.B1** — `Currency B1` PARTIAL — packages/kernel-rs/src/registry.rs `currency_of(region)` reads through the country (one fact, one writer). A party`s reporting in it is not built, and the `currency` module that would is imported by nothing (item 7)
- [ ] **TODO 7.CURRENCY.B2** — `Currency B2` PARTIAL — packages/kernel-rs/src/register.rs holds a row per (party, money), so a foreign holding is a real position. VERIFICATION 8.1: it is not acquired by a trade — settlement converts at par on arrival
- [ ] **TODO 7.CURRENCY.C2** — `Currency C2` PARTIAL — packages/kernel-rs/src/mechanisms/currency.rs `Rates::of` inverts one print rather than storing two (Law 4). The store is dead code (VERIFICATION 10.1)
- [ ] **TODO 7.CURRENCY.C3** — `Currency C3` PARTIAL — packages/kernel-rs/src/mechanisms/currency.rs `gap` measures the triangle and `arbitrage` bounds who closes it — the right shape, and C3.b`s no-triangulating-read is honoured by `Rates::of` answering None. All of it is dead code (VERIFICATION 10.1)
- [ ] **TODO 7.CURRENCY.E3** — `Currency E3` PARTIAL — packages/kernel-rs/src/mechanisms/currency.rs has no written path and `Rates::of` refuses to invent one, which is the FORBID holding by absence. It holds in dead code (VERIFICATION 10.1)

### 7. Spot FX — 11 missing, 4 partial

> **Required review before this block:** read the **Spot FX** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1575), then inspect `packages/kernel-rs/src/mechanisms/spot_fx.rs`, `packages/kernel-rs/src/mechanisms/currency.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 7.SPOT-FX.B3** — `Spot FX B3` MISSING — no portfolio allocator posts a currency order
- [ ] **TODO 7.SPOT-FX.B5** — `Spot FX B5` MISSING — no dealer participant posts a two-sided FX schedule or owns currency inventory
- [ ] **TODO 7.SPOT-FX.B6** — `Spot FX B6` MISSING — no central-bank policy decision posts a bounded FX order
- [ ] **TODO 7.SPOT-FX.C3** — `Spot FX C3` MISSING — no dealer posts the two-sided schedules from which a bid-offer could emerge
- [ ] **TODO 7.SPOT-FX.C6** — `Spot FX C6` MISSING — the production world has no pair-keyed history from which zero-flow drift and one-way-flow movement can be measured
- [ ] **TODO 7.SPOT-FX.D1** — `Spot FX D1` MISSING — the settled counterparty positions are real, but no FX dealer participant fills a client and owns the resulting inventory
- [ ] **TODO 7.SPOT-FX.D2** — `Spot FX D2` MISSING — packages/kernel-rs/src/mechanisms/spot_fx.rs `square` names a counterparty but no production dealer calls it
- [ ] **TODO 7.SPOT-FX.D3** — `Spot FX D3` MISSING — packages/kernel-rs/src/mechanisms/spot_fx.rs can calculate inventory revaluation, but no production dealer carries that inventory
- [ ] **TODO 7.SPOT-FX.D4** — `Spot FX D4` MISSING — packages/kernel-rs/src/mechanisms/spot_fx.rs expresses a dealer inventory limit, but no production dealer consults it
- [ ] **TODO 7.SPOT-FX.F1** — `Spot FX F1` MISSING — ordinary goods settlement still pays in the buyer`s account money rather than applying one seller-money convention
- [ ] **TODO 7.SPOT-FX.F1B** — `Spot FX F1.b` MISSING — packages/kernel-rs/src/session.rs still chooses the buyer`s account instrument, so the settlement money changes with the buyer
- [ ] **TODO 7.SPOT-FX.A3** — `Spot FX A3` PARTIAL — packages/kernel-rs/src/mechanisms/currency.rs measures direct-versus-cross inconsistency, but production clears each offered currency independently and no bounded arbitrage order consumes the gap (item 7)
- [ ] **TODO 7.SPOT-FX.C2** — `Spot FX C2` PARTIAL — packages/kernel-rs/src/mechanisms/spot_fx.rs clears each offered currency, but cross-pair consistency is neither a clearing constraint nor consumed by bounded arbitrage (item 7)
- [ ] **TODO 7.SPOT-FX.C5** — `Spot FX C5` PARTIAL — packages/kernel-rs/src/mechanisms/spot_fx.rs uses one clearing rate for both reciprocal settlement legs and journals it, but the rate is not yet a pair-keyed production price used by valuation (item 7)
- [ ] **TODO 7.SPOT-FX.E3** — `Spot FX E3` PARTIAL — packages/kernel-rs/src/mechanisms/currency.rs `arbitrage` bounds the arbitrageur by its own capital, which is the clause. It is dead code (VERIFICATION 10.1)

### 7. Indices — 12 missing, 1 partial

> **Required review before this block:** read the **Indices** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2299), then inspect `packages/kernel-rs/src/mechanisms/benchmarks.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 7.INDICES.A1** — `Indices A1` MISSING — VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.A4** — `Indices A4` MISSING — VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.B1** — `Indices B1` MISSING — VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.B2** — `Indices B2` MISSING — VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.B3** — `Indices B3` MISSING — VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.B4** — `Indices B4` MISSING — VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.C1** — `Indices C1` MISSING — VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.C2** — `Indices C2` MISSING — VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.C2A** — `Indices C2.a` MISSING — VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.C3** — `Indices C3` MISSING — VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.C4** — `Indices C4` MISSING — VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.E3** — `Indices E3` MISSING — VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.D3** — `Indices D3` PARTIAL — packages/kernel-rs/src/mechanisms/benchmarks.rs `Fixes` publishes an overnight fixing from a cleared print and refuses anything else. VERIFICATION 3.2: one book of 1,546 prints per period, so there is almost never a fixing to publish

### 7. Cross-Border — 19 missing, 1 partial

> **Required review before this block:** read the **Cross-Border** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3966), then inspect `packages/kernel-rs/src/mechanisms/cross_border.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 7.CROSS-BORDER.A2** — `Cross-Border A2` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.A3** — `Cross-Border A3` MISSING — VERIFICATION 8.1: a payment does NOT reach an account in the currency it was made in — `across` lands it in the payee`s bank`s money whatever that money is
- [ ] **TODO 7.CROSS-BORDER.A4** — `Cross-Border A4` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.B1** — `Cross-Border B1` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.B2** — `Cross-Border B2` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.B3** — `Cross-Border B3` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.B4** — `Cross-Border B4` MISSING — VERIFICATION 1.4: nothing reconciles one region`s exports against another`s imports; the Flows family is NOT BUILT
- [ ] **TODO 7.CROSS-BORDER.C1** — `Cross-Border C1` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.C2** — `Cross-Border C2` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.C3** — `Cross-Border C3` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.C4** — `Cross-Border C4` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.C5** — `Cross-Border C5` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.D4** — `Cross-Border D4` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.D6** — `Cross-Border D6` MISSING — see B4 — nothing sums the regions
- [ ] **TODO 7.CROSS-BORDER.E1** — `Cross-Border E1` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.E2** — `Cross-Border E2` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.E3** — `Cross-Border E3` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.E4** — `Cross-Border E4` MISSING — VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1)
- [ ] **TODO 7.CROSS-BORDER.F1** — `Cross-Border F1` MISSING — VERIFICATION 8.4: the assembled world has one currency, so nothing about a region being open can be exercised
- [ ] **TODO 7.CROSS-BORDER.D3** — `Cross-Border D3` PARTIAL — `CrossBorder` derives the imbalance and reports it with a size rather than asserting it away, which is what D3 asks. VERIFICATION 1.4: no family checks it

### 8. M&A — 20 missing, 1 partial

> **Required review before this block:** read the **M&A** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3337), then inspect `packages/kernel-rs/src/mechanisms/control.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 8.M-A.A2** — `M&A A2` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.A3** — `M&A A3` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.A4** — `M&A A4` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.A5** — `M&A A5` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.B1** — `M&A B1` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.B2** — `M&A B2` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.B3** — `M&A B3` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.B4** — `M&A B4` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.B5** — `M&A B5` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals it. Nothing bids, nothing is accepted and nothing is refused, so a deal happens because a rule fired — which is what this FORBID names
- [ ] **TODO 8.M-A.C1** — `M&A C1` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.C2** — `M&A C2` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.C3** — `M&A C3` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.C4** — `M&A C4` MISSING — VERIFICATION 3.2: with one book of 1,546 printing per period there is no cheap firm to attract a bid, and nothing bids
- [ ] **TODO 8.M-A.D1** — `M&A D1` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.D2** — `M&A D2` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.D3** — `M&A D3` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.D4** — `M&A D4` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.D5** — `M&A D5` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.E2** — `M&A E2` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.E3** — `M&A E3` MISSING — VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1)
- [ ] **TODO 8.M-A.A1** — `M&A A1` PARTIAL — packages/kernel-rs/src/mechanisms/control.rs `Control` reads an acquirer`s own valuation against a target`s price and opens a process. No price per share is agreed and no owner accepts — a decision missing inside a system that runs (item 8)

### 8. Ratings — 14 missing, 7 partial

> **Required review before this block:** read the **Ratings** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 4031), then inspect `packages/kernel-rs/src/mechanisms/ratings.rs`, `packages/kernel-rs/src/mechanisms/second_opinion.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 8.RATINGS.A3** — `Ratings A3` MISSING — VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.A4** — `Ratings A4` MISSING — VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.B1** — `Ratings B1` MISSING — VERIFICATION 15.2: the probability of failing is written as a literal 0.0
- [ ] **TODO 8.RATINGS.B2** — `Ratings B2` MISSING — VERIFICATION 15.2: the loss given failure is written as a literal 0.0
- [ ] **TODO 8.RATINGS.B3** — `Ratings B3` MISSING — VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.C4** — `Ratings C4` MISSING — VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.C5** — `Ratings C5` MISSING — VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.D1** — `Ratings D1` MISSING — VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.D2** — `Ratings D2` MISSING — VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.D3** — `Ratings D3` MISSING — VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.D4** — `Ratings D4` MISSING — VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.D5** — `Ratings D5` MISSING — VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.E2** — `Ratings E2` MISSING — VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.E3** — `Ratings E3` MISSING — VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.A1** — `Ratings A1` PARTIAL — packages/kernel-rs/src/mechanisms/ratings.rs `Grading` writes a `standing::GRADE` per (house, name) and says it publicly. It is ordinal and published; VERIFICATION 15.3: an ungraded name is given one anyway
- [ ] **TODO 8.RATINGS.A2** — `Ratings A2` PARTIAL — `Grading` reads published income and its trend, and leverage as what a name owes against what it holds. Age, size and sector are not read (item 8)
- [ ] **TODO 8.RATINGS.C1** — `Ratings C1` PARTIAL — `agreed::MANDATE` carries the lowest grade a pool may hold and packages/kernel-rs/src/mechanisms/forced_sale.rs `ForcedSeller` reads a breach of it. VERIFICATION 9.1: it journals the breach and sells nothing
- [ ] **TODO 8.RATINGS.C2** — `Ratings C2` PARTIAL — packages/kernel-rs/src/mechanisms/bank_capital.rs `BankCapital` weights an asset by `ratings::haircut(grade, 1.0)`, so a grade does consume capital. Nothing follows from the number (Banks Capital)
- [ ] **TODO 8.RATINGS.C3** — `Ratings C3` PARTIAL — `bank_capital::haircut(grade, …)` is per issuer credit rather than one number per instrument type, which is what C3 asks for, and it rises with every notch. Nothing pledges anything, so no haircut is ever applied to a borrowing: the module that would post it is imported by nothing (item 8)
- [ ] **TODO 8.RATINGS.E1** — `Ratings E1` PARTIAL — C2 and C3 give a grade two consumers inside `BankCapital`. Neither changes what anybody does (item 8)
- [ ] **TODO 8.RATINGS.E4** — `Ratings E4` PARTIAL — the distribution is a read over the standings. VERIFICATION 15.3 makes every ungraded name part of it at a stated grade

### 8. Reporting — 19 missing, 6 partial

> **Required review before this block:** read the **Reporting** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 4103), then inspect `packages/kernel-rs/src/mechanisms/reporting.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 8.REPORTING.A5** — `Reporting A5` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.B1** — `Reporting B1` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.B2** — `Reporting B2` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.B3** — `Reporting B3` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.B4** — `Reporting B4` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.C4** — `Reporting C4` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.D1** — `Reporting D1` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.D2** — `Reporting D2` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.E1** — `Reporting E1` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.F1** — `Reporting F1` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.F2** — `Reporting F2` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.F3** — `Reporting F3` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.F4** — `Reporting F4` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.G1** — `Reporting G1` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.G3** — `Reporting G3` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.H1** — `Reporting H1` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.H2** — `Reporting H2` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.H3** — `Reporting H3` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.H4** — `Reporting H4` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.A2** — `Reporting A2` PARTIAL — `Publishes` reads the register and the journal. VERIFICATION 6.1: what it reads is valued at cost, and G2`s "the equity account`s movement over the fiscal period" has no equity account (Audit B5)
- [ ] **TODO 8.REPORTING.A4** — `Reporting A4` PARTIAL — `reporting.asymmetry` = 45 days is the lag. Nothing is known privately in between, because nothing is known at all until it is said (item 8)
- [ ] **TODO 8.REPORTING.C1** — `Reporting C1` PARTIAL — packages/kernel-rs/src/systems.rs wires `Publishes`, whose production path forms a holder-bank estimate from that bank's prior standing estimate and the company's newly published book value. It does not publish the estimate and does not consume guidance or the company's cleared operating markets (item 12)
- [ ] **TODO 8.REPORTING.C2** — `Reporting C2` PARTIAL — packages/kernel-rs/src/mechanisms/reporting.rs stores each production estimate as a standing keyed by the named bank and company, and the standing store dates changes. It is not emitted publicly, so visibility is missing (item 12)
- [ ] **TODO 8.REPORTING.C3** — `Reporting C3` PARTIAL — packages/kernel-rs/src/mechanisms/reporting.rs uses each bank's own prior estimate and `Parties::outlook_memory`, so histories can disagree. The result does not reach a share-book participant, so disagreement is not yet load-bearing (item 12)
- [ ] **TODO 8.REPORTING.G2** — `Reporting G2` PARTIAL — packages/kernel-rs/src/mechanisms/reporting.rs publishes income as the change from the last published booked equity. The production path does not decompose capital raised, distributions, settled instructions and marks, although the test-only `Books` helper models that decomposition (item 12)

### 9. Central Bank — 20 missing, 2 partial

> **Required review before this block:** read the **Central Bank** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2929), then inspect `packages/kernel-rs/src/mechanisms/money.rs`, `packages/kernel-rs/src/mechanisms/bank_funding.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 9.CENTRAL-BANK.A2** — `Central Bank A2` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.A2C** — `Central Bank A2.c` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.A3** — `Central Bank A3` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.A4** — `Central Bank A4` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.B1** — `Central Bank B1` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.B3** — `Central Bank B3` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.B4** — `Central Bank B4` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.C1** — `Central Bank C1` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.C2** — `Central Bank C2` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.C3** — `Central Bank C3` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.C4** — `Central Bank C4` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.D2** — `Central Bank D2` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.E1** — `Central Bank E1` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.E3** — `Central Bank E3` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.E4** — `Central Bank E4` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.E5** — `Central Bank E5` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.F1** — `Central Bank F1` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.F2** — `Central Bank F2` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.F3** — `Central Bank F3` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.F4** — `Central Bank F4` MISSING — VERIFICATION 14.1: §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.A1** — `Central Bank A1` PARTIAL — packages/kernel-rs/src/instruments.rs makes the named central bank the issuer of reserves, while packages/kernel-rs/src/mechanisms/bank_funding.rs can mint and lend those reserves against collateral at a declared penalty. The broader policy-rate and corridor system remains absent (item 9)
- [ ] **TODO 9.CENTRAL-BANK.D1** — `Central Bank D1` PARTIAL — packages/kernel-rs/src/mechanisms/bank_funding.rs implements a standing facility bounded by the borrowing bank's free eligible claim holdings and atomically encumbers the pledged units. It runs after the money-market session rather than as a seat in that session (item 9)

### 9. Polity — 24 missing, 1 partial

> **Required review before this block:** read the **Polity** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3005), then inspect `packages/kernel-rs/src/mechanisms/polity.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 9.POLITY.A2** — `Polity A2` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.A3** — `Polity A3` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.B1** — `Polity B1` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.B1A** — `Polity B1.a` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.B2** — `Polity B2` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.B2B** — `Polity B2.b` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.B3** — `Polity B3` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.B4** — `Polity B4` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.C1** — `Polity C1` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.C2** — `Polity C2` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.C3** — `Polity C3` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.C3A** — `Polity C3.a` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.C3B** — `Polity C3.b` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.C4** — `Polity C4` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.D1** — `Polity D1` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.D2** — `Polity D2` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.D3** — `Polity D3` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.D4** — `Polity D4` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.E1** — `Polity E1` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.E2** — `Polity E2` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.E3** — `Polity E3` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.E4** — `Polity E4` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.F3** — `Polity F3` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.F5** — `Polity F5` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.A1** — `Polity A1` PARTIAL — `parliament.seats` = 100 is declared `Kind::Policy`, `Owner::Constitution` — the one number no mechanism produces, as A1 asks. The rest of A1 waits on `polity`, which is imported by nothing (item 9)

### 10. Derivative — 10 missing, 5 partial

> **Required review before this block:** read the **Derivative** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 964), then inspect `packages/kernel-rs/src/stores.rs`, `packages/kernel-rs/src/mechanisms/derivative_layer.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.DERIVATIVE.D1B** — `Derivative D1.b` MISSING — VERIFICATION 1.4: the ZeroSum family has no contribution and is declared NOT BUILT every period
- [ ] **TODO 10.DERIVATIVE.D3** — `Derivative D3` MISSING — `AgreementTerms::PriceForward` types its underlying as an instrument, but no production mechanism strikes that contract, so no live derivative can yet be checked against a price this world clears
- [ ] **TODO 10.DERIVATIVE.D3A** — `Derivative D3.a` MISSING — there is no underlying field, so the FORBID has nothing to refuse
- [ ] **TODO 10.DERIVATIVE.D4** — `Derivative D4` MISSING — no payoff function is carried on a contract; `derivative_layer.rs` variants are dead code
- [ ] **TODO 10.DERIVATIVE.D5** — `Derivative D5` MISSING — derivative terms now carry a typed settlement currency, and FX forwards carry both currencies, but a general derivative still has no typed payment-leg schedule with a currency on each leg
- [ ] **TODO 10.DERIVATIVE.D7** — `Derivative D7` MISSING — nothing strikes a derivative at a cleared level; `Derivatives` proposes nothing and opens no book (VERIFICATION 9.1)
- [ ] **TODO 10.DERIVATIVE.D8** — `Derivative D8` MISSING — VERIFICATION 6.3: nothing marks a contract after inception
- [ ] **TODO 10.DERIVATIVE.D9** — `Derivative D9` MISSING — `derivatives.collateral` is one of the three nouns with NO KERNEL HOME that `npm run world:runs` prints every run. `derivative_layer.rs initial_margin` and `variation` are dead code (VERIFICATION 11.1)
- [ ] **TODO 10.DERIVATIVE.D10** — `Derivative D10` MISSING — VERIFICATION 4.1: no claim is ever filed against an estate, so an in-the-money party has nothing
- [ ] **TODO 10.DERIVATIVE.X2** — `Derivative X2` MISSING — VERIFICATION 9.1: `Derivatives` proposes no instruction, so no margin, premium or periodic payment leaves any account
- [ ] **TODO 10.DERIVATIVE.D1** — `Derivative D1` PARTIAL — packages/kernel-rs/src/mechanisms/derivative_layer.rs `agreed::DERIVATIVE` is a two-party agreement, so the two sides are named. Nothing makes it an asset to one and a liability to the other: no mark exists (VERIFICATION 6.3) and the Accounts family is NOT BUILT (1.4)
- [ ] **TODO 10.DERIVATIVE.D2** — `Derivative D2` PARTIAL — a notional can sit in an agreement`s terms. `derivative_layer.rs gross_notional` is dead code (VERIFICATION 11.1), so D2.a`s distinction between notional and exposure is drawn nowhere
- [ ] **TODO 10.DERIVATIVE.D6** — `Derivative D6` PARTIAL — packages/kernel-rs/src/stores.rs `Agreements` carries an `until`, so a term can end. Payment dates on a leg are not represented, and `systems.rs` wires `derivative_layer::Derivatives`, but its production path does not perform this transition (item 10)
- [ ] **TODO 10.DERIVATIVE.D11** — `Derivative D11` PARTIAL — packages/kernel-rs/src/stores.rs `Agreements::ends` removes the relation from both parties at once. `derivative_layer.rs close_out` is dead code (item 10)
- [ ] **TODO 10.DERIVATIVE.D12** — `Derivative D12` PARTIAL — an agreement is its own row and each derivative class has typed identity fields, so an underlying/reference can no longer be confused with a strike or currency. The store does not yet enforce economic-key uniqueness (item 10)

### 10. Securities Lending — 18 missing, 1 partial

> **Required review before this block:** read the **Securities Lending** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1724), then inspect `packages/kernel-rs/src/mechanisms/securities_lending.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.SECURITIES-LENDING.A1** — `Securities Lending A1` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.A2** — `Securities Lending A2` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.A3** — `Securities Lending A3` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.A4** — `Securities Lending A4` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.A5** — `Securities Lending A5` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.B1** — `Securities Lending B1` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.B2** — `Securities Lending B2` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.B3** — `Securities Lending B3` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.B4** — `Securities Lending B4` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.C1** — `Securities Lending C1` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.C2** — `Securities Lending C2` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.C3** — `Securities Lending C3` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.C5** — `Securities Lending C5` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.D1** — `Securities Lending D1` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.D2** — `Securities Lending D2` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.D2A** — `Securities Lending D2.a` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.D3** — `Securities Lending D3` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.E3** — `Securities Lending E3` MISSING — VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1)
- [ ] **TODO 10.SECURITIES-LENDING.E2** — `Securities Lending E2` PARTIAL — the lender`s exposure and the borrower`s title would be two reads of one security; with no loan ever struck there is nothing to double-count. VERIFICATION 1.3: the sum-to-issued check that would catch it does not exist

### 10. Prime Brokerage — 21 missing, 1 partial

> **Required review before this block:** read the **Prime Brokerage** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1785), then inspect `packages/kernel-rs/src/mechanisms/prime_brokerage.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.PRIME-BROKERAGE.A2** — `Prime Brokerage A2` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.A3** — `Prime Brokerage A3` MISSING — packages/kernel-rs/src/mechanisms/prime_brokerage.rs `Broking` gives each client one broker by `brokers[n % brokers.len()]`, and says so — so A3`s blind spot, which E3 depends on, cannot arise
- [ ] **TODO 10.PRIME-BROKERAGE.A4** — `Prime Brokerage A4` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.B1** — `Prime Brokerage B1` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.B2** — `Prime Brokerage B2` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.B3** — `Prime Brokerage B3` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.B4** — `Prime Brokerage B4` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.B5** — `Prime Brokerage B5` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.C1** — `Prime Brokerage C1` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.C2** — `Prime Brokerage C2` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.C3** — `Prime Brokerage C3` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.C4** — `Prime Brokerage C4` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.C4A** — `Prime Brokerage C4.a` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.C5** — `Prime Brokerage C5` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.D1** — `Prime Brokerage D1` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.D2** — `Prime Brokerage D2` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.D3** — `Prime Brokerage D3` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.D4** — `Prime Brokerage D4` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.E1** — `Prime Brokerage E1` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.E2** — `Prime Brokerage E2` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.E3** — `Prime Brokerage E3` MISSING — VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.A1** — `Prime Brokerage A1` PARTIAL — `agreed::PRIME_BROKERAGE` is a named bank and a named client, and packages/kernel-rs/src/mechanisms/prime_brokerage.rs `Broking` reads it and is wired. Nothing in that production path ends it (item 10)

### 10. Derivative Layer — 25 missing, 1 partial

> **Required review before this block:** read the **Derivative Layer** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1856), then inspect `packages/kernel-rs/src/mechanisms/derivative_layer.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.DERIVATIVE-LAYER.A1** — `Derivative Layer A1` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.A2** — `Derivative Layer A2` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.A4** — `Derivative Layer A4` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.B1** — `Derivative Layer B1` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.B3** — `Derivative Layer B3` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.B4** — `Derivative Layer B4` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.C1** — `Derivative Layer C1` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.C2** — `Derivative Layer C2` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.C3** — `Derivative Layer C3` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.C4** — `Derivative Layer C4` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.D1** — `Derivative Layer D1` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.D2B** — `Derivative Layer D2.b` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.D3** — `Derivative Layer D3` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.D4** — `Derivative Layer D4` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.D5** — `Derivative Layer D5` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.E1** — `Derivative Layer E1` MISSING — `derivative_layer.rs admitted`/`cut_to`/`struck_beyond_capacity` are the capacity limit and have no caller (VERIFICATION 11.1)
- [ ] **TODO 10.DERIVATIVE-LAYER.E2** — `Derivative Layer E2` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.E3** — `Derivative Layer E3` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.E4** — `Derivative Layer E4` MISSING — see E1: what was struck beyond what members could margin is exactly `struck_beyond_capacity`, which nothing calls
- [ ] **TODO 10.DERIVATIVE-LAYER.F1** — `Derivative Layer F1` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.F2** — `Derivative Layer F2` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.F3** — `Derivative Layer F3` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.F4** — `Derivative Layer F4` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.G2** — `Derivative Layer G2` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.G4** — `Derivative Layer G4` MISSING — VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.D2** — `Derivative Layer D2` PARTIAL — packages/kernel-rs/src/mechanisms/derivative_layer.rs compares consecutive bilateral marks and schedules the change as a dated call; the agreement still does not name the settlement currency (item 10)

### 10. CDS — 22 missing, 0 partial

> **Required review before this block:** read the **CDS** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1964), then inspect `packages/kernel-rs/src/mechanisms/cds.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.CDS.A1** — `CDS A1` MISSING — `Protection` now strikes a typed two-party CDS agreement naming its reference, spread, tenor and settlement currency. It still selects terms without a real CDS order book and does not schedule premium or protection cash flows, so the specified contract is not yet operating
- [ ] **TODO 10.CDS.A2** — `CDS A2` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.A3** — `CDS A3` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.A4** — `CDS A4` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.A5** — `CDS A5` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.B1** — `CDS B1` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.B2** — `CDS B2` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.B3** — `CDS B3` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.B4** — `CDS B4` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.B5** — `CDS B5` MISSING — packages/kernel-rs/src/mechanisms/cds.rs Protection crosses two OWN_VIEW standings and refuses to clear on one, but every holder of a view is a lender to the name, so both sides are hedging and the speculative participant is on neither. NOT GUARDED, and it cannot be: who is at a book is a property of the parties, not an absence in the text (item 10)
- [ ] **TODO 10.CDS.C1** — `CDS C1` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.C3** — `CDS C3` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.C4** — `CDS C4` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.D1** — `CDS D1` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.D2** — `CDS D2` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.D3** — `CDS D3` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.D4** — `CDS D4` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.D5** — `CDS D5` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.E1** — `CDS E1` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.E2** — `CDS E2` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.E3** — `CDS E3` MISSING — VERIFICATION 12.1, 9.1: the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.E4** — `CDS E4` MISSING — VERIFICATION 9.1: `Protection` proposes no instruction, so a seller`s ability to pay is never part of anything

### 10. IRS — 18 missing, 0 partial

> **Required review before this block:** read the **IRS** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2050), then inspect `packages/kernel-rs/src/mechanisms/irs.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.IRS.A1** — `IRS A1` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.A2** — `IRS A2` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.A3** — `IRS A3` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.B1** — `IRS B1` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.B2** — `IRS B2` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.B3** — `IRS B3` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.B4** — `IRS B4` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.B5** — `IRS B5` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.C1** — `IRS C1` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.C2** — `IRS C2` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.C3** — `IRS C3` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.C4** — `IRS C4` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.D1** — `IRS D1` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.D2** — `IRS D2` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.D3** — `IRS D3` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.D4** — `IRS D4` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.E1** — `IRS E1` MISSING — VERIFICATION 12.2: the `irs` system runs `mechanisms/lending.rs Servicing`, the same mechanism `lending` runs — it pays what fell due on a schedule out of the payer`s account. That is principal and interest moving, which is what this FORBID calls a loan
- [ ] **TODO 10.IRS.E2** — `IRS E2` MISSING — VERIFICATION 12.2: `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark

### 10. FX Forwards — 19 missing, 1 partial

> **Required review before this block:** read the **FX Forwards** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2111), then inspect `packages/kernel-rs/src/mechanisms/fx_forwards.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.FX-FORWARDS.A2** — `FX Forwards A2` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.A3** — `FX Forwards A3` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.A4** — `FX Forwards A4` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.B1** — `FX Forwards B1` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.B2** — `FX Forwards B2` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.B3** — `FX Forwards B3` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.B3B** — `FX Forwards B3.b` MISSING — VERIFICATION 10.2: there are two bases and two parity points — `currency.rs covered`/`basis` and `fx_forwards.rs parity`/`basis`, the same expressions under two names, each documented as the only one
- [ ] **TODO 10.FX-FORWARDS.B4** — `FX Forwards B4` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.C1** — `FX Forwards C1` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.C2** — `FX Forwards C2` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.C3** — `FX Forwards C3` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.C4** — `FX Forwards C4` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.D1** — `FX Forwards D1` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.D2** — `FX Forwards D2` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.D3** — `FX Forwards D3` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.D4** — `FX Forwards D4` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.E2** — `FX Forwards E2` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.E3** — `FX Forwards E3` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.E4** — `FX Forwards E4` MISSING — VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.A1** — `FX Forwards A1` PARTIAL — production now strikes only between opposite currency needs and records both typed currencies, rate, amount, tenor and counterparties. The terms still come from a parity reservation rather than a real forward book, and maturity legs are not scheduled (item 10)

### 10. Commodity Futures — 18 missing, 0 partial

> **Required review before this block:** read the **Commodity Futures** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2181), then inspect `packages/kernel-rs/src/mechanisms/commodities.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.COMMODITY-FUTURES.A1** — `Commodity Futures A1` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.A2** — `Commodity Futures A2` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.A3** — `Commodity Futures A3` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.A4** — `Commodity Futures A4` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.B1** — `Commodity Futures B1` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.B2** — `Commodity Futures B2` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.B3** — `Commodity Futures B3` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.B4** — `Commodity Futures B4` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.B5** — `Commodity Futures B5` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.C1** — `Commodity Futures C1` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.C2** — `Commodity Futures C2` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.C3** — `Commodity Futures C3` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.C4** — `Commodity Futures C4` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.D1** — `Commodity Futures D1` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.D2** — `Commodity Futures D2` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.D3** — `Commodity Futures D3` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.D4** — `Commodity Futures D4` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.E2** — `Commodity Futures E2` MISSING — VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered

### 10. Hedge Funds — 21 missing, 2 partial

> **Required review before this block:** read the **Hedge Funds** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2742), then inspect `packages/kernel-rs/src/mechanisms/hedge_funds.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.HEDGE-FUNDS.A1** — `Hedge Funds A1` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.A3** — `Hedge Funds A3` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.A4** — `Hedge Funds A4` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.A5** — `Hedge Funds A5` MISSING — VERIFICATION 6.1: its book is valued at cost by `instruments.rs equity`, so its equity does not move when a price does
- [ ] **TODO 10.HEDGE-FUNDS.B1** — `Hedge Funds B1` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.B2** — `Hedge Funds B2` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.B3** — `Hedge Funds B3` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.B4** — `Hedge Funds B4` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.B5** — `Hedge Funds B5` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.C1** — `Hedge Funds C1` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.C2** — `Hedge Funds C2` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.C3** — `Hedge Funds C3` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.C4** — `Hedge Funds C4` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D1** — `Hedge Funds D1` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D2** — `Hedge Funds D2` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D3** — `Hedge Funds D3` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D4** — `Hedge Funds D4` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D5** — `Hedge Funds D5` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D6** — `Hedge Funds D6` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D7** — `Hedge Funds D7` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.E2** — `Hedge Funds E2` MISSING — VERIFICATION 12.1, 9.1: `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.A2** — `Hedge Funds A2` PARTIAL — `agreed::SUBSCRIPTION` gives investors a share count they could redeem. Nothing redeems (Fund Shares C2), and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause (item 10)
- [ ] **TODO 10.HEDGE-FUNDS.E3** — `Hedge Funds E3` PARTIAL — packages/kernel-rs/src/mechanisms/mortality.rs `Failing` ceases any party whose equity is below zero, a fund included. VERIFICATION 7.2: ceasing does nothing — it keeps what it held and its investors lose nothing

### 10. Private Equity — 21 missing, 1 partial

> **Required review before this block:** read the **Private Equity** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2796), then inspect `packages/kernel-rs/src/mechanisms/private_equity.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.PRIVATE-EQUITY.A1** — `Private Equity A1` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.A3** — `Private Equity A3` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.A4** — `Private Equity A4` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.A5** — `Private Equity A5` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.B1** — `Private Equity B1` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.B2** — `Private Equity B2` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.B3** — `Private Equity B3` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.B4** — `Private Equity B4` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.B5** — `Private Equity B5` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.C1** — `Private Equity C1` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.C2** — `Private Equity C2` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.C3** — `Private Equity C3` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.C4** — `Private Equity C4` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.C5** — `Private Equity C5` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.D1** — `Private Equity D1` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.D2** — `Private Equity D2` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.D3** — `Private Equity D3` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.D4** — `Private Equity D4` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.D5** — `Private Equity D5` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.E1** — `Private Equity E1` MISSING — VERIFICATION 12.1: `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.E3** — `Private Equity E3` MISSING — VERIFICATION 12.1: packages/kernel-rs/src/systems.rs wires `private_equity::Calling`, but that production path never calls `Exit` or `exit`, so nothing exits at any price. NOT GUARDED: an exit at a price nobody paid is a thing the code would DO rather than a word it would write, so what holds it is the item that wires the module against a market that clears (0r), not a check (item 10)
- [ ] **TODO 10.PRIVATE-EQUITY.A2B** — `Private Equity A2.b` PARTIAL — the call falls due like any other payment and the wire refuses or queues it where the investor is short, so it is not bounded by spare cash. What is missing is the other half: an investor that does not pay does not DEFAULT on it — `mechanisms/loss.rs` crosses a standing on a LINE and a bilateral obligation has none, so the row runs out of periods and no fund learns of it (item 10.7)

### 11. Firm Birth — 21 missing, 2 partial

> **Required review before this block:** read the **Firm Birth** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3266), then inspect `packages/kernel-rs/src/mechanisms/firms.rs`, `packages/kernel-rs/src/parties.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 11.FIRM-BIRTH.A1** — `Firm Birth A1` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.A2** — `Firm Birth A2` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.A2A** — `Firm Birth A2.a` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.A3** — `Firm Birth A3` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.A4** — `Firm Birth A4` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.A5** — `Firm Birth A5` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.B1** — `Firm Birth B1` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.B2** — `Firm Birth B2` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.B3** — `Firm Birth B3` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.B4** — `Firm Birth B4` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.C1** — `Firm Birth C1` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.C2** — `Firm Birth C2` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.C3** — `Firm Birth C3` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.D1** — `Firm Birth D1` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.D2** — `Firm Birth D2` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.D3** — `Firm Birth D3` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.D4** — `Firm Birth D4` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.D5** — `Firm Birth D5` MISSING — VERIFICATION 7.2: a ceased party keeps its cash and its holdings for ever, and no estate is opened
- [ ] **TODO 11.FIRM-BIRTH.D6** — `Firm Birth D6` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.E2** — `Firm Birth E2` MISSING — VERIFICATION 7.2, 4.1: nothing has a destination — not an asset, not a liability, not an employee, not a contract
- [ ] **TODO 11.FIRM-BIRTH.E4** — `Firm Birth E4` MISSING — VERIFICATION 12.1: `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.C4** — `Firm Birth C4` PARTIAL — `Failing` traces a cessation to the equity read that caused it and journals the size. VERIFICATION 6.1: that read is at cost, so no price move can cause one
- [ ] **TODO 11.FIRM-BIRTH.E1** — `Firm Birth E1` PARTIAL — packages/kernel-rs/src/mechanisms/mortality.rs `Failing` ceases any party whose equity is below zero, the central bank excepted by its profile (XI-3`s one exception, and for the reason XI-3 gives). VERIFICATION 7.2: ceasing sets a flag and nothing else

### 12. Audit — 6 missing, 3 partial

> **Required review before this block:** read the **Audit** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 685), then inspect `packages/kernel-rs/src/audit.rs`, `packages/kernel-rs/src/assembly.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 12.AUDIT.B3** — `Audit B3` MISSING — VERIFICATION 1.4: the Prices family has no contribution; `audit.rs Audit::over` declares it NOT BUILT in the assembled world
- [ ] **TODO 12.AUDIT.B4** — `Audit B4` MISSING — VERIFICATION 1.4: the CrossMarket family has no contribution and is declared NOT BUILT. (It was PARTIAL naming worklist 12/13c; the mark is the same fact stated as a status)
- [ ] **TODO 12.AUDIT.B5** — `Audit B5` MISSING — VERIFICATION 1.4: the Accounts family has no contribution and is declared NOT BUILT. No code reads assets minus liabilities against an equity account
- [ ] **TODO 12.AUDIT.B8** — `Audit B8` MISSING — independence cannot be measured while seven of the ten families are NOT BUILT (VERIFICATION 1.4)
- [ ] **TODO 12.AUDIT.D3** — `Audit D3` MISSING — VERIFICATION 1.5: no seeded generator exists in packages/kernel-rs. `Draw` in the bins is a counter-based sequence with a hard-coded start and nothing takes a seed value
- [ ] **TODO 12.AUDIT.D4** — `Audit D4` MISSING — a Part XII measurement, and it needs families that are built (VERIFICATION 1.4)
- [ ] **TODO 12.AUDIT.A1** — `Audit A1` PARTIAL — packages/kernel-rs/src/audit.rs `Contribution` (a family is a read of the state, run where it is meant to be consistent). VERIFICATION 1.1: `LotsAgainstQuantity`, one of the four contributions that exist, reads one thing against itself, which A1.a forbids and which always passes
- [ ] **TODO 12.AUDIT.B7** — `Audit B7` PARTIAL — packages/kernel-rs/src/audit.rs `FlowsAreComplete` compares every non-money holding`s change against the legs that said why it moved, over instructions that SETTLED. It does not cover money: a money leg does not state where its units land, and a family re-deriving settlement`s routing would be reading its answer (Audit C3) — that half is `MoneyIsConserved`, per currency. Per-account money flows are unchecked (item 12, when the routing is a read a family can take)
- [ ] **TODO 12.AUDIT.C3** — `Audit C3` PARTIAL — packages/kernel-rs/src/audit.rs `Audit::run` runs the same contributions every period. What is not true is the rest of C3: seven families are NOT BUILT, so the invariants are not the same set the spec names (VERIFICATION 1.4)

### 13. Seed — 20 missing, 1 partial

> **Required review before this block:** read the **Seed** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 753), then inspect `packages/kernel-rs/src/opening.rs`, `packages/kernel-rs/src/assembly.rs`, `packages/kernel-rs/src/src/bin/world_runs.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 13.SEED.A1** — `Seed A1` MISSING — VERIFICATION 2.1: §5 is not built. The only opening state in the repository is packages/kernel-rs/src/bin/world_runs.rs, whose own header declares it arbitrary and not a seed
- [ ] **TODO 13.SEED.A2** — `Seed A2` MISSING — VERIFICATION 2.1: there is no period zero, and the Accounts family that would audit it is NOT BUILT (1.4)
- [ ] **TODO 13.SEED.A3** — `Seed A3` MISSING — VERIFICATION 2.1
- [ ] **TODO 13.SEED.A4** — `Seed A4` MISSING — VERIFICATION 2.1
- [ ] **TODO 13.SEED.A5** — `Seed A5` MISSING — VERIFICATION 1.5: nothing in packages/kernel-rs takes a seed value. The one generator is `Draw`, redeclared in each of eight binaries with its own hard-coded constant
- [ ] **TODO 13.SEED.B1** — `Seed B1` MISSING — VERIFICATION 2.1: the populations in world_runs.rs are drawn from a counter and declared arbitrary
- [ ] **TODO 13.SEED.B4** — `Seed B4` MISSING — VERIFICATION 2.1: sizes in world_runs.rs come from `Draw::spread`, which is the arbitrary world, not a seed
- [ ] **TODO 13.SEED.B5** — `Seed B5` MISSING
- [ ] **TODO 13.SEED.C1** — `Seed C1` MISSING — VERIFICATION 2.1, 1.4: no opening state, and no Accounts family to check that it balances
- [ ] **TODO 13.SEED.C2** — `Seed C2` MISSING — VERIFICATION 2.1
- [ ] **TODO 13.SEED.C3** — `Seed C3` MISSING — VERIFICATION 2.1
- [ ] **TODO 13.SEED.C4** — `Seed C4` MISSING — VERIFICATION 2.1
- [ ] **TODO 13.SEED.C5** — `Seed C5` MISSING
- [ ] **TODO 13.SEED.D1** — `Seed D1` MISSING — VERIFICATION 2.1
- [ ] **TODO 13.SEED.D2** — `Seed D2` MISSING
- [ ] **TODO 13.SEED.D3** — `Seed D3` MISSING
- [ ] **TODO 13.SEED.D4** — `Seed D4` MISSING — VERIFICATION 2.1: there is no seed, and the test the row cited is gone with the TypeScript engine
- [ ] **TODO 13.SEED.E1** — `Seed E1` MISSING
- [ ] **TODO 13.SEED.E2** — `Seed E2` MISSING — VERIFICATION 2.1
- [ ] **TODO 13.SEED.E3** — `Seed E3` MISSING — VERIFICATION 2.1: there is no seed parameter to change, and the read the row cited is gone with the TypeScript engine
- [ ] **TODO 13.SEED.B2** — `Seed B2` PARTIAL — packages/kernel-rs/src/parties.rs gives a party an identity that survives the run. What is missing is the seed that would create them (VERIFICATION 2.1)

### 14. Observer — 18 missing, 3 partial

> **Required review before this block:** read the **Observer** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 4210), then inspect `packages/kernel-rs/src/mechanisms/observer.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 14.OBSERVER.A2** — `Observer A2` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.A3** — `Observer A3` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.A5** — `Observer A5` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.B1** — `Observer B1` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.B2** — `Observer B2` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.B3** — `Observer B3` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.B4** — `Observer B4` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.B5** — `Observer B5` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.C1** — `Observer C1` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.C2** — `Observer C2` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.C2A** — `Observer C2.a` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.C3** — `Observer C3` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.C4** — `Observer C4` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.D2** — `Observer D2` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.D3** — `Observer D3` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.F1** — `Observer F1` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.F2** — `Observer F2` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.F4** — `Observer F4` MISSING — VERIFICATION 12.1: `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface
- [ ] **TODO 14.OBSERVER.A1** — `Observer A1` PARTIAL — packages/kernel-rs/src/prices.rs `Print` carries its instrument, period, unit and provenance, so a stale mark is visibly stale (A1.a). Nothing displays one: the surface is item 24
- [ ] **TODO 14.OBSERVER.A4** — `Observer A4` PARTIAL — packages/kernel-rs/src/journal.rs `is_public`/`subjects_of` is the rule, and packages/kernel-rs/src/module.rs `ParticipantView` holds one party. VERIFICATION 4.2: `public_event`, the check that applies it, has no caller outside its own test. NOT GUARDED: the surface that runs is mechanisms/observer.rs Observing, outside mechanisms/observer.rs, so a module-scoped rule cannot reach it — item 0r moves it home and 0j.6 records why the guard waits
- [ ] **TODO 14.OBSERVER.E1** — `Observer E1` PARTIAL — nothing is displayed at all, so there is no display-only number. The observer module that would display is unreached, and the surface is item 24

<!-- /plan:gaps -->
