# Project Phoenix — the implementation

> **This is the one file that changes every week. The other three do not.** The specification says
> what is TRUE of the world; `ARCHITECTURE.md` says what the code IS; `COVERAGE.md` says what of the
> specification the code MEETS. None of them names an item in this file, ever: a coverage row says
> what is short, never who fixes it; a source comment says why, never which item; a noun's home
> names the mechanism that houses it, never the item that builds one. A reference from a fixed file
> into a moving one is dead the week after it is written, and `npm run check:existence` refuses it.
>
> **Parts 1–2 are the order; Part 4 is the exhaustive clause ledger.** Take the first open item in
> Part 2 whose prerequisites are met. Finish it — checks green, `world:runs` green, COVERAGE re-marked,
> Part 4 regenerated — before opening the next. A new idea is INSERTED at its dependency position,
> never appended, and the insertion says where and why.
>
> **NOTHING DONE IS IN THIS FILE.** When an item closes, delete it. What it produced is the code.
>
> **THIS IS WHERE A FINDING LIVES.** Part 0.3 is the ledger of what the last verification pass
> measured. Each finding sits under the item that fixes it; a finding that fits no item becomes one,
> inserted where its dependencies put it. A finding leaves this file only by being fixed.
>
> **Every block below opens with what to read first.** The specification section, the laws it
> leans on, and the production source. Do not implement from the item text alone: the item says
> what closes it, the specification says what it must be.

## Part 0 — The measured state

### 0.1 By spec system

`npm run check:existence` over `docs/COVERAGE.md` prints this live and holds this file to it with
`--verify`. A MET mark is a **claim** that the cited module implements the clause; it refuses one
whose cited item lies outside the closure the world reaches from `systems::all` and a stepped
`World`, one naming something no file in the tree contains at all, and one citing a module no other
module enters. Four ratchets carry what is left of the same defect and may only fall: the public
items that closure does not contain (`npm run check:reach`), MET rows naming no item at all, rows
whose reason is word for word another row's, and unmet rows naming what the tree has not got.
A mark is still not a run's confirmation. Every clause in the specification is counted, sub-clauses
included: 1,859 of them, of which 485 are MET and 1,374 are not. `npm run coverage:spec` names any
clause that has no row at all, and there are none.

| system                   | MET   | PARTIAL | MISSING | UNMEASURED | total |
| ------------------------ | ----- | ------- | ------- | ---------- | ----- |
| Money                    | 58    | 4       | 7       | 0          | 69    |
| Register                 | 37    | 2       | 1       | 0          | 40    |
| Clearing                 | 32    | 2       | 0       | 0          | 34    |
| Audit                    | 24    | 3       | 2       | 0          | 29    |
| **Seed**                 | **1** | 1       | **26**  | 0          | 28    |
| **Currency**             | **4** | 7       | **21**  | 0          | 32    |
| Bond                     | 12    | 4       | 8       | 0          | 24    |
| **Derivative**           | **3** | 7       | **16**  | 0          | 26    |
| **Corporate Credit**     | **4** | 8       | **76**  | 0          | 88    |
| Sovereign                | 27    | 21      | 27      | 0          | 75    |
| Short-Term Debt          | 4     | 6       | 17      | 0          | 27    |
| Equity                   | 15    | 2       | 39      | 0          | 56    |
| Money Market             | 12    | 3       | 26      | 0          | 41    |
| Spot FX                  | 11    | 5       | 13      | 0          | 29    |
| **Fund Shares**          | **4** | 7       | **24**  | 0          | 35    |
| **Securities Lending**   | **2** | 1       | **22**  | 0          | 25    |
| **Prime Brokerage**      | **2** | 1       | **25**  | 0          | 28    |
| Derivative Layer         | 5     | 2       | 36      | 0          | 43    |
| **CDS**                  | **3** | 0       | **33**  | 0          | 36    |
| **IRS**                  | **2** | 0       | **26**  | 0          | 28    |
| **FX Forwards**          | **2** | 2       | **26**  | 0          | 30    |
| **Commodity Futures**    | **2** | 0       | **26**  | 0          | 28    |
| Commodities Spot         | 21    | 3       | 4       | 0          | 28    |
| Indices                  | 11    | 2       | 14      | 0          | 27    |
| **Banks Lending**        | **5** | 3       | **39**  | 0          | 47    |
| **Banks Funding**        | **3** | 3       | **38**  | 0          | 44    |
| **Banks Capital**        | **7** | 5       | **24**  | 0          | 36    |
| **Dealer Desks**         | **2** | 7       | **22**  | 0          | 31    |
| **Insurers**             | **0** | 1       | **30**  | 0          | 31    |
| **Hedge Funds**          | **1** | 3       | **23**  | 0          | 27    |
| **Private Equity**       | **3** | 1       | **25**  | 0          | 29    |
| Treasury                 | 29    | 1       | 3       | 0          | 33    |
| **Central Bank**         | **8** | 4       | **26**  | 0          | 38    |
| **Polity**               | **8** | 1       | **26**  | 0          | 35    |
| Firm                     | 10    | 8       | 20      | 0          | 38    |
| Capital Programme        | 6     | 7       | 24      | 0          | 37    |
| **Firm Birth**           | **2** | 4       | **26**  | 0          | 32    |
| **M&A**                  | **1** | 1       | **24**  | 0          | 26    |
| **Trade Credit**         | **2** | 3       | **20**  | 0          | 25    |
| Goods                    | 18    | 6       | 28      | 0          | 52    |
| **Freight**              | **0** | 3       | **21**  | 0          | 24    |
| **Labour**               | **7** | 4       | **28**  | 0          | 39    |
| **Housing**              | **1** | 11      | **23**  | 0          | 35    |
| **Households**           | **7** | 4       | **37**  | 0          | 48    |
| **Small-Business Pools** | **2** | 1       | **32**  | 0          | 35    |
| **Cross-Border**         | **6** | 1       | **24**  | 0          | 31    |
| **Ratings**              | **2** | 9       | **15**  | 0          | 26    |
| Reporting                | 14    | 6       | 20      | 0          | 40    |
| **Observer**             | **6** | 4       | **19**  | 0          | 29    |
| Expectations             | 24    | 9       | 10      | 0          | 43    |
| Geography                | 16    | 9       | 17      | 0          | 42    |

There are two absent sectors: Freight and Insurers hold no clause MET. Sovereign is
23/21/31 because twenty-five of `sovereign.rs`'s forty-one public items sit outside the world's
closure, and the rows that turn on them say so. Money is 59/4/6, and among the six is _"the population changes only by
split"_.

### 0.2 What the world actually does

`npm run world:runs` is the reachability gate: it steps the arbitrary scale world four weeks and
asserts only that nothing throws. It is **green** — 10,318 parties, 16,750 instruments, 1,546 books,
51 systems in 80 phases, four weeks, worst week 7.0 s against the 3.0 s the migration was judged on.

That world is ARBITRARY and declared arbitrary: every number is a counter, nothing was cleared or
decided, and it is not a seed. It proves that every wired system runs, that the phase order holds
at scale, and that what a mechanism proposes goes over the wire — not that every module is entered,
which 0.1's closure answers and 511 public items fail. It proves nothing about behaviour, and
findings F1–F4, F41 and F43 are what it reports every week. Item 13 replaces it.

### 0.3 The findings ledger

What the verification pass of 21 September 2026 measured, code-first, against Laws 1–19 and
Appendix B, and what the reachability gate has reported since it started running. Each finding names
its site and the item that fixes it. The verdicts that changed the plan: **no book has ever
crossed; two systems are marked complete on code the world does not run; four guards exist and are
fed nothing; three of five population events have no caller; the money-market price is a
constant.**

| #   | finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | site                                                                         | item          |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ------------- |
| F1  | **No book has ever cleared.** Four weeks, 64,734 asks, 3,130 resting orders, **0 books cleared and 0 trades**. Every price in this world is therefore a seeded or carried print, and no VERIFY that reads a cleared price can be taken                                                                                                                                                                                                                                                                                                                                                                     | `world:runs` output                                                          | 13, blocks 12 |
| F2  | **16,750 ownership violations every week**: holdings against zero or **negative** issued (`instrument 1: 2298380 held against -1072720 issued`). The arbitrary fixture credits holdings without issuing and destroys against what was never made; `Instruments::moves` has no guard that issued stays non-negative                                                                                                                                                                                                                                                                                         | `instruments.rs:436`, `world_runs.rs`                                        | 13.7          |
| F3  | **1,357 flow violations every week**: `Audit B7 · XI-9` — the wire's latest outcome `ShortOfMoney` disagrees with the schedule state `Queued`. Two records of one payment's fate                                                                                                                                                                                                                                                                                                                                                                                                                           | `audit.rs` (`ScheduleOutcomesMatch`)                                         | 4.1           |
| F4  | **32 of 49 wired systems only counted** over four weeks: they ran, journalled a number and changed nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `world:runs` output                                                          | 3–11          |
| F13 | Three of the five weight events are called by nothing: `household_death`, `promote_small_business` and `merge`. Entry has a caller in `World::admit` and split has one in `employment.rs`, so two of the five move a population and the other three are doors nobody opens                                                                                                                                                                                                                                                                                                                                 | `parties.rs`                                                                 | 11            |
| F21 | The five population slots are declared and empty: three of the five weight events have no caller, and the one split there is runs at d5, where the engagement that causes it is struck                                                                                                                                                                                                                                                                                                                                                                                                                     | `systems.rs`, `employment.rs`                                                | 11            |
| F24 | The production dealer quote has **no carry**, and `bid == offer` whenever the desk agrees with the print; the spec-shaped `quote()` with carry, risk and adverse selection is called only from tests                                                                                                                                                                                                                                                                                                                                                                                                       | `dealing.rs:95-120, 45-62`                                                   | 5.4           |
| F25 | Five party-kind branches inside mechanisms (`employment.rs:512`, `treasury.rs:382/453/483`, `sovereign.rs:496`), each reading the kind and comparing it with a declared one. The sovereign curve's is gone: it asks the registry for the issuer's declared failure capability instead                                                                                                                                                                                                                                                                                                                      | those lines                                                                  | 3.6           |
| F26 | `MoneyAmount` guards Money A2.b in **2 files of 83**; every other amount is a bare `f64`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `num.rs`, `money.rs`                                                         | 7.4           |
| F30 | `derivative_layer.rs:223` floors a waterfall line at zero (`if paid > 0.0 { paid } else { 0.0 }`): a house with negative capital is read as zero. `hedge_funds.rs:267` and `private_equity.rs:222` floor a shortfall the same way                                                                                                                                                                                                                                                                                                                                                                          | `derivative_layer.rs:221-223`                                                | 10.3          |
| F31 | `Leg::Dispatch` records `arrives: week + 1` for every route, whatever the distance; §49 F4 wants the transit time to come from the route itself                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `ledger.rs` (`Leg::Dispatch`)                                                | 3.7           |
| F39 | `polity.rs:454` reads the week of a state's last election as `unwrap_or(&0)`, so a state that has never held one is treated as having held it in week zero and its first election is due immediately                                                                                                                                                                                                                                                                                                                                                                                                       | `polity.rs:454`                                                              | 9.2           |
| F41 | A position its holder marks, in a book that has not printed, has no value and its holder's equity cannot be read: `world:runs` reports three `[XI-6]` and three `[Audit B5]` violations a week (holdings 10407, 10435, 10491; parties 38, 40, 47). Downstream of F1 — no book in that world has ever crossed, so the only prints are seeded ones                                                                                                                                                                                                                                                           | `audit.rs` (`MarketValuesExist`), `instruments.rs` `carrying_value`          | 13            |
| F42 | Five of the seven scale bins abort: `world-at-scale`, `session-at-scale` and `module-at-scale` index past the end at `instruments.rs:475`, and `week-at-scale` and `wire-at-scale` on a `Units` of nothing. Only `register-at-scale` and `audit-at-scale` run, so what a week costs is measured on two of its parts                                                                                                                                                                                                                                                                                        | `src/bin`, `instruments.rs:475`                                              | 14.3          |
| F43 | Almost nothing a balance sheet does reaches the account it should move: `world:runs` reports 11,234 `Accounts` violations a week against 3 before the account existed, the largest being party 2 at 1,010,429 money. What settlement books is capital paid in, the income and cost receipts, and what a disposal realised; what moves the residual and not the account is units destroyed with no proceeds, units created with no cost paid, a transfer with no consideration, and a mark on a market-carried position. Most of the size is downstream of F2, which invents the claims the residual counts | `world:runs` output, `ledger.rs` (`moves_equity`)                            | 3, 13         |
| F46 | Accrued interest is on nobody's balance sheet. `accrued_per_unit` is the one read of it, taken by the book for the dirty price and said at b1, and no sheet carries the receivable or the payable. Adding them moves each party's residual while its equity account stays put, because income here is cash received — so the spec must say whether an accrued receivable is an asset before a sheet can carry one                                                                                                                                                                                          | `module.rs` (`accrued_per_unit`), `audit.rs` (`AccountsBalance`)             | 13            |
| F47 | A kernel slot name that is not imported into `assembly.rs` is parsed as a binding and swallows every other arm: `(KERNEL, B1)` silently took A2's, E2's, F's, I1's and I2's work and the world ran four weeks with no book and no audit. `cargo` warns `unreachable_patterns` and nothing in the gate reads warnings                                                                                                                                                                                                                                                                                       | `assembly.rs` (`step`), `lib.rs`                                             | 12.1          |
| F48 | `central_bank_remittance` is the one sovereign helper 2.2 could neither delete nor wire: no module acts for the central bank, so nothing owns the payment, and the only mechanism at b2 is `lending`'s servicing of what is already on a schedule. The remittance needs the payer to exist before it can be paid                                                                                                                                                                                                                                                                                           | `sovereign.rs` (`central_bank_remittance`), `systems.rs`                     | 8             |
| F51 | 4,497 books are open in week 1 and 11,474 by week 4, and not one of them clears. No line in this world has ever printed — `Provenance::Seeded` is READ in three places and written nowhere — so no party holds a price outlook of anything, and every bid now rests on `fair_value`, which needs the bidder's own `WHAT_CREDIT_COSTS`. Which side of each book is empty, and why, is the measurement this waits on                                                                                                                                                                                         | `prices.rs` (`Provenance::Seeded`), `opening.rs`, `module.rs` (`fair_value`) | 13            |
| F52 | A line does not carry the accrual convention it was struck under. `Brings` names one, `schedule_of` uses it to build the dues and it is then thrown away, so nothing can value or re-derive a yield on the line's own terms — `fair_value` discounts on Actual/365 because that is all it can know. Bond N6 makes the convention a term of the paper                                                                                                                                                                                                                                                       | `instruments.rs` (`issue`), `module.rs` (`fair_value`)                       | 4             |
| F54 | Nothing in this world declines to act for want of a gap (§46 F2.b). A limit at a party's own value means it trades AT its value and gains nothing rather than standing aside; `dealing.rs` quotes both sides every week it has a level and `money_market.rs` bids whenever it has spare cash. What a party requires over its own value before it is worth transacting is nobody's number yet                                                                                                                                                                                                               | `module.rs` (`values`), `dealing.rs`, `money_market.rs`                      | 2.5e          |
| F55 | An index's constituent set cannot change: `Registry::index` creates one from a slice at assembly and nothing adds to or removes from it. A bond that matures stays in its basket and a line brought this week can never enter one, so §22 B2 and B2.a — the chaining that keeps a level continuous across a rebalance — are unreachable rather than unmet, and the sovereign curve cannot be a declared index                                                                                                                                                                                              | `registry.rs` (`index`), `benchmarks.rs`                                     | 2.3           |
| F56 | A bank brings what it is short of and names no reserve, so its funding auction has a seller that cannot refuse: whatever the book clears at is what it pays, however far that is past what the standing facility would have cost it. `MoneyMarketBanks::levels` is the number it would refuse beyond, and the mechanism that brings the paper does not read it                                                                                                                                                                                                                                             | `money_market.rs` (`Interbank`, `levels`)                                    | 2.2           |
| F60 | §46 B4's guard is gone with the copy that held it. `expectations::Outlook::observe` refused an observation from the week it was acting in; `Outlooks::observe` takes no such week — the kernel records at the current one and the e1 slot is what keeps a read later than a write. The rule is now structural rather than a check that throws, and `Outlooks::formed` is all a reader has                                                                                                                                                                                                                  | `stores.rs` (`Outlooks`), `assembly.rs`                                      | 12            |
| F50 | A depositor's alternative is a money fund's published yield — `funds.rs` `beats_the_deposit` is that comparison and nothing reaches it, and no fund publishes a yield. `bank_funding.rs` hands `will_pay_on_deposits` the weekly funding fixing instead, so a bank's own wholesale price stands in for its depositors' outside option and the two move together by construction                                                                                                                                                                                                                            | `bank_funding.rs`, `funds.rs` (`beats_the_deposit`)                          | 6             |

## Part 1 — The order

| #   | milestone                                                                  | dependency reason                                                                                         |
| --- | -------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| 1   | Make kernel contracts, market state and the week's order unambiguous       | every place, route, derivative, cure, delivery, benchmark and audit reads these identities and this order |
| 2   | Finish sovereign funding and the transacted benchmark                      | Part XIII places a sovereign that can fail before anything priced over its curve                          |
| 3   | Finish firm, household, labour and goods operating flows                   | credit, ratings and loss need real cash flow and named counterparties                                     |
| 4   | Finish credit rows, loss rights and resolution                             | loans, collateral, recovery and failure must exist before risk transfer                                   |
| 5   | Finish redeemable claims, equity, dealers and cost-of-capital transmission | forced selling and investment require real claims, inventory limits and cleared prices                    |
| 6   | Finish bank funding, capital and resolution as one balance sheet           | deposits, reserves, collateral and capital must reconcile                                                 |
| 7   | Finish currency, cross-border and benchmark curves                         | derivatives and foreign funding require named currencies, pairs and transacted references                 |
| 8   | Finish reporting, estimates, housing, employment and corporate control     | these consume stable accounting, price and credit layers and supply the polity                            |
| 9   | Finish polity and policy transmission                                      | votes require cell-level lived outcomes; mandates change only policy-owned primitives                     |
| 10  | Finish derivatives and risk transfer, one typed contract at a time         | CDS, IRS, FX derivatives, futures and securitisation require all earlier underlyings and failure states   |
| 11  | Complete the cell life cycle and physical delivery                         | entry, death, promotion and merge, and goods in transit, must preserve ownership and relationships        |
| 12  | Close and audit the sixteen causal chains                                  | only now do all endpoints exist                                                                           |
| 13  | Generate the calibrated opening world                                      | seed primitives, histories and reasons only after the consuming mechanisms are stable                     |
| 14  | Measure, optimise, then build the observer and the app                     | Laws 11 and 18 put diagnosis and performance after behaviour                                              |

## Part 2 — The items

The week every item is placed against is Money G2, whose nine stages carry internal orders of their
own. Written out, that is **31 slots**, and every system sits in exactly one:

```
a1 offers expire → a2 payments given up as fails
b1 what accrues accrues → b2 due paid or arrear → b3 loss lands on named holders → b4 party ceases → b5 estate distributes
c1 entry → c2 death → c3 promotion → c4 split → c5 merge
d1 lines draw inputs → d2 batches finish → d3 plant wears → d4 goods move → d5 engagements made and ended → d6 whoever is short brings paper
e1 every outlook is formed, once → e2 every participant posts
f  the books clear, once, per market and instrument
g1 positions marked → g2 gains and losses landed → g3 derived levels read → g4 constraints tested → g5 accounts published → g6 opinions formed → g7 made visible
h  obligations for the week after, and nothing else
i1 gridlock pass → i2 audit
```

A fact produced at one slot is readable this week only by a later slot. Every feedback loop in the
model therefore has at least one edge that crosses the week boundary, and that edge is its period.
Two systems meet _simultaneously_ in one place only — as counterparties in a book — and a book is
`(market, subject instrument, currency, venue)`. No system calls another; A writes a store, B reads
it. The twelve channels are `MechanismContext`'s write doors and there is no thirteenth.

---

### 2. Finish sovereign funding and the transacted benchmark

**Read before this block:** Sovereign in full, Treasury D–E, Central Bank B–D, Money Market A–C,
Indices D3–D5, Bond N5–N7, XI-7, XI-9, Laws 3, 4, 19. Then `sovereign.rs`, `treasury.rs`,
`money_market.rs`, `benchmarks.rs`, `bank_funding.rs`, `schedules` in `stores.rs`.

- [ ] **2.2 A borrower names what it will pay.** `Interbank` brings what a bank is short of and
      names no rate, so the auction alone decides what it costs and a borrower cannot refuse a
      price (§11 B1: every bank posts a schedule out of its own position AND its own cost of
      funds). The issuer's reserve — the most it will pay rather than go to the facility — is
      `levels(..).1`, which the participant already computes for itself. Fixes F56.
- [ ] **2.3 An index's constituents are a rule, and the level is chained across a change in them.**
      A constituent set declared once cannot change (§22 B2). A set stated as a LIST has to be edited by somebody, and the registry is data with
      one writer — so the declaration states the RULE instead (the subject, the scope, and the basis
      B1 says the weights come from), and the set is a read of what currently qualifies. A matured
      bond then leaves on its own and a brought line enters on its own, the sovereign curve becomes a
      declared index rather than a capability test in `benchmarks.rs`, and the level is chained
      across the change (§22 B2.a) so continuity survives it. Fixes F55.

- [ ] **2.4 A term issue beside the week's.** §11 B6 is the week AND term, each with its own book,
      and `Interbank` brings one tenor — so `term_spread` has one rate to read and not two, and
      B6.a's "information about expected stress" is unreachable. What a bank terms out is its own
      REASON and not a share somebody declared: a bank whose own outlook says credit will cost more
      than it does now funds longer, and one that expects it to cost less funds by the week. The
      outlook is `about::WHAT_CREDIT_COSTS`, which is already the store's.

### 3. Finish firm, household, labour and goods operating flows

**Read before this block:** Firm, Goods, Freight, Labour, Housing, Households, Capital Programme,
Commodities Spot, Expectations, XI-10, XI-15, XI-16, Laws 2, 5, 15, 19. Then `firms.rs`,
`goods.rs`, `freight.rs`, `employment.rs`, `housing.rs`, `households.rs`, `capital_programme.rs`,
`commodities.rs`, `expectations.rs`, and the Part 4 blocks for these systems.

- [ ] **3.1 A good is worth what it is used for.** Before the operating flows, because a buyer of a
      good that cannot value one bids at a forecast of the print (§46 F1, F3, §21, §37).
      `goods.rs` answers `Valuer` for `Class::Good` — to whoever consumes it, what it is worth in
      use; to whoever works it, what the ways of making with it yield — at what that party requires.
      `Expectations F1`, `F1.a`, `F3` re-marked from what is then reached.
- [ ] **3.2 Place the real work in d1–d6.** `recipe`/`goods` (d1, d2), `capital_programme` (d3),
      `freight`, `commodities` (d4), `employment`, `housing`, `trade_credit`, `small_business`,
      `insurers`, `securities_lending` (d5), `treasury`, `short_term_debt`, `corporate_credit`,
      `equity`, `securitisation` (d6). The treasury sizes its need **after** the week's depreciation and
      payroll have landed.
- [ ] **3.3 Goods downstream of the line.** Wire `carry` (lower of cost and net realisable value,
      write-down as a dated income event), `Consignment::landed_cost`, cost of goods sold into the
      income statement. Goods E2, E2.c, E3, F5 re-marked.
- [ ] **3.4 Labour clears on the wage.** `Posting` as a bid at the employer's wage, `matching` as
      `clear` with `BuyersCompete` and pro-rata ties, `Separation`/`Ended` as events, the seeker's
      reservation from the benefit it is paid. Every hire and separation of part of a cell splits it at
      c4. The Labour block in Part 4.
- [ ] **3.5 Housing moves dwellings.** Bids from what §23 will lend, offers from tenure ending and
      completions, a cross, a `Leg::Asset` for the dwelling, rent as a scheduled obligation, foreclosure
      returning supply to the book. The Housing block in Part 4.
- [ ] **3.6 A wage takes one path.** `employment.rs:512`'s branch on the employer's kind becomes a
      `KindProfile` capability (`originates_payroll_by_contract`); the treasury's payroll and a firm's
      are one mechanism reading it. Fixes F25 (employment).
- [ ] **3.7 Transit is a property of the route.** `Leg::Dispatch.arrives` is computed from the
      route's declared legs and the carrier's speed (§49 F4), never `week + 1`. Fixes the second half of
      F31.
- [ ] **3.8 The remaining Part 4 clauses of Firm, Goods, Freight, Labour, Housing, Households,
      Capital Programme, Commodities Spot and Expectations**, in the order Part 4 lists them.

### 4. Finish credit rows, loss rights and resolution

**Read before this block:** Corporate Credit, Short-Term Debt, Banks Lending, Trade Credit,
Small-Business Pools, the Bond contract, XI-1, XI-8, Laws 3, 4, 12. Then `lending.rs`,
`corporate_credit.rs`, `short_term_debt.rs`, `trade_credit.rs`, `small_business.rs`, `loss.rs`,
`estate.rs`, `mortality.rs`, and their Part 4 blocks.

- [ ] **4.1 Place b2–b5.** `lending` (servicing, b2), `cds` premium at b2 and its event at b3,
      `loss` (b3), `mortality` (b4), `estate` (b5), in that order and no other.
- [ ] **4.2 A loan is priced from four terms.** Cost of funds (Banks Funding B2), the lender's own
      expected loss, the capital the loan consumes times its required return, and an operating cost
      (Banks Lending C1.a–d); the borrower shops and the keenest quote wins (C2).
- [ ] **4.3 The remaining Part 4 clauses of Bond, Corporate Credit, Short-Term Debt, Banks Lending,
      Trade Credit and Small-Business Pools**, in order.

### 5. Finish redeemable claims, equity, dealers and cost-of-capital transmission

**Read before this block:** Equity, Fund Shares, Dealer Desks, Capital Programme B, XI-2, XI-4,
XI-13, Clearing A4 and C2, Laws 3, 6. Then `funds.rs`, `redeemable.rs`, `equity.rs`, `dealing.rs`,
`forced_sale.rs`, `hedge_funds.rs`, `capital_programme.rs`, `cost_of_capital.rs`.

- [ ] **5.1 Place g1–g4.** `firms` (g2), `money` (g1 read), `bank_capital`, `bank_funding`,
      `prime_brokerage`, `hedge_funds`, `derivative_layer` (g4) run **after** `benchmarks`, `indices`,
      `cost_of_capital`, `spot_fx`, `fx_forwards`, `cross_border` (g3). `consumer_prices` is g3.
- [ ] **5.2 Place g5–g7.** `reporting` (g5), `ratings`, `second_opinion` (g6), `observer` (g7,
      last, always).
- [ ] **5.3 A share is worth the residual it is a claim on.** `equity.rs` answers `Valuer` for
      `Class::Share` out of what the company publishes (§10 A1, §48) at what the holder requires,
      and `control.rs` values a company's WHOLE for an acquirer (§35) — the same terms read by a
      party buying all of it rather than a unit of it. `Expectations F1`, `F1.a`, `F3` re-marked
      from what is then reached.
- [ ] **5.4 The desk pays rent on its inventory.** `reservation()` gains the carry term — the
      desk's own cost of funds on the inventory's carried value, per week — and a risk term from its
      confidence; `quote()` is deleted or becomes the production path (one function, not two). A desk
      that agrees with the print still quotes a width, because carrying is not free. Fixes F24.
- [ ] **5.5 A party requires a gap before it acts.** A limit at a party's own value means it
      trades AT its value and gains nothing rather than standing aside (§46 F2.b). What a party
      requires over its own value before transacting is its own number — a PREFERENCE dispersed at
      entry like the memory horizon, not a rate anybody states — and the side it acts on follows
      from which way the gap runs. Fixes F54.
- [ ] **5.6 The remaining Part 4 clauses of Equity, Fund Shares, Dealer Desks and Capital
      Programme**, in order.

### 6. Finish bank funding, capital and resolution as one balance sheet

**Read before this block:** Banks Funding, Banks Capital, Money Market C–E, Insurers, XI-3, XI-8,
Laws 4, 6. Then `bank_funding.rs`, `bank_capital.rs`, `insurers.rs`, `mortality.rs`, `estate.rs`.

- [ ] **6.1 The remaining Part 4 clauses of Money Market, Banks Funding, Banks Capital and
      Insurers**, in order — deposit classes by stickiness, the blended cost of funds as a read, the
      liquidity ladder, the two failure triggers, the bail-in hierarchy, the scheduled insurer liability
      discounted at a market rate.

### 7. Finish currency, cross-border and benchmark curves

**Read before this block:** Currency, Spot FX, FX Forwards B, Indices, Cross-Border, XI-7, XI-12,
Money A2.b, Laws 3, 4. Then `currency.rs` (unwired), `spot_fx.rs`, `cross_border.rs`,
`benchmarks.rs`, `num.rs`.

- [ ] **7.1 The FX pair set clears jointly.** One solve over all `n(n−1)/2` pair books with
      cross-consistency as a **constraint** (§12 C2.a); the residual inconsistency is left standing as a
      measurable arbitrage that bounded participants may or may not close (§12 E3). This is the one
      place two books are not independent, and `run_books` gains a joint path for it.
- [ ] **7.2 `currency.rs` is wired or deleted.** A module with no `Mechanism`, no `Participant`
      and no registration is not a system.
- [ ] **7.3 Revaluation at g2.** Every foreign position revalues when the rate moves, into equity
      for a firm or bank and a revaluation account for an issuer, before anything uses the new rate.
- [ ] **7.4 `MoneyAmount` is how money moves.** `Leg::Money`, `Schedules`, `Claims`, `Standing`
      terms in money and every participant's `own_cash` carry `MoneyAmount`; a bare `f64` amount
      outside `register.rs`'s totals is refused by `phoenix-check`. Fixes F26; `Currency A3.a` re-marked.
- [ ] **7.5 The remaining Part 4 clauses of Currency, Spot FX, Indices and Cross-Border**, in order.

### 8. Finish reporting, estimates, housing, employment and corporate control

**Read before this block:** Reporting, Ratings, M&A, Labour, Housing, §32 E7, XI-10, XI-13, Laws
4, 17. Then `reporting.rs`, `ratings.rs`, `second_opinion.rs`, `control.rs`, `employment.rs`,
`housing.rs`.

- [ ] **8.1 The remaining Part 4 clauses of Reporting, Ratings and M&A**, in order.

### 9. Finish polity and policy transmission

**Read before this block:** Polity, Central Bank, Treasury B–C, XI-17, Laws 2, 15. Then `polity.rs`,
`treasury.rs`, `bank_funding.rs`'s facility, `params.rs`.

- [ ] **9.1 The central bank as a party.** A `Mechanism` that sets the administered rates from its
      mandate, books the corridor's quantity response on both sheets, runs open-market operations in a
      size it chooses, and remits income — never revaluation — to the treasury.
- [ ] **9.2 The remaining Part 4 clauses of Central Bank and Polity**, in order — the vote per
      cell, seats, coalition, the mandate as the only writer of a parliament-owned primitive.

### 10. Finish derivatives and risk transfer, one typed contract at a time

**Read before this block:** the Derivative contract, Derivative Layer, CDS, IRS, FX Forwards,
Commodity Futures, Securities Lending, Prime Brokerage, Hedge Funds, Private Equity, XI-11,
Sovereign I, Laws 3, 5, 6. Then `derivative_layer.rs`, `cds.rs`, `irs.rs` (no `Mechanism`),
`fx_forwards.rs`, `commodities.rs` (futures helpers), `securities_lending.rs`,
`prime_brokerage.rs`, `hedge_funds.rs`, `private_equity.rs`, `securitisation.rs`.

- [ ] **10.1 The strike is one pass.** A derivative's strike, its cut to the smaller of the two
      members' admitted shares, and its initial-margin instruction are one atomic settlement (§16 E2),
      in `session.rs` beside the trade's own legs.
- [ ] **10.2 `irs.rs` gets a `Mechanism` and a book.** The floating leg fixes on §11's cleared
      weekly print, compounded; the par rate clears; the swap curve is a read of cleared rates.
- [ ] **10.3 The waterfall reads a negative house.** Delete the floor at `derivative_layer.rs:223`
      in the same change that makes a house's negative capital a recorded event (§16 C5) consumed by
      b4. Fixes F30.
- [ ] **10.4 Initial margin is sized from the underlying's own measured move** (§16 D1), scaled by
      notional and remaining life; `broker.could_move` is deleted with it, and the placeholder count
      falls by one.
- [ ] **10.5 The remaining Part 4 clauses of Derivative, Derivative Layer, CDS, IRS, FX Forwards,
      Commodity Futures, Securities Lending, Prime Brokerage, Hedge Funds and Private Equity**, in
      order.
- [ ] **10.6 The deliverable future on the benchmark bond** (Sovereign I1–I3), from the helpers
      2.2 parked, once Commodity Futures' delivery mechanism exists to share.

### 11. Complete the cell life cycle and physical delivery

**Read before this block:** XI-15, Households F, Small-Business Pools A6 and E, Firm Birth, Freight
G, §49 G, Money G2.c, Appendix A (populations), Law 2. Then `parties.rs`, `assembly.rs`
(`split_cell`), `households.rs`, `small_business.rs`, `freight.rs`, `ledger.rs` (`Dispatch`).

- [ ] **11.1 A dwelling is worth its rent and its ground.** A household that cannot value a roof
      cannot bid for one (§46 F1, F3, §40). `housing.rs` answers `Valuer` for
      `Class::Good` dwellings out of the rent the household would otherwise pay and the ground it
      stands on, at what that household requires for waiting. `Expectations F1`, `F1.a`, `F3`
      re-marked from what is then reached.
- [ ] **11.2 Entry has a cause.** Household formation (§41 F1.b) and firm birth (§34 A) call
      `enter_household`/`Parties::add` at c1, funded from a named account.
- [ ] **11.3 Death has a cause.** A cell that dissolved at b4 loses its weight at c2, and its
      wealth moves to a **named heir cell** (§41 F2.a).
- [ ] **11.4 Promotion has a cause.** A small-business cell that can reach the bond market is
      promoted at c3 (§42 A6.c).
- [ ] **11.5 Merge has a cause.** Two live cells at one key and one state merge at c5.
- [ ] **11.6 Ageing is a split at the cohort boundary, by date** (§41 F1.a), at c4.
- [ ] **11.7 The remaining Part 4 clauses of Firm Birth**, and Freight G's dispatch lifecycle.

### 12. Close and audit the sixteen causal chains

**Read before this block:** Part XII in full, Audit B8 and D4, Laws 7, 11, 17. Then `audit.rs`,
`tools/phoenix-check/src/spec.rs` (the chain table reader), `src/bin/*`.

- [ ] **12.1 A warning the gate does not read is a rule nobody holds.** `cargo` warns
      `unreachable_patterns` and the suite never looks, so an unimported slot name in `World::step`
      parsed as a binding and silently swallowed five kernel arms — the world ran four weeks with no
      book and no audit. The crate denies the lints that mean "this code cannot run", and
      `npm run check` fails on a warning rather than printing one. Fixes F47.
- [ ] **12.2 One family per chain.** Each Part XII chain gains the invariant contribution its
      "settled state" column names, and its falsification test as a `src/bin` experiment that runs the
      chain with its producer held fixed.
- [ ] **12.3 The remaining Part 4 clauses of Audit** — D3 (a seeded run reproduces), D4 (runs of
      different lengths).

### 13. Generate the calibrated opening world

**Read before this block:** Seed in full, §49 D, XI-14, Law 2 (primitives may be imported,
equilibria may not). Then `opening.rs`, `src/bin/world_runs.rs` (what it replaces).

- [ ] 13.1 Generate parties through `OpeningState`.
- [ ] 13.2 Generate instruments through `OpeningState`, with terms and a spread maturity profile.
- [ ] 13.3 Generate holdings through `OpeningState`, each a named issuer's liability.
- [ ] 13.4 Generate agreements through `OpeningState`.
- [ ] 13.5 Generate schedules through `OpeningState`, from a stated accrual position.
- [ ] 13.6 Draw every stochastic opening input from the single run stream.
- [ ] 13.7 Reconcile opening stocks before period zero, and make `Instruments::moves` refuse an
      issued amount that would go negative — arithmetic impossibility, so the refusal is Law 6's one
      admissible bound, and it lands with the seed that satisfies it (F2).
- [ ] 13.8 Reconcile opening flows before period zero: period one must be quiet.
- [ ] 13.9 Supply the minimum required lagged histories, and no index history that is a walk.
- [ ] 13.10 Pass the period-zero audit without allowances.
- [ ] 13.11 Record provenance for each calibrated primitive; no observed ratio is copied in.
- [ ] 13.12 Delete `world_runs.rs`'s arbitrary construction; `world:runs` steps the seed.

### 14. Measure, optimise, then build the observer and the app

**Read before this block:** Observer in full, Part XII (the run ladder, standing observations),
Laws 11, 18. Then `observer.rs`, `src/bin/*`, `ARCHITECTURE.md` §12.

- [ ] 14.1 Run each Part XII experiment on the calibrated world; every VERIFY becomes a read.
- [ ] 14.2 Population resolution: the same seed at one, two and four times the cell count.
- [ ] 14.3 Hold model shape fixed between scaling rungs; plot runtime against measured work.
- [ ] 14.4 Profile only after behavioural gates pass; preserve causal depth while optimising.
- [ ] 14.5 Publish immutable observer snapshots; the observer has no write door but its own journal.
- [ ] 14.6 Enforce participant action boundaries at the observer interface (Observer C2.a).
- [ ] 14.7 Benchmark candidate deployment runtimes on the target device and select one.
- [ ] 14.8 Build the shared web application shell, then the Android wrapper around it.
- [ ] 14.9 Restore only CI jobs backed by existing application packages.

## Part 3 — Where every system sits in the week

The target placement for item 1.12, derived from Money G2.a–i. A row's slot is where its
**mechanism** runs; a participant posts at e2 whatever else its system does.

| slot   | systems                                                                                                                                                                                                       |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| a1, a2 | kernel only                                                                                                                                                                                                   |
| b1     | the accrual pass — every dated claim by its own convention (2.1)                                                                                                                                              |
| b2     | `lending` (servicing), `cds` premium, `treasury` outlays, remittance                                                                                                                                          |
| b3     | `loss`, `cds` event, `trade_credit` loss, `housing` foreclosure                                                                                                                                               |
| b4     | `mortality` — every kind's own trigger                                                                                                                                                                        |
| b5     | `estate`                                                                                                                                                                                                      |
| c1–c5  | entry, death, promotion, split, merge (11.1–11.5)                                                                                                                                                             |
| d1     | `recipe`, `goods` draws, `commodities` production, `freight` voyages                                                                                                                                          |
| d2     | `goods` batches finish                                                                                                                                                                                        |
| d3     | `capital_programme`                                                                                                                                                                                           |
| d4     | `freight` dispatch and arrival                                                                                                                                                                                |
| d5     | `employment`, `housing` tenancies and mortgages, `trade_credit`, `small_business`, `insurers`, `securities_lending`, `prime_brokerage`, `private_equity` calls                                                |
| d6     | `treasury`, `short_term_debt`, `corporate_credit`, `equity`, `securitisation`, `bank_capital` raise, `bank_funding` wholesale                                                                                 |
| e1     | `expectations`, and nothing before it                                                                                                                                                                         |
| e2     | every `Participant` — `households`, `stockists`, `funds`, `insurers`, `dealing`, `hedge_funds`, `treasury`, `money_market`, `capital_programme`, `forced_sale`, `goods` sellers, §8's dealers, §22's trackers |
| f      | kernel: one solver per book; the FX pair set jointly (7.1); a derivative's strike with its cut and margin (10.1)                                                                                              |
| g1     | marks — `money` read, every position through its declared treatment                                                                                                                                           |
| g2     | `firms` result, FX revaluation (7.3), variation margin, inventory write-down                                                                                                                                  |
| g3     | `benchmarks`, `indices`, `consumer_prices` and producer prices, `cost_of_capital`, `spot_fx` cross rates, `cross_border`, the sovereign curve, the swap curve                                                 |
| g4     | `bank_capital`, `bank_funding`, `prime_brokerage`, `hedge_funds`, `derivative_layer`, `redeemable` NAV, mandate tests                                                                                         |
| g5     | `reporting`, `firms`' published expectation                                                                                                                                                                   |
| g6     | `ratings`, `second_opinion`, each bank's estimate                                                                                                                                                             |
| g7     | `observer`, last                                                                                                                                                                                              |
| h      | `private_equity`, `forced_sale` (the call), `control`, `polity`, margin calls, covenant demands, redemptions                                                                                                  |
| i1, i2 | kernel only                                                                                                                                                                                                   |

The loops the placement closes, with their periods in weeks: XI-2 forced seller (1 per hop); §26
dealer inventory → quote (1); §22 index inclusion (1); XI-16 expectation (1, damped by memory); §36
supply-network contagion (1 per hop); §16/§28 margin spiral (1 per turn); §24 bank run (1–2); XI-12
currency ↔ basis (1–2); XI-1 loss → capital → lending (2); §44 downgrade (2); §40 housing (2);
§21/§20 inventory → curve (2); XI-11 risk transfer → capacity (2); XI-4 cost of capital →
investment → output (3 + the build lag); XI-17 the mandate (the term).

<!-- plan:gaps -->

## Part 4 — Owned implementation backlog

**1374 clauses: 1162 MISSING, 212 PARTIAL.** Generated from
`docs/COVERAGE.md` by `npm run plan:gaps`, ordered by the Part 1 milestone that owns each clause.
Every checkbox is one uniquely named to-do point and owns exactly one unmet requirement. Work
top-to-bottom by milestone; within a milestone, satisfy prerequisites before dependent points.
A MISSING clause is a mechanism nobody has written; a PARTIAL
one is a mechanism that exists and does not yet do all the clause says, and its row says what is
short. Neither is a finding — a finding is a defect in something that was built — and neither
waits on a measurement: an absence is not measurable (Audit E1), which is exactly why it has to be
in this executable list rather than in a table somebody reads later.

Re-mark the row in `docs/COVERAGE.md` in the change that meets it, and re-run `npm run plan:gaps`
in the same commit. Nothing here is ticked by hand. A point leaves this list only when its
coverage row becomes MET; therefore no MISSING or PARTIAL clause can be unowned.

### 1. Money — 7 missing, 4 partial

> **Required review before this block:** read the **Money** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 376), then inspect `packages/kernel-rs/src/instruments.rs`, `packages/kernel-rs/src/register.rs`, `packages/kernel-rs/src/ledger.rs`, `packages/kernel-rs/src/mechanisms/money.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 1.MONEY.A1C** — `Money A1.c` MISSING — two money lines exist, a central-bank reserve line and each bank's deposit line, and neither is currency in circulation. No third money line is issued to a holder outside the banks, so the clause has no instrument to be true of
- [ ] **TODO 1.MONEY.B3A** — `Money B3.a` MISSING — packages/kernel-rs/src/mechanisms/money.rs declares the whole door: `Issuer` asks the bank, `Overdraft` carries the lend-or-refuse answer and `as_legs` turns a permitted one into a deposit movement and a named loan. Nothing implements `Issuer` for a bank and nothing asks, so no customer is ever overdrawn and no credit decision is taken
- [ ] **TODO 1.MONEY.C4A** — `Money C4.a` MISSING — packages/kernel-rs/src/ledger.rs `Leg::Mint` is the one door money comes through, and the only bank that mints is the one drawing on the central-bank facility. No loan origination mints anything, so bank lending moves deposits that already existed and the stock of money does not answer to credit
- [ ] **TODO 1.MONEY.E1B** — `Money E1.b` MISSING — the arrears register cited is gone with the TypeScript. packages/kernel-rs/src/ledger.rs `Queue` records that a payment was late; nothing writes the payee a receivable
- [ ] **TODO 1.MONEY.F1** — `Money F1` MISSING — packages/kernel-rs/src/mechanisms/money.rs `statements` derives one settled cash-flow statement per named owner and currency from the numbered wire, and nothing in the world asks it for one, so no owner has a statement
- [ ] **TODO 1.MONEY.F2** — `Money F2` MISSING — packages/kernel-rs/src/mechanisms/money.rs `Statement` carries `gross_in`, `gross_out` and their distinct `net` for an owner and currency, and no statement is ever produced for it to report
- [ ] **TODO 1.MONEY.G2C** — `Money G2.c` MISSING — the five slots are declared and every one of them is empty. Three of the five cell events have no caller, and the one split there is runs at d5, where the engagement that causes it is struck
- [ ] **TODO 1.MONEY.A2A** — `Money A2.a` PARTIAL — packages/kernel-rs/src/ledger.rs `account_of` answers which money a party holds, its own bank's or its own issue where it banks nowhere, so a payment lands in the money the payee is paid in. The reporting half is short: packages/kernel-rs/src/mechanisms/money.rs `statements` would key every book by owner and currency together and nothing calls it
- [ ] **TODO 1.MONEY.B3** — `Money B3` PARTIAL — one holder's answer exists: packages/kernel-rs/src/mechanisms/bank_funding.rs turns a bank reserve shortfall into a collateralised central-bank facility with a priced claim and minted reserves. The customer's does not — packages/kernel-rs/src/mechanisms/money.rs `as_legs` is named by no production code (B3.a) — and no balance is ever negative at all, because packages/kernel-rs/src/ledger.rs refuses rather than overdrawing
- [ ] **TODO 1.MONEY.B3B** — `Money B3.b` PARTIAL — packages/kernel-rs/src/mechanisms/bank_funding.rs turns a reserve shortfall into a collateralised advance from the central bank, with a rate, a maturity and pledged collateral, so the borrowing is real. The corridor is not what prices it: `facility_rate` takes a money-fund yield and a declared penalty, and no central bank sets a rate on either side of it
- [ ] **TODO 1.MONEY.C4B** — `Money C4.b` PARTIAL — the lending half holds: packages/kernel-rs/src/mechanisms/bank_funding.rs mints reserves into the borrowing bank in the same instruction that books the advance. The buying half is absent, because no central bank buys anything in any book

### 1. Register — 1 missing, 2 partial

> **Required review before this block:** read the **Register** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 522), then inspect `packages/kernel-rs/src/register.rs`, `packages/kernel-rs/src/instruments.rs`, `packages/kernel-rs/src/parties.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 1.REGISTER.D5B** — `Register D5.b` MISSING — packages/kernel-rs/src/register.rs `Lien` records one pledge from a holder to a taker and nothing pledges the same units onward. packages/kernel-rs/src/mechanisms/securities_lending.rs `chain_from` would walk such a chain and no production code names it, so no default reaches a party that never traded with the defaulter
- [ ] **TODO 1.REGISTER.A1C** — `Register A1.c` PARTIAL — packages/kernel-rs/src/instruments.rs declares a `UnitId` on every instrument, so the unit is part of what the paper IS. It does not travel: `unit_of` is named by no production code, so a quantity is read without asking what one of it is
- [ ] **TODO 1.REGISTER.E1A** — `Register E1.a` PARTIAL — packages/kernel-rs/src/mechanisms/lending.rs `Servicing` reads the holders at the moment it applies the action, with no earlier record date to remember, which is the first half. The second is absent: packages/kernel-rs/src/instruments.rs `accrued` is not added to any trade price, so a coupon is a windfall to whoever holds the line on the date

### 1. Clearing — 0 missing, 2 partial

> **Required review before this block:** read the **Clearing** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 621), then inspect `packages/kernel-rs/src/clearing.rs`, `packages/kernel-rs/src/protocols.rs`, `packages/kernel-rs/src/session.rs`, `packages/kernel-rs/src/prices.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 1.CLEARING.B3A** — `Clearing B3.a` PARTIAL — packages/kernel-rs/src/mechanisms/dealing.rs `Lines` holds a per-dealer position limit and `Dealers` reads it before quoting, so a desk is not an unlimited counterparty. The limit is a declared number rather than a read of the capital and the inventory behind it
- [ ] **TODO 1.CLEARING.C4A** — `Clearing C4.a` PARTIAL — the outcome propagates for one consequence: packages/kernel-rs/src/mechanisms/sovereign.rs reads a short fill and `handle` takes the issuer to its buffer, a deferred outlay or back to the market. A seller keeping inventory and a maturity left unrolled change nothing for anybody, because no participant reads a failed book

### 1. Geography — 17 missing, 9 partial

> **Required review before this block:** read the **Geography** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 4444), then inspect `packages/kernel-rs/src/geography.rs`, `packages/kernel-rs/src/registry.rs`, `packages/kernel-rs/src/places.rs`, `packages/kernel-rs/src/opening.rs`, `packages/kernel-rs/src/mechanisms/freight.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 1.GEOGRAPHY.B3** — `Geography B3` MISSING — packages/kernel-rs/src/geography.rs carries surface, elevation and the infrastructure present on a tile. No carrier observes any of it: packages/kernel-rs/src/mechanisms/freight.rs `Carriage` counts live agreements and reads no terrain
- [ ] **TODO 1.GEOGRAPHY.E1** — `Geography E1` MISSING — packages/kernel-rs/src/geography.rs `NetworkAsset` carries a site, a finite capacity, a life, maintenance and a failure state, and `add_asset` that would create one is unreached. No road, bridge, tunnel or port is a holding in packages/kernel-rs/src/register.rs
- [ ] **TODO 1.GEOGRAPHY.E2** — `Geography E2` MISSING — no opening infrastructure is reconciled into the register, and packages/kernel-rs/src/mechanisms/capital_programme.rs builds plant that has no site, so later infrastructure cannot be built, paid for, owned, depreciated or maintained as network capital
- [ ] **TODO 1.GEOGRAPHY.E3** — `Geography E3` MISSING — packages/kernel-rs/src/geography.rs refuses a port that is not a land tile beside navigable water, and the refusal has no caller. Nothing transfers between a land and a maritime leg because there are no legs
- [ ] **TODO 1.GEOGRAPHY.E5** — `Geography E5` MISSING — there is no network, so none is free: no connectivity supplies a carrier, a capacity, a price or a payment, and no road cost is posted as a freight price
- [ ] **TODO 1.GEOGRAPHY.F1** — `Geography F1` MISSING — packages/kernel-rs/src/geography.rs `add_route` builds a durable sequence of compatible legs between named sites, with its mode and transfers stated, and is unreached. packages/kernel-rs/src/mechanisms/freight.rs `Route` is a from-region and a to-region with no legs at all
- [ ] **TODO 1.GEOGRAPHY.F2** — `Geography F2` MISSING — packages/kernel-rs/src/geography.rs holds the rule that a road leg needs connected road capacity and a maritime leg compatible ports and water, and no caller applies it. No shipment records a transfer because no shipment has legs
- [ ] **TODO 1.GEOGRAPHY.F3** — `Geography F3` MISSING — packages/kernel-rs/src/mechanisms/freight.rs's participant offers its plant at that plant's upkeep. No distance, vehicle technology, energy, toll, port charge or available capacity enters the offer, so there is nothing for a freight price to clear over
- [ ] **TODO 1.GEOGRAPHY.F4** — `Geography F4` MISSING — packages/kernel-rs/src/ledger.rs gives every dispatch an arrival one week out, whatever the route, so physical travel time and loading produce nothing. Expected, promised and realised arrival are one number
- [ ] **TODO 1.GEOGRAPHY.F5** — `Geography F5` MISSING — packages/kernel-rs/src/geography.rs can shut a segment and remove its capacity and connectivity, and nothing calls it. No disruption can cause a refusal, a delay or a reroute, because no route exists to be disrupted
- [ ] **TODO 1.GEOGRAPHY.F6** — `Geography F6` MISSING — nothing chooses a route, so no route exists where no path does and no party routes on information it cannot observe. What does move is instantaneous in everything but its one-week arrival and costs the shipper nothing per kilometre
- [ ] **TODO 1.GEOGRAPHY.G1** — `Geography G1` MISSING — packages/kernel-rs/src/ledger.rs `Leg::Dispatch` names a carrier and carries a capacity number on the leg, and reserves nothing on any segment. A dispatch is admitted against that number rather than against a feasible reservation
- [ ] **TODO 1.GEOGRAPHY.G2** — `Geography G2` MISSING — packages/kernel-rs/src/ledger.rs records a dispatch's owner, carrier, route and arrival, and no `Shipment` is ever written, so the cargo an audit could follow does not exist. Nothing delivers either, so goods in transit are never at a destination to be sold
- [ ] **TODO 1.GEOGRAPHY.G3** — `Geography G3` MISSING — packages/kernel-rs/src/mechanisms/goods.rs `Consignment::landed_cost` is ex-works plus freight plus duty and has no caller. No freight, toll or handling payment names a payer and a payee on the wire, so there is no settled consideration for a landed cost to read
- [ ] **TODO 1.GEOGRAPHY.G4** — `Geography G4` MISSING — packages/kernel-rs/src/ledger.rs throws on a dispatch beyond the carrier's capacity rather than recording a refusal, so over-capacity is a stopped process and not a named outcome. There is no delivery, so none can be missed, and no path to close
- [ ] **TODO 1.GEOGRAPHY.H2** — `Geography H2` MISSING — packages/kernel-rs/src/mechanisms/observer.rs publishes no tiles, surfaces, borders, sites, infrastructure or shipments. It cannot become a second geography store, because it copies nothing from the first one
- [ ] **TODO 1.GEOGRAPHY.H3** — `Geography H3` MISSING — no carrier cost, inventory or intended route becomes public by being locatable, because no carrier has a route and no cargo has a location. The FORBID holds by the mechanism being absent
- [ ] **TODO 1.GEOGRAPHY.H4** — `Geography H4` MISSING — no decision in this world reads a distance, so lengthening one, adding a water barrier, closing a segment or paying for capacity changes nothing — through the mechanisms or otherwise. The experiment has no input to hold
- [ ] **TODO 1.GEOGRAPHY.A5** — `Geography A5` PARTIAL — terrain constrains a site: packages/kernel-rs/src/geography.rs `site` refuses water and `stand` needs land the region actually holds. It constrains no route, because no route is built. packages/kernel-rs/src/assembly.rs `every_store_is_declared` refuses a second map that nobody declared
- [ ] **TODO 1.GEOGRAPHY.B5** — `Geography B5` PARTIAL — packages/kernel-rs/src/geography.rs `add_segment` refuses a leg whose ends are not neighbours and a land leg over water with no asset, and `PathLegsAreCompatible` guards the same two in the world. Nothing calls `add_segment`, so no segment exists to be obstructed
- [ ] **TODO 1.GEOGRAPHY.C3** — `Geography C3` PARTIAL — packages/kernel-rs/src/geography.rs `tiles_of` enumerates a region's ground and `stand` reads it. No market asks how far one region is from another, so the read of sites and paths that would answer it has no caller and no regional constant stands in for it
- [ ] **TODO 1.GEOGRAPHY.C4** — `Geography C4` PARTIAL — packages/kernel-rs/src/assembly.rs `admit` stands every party on an exact site through packages/kernel-rs/src/geography.rs `stand`, and packages/kernel-rs/src/module.rs `money_of` reads its country THROUGH that site with `where_is`. An establishment, dwelling, plant, warehouse or port still has no site of its own
- [ ] **TODO 1.GEOGRAPHY.D4** — `Geography D4` PARTIAL — packages/kernel-rs/src/geography.rs `reject` records the construction condition a candidate world failed and has no caller, because `generate` has one condition and returns `GeographyError::InvalidShape` rather than adjusting the sea level until a world passes
- [ ] **TODO 1.GEOGRAPHY.E4** — `Geography E4` PARTIAL — packages/kernel-rs/src/geography.rs `reserve` takes capacity on the one `NetworkAsset` every route through a segment shares, and `SegmentCapacityIsShared` measures each week's reservations against that capacity and refuses one taken on capital that is not operating. Nothing builds a segment, so nothing contends yet
- [ ] **TODO 1.GEOGRAPHY.G5** — `Geography G5` PARTIAL — packages/kernel-rs/src/geography.rs `DeliveriesLandOnce` measures that nothing is past its promise without becoming a named outcome, and `ShipmentState` makes a second delivery unconstructible. No contribution reconciles origin stock against what was dispatched and what remains
- [ ] **TODO 1.GEOGRAPHY.G6** — `Geography G6` PARTIAL — packages/kernel-rs/src/geography.rs `CargoHasAnOwner` refuses cargo in transit with no live owner or carrier and `SegmentCapacityIsShared` refuses duplicate capacity. Nothing ships, so nothing teleports either, and no price basis substitutes for a shipment because there is no location basis

### 2. Sovereign — 27 missing, 21 partial

> **Required review before this block:** read the **Sovereign** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1200), then inspect `packages/kernel-rs/src/mechanisms/sovereign.rs`, `packages/kernel-rs/src/mechanisms/treasury.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 2.SOVEREIGN.B3A** — `Sovereign B3.a` MISSING — packages/kernel-rs/src/instruments.rs `issue` allocates a new id every time and has no door that adds to an existing line, so every tap would be a separate instrument. Nothing re-opens anything, and the line and the issue are therefore the same object
- [ ] **TODO 2.SOVEREIGN.C1** — `Sovereign C1` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `AuctionAnnouncement` carries the line and the size an auction is announced in, and nothing constructs one. The treasury brings its paper and posts in the same week, so no bidder ever sees a calendar ahead of the book
- [ ] **TODO 2.SOVEREIGN.C1A** — `Sovereign C1.a` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `AuctionAnnouncement` carries the line and the size an auction would be announced in, and nothing constructs one. No bidder sees a calendar, because none is published
- [ ] **TODO 2.SOVEREIGN.C4** — `Sovereign C4` MISSING — packages/kernel-rs/src/session.rs keeps every auction`s requested units, filled units and proceeds as a durable session, so the tail and the cover ratio are readable from it. Nothing reads them: neither statistic is computed or published
- [ ] **TODO 2.SOVEREIGN.D2** — `Sovereign D2` MISSING — packages/kernel-rs/src/instruments.rs `yield_to` derives a return from its price and only this way round, on the line's own `Convention`. No sovereign line's yield is published, so nothing downstream can invert one either
- [ ] **TODO 2.SOVEREIGN.D4** — `Sovereign D4` MISSING — nothing prices as a spread to the sovereign curve. packages/kernel-rs/src/mechanisms/benchmarks.rs selects the lines whose issuer is a treasury and fits a curve; no lender, holder or issuer reads the result, so the benchmark benchmarks nothing
- [ ] **TODO 2.SOVEREIGN.D5** — `Sovereign D5` MISSING — packages/kernel-rs/src/register.rs can encumber units through a lien and packages/kernel-rs/src/mechanisms/money_market.rs `lends_against` is the haircut a lender applies, refusing collateral already pledged. No repo pledges sovereign paper, so neither is exercised on a sovereign line
- [ ] **TODO 2.SOVEREIGN.D6** — `Sovereign D6` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `bid_offer` reads a width off the two best posted levels rather than applying a prior, and has no caller. A primary dealer posts a bid and the treasury an offer, so a width exists in the book; nothing measures it
- [ ] **TODO 2.SOVEREIGN.E2** — `Sovereign E2` MISSING — the clause asks for six holder classes buying for six different reasons, which is what gives an auction two sides. None posts: no bank buys a liquidity buffer, no insurer buys duration, no central bank buys as policy, no foreign official buys reserves, no fund takes relative value and no household buys directly
- [ ] **TODO 2.SOVEREIGN.E2A** — `Sovereign E2.a` MISSING — packages/kernel-rs/src/mechanisms/bank_capital.rs weights an asset by its issuer's grade, which is the reason a bank would hold sovereign paper. No bank holds any, so the buffer motive reaches no book
- [ ] **TODO 2.SOVEREIGN.E2B** — `Sovereign E2.b` MISSING — packages/kernel-rs/src/mechanisms/insurers.rs `InsurerMatching` bids from a price outlook rather than from a liability it must match, and no insurer holds a sovereign line. Duration is nobody's reason for anything
- [ ] **TODO 2.SOVEREIGN.E2C** — `Sovereign E2.c` MISSING — no central bank holds sovereign paper, because packages/kernel-rs/src/mechanisms/sovereign.rs `OpenMarketPurchase` is constructed by nothing. Policy is nobody's reason to hold a line
- [ ] **TODO 2.SOVEREIGN.E2D** — `Sovereign E2.d` MISSING — no foreign official holder exists, because the world has one country and one currency. Nothing holds a line as a reserve
- [ ] **TODO 2.SOVEREIGN.E2E** — `Sovereign E2.e` MISSING — packages/kernel-rs/src/mechanisms/funds.rs `FundMandates` carries what a fund may hold and no fund holds a sovereign line, so relative value is nobody's reason either
- [ ] **TODO 2.SOVEREIGN.E2F** — `Sovereign E2.f` MISSING — no household or firm holds a bill directly, so no saver substitutes between a fund and the paper that fund would have bought. packages/kernel-rs/src/mechanisms/short_term_debt.rs `prefers_paper` is that choice and no production code names it
- [ ] **TODO 2.SOVEREIGN.F5** — `Sovereign F5` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `CurveOperation` carries the line bought in, the line switched into and what the operation costs, and nothing constructs one. The issuer never manages its own curve
- [ ] **TODO 2.SOVEREIGN.G1** — `Sovereign G1` MISSING — the VERIFY asks that a sovereign short in its own money inflates rather than defaults. Neither half is reachable: there is no central bank to create the money and no price level a creation would move
- [ ] **TODO 2.SOVEREIGN.H1** — `Sovereign H1` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `OpenMarketPurchase` carries the buyer, the seller, the line, the face and the price, and nothing constructs one. No central bank posts in any book, so no policy purchase is ever sized or made
- [ ] **TODO 2.SOVEREIGN.H2** — `Sovereign H2` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `reserve_creation` values a purchase and refuses one whose seller is the issuer, and it is unreached. With no purchase there is no reserve created and the base does not move
- [ ] **TODO 2.SOVEREIGN.H3** — `Sovereign H3` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `central_bank_remittance` is unreached. No central bank holds sovereign paper, so no coupon accrues on its book and nothing returns to the treasury
- [ ] **TODO 2.SOVEREIGN.H3A** — `Sovereign H3.a` MISSING — no central bank holds a sovereign line, so nothing accrues on its book and no receivable stands on its sheet. packages/kernel-rs/src/instruments.rs `accrued` is the read that would produce the figure and no holder calls it
- [ ] **TODO 2.SOVEREIGN.H5** — `Sovereign H5` MISSING — the VERIFY compares two readings of one debt and there is nothing to read. No central bank holds a sovereign line, so neither the consolidated statement nor the accounting one has a subject
- [ ] **TODO 2.SOVEREIGN.I1** — `Sovereign I1` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `BondFuture` names a deliverable line, a contract count, face per contract, a delivery week and the margin posted, and `future_settlement` marks it to the cash price of that bond. Nothing constructs one and no market in a future is declared
- [ ] **TODO 2.SOVEREIGN.I1A** — `Sovereign I1.a` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `net_basis` is the future price less the financed cash position, and it is unreached. There is no future price to measure against and nothing finances a sovereign line
- [ ] **TODO 2.SOVEREIGN.I2** — `Sovereign I2` MISSING — the clause names three reasons to be on one side or the other and no participant posts a bond future at all. A duration mandate short of duration, a holder over its sovereign target and a dealer quoting both ways are absent from that book because the book does not exist
- [ ] **TODO 2.SOVEREIGN.I3** — `Sovereign I3` MISSING — nothing goes long a cash sovereign line against a short future, because neither leg exists: no future is constructed, and `Leg::Pledge` never encumbers sovereign paper to finance one. The repo demand the clause calls the largest real source of it is not in the model
- [ ] **TODO 2.SOVEREIGN.I3A** — `Sovereign I3.a` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `basis_trade_allowed` refuses a position whose posted margin does not cover the drawdown, and it is unreached. The FORBID holds only because no basis trader exists to lose anything
- [ ] **TODO 2.SOVEREIGN.A1A** — `Sovereign A1.a` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs `Funding` creates a tax due against the named party that remits it, off each settled wage and sale leg and off reported firm income, so no rate is applied to an aggregate. Only three bases exist: nothing taxes payroll separately
- [ ] **TODO 2.SOVEREIGN.A1B** — `Sovereign A1.b` PARTIAL — `Funding` creates purchases, transfers and public wages as dues to named payees, so three of the four kinds reach somebody. Interest is not among them: no sovereign coupon is an outlay the programme plans for
- [ ] **TODO 2.SOVEREIGN.A2C** — `Sovereign A2.c` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs `Funding` reads a tenor from `params` and issues at it, so a maturity is chosen. It is one declared tenor rather than a mix, and nothing weighs a cheap short issue that rolls against a dear long one that does not
- [ ] **TODO 2.SOVEREIGN.A4B** — `Sovereign A4.b` PARTIAL — `default_cause` makes non-payment in the issuer's own money a refusal and non-payment in a money it cannot create an inability, which is the distinction the clause calls the whole of sovereign credit risk. Nothing exercises it: a treasury owes only its own region's money
- [ ] **TODO 2.SOVEREIGN.B1** — `Sovereign B1` PARTIAL — packages/kernel-rs/src/mechanisms/sovereign.rs `SovereignPaper::coupon` answers `None` for a bill and a rate for a bond, and each produces its own schedule — a zero-coupon line falls due in principal alone, and packages/kernel-rs/src/module.rs `accrued_per_unit` accretes its discount rather than paying it a coupon. They are still one `Class::Claim` distinguished by a field, which is the flag B1 says they must not be
- [ ] **TODO 2.SOVEREIGN.B3** — `Sovereign B3` PARTIAL — packages/kernel-rs/src/register.rs keys a holding by (holder, instrument), so every holder of one line is fungible in it and the issued amount is one number. A re-opening that adds to an existing line rather than creating a new one is not built: each week`s funding brings a fresh line
- [ ] **TODO 2.SOVEREIGN.B6** — `Sovereign B6` PARTIAL — no sovereign line carries a call, a make-whole or a non-call period, so there is no early-termination regime to exercise. The buyback and switch the issuer manages its curve with instead is `CurveOperation`, which has no caller
- [ ] **TODO 2.SOVEREIGN.C1B** — `Sovereign C1.b` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs `TreasuryIssues` posts the shortfall packages/kernel-rs/src/module.rs `must_raise` computed from its own gap, so the size is the issuer's own and comes out of its programme. It is not announced ahead of the book, so nobody can prepare against it
- [ ] **TODO 2.SOVEREIGN.C3A** — `Sovereign C3.a` PARTIAL — packages/kernel-rs/src/mechanisms/sovereign.rs `obliged_size` answers nothing for a desk at its position limit or out of cash, and packages/kernel-rs/src/clearing.rs answers `NoDemand` where nobody bid — so an auction fails exactly when the dealers step back, and no central bank stands behind it. What the privilege IS in exchange is not modelled, so the bargain is one-sided
- [ ] **TODO 2.SOVEREIGN.C5** — `Sovereign C5` PARTIAL — weak demand resolves as a worse price, because the treasury posts a size and the book clears where the bids reach. The other resolution is not built: `sovereign.rs handle` can return `ComeBackToTheMarket` at a different size or maturity and nothing acts on it
- [ ] **TODO 2.SOVEREIGN.D1** — `Sovereign D1` PARTIAL — packages/kernel-rs/src/systems.rs declares one book per sovereign line and packages/kernel-rs/src/session.rs prints a price per unit where it crosses. No participant posts into a sovereign line after issue, so the secondary market has a venue and no parties
- [ ] **TODO 2.SOVEREIGN.D3** — `Sovereign D3` PARTIAL — packages/kernel-rs/src/mechanisms/benchmarks.rs `curve_at` fits a tenor through observed points and is the one owner of the fit. It fits over points a caller hands it, and no caller builds those points from sovereign prints, so the curve is a function with no observations
- [ ] **TODO 2.SOVEREIGN.D3B** — `Sovereign D3.b` PARTIAL — packages/kernel-rs/src/mechanisms/benchmarks.rs `CurvePoint` carries a `CurveProvenance`, so a point says whether it was traded, interpolated, extrapolated or never traded, and `curve_at` never reads its own previous output. A consumer that needs a real price is not told: nothing reads the provenance before using a point
- [ ] **TODO 2.SOVEREIGN.D3C** — `Sovereign D3.c` PARTIAL — packages/kernel-rs/src/calendar.rs `Convention` is the one place a compounding basis is stated and `year_fraction` the one function that applies it, so two conventions cannot answer one question. The curve does not publish which it used, so a consumer takes the convention rather than being told it
- [ ] **TODO 2.SOVEREIGN.E4** — `Sovereign E4` PARTIAL — packages/kernel-rs/src/ledger.rs `Leg::Pledge` encumbers named units and packages/kernel-rs/src/register.rs refuses to move what is pledged, so paper CAN be pledged and cannot be pledged twice. Nothing pledges a sovereign line, so the haircut packages/kernel-rs/src/mechanisms/money_market.rs `lends_against` would apply is never applied to one
- [ ] **TODO 2.SOVEREIGN.E5** — `Sovereign E5` PARTIAL — packages/kernel-rs/src/mechanisms/bank_capital.rs weights an asset from the issuer`s grade through `classified_weight`, and a sovereign issuer is one `FailureMode::Never` cannot reach — so it weights lightest. It is not a declared zero, and no bank`s holding of sovereign paper is what puts it there: no bank holds any
- [ ] **TODO 2.SOVEREIGN.G2** — `Sovereign G2` PARTIAL — packages/kernel-rs/src/mechanisms/sovereign.rs `default_cause` separates the two: an issuer that is willing but owes money it cannot create fails by inability, and every other non-payment is refusal. The foreign branch is unreachable, because a treasury issues and owes only the money of its own region, so no sovereign is ever in the position the clause is about
- [ ] **TODO 2.SOVEREIGN.G3** — `Sovereign G3` PARTIAL — a failed due opens a `SOVEREIGN_EXCHANGE` process on the LINE that failed rather than on the issuer, so a default is selective, and no estate opens because a treasury reaches none of the three doors in packages/kernel-rs/src/mechanisms/mortality.rs that create one. The negotiation is absent: the process opens, restates its holdout face each week, and nothing offers terms or closes it
- [ ] **TODO 2.SOVEREIGN.G4** — `Sovereign G4` PARTIAL — packages/kernel-rs/src/mechanisms/sovereign.rs `holdout_face` sums the face standing outside the hands of the issuer for every open exchange, and the mechanism journals it each week. There is no offer to hold out from: no replacement line is proposed, no holder tenders, and no paper is exchanged
- [ ] **TODO 2.SOVEREIGN.G5** — `Sovereign G5` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs `TreasuryIssues` posts no market at all once a default event stands against the issuer, so the consequence is exclusion, and nothing liquidates it because no estate opens. The rating half is absent: packages/kernel-rs/src/mechanisms/ratings.rs grades a party off published accounts and no treasury publishes any, so a sovereign grade has neither producer nor consumer
- [ ] **TODO 2.SOVEREIGN.H4** — `Sovereign H4` PARTIAL — the forbidden side of the boundary holds, because a treasury raises before it spends and no advance reaches it. The permitted side does not exist: there are no open-market operations, so nothing is being drawn between and the policy choice the clause describes is not yet a choice anybody can make

### 2. Treasury — 3 missing, 1 partial

> **Required review before this block:** read the **Treasury** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2874), then inspect `packages/kernel-rs/src/mechanisms/treasury.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 2.TREASURY.B3A** — `Treasury B3.a` MISSING — no outlay moves with the cycle, because nothing measures one and no transfer responds to a household losing work. packages/kernel-rs/src/mechanisms/treasury.rs reads a declared per-head transfer from `params`, so outlays are a constant and the constraint never tightens from that side
- [ ] **TODO 2.TREASURY.D3A** — `Treasury D3.a` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `OpenMarketPurchase` carries the buyer, the seller, the line, the face and the price, and `reserve_creation` refuses one whose seller is the issuer — which is exactly the line between a policy purchase and an overdraft. Nothing constructs either, so the permitted act never happens
- [ ] **TODO 2.TREASURY.D4A** — `Treasury D4.a` MISSING — packages/kernel-rs/src/instruments.rs `maturing_by` is the maturity profile — what falls due by a week, read off the lines themselves — and no production code names it. Every issue carries one declared tenor, so the profile is a single wall by construction, and nothing is pre-funded
- [ ] **TODO 2.TREASURY.C1A** — `Treasury C1.a` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs `Funding` creates a tax due against each named payer and ordinary settlement debits that payer's own account, so the flow is real at both ends. The base for the corporate tax is the income packages/kernel-rs/src/mechanisms/firms.rs reported, which is the payer's own statement; the wage and sale bases are read off settled legs rather than off anything the payer stated

### 3. Commodities Spot — 4 missing, 3 partial

> **Required review before this block:** read the **Commodities Spot** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2239), then inspect `packages/kernel-rs/src/mechanisms/commodities.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.COMMODITIES-SPOT.B3** — `Commodities Spot B3` MISSING — `goods.rs` records normal recipe waste as physical units and `commodities.rs disrupted` expresses a physical capacity loss, but no production caller supplies a disruption event
- [ ] **TODO 3.COMMODITIES-SPOT.D4** — `Commodities Spot D4` MISSING — spot now clears from physical orders and `Storing` reads storage cost and the cleared print, but the futures production path is not wired yet, so no traded forward relationship exists
- [ ] **TODO 3.COMMODITIES-SPOT.E3** — `Commodities Spot E3` MISSING — `cross_border.rs` preserves party-to-party physical export flows, but commodity terms of trade are not consumed by the currency-fundamentals decision
- [ ] **TODO 3.COMMODITIES-SPOT.E4** — `Commodities Spot E4` MISSING — the physical price feeds firm input cost and household consumption, but the complete margin-to-inflation-to-policy chain has no wired monetary-policy consumer yet
- [ ] **TODO 3.COMMODITIES-SPOT.A1** — `Commodities Spot A1` PARTIAL — `mechanisms/commodities.rs Storing` reads each `Class::Good` line, its named production rows, settled consumption, physical stock and that line’s own cleared print, and packages/kernel-rs/src/prices.rs keys a print by (market, instrument, week), so the same grade in two places is two levels and `of_line` refuses to pick between them. The declaration is still one book per good rather than one per place, so no line has a second one yet
- [ ] **TODO 3.COMMODITIES-SPOT.A1A** — `Commodities Spot A1.a` PARTIAL — packages/kernel-rs/src/mechanisms/commodities.rs `Grade` carries the place as part of what the good IS, so the same quality in two regions is two things. Both do not price: packages/kernel-rs/src/systems.rs declares one book per good rather than one per region and good, so there is one price and no transport difference between two
- [ ] **TODO 3.COMMODITIES-SPOT.D1** — `Commodities Spot D1` PARTIAL — `session.rs` clears one book at a time and packages/kernel-rs/src/prices.rs keys what it prints by the market as well as the line, so two places in one grade keep two runs and a reader that names neither is refused rather than served the wrong one. What is short is the declaration: one book per good is opened, so the second place has no book to clear in

### 3. Firm — 20 missing, 8 partial

> **Required review before this block:** read the **Firm** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3108), then inspect `packages/kernel-rs/src/mechanisms/firms.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.FIRM.A3** — `Firm A3` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.A4** — `Firm A4` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.B4** — `Firm B4` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.B5** — `Firm B5` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.B6** — `Firm B6` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.C1** — `Firm C1` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.C2** — `Firm C2` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.C4** — `Firm C4` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.C4B** — `Firm C4.b` MISSING — packages/kernel-rs/src/mechanisms/trade_credit.rs `receivable_of` and `payable_of` would sum the invoice book and no production code names either. No invoice is outstanding at all, so there is nothing to report and no ratio standing in for it
- [ ] **TODO 3.FIRM.D2** — `Firm D2` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.D3** — `Firm D3` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.D4** — `Firm D4` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.E1** — `Firm E1` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.E2** — `Firm E2` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.E3** — `Firm E3` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.E4** — `Firm E4` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.E4A** — `Firm E4.a` MISSING — no firm holds a leverage target, so none approaches one or pays down towards it. packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` sizes an investment programme and no funding decision reads it
- [ ] **TODO 3.FIRM.E5** — `Firm E5` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.E6** — `Firm E6` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.F4** — `Firm F4` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and operating-cost legs into typed revenue, costs and operating cash, and `expectations::Forming` consumes that result into the firm's next party-local earnings observation. `Funds`, `LeverageTarget` and the programme-financing choice remain unwired
- [ ] **TODO 3.FIRM.A2** — `Firm A2` PARTIAL — packages/kernel-rs/src/parties.rs puts it in a region and packages/kernel-rs/src/registry.rs reads its money through the country. It has no sector: nothing says what it buys and sells beyond the lines it holds, and what a firm makes is the seeding's
- [ ] **TODO 3.FIRM.B1** — `Firm B1` PARTIAL — packages/kernel-rs/src/session.rs prints revenue where a goods book crosses, and `GoodsSellers` posts from the firm`s own cost. one book of 1,546 crosses per period
- [ ] **TODO 3.FIRM.B1B** — `Firm B1.b` PARTIAL — packages/kernel-rs/src/mechanisms/expectations.rs `Forming` moves a firm's demand outlook towards what it actually sold, so the baseline is its own history. Nothing compares a reported figure with that baseline, so the comparison the clause asks for is not taken
- [ ] **TODO 3.FIRM.B4A** — `Firm B4.a` PARTIAL — no cost line is struck as a share of revenue anywhere, so no margin is an attractor and none was fitted to. Nothing reads a margin either, so the figure the clause calls a read is not taken
- [ ] **TODO 3.FIRM.B4B** — `Firm B4.b` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs and packages/kernel-rs/src/mechanisms/employment.rs create wages and public purchases as dues to named payees, and packages/kernel-rs/src/mechanisms/goods.rs buys inputs from named sellers. Interest, rent and tax on a firm are not lines it pays to anybody
- [ ] **TODO 3.FIRM.C4A** — `Firm C4.a` PARTIAL — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled sale and cost legs, so what it reports is cash that actually moved. Profit and cash are therefore the same number, because nothing accrues a sale ahead of its receipt and no firm dies of the gap
- [ ] **TODO 3.FIRM.D5** — `Firm D5` PARTIAL — packages/kernel-rs/src/ledger.rs `Queue::gave_up` records the arrear when the days run out. Nothing follows: no event, no lender loss, no rating action — the loss chain terminates nowhere
- [ ] **TODO 3.FIRM.E7A** — `Firm E7.a` PARTIAL — packages/kernel-rs/src/mechanisms/expectations.rs `Forming` gives every firm its own expectation of what it sells, public or not, and packages/kernel-rs/src/mechanisms/reporting.rs `Publishes` publishes a result against it. No calendar says when, and no bank publishes an estimate of the same lines

### 3. Goods — 28 missing, 6 partial

> **Required review before this block:** read the **Goods** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3468), then inspect `packages/kernel-rs/src/mechanisms/goods.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.GOODS.A3** — `Goods A3` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.A4** — `Goods A4` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.B1D** — `Goods B1.d` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.B4** — `Goods B4` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.B5** — `Goods B5` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.B5A** — `Goods B5.a` MISSING — packages/kernel-rs/src/mechanisms/goods.rs capitalises a batch's cost into the lot it creates, so a period that started nothing capitalises nothing. The other half is absent: the cost of an idle line is incurred by nobody, because no cost is charged outside a batch
- [ ] **TODO 3.GOODS.B5B** — `Goods B5.b` MISSING — a batch carries whatever it cost over the units it made, so a smaller batch does give a higher unit cost. Nothing throttles a line: `Making` starts whole batches or none, so the state the clause describes never arises
- [ ] **TODO 3.GOODS.C1** — `Goods C1` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.C3** — `Goods C3` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.C4** — `Goods C4` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.C6** — `Goods C6` MISSING — the buyer pays out of its OWN account, not the seller`s money, and nothing is bought from anybody
- [ ] **TODO 3.GOODS.D1** — `Goods D1` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.D2** — `Goods D2` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.D3** — `Goods D3` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.D4** — `Goods D4` MISSING — packages/kernel-rs/src/mechanisms/goods.rs `Consignment::landed_cost` is ex-works plus freight plus duty and has no caller
- [ ] **TODO 3.GOODS.D5** — `Goods D5` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.E2** — `Goods E2` MISSING — packages/kernel-rs/src/mechanisms/goods.rs `carry` implements lower of cost and net realisable value with the broker-dealer exception, exactly as E2/E2.b ask, and it has no caller: of 19 items in the module only `CostFlow`, `Lot`, `take` and `Perishing` are reached
- [ ] **TODO 3.GOODS.E2A** — `Goods E2.a` MISSING — nothing writes inventory down, so nothing reverses one either. packages/kernel-rs/src/register.rs carries a lot at its own basis and no mechanism compares that basis with a print, which is where both halves would happen
- [ ] **TODO 3.GOODS.E2B** — `Goods E2.b` MISSING — packages/kernel-rs/src/mechanisms/goods.rs `Stockist` is the dealer whose inventory is its position and it carries lots at basis like everybody else. No holder is carried at fair value through income
- [ ] **TODO 3.GOODS.E3** — `Goods E3` MISSING — see E2 — no holding loss is ever an event with a date and an income line
- [ ] **TODO 3.GOODS.F1** — `Goods F1` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.F2** — `Goods F2` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.F3** — `Goods F3` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.F4** — `Goods F4` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.G1C** — `Goods G1.c` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.G2** — `Goods G2` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.G3** — `Goods G3` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.G4** — `Goods G4` MISSING — `goods` is wired as a participant (`GoodsSellers`) and the making is `mechanisms/goods.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index
- [ ] **TODO 3.GOODS.E2C** — `Goods E2.c` PARTIAL — packages/kernel-rs/src/mechanisms/goods.rs declares `Carrying::Cost` on what a maker makes and on what a stockist buys, so no inventory is carried at market. The FORBID holds by declaration and is unguarded: nothing refuses a non-dealer that declares `Carrying::Market`, and `carry` — the lower of cost and net realisable value — still has no caller
- [ ] **TODO 3.GOODS.E4A** — `Goods E4.a` PARTIAL — packages/kernel-rs/src/mechanisms/goods.rs `Perishing` destroys units with `Gone::Perished` and packages/kernel-rs/src/mechanisms/commodities.rs `Storing` proposes a cash fee to the stockist, so the two are different things in different places. Neither is summed into the other, and no distributor margin covers both because no distributor charges one
- [ ] **TODO 3.GOODS.F5** — `Goods F5` PARTIAL — packages/kernel-rs/src/mechanisms/goods.rs computes cost of goods sold over the lots that left. Nothing books it: no income statement is produced from it, because `Publishes` reads the equity account and not the cost
- [ ] **TODO 3.GOODS.F5A** — `Goods F5.a` PARTIAL — packages/kernel-rs/src/mechanisms/goods.rs `costs` charges the lots that actually left, so a firm that produced and did not sell carries the cost in its stock. No period cost exists: an idle line's payroll is charged to nobody, and no income statement is produced to charge it to
- [ ] **TODO 3.GOODS.G1A** — `Goods G1.a` PARTIAL — packages/kernel-rs/src/registry.rs declares a producer index over its own constituents and packages/kernel-rs/src/mechanisms/benchmarks.rs computes it from cleared prints, so a factory-gate reading exists and is separate. Nothing weights it by production, and no freight, margin or tax is excluded because none is charged anywhere
- [ ] **TODO 3.GOODS.G1B** — `Goods G1.b` PARTIAL — packages/kernel-rs/src/registry.rs declares a consumer index computed from its own constituents, so a household-facing reading exists. It is not weighted by household expenditure, and it includes no freight, margin or consumption tax because none of the three is ever added to a price

### 3. Freight — 21 missing, 3 partial

> **Required review before this block:** read the **Freight** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3571), then inspect `packages/kernel-rs/src/mechanisms/freight.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.FREIGHT.A1** — `Freight A1` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.A2** — `Freight A2` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.A3** — `Freight A3` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.A3A** — `Freight A3.a` MISSING — packages/kernel-rs/src/ledger.rs `Leg::Dispatch` moves title and goods together, so nothing sits in transit as a separate asset. No working capital is tied up by a shipment and no balance sheet carries one
- [ ] **TODO 3.FREIGHT.A4** — `Freight A4` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.B2** — `Freight B2` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.B2A** — `Freight B2.a` MISSING — no freight price clears, so nothing can be inelastic or move violently. packages/kernel-rs/src/mechanisms/freight.rs `Carriage` journals a count and posts no schedule into a freight book
- [ ] **TODO 3.FREIGHT.B3** — `Freight B3` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.B4** — `Freight B4` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.C1** — `Freight C1` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.C1A** — `Freight C1.a` MISSING — no freight demand is derived from trade volumes, because no freight is bought. packages/kernel-rs/src/mechanisms/freight.rs `Carriage` reads what shipped and posts nothing, so the dependence the clause turns on carries nothing
- [ ] **TODO 3.FREIGHT.C2** — `Freight C2` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.C3** — `Freight C3` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.D1** — `Freight D1` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.D2** — `Freight D2` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.D3** — `Freight D3` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.D3A** — `Freight D3.a` MISSING — no price difference between two regions exists, because packages/kernel-rs/src/systems.rs declares one book per good rather than one per place. Nobody ships to close a gap, so no capacity binds and no basis widens
- [ ] **TODO 3.FREIGHT.D4** — `Freight D4` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.D5** — `Freight D5` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.E1** — `Freight E1` MISSING — nothing transports anything, so there is no transport to be instantaneous or costless — the FORBID holds by the system being absent
- [ ] **TODO 3.FREIGHT.E3** — `Freight E3` MISSING — The `freight` system posts berth-plant supply and runs `Carriage`, but `Carriage::run` only journals the count of all live agreements. The typed route, booking, dispatch, capacity, transit and delivery-outcome helpers are not called, and `Dispatches` is not a kernel store
- [ ] **TODO 3.FREIGHT.B1** — `Freight B1` PARTIAL — packages/kernel-rs/src/systems.rs wires `LetsItsPlant` as the freight participant and `Carriage` as its mechanism. The former offers plant use and the latter only counts live agreements; neither creates a route, shipment or capacity
- [ ] **TODO 3.FREIGHT.D6** — `Freight D6` PARTIAL — capacity rations the quantity twice over: packages/kernel-rs/src/session.rs `dispatch_plan` fits a trade across the carriers that have room this week and moves only what fits, and packages/kernel-rs/src/ledger.rs refuses an instruction whose dispatch would exceed what the carrier has left, recording `NoCapacity` against it — a shipper turned away, not an abort. What is missing is the rest of the lifecycle: no route carries its own transit time and no delivery has an outcome
- [ ] **TODO 3.FREIGHT.E2** — `Freight E2` PARTIAL — a dispatch leg names its carrier and carries the capacity `dispatch_plan` derived from the plant that carrier itself holds, and settlement refuses a shipment beyond it. What is missing is that no trade in the run has ever reached a dispatch, so the refusal has never had to fire

### 3. Labour — 28 missing, 4 partial

> **Required review before this block:** read the **Labour** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3625), then inspect `packages/kernel-rs/src/mechanisms/employment.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.LABOUR.A3** — `Labour A3` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.A3A** — `Labour A3.a` MISSING — there is one labour book and no skill or place divides it, so a seeker and a vacancy that cannot meet do not exist. Unemployment and vacancies cannot be high together because the market has no segments to be mismatched across
- [ ] **TODO 3.LABOUR.A3B** — `Labour A3.b` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.A4C** — `Labour A4.c` MISSING — packages/kernel-rs/src/parties.rs has SPLIT as one of the five events a weight moves by, so splitting a cell is available. No hire or separation calls it, because neither happens, and no cell is ever part employed
- [ ] **TODO 3.LABOUR.B1** — `Labour B1` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.B1A** — `Labour B1.a` MISSING — no seeker is matched and none accepts or refuses, so no outside option sets a floor under what is paid. packages/kernel-rs/src/mechanisms/households.rs holds the benefit a household receives and no wage decision reads it
- [ ] **TODO 3.LABOUR.B2** — `Labour B2` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.B3** — `Labour B3` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.B4** — `Labour B4` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.B5** — `Labour B5` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.C1** — `Labour C1` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.C1A** — `Labour C1.a` MISSING — no firm computes what another hour is worth to it, so neither its output price nor its capital enters a wage. packages/kernel-rs/src/mechanisms/goods.rs reads the hours an engagement already gives it and never asks for more
- [ ] **TODO 3.LABOUR.C2** — `Labour C2` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.C3** — `Labour C3` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.C4** — `Labour C4` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.C5** — `Labour C5` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.D1** — `Labour D1` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.D1A** — `Labour D1.a` MISSING — no employer bids a wage, so a higher offer fills nothing more than a lower one. packages/kernel-rs/src/clearing.rs would ration by level in a labour book and no participant posts into one
- [ ] **TODO 3.LABOUR.D1B** — `Labour D1.b` MISSING — no firm is rationed or filled, so none revises what it bids next week. packages/kernel-rs/src/stores.rs `Outlooks` is where such a revision would be kept and no wage outlook is formed by anybody
- [ ] **TODO 3.LABOUR.D1C** — `Labour D1.c` MISSING — nothing reads a going rate off what is actually paid. The engagements carry the wages that would be weighted and no mechanism walks them for an average
- [ ] **TODO 3.LABOUR.D2** — `Labour D2` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.D2A** — `Labour D2.a` MISSING — employment never moves, because nothing hires and nothing separates. No adjustment falls on quantity, and none falls on the wage either, so a downturn changes neither
- [ ] **TODO 3.LABOUR.D2B** — `Labour D2.b` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.D3** — `Labour D3` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.D4** — `Labour D4` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.D5** — `Labour D5` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.E3** — `Labour E3` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.E4** — `Labour E4` MISSING — `employment` runs `mechanisms/employment.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached. Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state
- [ ] **TODO 3.LABOUR.A4A** — `Labour A4.a` PARTIAL — `agreed::ENGAGEMENT` names a firm, a worker and a wage and persists across weeks, so an employment relationship is a state rather than a count and packages/kernel-rs/src/mechanisms/employment.rs `Wages` pays off it. The reads it should support are absent: nothing derives an unemployment rate or a separation flow from it, because nothing separates
- [ ] **TODO 3.LABOUR.A4B** — `Labour A4.b` PARTIAL — packages/kernel-rs/src/parties.rs carries an integer weight on a cell, so a cohort is a party that can be told and a headcount can only be a whole number. No engagement carries a headcount and no hire moves one, so the integer the clause protects is never moved
- [ ] **TODO 3.LABOUR.E2** — `Labour E2` PARTIAL — packages/kernel-rs/src/mechanisms/goods.rs carries wages into batch unit cost and GoodsSellers combines own price outlook, lot basis and holding cost into its ask; broader labour-price feedback remains incomplete
- [ ] **TODO 3.LABOUR.E2A** — `Labour E2.a` PARTIAL — packages/kernel-rs/src/mechanisms/goods.rs carries wages into the unit cost of a batch, so a wage is a cost that reaches a price. It is not demand: no household spends a wage it received, because no wage changes, so which side dominates is not a question the world can answer

### 3. Housing — 23 missing, 11 partial

> **Required review before this block:** read the **Housing** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3720), then inspect `packages/kernel-rs/src/mechanisms/housing.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.HOUSING.A2** — `Housing A2` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.A3** — `Housing A3` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.A4** — `Housing A4` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.A5** — `Housing A5` MISSING — packages/kernel-rs/src/mechanisms/housing.rs `Housing` reads `dwelling.upkeep` into `let _upkeep` and discards it. Nothing depreciates and nothing is maintained
- [ ] **TODO 3.HOUSING.B2** — `Housing B2` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.B3** — `Housing B3` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.B4** — `Housing B4` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.B4A** — `Housing B4.a` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.B5** — `Housing B5` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.C2** — `Housing C2` MISSING — see C1 — an `Agrees` carries terms and no `owing`, so no interest and no principal is ever payable
- [ ] **TODO 3.HOUSING.C3** — `Housing C3` MISSING — `housing.rs loan_to_value` is the read and is unreached
- [ ] **TODO 3.HOUSING.C4** — `Housing C4` MISSING — `housing.rs foreclose`/`Foreclosed` are unreached. Nothing defaults, nothing is seized and no foreclosed supply returns to the market
- [ ] **TODO 3.HOUSING.C4A** — `Housing C4.a` MISSING — no dwelling is seized and none is sold, so nothing returns to the market and no recovery is what a sale fetched. packages/kernel-rs/src/mechanisms/forced_sale.rs is the participant that would post it and no foreclosure reaches one
- [ ] **TODO 3.HOUSING.C4B** — `Housing C4.b` MISSING — no foreclosure happens, so none coincides with any other. The correlation the clause names needs a falling price to seize into and neither the seizure nor the resale exists
- [ ] **TODO 3.HOUSING.C6** — `Housing C6` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.D1** — `Housing D1` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.D2** — `Housing D2` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.D3** — `Housing D3` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.D4** — `Housing D4` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.D5** — `Housing D5` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.E1** — `Housing E1` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.E3** — `Housing E3` MISSING — `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached
- [ ] **TODO 3.HOUSING.E4** — `Housing E4` MISSING — nothing reconciles mortgage debt against mortgage assets, and a mortgage is an agreement rather than a claim anybody holds
- [ ] **TODO 3.HOUSING.A1** — `Housing A1` PARTIAL — packages/kernel-rs/src/instruments.rs `Class::Plant` with a unit, held by a named party, and packages/kernel-rs/src/places.rs puts it in a region. A dwelling is not counted in dwellings: it is a plant line, and `housing` is imported by nothing
- [ ] **TODO 3.HOUSING.A1A** — `Housing A1.a` PARTIAL — packages/kernel-rs/src/places.rs puts every dwelling in a region and packages/kernel-rs/src/mechanisms/housing.rs `clearing` crosses bids against offers per place, so location is part of what a dwelling is and there is a market per place. Nothing settles in any of them, so the several markets produce no several prices
- [ ] **TODO 3.HOUSING.B1** — `Housing B1` PARTIAL — packages/kernel-rs/src/mechanisms/housing.rs `clearing` crosses bids against offers per place and `Housing` runs it. nothing settles — no dwelling moves in the register and no money is paid
- [ ] **TODO 3.HOUSING.B1A** — `Housing B1.a` PARTIAL — packages/kernel-rs/src/mechanisms/housing.rs builds an offer from the seller's own reservation, so supply is what owners will take rather than a curve. No tenure ends, and no builder's completion joins the offers, so the two sources of supply the clause names are absent
- [ ] **TODO 3.HOUSING.B1B** — `Housing B1.b` PARTIAL — packages/kernel-rs/src/mechanisms/housing.rs builds a bid from what the buyer can borrow under the standing lending standard, so a bid is a borrowing capacity. Only one quote exists, because no lender competes with another for the mortgage
- [ ] **TODO 3.HOUSING.B1C** — `Housing B1.c` PARTIAL — packages/kernel-rs/src/mechanisms/housing.rs `clearing` crosses bids against reservations per place and leaves an offer nobody reached unsold, which is the cross the clause asks for. Nothing settles: no dwelling moves in the register and no money follows
- [ ] **TODO 3.HOUSING.B2A** — `Housing B2.a` PARTIAL — packages/kernel-rs/src/mechanisms/housing.rs builds every bid from the mortgage rate and the standing lending standard, so both are inputs to what a buyer can offer. The channel ends there: no policy rate exists to move the mortgage rate, so nothing upstream reaches a household balance sheet
- [ ] **TODO 3.HOUSING.C1** — `Housing C1` PARTIAL — packages/kernel-rs/src/mechanisms/housing.rs `Housing` strikes an `agreed::MORTGAGE` naming a lender and a borrower. It carries `[price, deposit_share]`, no term, no rate, no amortisation and NO SCHEDULE — so nothing ever falls due on a mortgage, and `housing` is imported by nothing
- [ ] **TODO 3.HOUSING.C5** — `Housing C5` PARTIAL — packages/kernel-rs/src/mechanisms/housing.rs `standard` is reached and `standing::LENDING_STANDARD` carries it, so the standard is a read of the lender`s own measurements rather than a constant. Nothing tightens it in response to anything — a decision missing inside a system that runs
- [ ] **TODO 3.HOUSING.C5A** — `Housing C5.a` PARTIAL — packages/kernel-rs/src/mechanisms/housing.rs `standard` reads the lender's own loan-to-value cross-section and its headroom, and `standing::LENDING_STANDARD` carries the result, so the standard is a read rather than a constant. The hurdle it reads against is a declared number rather than the lender's own
- [ ] **TODO 3.HOUSING.C5B** — `Housing C5.b` PARTIAL — the standard moves with the lender's own book and every bid is built from it, so a tightening does reach the next price. The loop does not close: no dwelling settles, so no price feeds back into the book the standard is read off

### 3. Households — 37 missing, 4 partial

> **Required review before this block:** read the **Households** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3794), then inspect `packages/kernel-rs/src/mechanisms/households.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.HOUSEHOLDS.A1** — `Households A1` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.A2** — `Households A2` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.A2A** — `Households A2.a` MISSING — packages/kernel-rs/src/parties.rs keys a cell on the lattice its kind declares, so income and wealth could be part of that key. No household cell is keyed on either, and packages/kernel-rs/src/mechanisms/households.rs spends the same share of cash whoever holds it
- [ ] **TODO 3.HOUSEHOLDS.A2B** — `Households A2.b` MISSING — cohort is not a dimension of the cell key: packages/kernel-rs/src/parties.rs carries one `key` and packages/kernel-rs/src/registry.rs declares no cohort
- [ ] **TODO 3.HOUSEHOLDS.A2C** — `Households A2.c` MISSING — no household cell is keyed on whether its members work, because nothing hires or separates. A cell cannot cross between employment states that do not exist
- [ ] **TODO 3.HOUSEHOLDS.A2D** — `Households A2.d` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.A2G** — `Households A2.g` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.B2** — `Households B2` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.B3** — `Households B3` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.B3A** — `Households B3.a` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.B4** — `Households B4` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.B5** — `Households B5` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.C1A** — `Households C1.a` MISSING — packages/kernel-rs/src/mechanisms/households.rs spends out of the cash a cell holds and never asks what it earned this week, so current income enters no decision
- [ ] **TODO 3.HOUSEHOLDS.C1B** — `Households C1.b` MISSING — no household reads what it owns, so an asset price reaches no spending decision. packages/kernel-rs/src/instruments.rs `worth` is the read that would value a cell's holdings and no household calls it
- [ ] **TODO 3.HOUSEHOLDS.C2** — `Households C2` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.C3** — `Households C3` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.C4** — `Households C4` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.C5** — `Households C5` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.D2** — `Households D2` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.D4** — `Households D4` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.D5** — `Households D5` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.D5A** — `Households D5.a` MISSING — no household chooses between a deposit, a money fund and a bill, because it buys none of the three. packages/kernel-rs/src/mechanisms/short_term_debt.rs `prefers_paper` is that substitution and no production code names it
- [ ] **TODO 3.HOUSEHOLDS.E1** — `Households E1` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.E2** — `Households E2` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.E3** — `Households E3` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.E3A** — `Households E3.a` MISSING — no household borrows, so nothing services a loan and neither interest nor principal is paid. packages/kernel-rs/src/stores.rs `Owing` distinguishes the two on a due, which is where the distinction would be kept
- [ ] **TODO 3.HOUSEHOLDS.E4** — `Households E4` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.E4A** — `Households E4.a` MISSING — no household defaults, so no collateral is taken and no lender books a loss. packages/kernel-rs/src/parties.rs has SPLIT for the part of a cell a default would apply to, and nothing calls it, so no credit record belongs to anybody
- [ ] **TODO 3.HOUSEHOLDS.E5** — `Households E5` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.F1** — `Households F1` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.F1A** — `Households F1.a` MISSING — packages/kernel-rs/src/mechanisms/households.rs `age_at_boundary` is the cohort split and is unreached. Nothing ages
- [ ] **TODO 3.HOUSEHOLDS.F1B** — `Households F1.b` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.F2** — `Households F2` MISSING — `households.rs Inherited` is unreached; nothing dissolves and nothing is inherited
- [ ] **TODO 3.HOUSEHOLDS.F2A** — `Households F2.a` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.F3** — `Households F3` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.F3A** — `Households F3.a` MISSING — no cohort crosses from one household cell to another, because nothing ages, marries or forms a household. packages/kernel-rs/src/parties.rs offers SPLIT for the members that would cross and no mechanism calls it
- [ ] **TODO 3.HOUSEHOLDS.F4** — `Households F4` MISSING — `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached
- [ ] **TODO 3.HOUSEHOLDS.A2F** — `Households A2.f` PARTIAL — packages/kernel-rs/src/mechanisms/households.rs `HouseholdBuyers::orders` is evaluated per cell and never at a sector mean. What it evaluates is `last print x 1.2` for every cell alike
- [ ] **TODO 3.HOUSEHOLDS.C1** — `Households C1` PARTIAL — packages/kernel-rs/src/mechanisms/households.rs `HouseholdBuyers` spends out of its own money and keeps `household.keeps` back. C1.a`s income, C1.b`s wealth and C1.c`s own expectation are not read
- [ ] **TODO 3.HOUSEHOLDS.C1C** — `Households C1.c` PARTIAL — packages/kernel-rs/src/mechanisms/households.rs `HouseholdBuyers` builds its level from the cell's own `price_outlook` and never from a published figure, so the expectation is its own. Confidence is not read: `confidence` is available per party and no household asks for it
- [ ] **TODO 3.HOUSEHOLDS.C1D** — `Households C1.d` PARTIAL — a cell spends only money it actually holds, keeping `household.keeps` back, so one that cannot borrow spends what it has. That is the whole of it, because no household can borrow at all and no liquidity constraint distinguishes two of them

### 3. Expectations — 10 missing, 9 partial

> **Required review before this block:** read the **Expectations** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 4292), then inspect `packages/kernel-rs/src/mechanisms/expectations.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 3.EXPECTATIONS.A2A** — `Expectations A2.a` MISSING — packages/kernel-rs/src/mechanisms/observer.rs journals a count and publishes no aggregate, so there is no statistic for a party to observe. Nothing lags, nothing revises, and no outlook has a published number among its observations
- [ ] **TODO 3.EXPECTATIONS.A5** — `Expectations A5` MISSING — packages/kernel-rs/src/stores.rs `Outlooks` carries a level and no unit or horizon; `about::` names the subject and not the periodicity
- [ ] **TODO 3.EXPECTATIONS.C2A** — `Expectations C2.a` MISSING — the clause asks for an outlook a party holds ABOUT another party, published and weighable. `repayment_of` and `loss_given_failure_of` key exactly that, and nothing forms either: no bank estimates a company's coming report and no such estimate is published
- [ ] **TODO 3.EXPECTATIONS.C6** — `Expectations C6` MISSING — see C1, and Polity: no cell votes
- [ ] **TODO 3.EXPECTATIONS.E1** — `Expectations E1` MISSING — a Part XII measurement; there is no dispersion to read while every reservation is the same constant
- [ ] **TODO 3.EXPECTATIONS.E2** — `Expectations E2` MISSING — outlooks are formed correctly by `mechanisms/expectations.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing
- [ ] **TODO 3.EXPECTATIONS.E3** — `Expectations E3` MISSING — outlooks are formed correctly by `mechanisms/expectations.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing
- [ ] **TODO 3.EXPECTATIONS.E4** — `Expectations E4` MISSING — outlooks are formed correctly by `mechanisms/expectations.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing
- [ ] **TODO 3.EXPECTATIONS.F2B** — `Expectations F2.b` MISSING — a limit at a party's own value means it trades at its value and gains nothing rather than standing aside. Nothing in the tree declines for want of a gap: packages/kernel-rs/src/mechanisms/dealing.rs quotes both sides every week it has a level and packages/kernel-rs/src/mechanisms/money_market.rs bids whenever it has spare cash
- [ ] **TODO 3.EXPECTATIONS.F7** — `Expectations F7` MISSING — a Part XII measurement, and it needs books that clear: packages/kernel-rs/src/bin/world_runs.rs reports 4,497 books open in week 1 and none of them clearing, so there is no depth to read dispersion against
- [ ] **TODO 3.EXPECTATIONS.B5** — `Expectations B5` PARTIAL — the mechanism the VERIFY needs is there: packages/kernel-rs/src/parties.rs disperses the memory horizon across parties and packages/kernel-rs/src/mechanisms/expectations.rs `Forming` corrects at the speed of each one, so lags differ by party by construction. The measurement against a turning point has not been taken
- [ ] **TODO 3.EXPECTATIONS.C2** — `Expectations C2` PARTIAL — packages/kernel-rs/src/mechanisms/goods.rs `Making` reads the firm`s own `HOW_MUCH_IT_SELLS`for the production decision, packages/kernel-rs/src/mechanisms/capital_programme.rs`Building` reads it for the investment one and packages/kernel-rs/src/mechanisms/employment.rs reads it for hiring — a firm with no view does not act on somebody else`s. It is one outlook doing all three: the price it expects to sell at is declared and nothing reads it
- [ ] **TODO 3.EXPECTATIONS.C5** — `Expectations C5` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs reads its own `price_outlook` on the line it is about to auction, so the public sector gets no better forecast than anybody else. The central-bank half is absent: no such party exists to hold an outlook or to read one
- [ ] **TODO 3.EXPECTATIONS.F1** — `Expectations F1` PARTIAL — packages/kernel-rs/src/module.rs `Valuer` is the seam: the kernel asks the system that owns a family what a thing of it is worth. Two families answer — a claim out of its dated payments in packages/kernel-rs/src/module.rs `fair_value`, and a plant out of what it can produce in packages/kernel-rs/src/mechanisms/capital_programme.rs `worth_per_unit`. A share, a company, a dwelling and a good have no value yet
- [ ] **TODO 3.EXPECTATIONS.F1A** — `Expectations F1.a` PARTIAL — packages/kernel-rs/src/module.rs `values` falls through a party's own outlook, then the registered valuer for that line's class, then paper comparable to what it has a view of — so a claim or a plant with no print still has a bid. A share, a company, a dwelling or a good with no print has none
- [ ] **TODO 3.EXPECTATIONS.F1C** — `Expectations F1.c` PARTIAL — packages/kernel-rs/src/module.rs `fair_value` answers per unit of the line, divided by what `outstanding_of` says is out — so it is comparable with the price the book quotes. It carries no currency beside it: the line's `ccy_of` says what money it is in and the value does not
- [ ] **TODO 3.EXPECTATIONS.F2** — `Expectations F2` PARTIAL — every participant's limit is now its OWN value: what a buyer will pay up to and what a seller will come down to, so the gap between that level and what the book crosses at is the party's own surplus. What is short is the other half of F2 — a party that declines for want of a gap, and packages/kernel-rs/src/mechanisms/capital_programme.rs `worth_doing` is the only test of a return against a hurdle in the tree
- [ ] **TODO 3.EXPECTATIONS.F2A** — `Expectations F2.a` PARTIAL — no rule in the tree assigns a side by party kind: packages/kernel-rs/src/mechanisms/funds.rs, packages/kernel-rs/src/mechanisms/hedge_funds.rs and packages/kernel-rs/src/mechanisms/insurers.rs each read their own level and post from it. What is short is the comparison that would make the side follow from the level rather than from which order the participant happens to build
- [ ] **TODO 3.EXPECTATIONS.F3** — `Expectations F3` PARTIAL — packages/kernel-rs/src/module.rs `Valuer` dispatches on the line's class and the answer is the family's own: packages/kernel-rs/src/module.rs `worth_of` takes a claim's dated payment, a chance of being paid and a required return; packages/kernel-rs/src/mechanisms/capital_programme.rs `worth_per_unit` takes a plant's capacity, upkeep and life against what its output earns. Nothing shared sits between them. The other four families have no terms written

### 4. Bond — 8 missing, 4 partial

> **Required review before this block:** read the **Bond** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 900), then inspect `packages/kernel-rs/src/instruments.rs`, `packages/kernel-rs/src/stores.rs`, `packages/kernel-rs/src/mechanisms/lending.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 4.BOND.N5B** — `Bond N5.b` MISSING — there is no floating coupon. `instruments.rs` has one `coupon: Option<f64>` and no reference rate; packages/kernel-rs/src/mechanisms/equity.rs `Floating` brings a line and journals, and never fixes a coupon on one
- [ ] **TODO 4.BOND.N9B** — `Bond N9.b` MISSING — packages/kernel-rs/src/instruments.rs `accrued` computes what has run since the last payment and no trade adds it to what settles. The cleared price is the whole of what changes hands, so a coupon is a windfall to whoever holds the line on the date
- [ ] **TODO 4.BOND.N10** — `Bond N10` MISSING — nothing redeems an instrument. `instruments.rs` and `register.rs` have no door that ceases a line or empties its holdings
- [ ] **TODO 4.BOND.N11** — `Bond N11` MISSING — no instrument carries an early-termination regime, and there is no field in which "none" could be stated
- [ ] **TODO 4.BOND.N11A** — `Bond N11.a` MISSING — no instrument carries an early-termination regime, so there is nothing for an issuer to pay to hold one. packages/kernel-rs/src/instruments.rs has no field in which a call, a make-whole or the price of either could be stated
- [ ] **TODO 4.BOND.N12** — `Bond N12` MISSING — no instrument carries a definition of default. packages/kernel-rs/src/mechanisms/sovereign.rs `Missed` is the shape and is dead code
- [ ] **TODO 4.BOND.N13** — `Bond N13` MISSING — packages/kernel-rs/src/mechanisms/estate.rs `Rank` is the ordering, but no instrument states what its holder is entitled to on failure, and no claim is ever filed
- [ ] **TODO 4.BOND.N13A** — `Bond N13.a` MISSING — packages/kernel-rs/src/mechanisms/estate.rs `Rank` orders claims in a wind-up and no instrument states where its own holder stands. The ranking is a property of the waterfall rather than of the paper, so two issues cannot say they differ
- [ ] **TODO 4.BOND.N2** — `Bond N2` PARTIAL — packages/kernel-rs/src/instruments.rs carries a `unit`, and the register counts units of it. there is no issued PRINCIPAL — no column records the amount owed
- [ ] **TODO 4.BOND.N3** — `Bond N3` PARTIAL — packages/kernel-rs/src/instruments.rs `ccy_of` (one currency per line), and since 0t.5 a schedule row carries the money its amount is in, which `mechanisms/lending.rs` `Servicing` compares with the payer's account rather than inferring from it. What is still not true is that EVERY figure about a line is in that money — there is no currency-carrying amount type (Money A2.b), and the payer that owes a money it does not bank in has no way to buy it
- [ ] **TODO 4.BOND.N5** — `Bond N5` PARTIAL — packages/kernel-rs/src/instruments.rs `coupon_of` is a fixed rate or `None` (N5.a and N5.c). N5.b has no representation: there is no margin, no reference-rate field, and nothing fixes a floating coupon — the `benchmarks` module that would print the fixing is imported by nothing
- [ ] **TODO 4.BOND.N14** — `Bond N14` PARTIAL — packages/kernel-rs/src/instruments.rs `display` builds issuer + coupon + maturity and the id is never the name. It is called by packages/kernel-rs/src/mechanisms/observer.rs `display_name`, which is itself dead code

### 4. Corporate Credit — 76 missing, 8 partial

> **Required review before this block:** read the **Corporate Credit** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1030), then inspect `packages/kernel-rs/src/mechanisms/corporate_credit.rs`, `packages/kernel-rs/src/mechanisms/lending.rs`, `packages/kernel-rs/src/mechanisms/loss.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 4.CORPORATE-CREDIT.A2** — `Corporate Credit A2` MISSING — the `corporate_credit` system runs its own `Brings`, a PLACEHOLDER that reads a borrower's receipts as nothing; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable — `capital.debt_share` = 0.6 is the capital structure, stated
- [ ] **TODO 4.CORPORATE-CREDIT.A2A** — `Corporate Credit A2.a` MISSING — packages/kernel-rs/src/instruments.rs issues one shape of dated paper and carries no seniority on any of it, so a company's debt cannot be a mix of kinds ranking differently. Every line an issuer has is the same instrument with a different date
- [ ] **TODO 4.CORPORATE-CREDIT.A2B** — `Corporate Credit A2.b` MISSING — nothing carries a leverage target or a covenant line. `corporate_credit.rs Covenant` is unreachable. the `corporate_credit` system runs its own `Brings`, a PLACEHOLDER that reads a borrower's receipts as nothing; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.A3B** — `Corporate Credit A3.b` MISSING — `corporate_credit.rs coverage` computes it and is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.A4** — `Corporate Credit A4` MISSING — no assessment is held by anybody. packages/kernel-rs/src/mechanisms/second_opinion.rs is not imported and packages/kernel-rs/src/mechanisms/ratings.rs `Rating` is dead code
- [ ] **TODO 4.CORPORATE-CREDIT.A4A** — `Corporate Credit A4.a` MISSING — see A4
- [ ] **TODO 4.CORPORATE-CREDIT.A4B** — `Corporate Credit A4.b` MISSING — see A4. every participant of a kind posts the same reservation, so nothing anywhere disagrees about a credit
- [ ] **TODO 4.CORPORATE-CREDIT.A4C** — `Corporate Credit A4.c` MISSING — packages/kernel-rs/src/mechanisms/ratings.rs `Grading` writes a grade each period and no participant reads a change in one. Nothing sells on a downgrade and no funding cost moves with it, so an assessment changes in private
- [ ] **TODO 4.CORPORATE-CREDIT.B1** — `Corporate Credit B1` MISSING — no instrument carries an early-termination regime (Bond N11)
- [ ] **TODO 4.CORPORATE-CREDIT.B2** — `Corporate Credit B2` MISSING — `corporate_credit.rs Covenant`/`standing`/`waive` implement B2, B2.a and B2.b and are unreachable. the `corporate_credit` system runs its own `Brings`, a PLACEHOLDER that reads a borrower's receipts as nothing; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.B2A** — `Corporate Credit B2.a` MISSING — packages/kernel-rs/src/instruments.rs `negotiated_covenant_of` is the read a covenant would be seen through and no production code names it. Nothing states a leverage or coverage promise, so credit is observable only as paying or gone
- [ ] **TODO 4.CORPORATE-CREDIT.B2B** — `Corporate Credit B2.b` MISSING — packages/kernel-rs/src/mechanisms/corporate_credit.rs `waive` prices a waiver and no production code names it. With no covenant to breach there is nothing to waive and no negotiation to have
- [ ] **TODO 4.CORPORATE-CREDIT.B3** — `Corporate Credit B3` MISSING — no instrument carries a seniority, and no claim is ever filed against an estate
- [ ] **TODO 4.CORPORATE-CREDIT.B4** — `Corporate Credit B4` MISSING — there is no floating coupon anywhere (Bond N5.b)
- [ ] **TODO 4.CORPORATE-CREDIT.C1** — `Corporate Credit C1` MISSING — `Funding` brings paper directly. No underwriter is appointed and none is paid. `corporate_credit.rs bring`/`Brought` is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.C2** — `Corporate Credit C2` MISSING — no book is built. `corporate_credit.rs Indication` is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.C2A** — `Corporate Credit C2.a` MISSING — no primary book for company paper is opened, so nobody indicates anything. packages/kernel-rs/src/clearing.rs `Order` is the size-at-a-level an indication would be, and no participant posts one against a corporate line
- [ ] **TODO 4.CORPORATE-CREDIT.C2B** — `Corporate Credit C2.b` MISSING — there is no book to read the shape of. packages/kernel-rs/src/mechanisms/corporate_credit.rs `Brings` journals an intention to issue and proposes nothing, so no demand is gathered and no deal is priced off what came in
- [ ] **TODO 4.CORPORATE-CREDIT.C4** — `Corporate Credit C4` MISSING — there is no walk-away: `Funding` brings the paper unconditionally
- [ ] **TODO 4.CORPORATE-CREDIT.C5** — `Corporate Credit C5` MISSING — nothing allocates a book
- [ ] **TODO 4.CORPORATE-CREDIT.C6** — `Corporate Credit C6` MISSING — no proceeds reach an issuer and no fee reaches an underwriter; `Funding` proposes no instruction
- [ ] **TODO 4.CORPORATE-CREDIT.C7** — `Corporate Credit C7` MISSING — `corporate_credit.rs` has the whole shape — `Brought`, `Basis`, `Member`, `syndicate`, `fee_gap` — and it is unreachable. the `corporate_credit` system runs its own `Brings`, a PLACEHOLDER that reads a borrower's receipts as nothing; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.C7A** — `Corporate Credit C7.a` MISSING — see C7
- [ ] **TODO 4.CORPORATE-CREDIT.C7B** — `Corporate Credit C7.b` MISSING — see C7; `fee_gap` is the read and is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.C8** — `Corporate Credit C8` MISSING — `corporate_credit.rs tap` is unreachable; `Funding` brings a fresh line every time it is short
- [ ] **TODO 4.CORPORATE-CREDIT.C9** — `Corporate Credit C9` MISSING — `agreed::COMMITMENT` is the shape and `corporate_credit.rs draw` decides it; the module is unreachable and nothing opens a line
- [ ] **TODO 4.CORPORATE-CREDIT.C10** — `Corporate Credit C10` MISSING — `corporate_credit.rs syndicate` is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.C10A** — `Corporate Credit C10.a` MISSING — see C10
- [ ] **TODO 4.CORPORATE-CREDIT.C10B** — `Corporate Credit C10.b` MISSING — see C10 — the FORBID is guarded inside `syndicate`, which nothing calls
- [ ] **TODO 4.CORPORATE-CREDIT.C10C** — `Corporate Credit C10.c` MISSING — see C10
- [ ] **TODO 4.CORPORATE-CREDIT.C11** — `Corporate Credit C11` MISSING — `corporate_credit.rs Basis` states the two bases and is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.C11A** — `Corporate Credit C11.a` MISSING — see C11
- [ ] **TODO 4.CORPORATE-CREDIT.C11B** — `Corporate Credit C11.b` MISSING — see C11
- [ ] **TODO 4.CORPORATE-CREDIT.C11C** — `Corporate Credit C11.c` MISSING — see C11
- [ ] **TODO 4.CORPORATE-CREDIT.C11D** — `Corporate Credit C11.d` MISSING — see C11 — a FORBID whose guard is in unreachable code
- [ ] **TODO 4.CORPORATE-CREDIT.C11E** — `Corporate Credit C11.e` MISSING — see C11
- [ ] **TODO 4.CORPORATE-CREDIT.D1** — `Corporate Credit D1` MISSING — `corporate_credit.rs secondary` is the schedule-posting side and is unreachable; no participant posts in a corporate bond book
- [ ] **TODO 4.CORPORATE-CREDIT.D2** — `Corporate Credit D2` MISSING — see D1 — nothing clears
- [ ] **TODO 4.CORPORATE-CREDIT.D3** — `Corporate Credit D3` MISSING — `dealing` is neither wired as a system nor imported by anything
- [ ] **TODO 4.CORPORATE-CREDIT.D3A** — `Corporate Credit D3.a` MISSING — packages/kernel-rs/src/mechanisms/dealing.rs `Dealers` quotes two sides in the books it is registered for and no corporate line is among them, so nothing makes a market in company paper
- [ ] **TODO 4.CORPORATE-CREDIT.D3B** — `Corporate Credit D3.b` MISSING — no dealer holds corporate inventory, so no balance sheet constrains one. packages/kernel-rs/src/mechanisms/dealing.rs `Lines` carries the limit that would bite and no corporate line is registered against it
- [ ] **TODO 4.CORPORATE-CREDIT.D3C** — `Corporate Credit D3.c` MISSING — nothing quotes company paper, so no width responds to an inventory filling up. The schedule that would widen is the dealer schedule that is never posted for these lines
- [ ] **TODO 4.CORPORATE-CREDIT.D4** — `Corporate Credit D4` MISSING — see D3
- [ ] **TODO 4.CORPORATE-CREDIT.D5** — `Corporate Credit D5` MISSING — see D1
- [ ] **TODO 4.CORPORATE-CREDIT.D7** — `Corporate Credit D7` MISSING — `corporate_credit.rs accrued` is unreachable, so nothing adds accrued to what a trade settles (Bond N9.b)
- [ ] **TODO 4.CORPORATE-CREDIT.E2** — `Corporate Credit E2` MISSING — nothing sums held against issued
- [ ] **TODO 4.CORPORATE-CREDIT.E4** — `Corporate Credit E4` MISSING — there is no revaluation, so no change in a mark reaches anybody's income. `corporate_credit.rs mark` is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.E4A** — `Corporate Credit E4.a` MISSING — packages/kernel-rs/src/instruments.rs `unrealised_difference` is the read that separates the two and no production code names it. Nothing holds corporate paper and nothing sells any, so neither figure exists to be distinguished
- [ ] **TODO 4.CORPORATE-CREDIT.E5** — `Corporate Credit E5` MISSING — a holder's reservation is a stated multiple of the last print, not its cost of funds, its expected loss and its capital
- [ ] **TODO 4.CORPORATE-CREDIT.E5A** — `Corporate Credit E5.a` MISSING — no buyer of company paper prices anything, so no cost of funds enters a level. packages/kernel-rs/src/mechanisms/bank_funding.rs computes a blended cost for a bank and no reservation on a corporate line reads it
- [ ] **TODO 4.CORPORATE-CREDIT.E5B** — `Corporate Credit E5.b` MISSING — packages/kernel-rs/src/stores.rs keys `repayment_of` and `loss_given_failure_of` to a named borrower and nothing forms either. With neither a probability nor a severity there is no expected loss to put in a price
- [ ] **TODO 4.CORPORATE-CREDIT.E5C** — `Corporate Credit E5.c` MISSING — packages/kernel-rs/src/mechanisms/bank_capital.rs weights an asset by the grade of its issuer, and no holder of corporate paper exists for it to weigh. No reservation anywhere reads a capital charge
- [ ] **TODO 4.CORPORATE-CREDIT.E5D** — `Corporate Credit E5.d` MISSING — see E5
- [ ] **TODO 4.CORPORATE-CREDIT.E6** — `Corporate Credit E6` MISSING — `corporate_credit.rs funding_withdrawn` is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.E6A** — `Corporate Credit E6.a` MISSING — nothing funds a position in company paper, because nothing holds one. packages/kernel-rs/src/stores.rs `Agreements` is where such a liability would name its lender and no row of that kind is ever struck
- [ ] **TODO 4.CORPORATE-CREDIT.E6B** — `Corporate Credit E6.b` MISSING — packages/kernel-rs/src/mechanisms/corporate_credit.rs `funding_withdrawn` is that link and no production code names it. packages/kernel-rs/src/mechanisms/forced_sale.rs sells for other causes and a lender walking away is not among them
- [ ] **TODO 4.CORPORATE-CREDIT.E7** — `Corporate Credit E7` MISSING — nothing charges regulatory capital against a position
- [ ] **TODO 4.CORPORATE-CREDIT.E9** — `Corporate Credit E9` MISSING — `observer` is not imported and packages/kernel-rs/src/mechanisms/observer.rs is dead code
- [ ] **TODO 4.CORPORATE-CREDIT.F1** — `Corporate Credit F1` MISSING — nothing accrues interest to a holder of record
- [ ] **TODO 4.CORPORATE-CREDIT.F4** — `Corporate Credit F4` MISSING — there is no early-termination regime to exercise (B1)
- [ ] **TODO 4.CORPORATE-CREDIT.F5** — `Corporate Credit F5` MISSING — `corporate_credit.rs refinanced_at` is unreachable; nothing retires an old issue with a new one
- [ ] **TODO 4.CORPORATE-CREDIT.F6** — `Corporate Credit F6` MISSING — nothing matures an instrument out of existence (Bond N10)
- [ ] **TODO 4.CORPORATE-CREDIT.G1** — `Corporate Credit G1` MISSING — no covenant exists and no missed payment is published as an event holders can observe
- [ ] **TODO 4.CORPORATE-CREDIT.G2** — `Corporate Credit G2` MISSING — nothing accelerates
- [ ] **TODO 4.CORPORATE-CREDIT.G3** — `Corporate Credit G3` MISSING — no claim is ever filed against an estate
- [ ] **TODO 4.CORPORATE-CREDIT.G4** — `Corporate Credit G4` MISSING — nothing realises an estate; a dead party keeps what it held
- [ ] **TODO 4.CORPORATE-CREDIT.G5** — `Corporate Credit G5` MISSING — packages/kernel-rs/src/mechanisms/estate.rs `waterfall` is correct and is never reached
- [ ] **TODO 4.CORPORATE-CREDIT.G5A** — `Corporate Credit G5.a` MISSING — packages/kernel-rs/src/mechanisms/estate.rs pays claims in rank order until the money runs out, so a junior claim left with nothing is the ordinary outcome. No corporate paper ever becomes a claim in an estate, so the outcome has no subject
- [ ] **TODO 4.CORPORATE-CREDIT.G6** — `Corporate Credit G6` MISSING — see G5
- [ ] **TODO 4.CORPORATE-CREDIT.G7** — `Corporate Credit G7` MISSING — nothing restructures; there are no holders to decide
- [ ] **TODO 4.CORPORATE-CREDIT.G8** — `Corporate Credit G8` MISSING — there is no assessment for a default to move (A4)
- [ ] **TODO 4.CORPORATE-CREDIT.H1** — `Corporate Credit H1` MISSING — packages/kernel-rs/src/mechanisms/benchmarks.rs `Index` and `Constituent` are dead code
- [ ] **TODO 4.CORPORATE-CREDIT.H2** — `Corporate Credit H2` MISSING — a VERIFY, and it needs both sides at once: enough graded names for an assessment to differ across, and enough traded paper for the difference to show in a spread. Every grade this world publishes is the worst one and the corporate books are thin. It is a MEASUREMENT and never a rule, and it waits on Part XII's
- [ ] **TODO 4.CORPORATE-CREDIT.H3** — `Corporate Credit H3` MISSING — a VERIFY over two claims on ONE issuer that rank differently, and this world's issuers have one line each. It is a measurement and never a rule, and it waits on an issuer having a junior tranche to be junior to
- [ ] **TODO 4.CORPORATE-CREDIT.H4** — `Corporate Credit H4` MISSING — the cash market clears (D2) and the synthetic is §19's CDS book, which exists and has never traded in a scale model (BUILT AND DEAD, `check:existence`). The BASIS between them is one derivation, and nothing computes it
- [ ] **TODO 4.CORPORATE-CREDIT.H4A** — `Corporate Credit H4.a` MISSING — the same basis, read the other way: neither leg sets the other. It is refused by construction — a derivative on an uncleared price is forbidden and both books clear on their own — and what is not built is the MEASUREMENT of the gap. Positioned at 21.137 with H4
- [ ] **TODO 4.CORPORATE-CREDIT.A1** — `Corporate Credit A1` PARTIAL — packages/kernel-rs/src/parties.rs and packages/kernel-rs/src/instruments.rs give a named issuer with a balance sheet. the `corporate_credit` system runs its own `Brings`, a PLACEHOLDER that reads a borrower's receipts as nothing; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable
- [ ] **TODO 4.CORPORATE-CREDIT.A3** — `Corporate Credit A3` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs `Funding` reads what falls due against what the party holds. `corporate_credit.rs coverage` is unreachable, so A3.b's read does not exist
- [ ] **TODO 4.CORPORATE-CREDIT.A3A** — `Corporate Credit A3.a` PARTIAL — packages/kernel-rs/src/stores.rs `Owing` distinguishes interest from principal on the schedule; nothing reads the two together as service, because `corporate_credit` is imported by nothing
- [ ] **TODO 4.CORPORATE-CREDIT.C3** — `Corporate Credit C3` PARTIAL — packages/kernel-rs/src/mechanisms/corporate_credit.rs brings discount claims with no model-authored coupon into a real book; the remaining gap is full underwriter/issuer issue pricing
- [ ] **TODO 4.CORPORATE-CREDIT.E3** — `Corporate Credit E3` PARTIAL — packages/kernel-rs/src/instruments.rs `carrying_value` answers `worth` — units times the cleared print — for a position whose holder declared `Carrying::Market`, which `mechanisms/dealing.rs` and `mechanisms/funds.rs` do for what they buy here. A holder that declared cost does not mark, which is the two levels XI-6 names; what is still missing is E4, the change in the mark reaching income
- [ ] **TODO 4.CORPORATE-CREDIT.E8** — `Corporate Credit E8` PARTIAL — packages/kernel-rs/src/register.rs `pledge` encumbers units. Nothing pledges a corporate bond, and no haircut is read: the module that would post it is imported by nothing
- [ ] **TODO 4.CORPORATE-CREDIT.F2** — `Corporate Credit F2` PARTIAL — packages/kernel-rs/src/mechanisms/lending.rs `Servicing` pays what fell due on the schedule out of the issuer's account. It pays the SCHEDULE's payee, not whoever the register says holds the paper then (Register A2.a) — a payment that reaches the wrong party is 0k's, at the wire
- [ ] **TODO 4.CORPORATE-CREDIT.F3** — `Corporate Credit F3` PARTIAL — see F2 — principal is an `Owing` on the schedule; nothing reduces the holding when it is paid (Register E2), which is the same wire defect as F2

### 4. Short-Term Debt — 17 missing, 6 partial

> **Required review before this block:** read the **Short-Term Debt** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1340), then inspect `packages/kernel-rs/src/mechanisms/short_term_debt.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 4.SHORT-TERM-DEBT.A1C** — `Short-Term Debt A1.c` MISSING — no instrument states where its holder ranks, so short-term paper cannot rank with an issuer's other senior debt. packages/kernel-rs/src/mechanisms/estate.rs `Rank` orders claims in a wind-up and no paper carries the rank it would be given
- [ ] **TODO 4.SHORT-TERM-DEBT.A2A** — `Short-Term Debt A2.a` MISSING — packages/kernel-rs/src/mechanisms/short_term_debt.rs `Paper::yield_on` takes a `Convention` and hands it to `yield_to`, so a day count would be part of the answer, and no production code names it. Nothing derives a yield on short paper at all, so there is no number for a convention to be a material part of
- [ ] **TODO 4.SHORT-TERM-DEBT.B2** — `Short-Term Debt B2` MISSING — `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller. No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.B3** — `Short-Term Debt B3` MISSING — `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller. No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.B3A** — `Short-Term Debt B3.a` MISSING — nothing rolls. packages/kernel-rs/src/mechanisms/short_term_debt.rs `Brings` journals an intention to issue and proposes no instruction, so no maturing paper is replaced by a new issue a market could decline
- [ ] **TODO 4.SHORT-TERM-DEBT.B3B** — `Short-Term Debt B3.b` MISSING — no run is possible because no roll is attempted. packages/kernel-rs/src/mechanisms/sovereign.rs `handle` is the only place an issuer short of cash goes looking, and no corporate or bank issuer reaches it
- [ ] **TODO 4.SHORT-TERM-DEBT.B4** — `Short-Term Debt B4` MISSING — `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller. No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.B5** — `Short-Term Debt B5` MISSING — `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller. No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.C1** — `Short-Term Debt C1` MISSING — `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller. No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.C2** — `Short-Term Debt C2` MISSING — `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller. No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.C2A** — `Short-Term Debt C2.a` MISSING — no saver ever chooses between a deposit and a bill, because no participant buys short-term paper at all. packages/kernel-rs/src/mechanisms/short_term_debt.rs `prefers_paper` is that choice and no production code names it
- [ ] **TODO 4.SHORT-TERM-DEBT.C3** — `Short-Term Debt C3` MISSING — `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller. No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.C4** — `Short-Term Debt C4` MISSING — `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller. No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.D1** — `Short-Term Debt D1` MISSING — `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller. No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.D2** — `Short-Term Debt D2` MISSING — `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller. No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.D3** — `Short-Term Debt D3` MISSING — `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller. No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.D4** — `Short-Term Debt D4` MISSING — `short_term_debt` runs its own `Brings` (a PLACEHOLDER, like `corporate_credit`'s, reading a borrower's receipts as nothing) — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller. No participant posts in a paper book
- [ ] **TODO 4.SHORT-TERM-DEBT.A1** — `Short-Term Debt A1` PARTIAL — packages/kernel-rs/src/mechanisms/treasury.rs `Funding` brings paper with a tenor and a coupon read from `params`. A1.a`s discount instrument does not exist: every line it brings carries a coupon
- [ ] **TODO 4.SHORT-TERM-DEBT.A1A** — `Short-Term Debt A1.a` PARTIAL — packages/kernel-rs/src/mechanisms/short_term_debt.rs `Paper` carries a face, a price and two dates and no coupon at all, so the discount is the whole return by construction. Nothing accretes it over the life, so the return arrives entirely in the week the paper repays
- [ ] **TODO 4.SHORT-TERM-DEBT.A1B** — `Short-Term Debt A1.b` PARTIAL — packages/kernel-rs/src/mechanisms/short_term_debt.rs reads its tenor in weeks from `params`, and the shape that would make a piece of paper short because its maturity is near — `Paper`, whose `term` is the distance between the week it was issued and the week it matures — is reached by nothing
- [ ] **TODO 4.SHORT-TERM-DEBT.A2** — `Short-Term Debt A2` PARTIAL — the price half holds: packages/kernel-rs/src/mechanisms/short_term_debt.rs `Brings` brings discount paper with no coupon into a declared book and packages/kernel-rs/src/session.rs prints where it crosses. The yield half does not: `Paper::yield_on` derives the rate from the cleared price and the term, in that direction, and no production code names it
- [ ] **TODO 4.SHORT-TERM-DEBT.A3** — `Short-Term Debt A3` PARTIAL — the short borrower is the issuer and discount paper is priced by its book rather than a shared coupon; issuer-specific buyer limits remain incomplete
- [ ] **TODO 4.SHORT-TERM-DEBT.E3** — `Short-Term Debt E3` PARTIAL — packages/kernel-rs/src/ledger.rs refuses a delivery beyond what is held, so an outstanding cannot go negative. Nothing matures, so a maturity passing without cash moving is what every line does (Bond N10) — the `redeemable` module that would retire a line is imported by nothing

### 4. Banks Lending — 39 missing, 3 partial

> **Required review before this block:** read the **Banks Lending** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2370), then inspect `packages/kernel-rs/src/mechanisms/lending.rs`, `packages/kernel-rs/src/mechanisms/corporate_credit.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 4.BANKS-LENDING.A2** — `Banks Lending A2` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.A3** — `Banks Lending A3` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.A3A** — `Banks Lending A3.a` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.A3B** — `Banks Lending A3.b` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.A4** — `Banks Lending A4` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.A5** — `Banks Lending A5` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.B1** — `Banks Lending B1` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.B1A** — `Banks Lending B1.a` MISSING — no loan is originated, so nothing creates a deposit and no reserve stays put. packages/kernel-rs/src/ledger.rs `Leg::Mint` is the door that would create the deposit and only the central-bank facility uses it
- [ ] **TODO 4.BANKS-LENDING.B2** — `Banks Lending B2` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.B2A** — `Banks Lending B2.a` MISSING — no loan is written, so none consumes capital. packages/kernel-rs/src/mechanisms/bank_capital.rs `classified_weight` is what would charge one against the ratio and no origination reads it
- [ ] **TODO 4.BANKS-LENDING.B2B** — `Banks Lending B2.b` MISSING — no bank's cash and collateral position changes what it is willing to lend, because nothing decides to lend. packages/kernel-rs/src/mechanisms/bank_funding.rs knows what a bank has left and no credit decision asks it
- [ ] **TODO 4.BANKS-LENDING.B2C** — `Banks Lending B2.c` MISSING — no bank holds a risk appetite of its own. packages/kernel-rs/src/stores.rs `Outlooks` is where such a view would live, keyed to the bank, and nothing forms one about lending
- [ ] **TODO 4.BANKS-LENDING.B2D** — `Banks Lending B2.d` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.C1** — `Banks Lending C1` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.C1A** — `Banks Lending C1.a` MISSING — packages/kernel-rs/src/mechanisms/bank_funding.rs computes a blended cost of funds for each bank from its own mix, and no loan price reads it because no loan is priced
- [ ] **TODO 4.BANKS-LENDING.C1B** — `Banks Lending C1.b` MISSING — packages/kernel-rs/src/stores.rs keys `repayment_of` and `loss_given_failure_of` to the named borrower and nothing forms either, so no bank assesses a probability or a severity for anybody
- [ ] **TODO 4.BANKS-LENDING.C1C** — `Banks Lending C1.c` MISSING — packages/kernel-rs/src/mechanisms/bank_capital.rs holds the ratios a loan would consume against, and no bank states a required return on the capital a loan uses. Neither figure enters a price
- [ ] **TODO 4.BANKS-LENDING.C1D** — `Banks Lending C1.d` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.C2** — `Banks Lending C2` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.C2A** — `Banks Lending C2.a` MISSING — no rate is struck between a bank and a borrower, so none is negotiated and none is imposed. packages/kernel-rs/src/clearing.rs `clear` is how two posted levels meet in this world and no credit book is declared
- [ ] **TODO 4.BANKS-LENDING.C3** — `Banks Lending C3` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.C3A** — `Banks Lending C3.a` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.D1** — `Banks Lending D1` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.D2** — `Banks Lending D2` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.D2A** — `Banks Lending D2.a` MISSING — no bank holds an assessment of a borrower, so none moves and no provision follows one. packages/kernel-rs/src/mechanisms/loss.rs `Losses` books a loss when one is an event, and a changed opinion is not among the events it reads
- [ ] **TODO 4.BANKS-LENDING.D2B** — `Banks Lending D2.b` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.D3** — `Banks Lending D3` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.D4** — `Banks Lending D4` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.D4A** — `Banks Lending D4.a` MISSING — packages/kernel-rs/src/stores.rs keys an agreement to two parties, so a syndicate would be several rows against one borrower at one margin. Nothing arranges one, nobody takes a lead fee, and no large-exposure limit binds anybody
- [ ] **TODO 4.BANKS-LENDING.D5** — `Banks Lending D5` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E1** — `Banks Lending E1` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E2** — `Banks Lending E2` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E3** — `Banks Lending E3` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E4** — `Banks Lending E4` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E5** — `Banks Lending E5` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E5A** — `Banks Lending E5.a` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.E6** — `Banks Lending E6` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.F2** — `Banks Lending F2` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.F3** — `Banks Lending F3` MISSING — `lending` runs `mechanisms/lending.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off
- [ ] **TODO 4.BANKS-LENDING.A1** — `Banks Lending A1` PARTIAL — `agreed::COMMITMENT` is a named lender to a named borrower and packages/kernel-rs/src/mechanisms/lending.rs `Rows` is a book of loan rows. Nothing originates one, and the origination IS the wiring
- [ ] **TODO 4.BANKS-LENDING.A1B** — `Banks Lending A1.b` PARTIAL — a loan sits in `Agreements` and never in the register, so it is not marked with the instruments that are. Nothing carries it at amortised cost either, because no origination books a carrying value at all
- [ ] **TODO 4.BANKS-LENDING.F1** — `Banks Lending F1` PARTIAL — packages/kernel-rs/src/mechanisms/lending.rs `Rows` is a sum of named rows and never a scalar. It has one caller and nothing writes a row into it

### 4. Trade Credit — 20 missing, 3 partial

> **Required review before this block:** read the **Trade Credit** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3396), then inspect `packages/kernel-rs/src/mechanisms/trade_credit.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 4.TRADE-CREDIT.A2** — `Trade Credit A2` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.A3** — `Trade Credit A3` MISSING — `trade_credit.rs Terms`/`Discount`/`implied_rate` are unreached, so there is no discount for paying early and therefore no implicit rate
- [ ] **TODO 4.TRADE-CREDIT.A4** — `Trade Credit A4` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.B1** — `Trade Credit B1` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.B2** — `Trade Credit B2` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.B3** — `Trade Credit B3` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.B4** — `Trade Credit B4` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.B5** — `Trade Credit B5` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.C1** — `Trade Credit C1` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.C1A** — `Trade Credit C1.a` MISSING — packages/kernel-rs/src/mechanisms/firms.rs `Reporting` reads settled legs, so revenue is recognised in the week cash arrived. No invoice defers a receipt, so the two periods the clause separates are one
- [ ] **TODO 4.TRADE-CREDIT.C2** — `Trade Credit C2` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.C3** — `Trade Credit C3` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.D2** — `Trade Credit D2` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.D2A** — `Trade Credit D2.a` MISSING — packages/kernel-rs/src/mechanisms/trade_credit.rs `on_death` is where a seller would wear what a dead buyer owed it, and no production code names it. No invoice is outstanding, so no seller is an involuntary lender to anybody
- [ ] **TODO 4.TRADE-CREDIT.D3** — `Trade Credit D3` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.D3A** — `Trade Credit D3.a` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.D4** — `Trade Credit D4` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.D4A** — `Trade Credit D4.a` MISSING — no supplier shortens terms on a doubted buyer, because no terms are offered. packages/kernel-rs/src/mechanisms/trade_credit.rs `along_the_chain` is how such a withdrawal would travel and no production code names it
- [ ] **TODO 4.TRADE-CREDIT.D5** — `Trade Credit D5` MISSING — `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached. No supplier decides to extend terms, none tightens them, and nothing runs along the supply network
- [ ] **TODO 4.TRADE-CREDIT.E3** — `Trade Credit E3` MISSING — nothing resolves when a debtor ceases — a queued payment owed by a dead party stays queued
- [ ] **TODO 4.TRADE-CREDIT.A1** — `Trade Credit A1` PARTIAL — packages/kernel-rs/src/ledger.rs `Queue::given_time` holds the SAME debt open longer rather than writing a second one — receivable and payable are one row, which is what A1 asks. Nothing extends terms: `TradeCredit` journals and decides nothing
- [ ] **TODO 4.TRADE-CREDIT.C4** — `Trade Credit C4` PARTIAL — the identity holds by construction — a queued payment is one obligation seen from two sides — and nothing checks it: the Flows family is NOT BUILT
- [ ] **TODO 4.TRADE-CREDIT.D1** — `Trade Credit D1` PARTIAL — packages/kernel-rs/src/ledger.rs `Queue` makes lateness a real state with a day. Nothing reads it as a stress on the seller

### 4. Small-Business Pools — 32 missing, 1 partial

> **Required review before this block:** read the **Small-Business Pools** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3882), then inspect `packages/kernel-rs/src/mechanisms/small_business.rs`, `packages/kernel-rs/src/mechanisms/securitisation.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A1** — `Small-Business Pools A1` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A2** — `Small-Business Pools A2` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A3** — `Small-Business Pools A3` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A4** — `Small-Business Pools A4` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A5** — `Small-Business Pools A5` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A5A** — `Small-Business Pools A5.a` MISSING — no credit tightening reaches anybody, because no bank decides whether to lend. packages/kernel-rs/src/mechanisms/small_business.rs `SmallBusiness` journals and proposes nothing, so this sector feels no more of a squeeze than any other
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A6A** — `Small-Business Pools A6.a` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A6B** — `Small-Business Pools A6.b` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A6C** — `Small-Business Pools A6.c` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.B1** — `Small-Business Pools B1` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.B2** — `Small-Business Pools B2` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.B3** — `Small-Business Pools B3` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.B4** — `Small-Business Pools B4` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.B4A** — `Small-Business Pools B4.a` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C1** — `Small-Business Pools C1` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C2** — `Small-Business Pools C2` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C2A** — `Small-Business Pools C2.a` MISSING — packages/kernel-rs/src/mechanisms/securitisation.rs `Securitising` is where tranche boundaries would be stated and losses walked up them, and no pool is ever struck. With no loss to allocate the rule has nothing to apply to
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C3** — `Small-Business Pools C3` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C4** — `Small-Business Pools C4` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C4A** — `Small-Business Pools C4.a` MISSING — no tranche is issued and none is retained, so no originator keeps a bottom piece and no risk stays behind. packages/kernel-rs/src/mechanisms/small_business.rs `retained_by` is that read and no production code names it
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C5** — `Small-Business Pools C5` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.C6** — `Small-Business Pools C6` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.D1** — `Small-Business Pools D1` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.D2** — `Small-Business Pools D2` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.D3** — `Small-Business Pools D3` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.D4** — `Small-Business Pools D4` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.D4A** — `Small-Business Pools D4.a` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.E1** — `Small-Business Pools E1` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.E2** — `Small-Business Pools E2` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.E3** — `Small-Business Pools E3` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.E4** — `Small-Business Pools E4` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.E6** — `Small-Business Pools E6` MISSING — `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached. No cell borrows from a named lender, none defaults, none is promoted and no pool is cut
- [ ] **TODO 4.SMALL-BUSINESS-POOLS.A2A** — `Small-Business Pools A2.a` PARTIAL — the sector is cells rather than one firm. every cell posts the same reservation, so a mean-preserving spread crosses nothing

### 5. Equity — 39 missing, 2 partial

> **Required review before this block:** read the **Equity** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1402), then inspect `packages/kernel-rs/src/mechanisms/equity.rs`, `packages/kernel-rs/src/mechanisms/redeemable.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 5.EQUITY.A5** — `Equity A5` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.A5A** — `Equity A5.a` MISSING — packages/kernel-rs/src/mechanisms/equity.rs `control_needs` is what a majority would cost in shares and no production code names it. No participant posts in an equity book, so no stake is ever accumulated towards one
- [ ] **TODO 5.EQUITY.A5B** — `Equity A5.b` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.B1** — `Equity B1` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.B2** — `Equity B2` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.B5** — `Equity B5` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.B6** — `Equity B6` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.C1B** — `Equity C1.b` MISSING — packages/kernel-rs/src/register.rs answers who holds how many shares and nothing marks a holder as an insider or as strategic, so every share counts as tradeable and the float is the whole issue
- [ ] **TODO 5.EQUITY.C2** — `Equity C2` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.C2A** — `Equity C2.a` MISSING — packages/kernel-rs/src/mechanisms/households.rs `HouseholdBuyers` posts in the books it is registered for and no share line is among them, so no household owns equity directly
- [ ] **TODO 5.EQUITY.C2B** — `Equity C2.b` MISSING — packages/kernel-rs/src/mechanisms/funds.rs `FundMandates` carries a mandate as a constraint on what a fund may hold, and no fund posts in an equity book. The constraint therefore binds nothing
- [ ] **TODO 5.EQUITY.C2C** — `Equity C2.c` MISSING — no fund tracks an index, and packages/kernel-rs/src/mechanisms/benchmarks.rs publishes no weight a holder could follow. Nothing in the world holds a line without pricing it
- [ ] **TODO 5.EQUITY.C2D** — `Equity C2.d` MISSING — packages/kernel-rs/src/mechanisms/equity.rs `ShareEvent::BoughtBack` takes shares out of the count rather than into the hands of the issuer, so a company never holds its own paper as an asset
- [ ] **TODO 5.EQUITY.C2E** — `Equity C2.e` MISSING — no holder is marked as an insider or a founder anywhere, so no holding is off the market for a reason of its own and every share faces the same book
- [ ] **TODO 5.EQUITY.C3** — `Equity C3` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.C4** — `Equity C4` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.C5** — `Equity C5` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.C5A** — `Equity C5.a` MISSING — packages/kernel-rs/src/mechanisms/prime_brokerage.rs is where a margined equity position would be financed and it proposes no instruction, so nothing is bought on margin and no call is ever made on one
- [ ] **TODO 5.EQUITY.C6** — `Equity C6` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.C7** — `Equity C7` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.D2** — `Equity D2` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.D2A** — `Equity D2.a` MISSING — packages/kernel-rs/src/mechanisms/equity.rs `apply` lowers `Line::outstanding` on a `ShareEvent::BoughtBack`, which would grow every remaining claim. Nothing constructs that event, so no count falls
- [ ] **TODO 5.EQUITY.D2B** — `Equity D2.b` MISSING — nothing buys back, so no cash leaves a company that way and the distinction between a distribution and an investment is drawn nowhere
- [ ] **TODO 5.EQUITY.D2C** — `Equity D2.c` MISSING — no company weighs a buyback against a dividend and against plant, because two of the three do not happen. packages/kernel-rs/src/mechanisms/capital_programme.rs is the only use of cash a firm decides on
- [ ] **TODO 5.EQUITY.D3** — `Equity D3` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.D3A** — `Equity D3.a` MISSING — packages/kernel-rs/src/mechanisms/equity.rs `dividend_allocations` splits a distributable sum across the holders of record and nothing calls it with one. No dividend is proposed, so nothing leaves a company for its owners
- [ ] **TODO 5.EQUITY.D3B** — `Equity D3.b` MISSING — no dividend is declared, so none can be cut. Nothing reads a distribution as a signal about a company because no distribution is ever recorded
- [ ] **TODO 5.EQUITY.D4** — `Equity D4` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.E1** — `Equity E1` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.E2** — `Equity E2` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.E3** — `Equity E3` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.E4** — `Equity E4` MISSING — nothing wipes a register on insolvency, because no estate is ever opened
- [ ] **TODO 5.EQUITY.F1** — `Equity F1` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.F2** — `Equity F2` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.F3** — `Equity F3` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.F4** — `Equity F4` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.G1** — `Equity G1` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.G2** — `Equity G2` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.G3** — `Equity G3` MISSING — the `equity` system runs `mechanisms/equity.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens
- [ ] **TODO 5.EQUITY.A1B** — `Equity A1.b` PARTIAL — packages/kernel-rs/src/mechanisms/equity.rs `residual` answers with two figures, what is left to shareholders and what is short to creditors, so a shortfall cannot become a claim on a holder. Nothing asks it: the FORBID holds because no residual is ever taken, not because the answer is used
- [ ] **TODO 5.EQUITY.A6** — `Equity A6` PARTIAL — packages/kernel-rs/src/instruments.rs `display` names a share by its issuer. Its one caller, `observer.rs display_name`, is dead code

### 5. Fund Shares — 24 missing, 7 partial

> **Required review before this block:** read the **Fund Shares** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1644), then inspect `packages/kernel-rs/src/mechanisms/funds.rs`, `packages/kernel-rs/src/mechanisms/redeemable.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 5.FUND-SHARES.B2A** — `Fund Shares B2.a` MISSING — packages/kernel-rs/src/prices.rs marks a print stale rather than refreshing it, so a stale input is visible. No subscription or redemption transacts against a NAV built on one, because value reaches no balance sheet and no redemption happens at all
- [ ] **TODO 5.FUND-SHARES.B3** — `Fund Shares B3` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.B4** — `Fund Shares B4` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.C1A** — `Fund Shares C1.a` MISSING — packages/kernel-rs/src/mechanisms/redeemable.rs `Subscribing` moves cash to the pool and shares to the holder, and nothing then buys anything with that cash. packages/kernel-rs/src/mechanisms/funds.rs `FundMandates` carries what the fund may hold and no purchase is proposed against it
- [ ] **TODO 5.FUND-SHARES.C2** — `Fund Shares C2` MISSING — `funds.rs Redeems`/`Redeemed` and `redeemable.rs holders_against_the_book` are the redemption and are unreachable. Nothing redeems, so C2.b`s forced-seller channel — "the point" — does not exist
- [ ] **TODO 5.FUND-SHARES.C2A** — `Fund Shares C2.a` MISSING — no investor can ask for its money back, so no fund has to find cash. packages/kernel-rs/src/mechanisms/redeemable.rs `Meeting` is where a buffer would be drawn on and a sale decided, and no production code names it
- [ ] **TODO 5.FUND-SHARES.C2B** — `Fund Shares C2.b` MISSING — nothing sells to meet a redemption, so the forced-seller channel carries nothing. packages/kernel-rs/src/mechanisms/forced_sale.rs `ForcedSeller` is the participant that would post that sale into a book that must clear, and no fund reaches it
- [ ] **TODO 5.FUND-SHARES.C4** — `Fund Shares C4` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.C4A** — `Fund Shares C4.a` MISSING — no redemption is met by selling, so no cost falls on the holders who stayed. With nothing to bear, a run has no engine and packages/kernel-rs/src/mechanisms/funds.rs `Winding` is reached by nothing
- [ ] **TODO 5.FUND-SHARES.C5** — `Fund Shares C5` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.D1** — `Fund Shares D1` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.D2** — `Fund Shares D2` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.D2A** — `Fund Shares D2.a` MISSING — no money fund publishes a yield and no saver compares one with a deposit rate, so nothing constrains what a bank pays. packages/kernel-rs/src/mechanisms/bank_funding.rs reads a weekly funding fixing that no depositor chose against an alternative
- [ ] **TODO 5.FUND-SHARES.D3** — `Fund Shares D3` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.D4** — `Fund Shares D4` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.D5** — `Fund Shares D5` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.E1** — `Fund Shares E1` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.E2** — `Fund Shares E2` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.E3** — `Fund Shares E3` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.E3A** — `Fund Shares E3.a` MISSING — no gap between a fund's price and its NAV is ever observed, because no fund share trades. Nothing is therefore closed by a participant with a reason, and nothing could persist for want of one
- [ ] **TODO 5.FUND-SHARES.E4** — `Fund Shares E4` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.F2** — `Fund Shares F2` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.F3** — `Fund Shares F3` MISSING — the only fund mechanism that moves anything is `mechanisms/redeemable.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller
- [ ] **TODO 5.FUND-SHARES.G1A** — `Fund Shares G1.a` MISSING — no redemption is met at all, in cash or in kind, so no vehicle is exempted from forced selling and none carries it instead. packages/kernel-rs/src/mechanisms/redeemable.rs has no in-kind path to choose between
- [ ] **TODO 5.FUND-SHARES.A2** — `Fund Shares A2` PARTIAL — packages/kernel-rs/src/mechanisms/redeemable.rs `Subscribing` counts shares off `agreed::SUBSCRIPTION` terms. They are agreement terms rather than an instrument holders hold, so A2`s "counted in shares" is a number on a relation, and the `redeemable` module that holds the claim is imported by nothing
- [ ] **TODO 5.FUND-SHARES.A3** — `Fund Shares A3` PARTIAL — packages/kernel-rs/src/mechanisms/redeemable.rs `Book::nav` computes assets minus liabilities over shares. Nothing checks that a fund`s equity is zero; the Accounts family is NOT BUILT
- [ ] **TODO 5.FUND-SHARES.A4** — `Fund Shares A4` PARTIAL — `agreed::MANDATE` carries the lowest grade a pool may hold, and packages/kernel-rs/src/mechanisms/funds.rs `FundMandates` reads it. Nothing else constrains what a fund buys, and `funds` is imported by nothing
- [ ] **TODO 5.FUND-SHARES.B1** — `Fund Shares B1` PARTIAL — packages/kernel-rs/src/mechanisms/redeemable.rs `Book::nav` is a read every time and is stored nowhere. its asset side is one of the three inline copies of units-times-price with a silent fallback to cost
- [ ] **TODO 5.FUND-SHARES.B2** — `Fund Shares B2` PARTIAL — see B1 — a position declared `Carrying::Market` whose line has not printed is now absent from `carrying_value` rather than silently falling back to cost, so whoever asked has to handle it (B2.a). What is still missing is the balance sheet it should reach
- [ ] **TODO 5.FUND-SHARES.C3** — `Fund Shares C3` PARTIAL — shares rise on a subscription. Nothing takes them back (C2): `redeemable::Meeting` has no caller
- [ ] **TODO 5.FUND-SHARES.G1** — `Fund Shares G1` PARTIAL — packages/kernel-rs/src/mechanisms/redeemable.rs is the shape of a redeemable claim. `Meeting` and `holders_against_the_book` have no caller, so no investor can ask for its money back

### 5. Dealer Desks — 22 missing, 7 partial

> **Required review before this block:** read the **Dealer Desks** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2600), then inspect `packages/kernel-rs/src/mechanisms/dealing.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 5.DEALER-DESKS.A4** — `Dealer Desks A4` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.B1** — `Dealer Desks B1` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.B2** — `Dealer Desks B2` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.B3** — `Dealer Desks B3` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.B4** — `Dealer Desks B4` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.C1** — `Dealer Desks C1` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.C3** — `Dealer Desks C3` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.C4** — `Dealer Desks C4` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.C5** — `Dealer Desks C5` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.C5A** — `Dealer Desks C5.a` MISSING — `bid = around - width - skew; ask = around + width - skew` is a stated width applied to a mid, which is what this FORBID names
- [ ] **TODO 5.DEALER-DESKS.C5B** — `Dealer Desks C5.b` MISSING — `dealer.width` = 0.02, declared "share of the mid", is the stated spread table this FORBID names
- [ ] **TODO 5.DEALER-DESKS.C5C** — `Dealer Desks C5.c` MISSING — no desk charges a client a conversion or assembly fee, and no such flow exists to charge on. packages/kernel-rs/src/mechanisms/dealing.rs prices only its own width in the books it quotes
- [ ] **TODO 5.DEALER-DESKS.D2** — `Dealer Desks D2` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.D3** — `Dealer Desks D3` MISSING — nothing charges a desk rent for its inventory. XI-4 joint three: a desk that carries inventory for free has no reason to shed it
- [ ] **TODO 5.DEALER-DESKS.D4** — `Dealer Desks D4` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.D5** — `Dealer Desks D5` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.E1** — `Dealer Desks E1` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.E1A** — `Dealer Desks E1.a` MISSING — no desk hedges anything, so no hedge is a trade with a counterparty and none is a number reduced instead. packages/kernel-rs/src/mechanisms/dealing.rs posts in its own books and takes no offsetting position elsewhere
- [ ] **TODO 5.DEALER-DESKS.E2** — `Dealer Desks E2` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.E3** — `Dealer Desks E3` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.E4** — `Dealer Desks E4` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.F2** — `Dealer Desks F2` MISSING — §26 runs `mechanisms/dealing.rs Dealers`, a participant posting a stated width around a constant. `mechanisms/dealing.rs` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.A1** — `Dealer Desks A1` PARTIAL — packages/kernel-rs/src/mechanisms/dealing.rs `Dealers` is a named party of `kinds::DEALER` with its own register rows. It has no balance sheet inside a bank (A1) and no funding cost, and `dealing` is imported by nothing
- [ ] **TODO 5.DEALER-DESKS.A2** — `Dealer Desks A2` PARTIAL — packages/kernel-rs/src/mechanisms/dealing.rs `Dealers::orders` posts a bid and an ask. no dealer kind is wired as a system, and 13.1: both sides come off a stated width applied to a constant
- [ ] **TODO 5.DEALER-DESKS.C2** — `Dealer Desks C2` PARTIAL — packages/kernel-rs/src/mechanisms/dealing.rs `Dealers::orders` skews on inventory against its limit, which is the one part of §26 C that is a consequence. The level it skews around is a constant
- [ ] **TODO 5.DEALER-DESKS.C2A** — `Dealer Desks C2.a` PARTIAL — packages/kernel-rs/src/mechanisms/dealing.rs `Dealers::orders` skews its two sides on how full its inventory is against its own limit, so a desk that has been hit posts differently next week without being told to. The level it skews around is a constant, so flow moves the width and not the price
- [ ] **TODO 5.DEALER-DESKS.D1** — `Dealer Desks D1` PARTIAL — packages/kernel-rs/src/mechanisms/dealing.rs `Dealers::orders` stops quoting at `held.abs() >= limit`. the comparison is units against a money amount
- [ ] **TODO 5.DEALER-DESKS.D4A** — `Dealer Desks D4.a` PARTIAL — packages/kernel-rs/src/clearing.rs answers `NoSupply` or `NoDemand` where a side did not turn up, so an auction failing is an outcome the mechanism can produce. No dealer has a bidding obligation to step back from, because no participant is a primary dealer anywhere
- [ ] **TODO 5.DEALER-DESKS.F3** — `Dealer Desks F3` PARTIAL — packages/kernel-rs/src/mechanisms/dealing.rs `Dealers` declares `Carrying::Market` on what a desk buys, so `instruments.rs` `carrying_value` marks its inventory and the equity read moves with it. No spread times a volume is computed anywhere. What is missing is the income line that books what the inventory did

### 5. Capital Programme — 24 missing, 7 partial

> **Required review before this block:** read the **Capital Programme** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3185), then inspect `packages/kernel-rs/src/mechanisms/capital_programme.rs`, `packages/kernel-rs/src/mechanisms/cost_of_capital.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 5.CAPITAL-PROGRAMME.A3** — `Capital Programme A3` MISSING — packages/kernel-rs/src/instruments.rs `worn` is the one schedule A3 asks for and it is unreached. Nothing depreciates: no charge against profit and no reduction in the stock
- [ ] **TODO 5.CAPITAL-PROGRAMME.A4** — `Capital Programme A4` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.A4A** — `Capital Programme A4.a` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` names one required plant line for every builder, so what a firm's plant is made of does not vary with its industry. One basket is shared by every buyer
- [ ] **TODO 5.CAPITAL-PROGRAMME.A5** — `Capital Programme A5` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.B1B** — `Capital Programme B1.b` MISSING — packages/kernel-rs/src/mechanisms/cost_of_capital.rs is where a marginal debt and equity cost would be read, and no builder reads one. Nothing prices new debt at the margin, because no company debt clears
- [ ] **TODO 5.CAPITAL-PROGRAMME.B1D** — `Capital Programme B1.d` MISSING — no management holds a hurdle or a horizon of its own. packages/kernel-rs/src/mechanisms/capital_programme.rs reads declared numbers from `params` for both, so every firm waits the same time for the same return
- [ ] **TODO 5.CAPITAL-PROGRAMME.B2** — `Capital Programme B2` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.B2A** — `Capital Programme B2.a` MISSING — nothing funds an investment programme, so no firm is stopped by being unable to. packages/kernel-rs/src/mechanisms/capital_programme.rs `Builder` bids out of the cash a firm already holds, which is a cash constraint rather than a credit one
- [ ] **TODO 5.CAPITAL-PROGRAMME.B3** — `Capital Programme B3` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.B4** — `Capital Programme B4` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.C1** — `Capital Programme C1` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.C2** — `Capital Programme C2` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.C4** — `Capital Programme C4` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.D1** — `Capital Programme D1` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.D2** — `Capital Programme D2` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.D3** — `Capital Programme D3` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.D4** — `Capital Programme D4` MISSING — capacity, output and utilisation are reconciled nowhere; `goods.rs utilisation` is unreached
- [ ] **TODO 5.CAPITAL-PROGRAMME.E1** — `Capital Programme E1` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.E2** — `Capital Programme E2` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.E3** — `Capital Programme E3` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.E4** — `Capital Programme E4` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.F1** — `Capital Programme F1` MISSING — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` now names the required plant line and `Builder` bids only for that line; packages/kernel-rs/src/session.rs reduces the monetary commitment only after its purchase settles. Funding the commitment and construction after purchase remain open
- [ ] **TODO 5.CAPITAL-PROGRAMME.F1A** — `Capital Programme F1.a` MISSING — packages/kernel-rs/src/mechanisms/mortality.rs `Failing` removes a firm and nothing admits one, so a sector's line-up only shrinks. With entry absent the roster is a consequence of deaths alone rather than of both
- [ ] **TODO 5.CAPITAL-PROGRAMME.F1B** — `Capital Programme F1.b` MISSING — packages/kernel-rs/src/bin/world_runs.rs hands every firm an opening stock and declares the whole state arbitrary rather than naming that stock as a stand-in for entry. Nothing marks it as a placeholder with a mechanism behind it
- [ ] **TODO 5.CAPITAL-PROGRAMME.A4B** — `Capital Programme A4.b` PARTIAL — packages/kernel-rs/src/instruments.rs `Plant` carries a life, so a good with no life is not plant to anybody. How much of that life has gone is read by `worn`, which nothing calls, so the life is declared and never consumed
- [ ] **TODO 5.CAPITAL-PROGRAMME.A4C** — `Capital Programme A4.c` PARTIAL — packages/kernel-rs/src/mechanisms/goods.rs consumes what a firm's own recipe names as an input and packages/kernel-rs/src/instruments.rs treats what carries a life as plant, so two of the three answers are the buyer's. Nothing is classed as an operating expense, because no third branch exists
- [ ] **TODO 5.CAPITAL-PROGRAMME.A6A** — `Capital Programme A6.a` PARTIAL — packages/kernel-rs/src/ledger.rs moves plant only as a `Leg::Asset` with two named sides and records wear as a `Leg::Depreciate` on the holder's own book, so both halves have a shape. Nothing moves plant out of an estate, a carve-out, a merger or a resolution, and nothing scraps or abandons any
- [ ] **TODO 5.CAPITAL-PROGRAMME.B1** — `Capital Programme B1` PARTIAL — packages/kernel-rs/src/mechanisms/capital_programme.rs `Building` reads the firm`s own quantity and named-price outlooks, names the required plant line, and `Builder` submits a bounded bid only in that book. The programme still needs an explicit financing handoff
- [ ] **TODO 5.CAPITAL-PROGRAMME.B1C** — `Capital Programme B1.c` PARTIAL — `Building` reads price outlooks, and an outlook moves only when a print surprised the firm, so a market price does reach a real investment decision. The other side is missing: no cost of capital enters the decision, so half the channel is absent
- [ ] **TODO 5.CAPITAL-PROGRAMME.C1A** — `Capital Programme C1.a` PARTIAL — packages/kernel-rs/src/mechanisms/capital_programme.rs `Builder` bids into the plant book in the week it decides, so investment is demand now. The capacity arrives when the plant enters service, so the two do land in different weeks; what is missing is any purchase completing, because no plant book has crossed
- [ ] **TODO 5.CAPITAL-PROGRAMME.C3** — `Capital Programme C3` PARTIAL — `invest.takes` is the declared build horizon; the programme now completes its purchase commitment only from settled plant trades, but construction/in-service transition after that purchase remains open

### 6. Money Market — 26 missing, 3 partial

> **Required review before this block:** read the **Money Market** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1491), then inspect `packages/kernel-rs/src/mechanisms/money_market.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 6.MONEY-MARKET.A1** — `Money Market A1` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.A1A** — `Money Market A1.a` MISSING — no bank lends reserves to another, so nothing is redistributed. packages/kernel-rs/src/audit.rs `MoneyIsConserved` would see such a movement leave the total unchanged, and no interbank loan is ever proposed for it to see
- [ ] **TODO 6.MONEY-MARKET.A1B** — `Money Market A1.b` MISSING — nothing pairs one bank's deficit with another's surplus. packages/kernel-rs/src/mechanisms/money_market.rs `MoneyMarketBanks` posts a schedule and no book is declared for it, so the two sides never meet
- [ ] **TODO 6.MONEY-MARKET.A2** — `Money Market A2` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.A2A** — `Money Market A2.a` MISSING — no bank derives a buffer from how liquid its own funding is. packages/kernel-rs/src/mechanisms/bank_funding.rs reads a declared ratio from `params`, which is the floor the clause says may sit under a preference rather than the preference itself
- [ ] **TODO 6.MONEY-MARKET.A2B** — `Money Market A2.b` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.B2** — `Money Market B2` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.B2A** — `Money Market B2.a` MISSING — no lender forms a view of a borrowing bank, so a doubted name pays no more and is refused by nobody. packages/kernel-rs/src/clearing.rs returns `NoDemand` for a side nobody stood on, and no bank ever posts to be refused
- [ ] **TODO 6.MONEY-MARKET.B2B** — `Money Market B2.b` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.B3** — `Money Market B3` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.B3A** — `Money Market B3.a` MISSING — packages/kernel-rs/src/mechanisms/bank_funding.rs accepts eligible claim holdings for a central-bank draw, and no eligibility is defined for lending between banks because no bank lends to another. Nothing is ineligible in a market that does not meet
- [ ] **TODO 6.MONEY-MARKET.B3B** — `Money Market B3.b` MISSING — packages/kernel-rs/src/mechanisms/bank_capital.rs `haircut` takes a grade and a tenor, so a haircut can differ by the credit of the issuer. Nothing applies it to interbank collateral, and the facility draw uses one declared advance ratio for every asset
- [ ] **TODO 6.MONEY-MARKET.B4** — `Money Market B4` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.B5** — `Money Market B5` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.B5A** — `Money Market B5.a` MISSING — no non-bank lends in a money market, so none of them weighs a central-bank floor or a bill against doing so. packages/kernel-rs/src/mechanisms/money_market.rs declares no deposit facility and no bill is bought by anybody
- [ ] **TODO 6.MONEY-MARKET.B6** — `Money Market B6` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.B6A** — `Money Market B6.a` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.C1** — `Money Market C1` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.C1A** — `Money Market C1.a` MISSING — no deposit facility exists, so no cash is parked at a central bank and nothing leaves the banking system that way. The only movement between a bank and the central bank is the collateralised advance, which runs the other way
- [ ] **TODO 6.MONEY-MARKET.C3** — `Money Market C3` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.D1** — `Money Market D1` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.D2** — `Money Market D2` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.D5** — `Money Market D5` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.D5B** — `Money Market D5.b` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.D6** — `Money Market D6` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.E1** — `Money Market E1` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.E2** — `Money Market E2` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.E3** — `Money Market E3` MISSING — `Interbank` reads each bank's reserve position at d6, after the week's flows have left it where they left it, and a bank short of its buffer BRINGS a week's paper on its own name with a book of its own — so one borrower's paper prices differently from another's and a name the market doubts finds no bid. `MoneyMarketBanks` lends into those books at its own view of that name, valuing paper it has no history of from the issuer's other paper or from the contract itself. Repo beside unsecured funding, a term beside the week, and the `Schedule`, `View`, `Collateral` and `session` shapes in `mechanisms/money_market.rs` are all still absent
- [ ] **TODO 6.MONEY-MARKET.B1** — `Money Market B1` PARTIAL — packages/kernel-rs/src/mechanisms/money_market.rs `Interbank` puts every short bank's own paper in its own book and `MoneyMarketBanks` posts each lender's own levels into whichever of them it has a view of — so who lends and who borrows follows from the positions the week left. What is short is the borrower's own schedule: it brings what it is short of and names no rate, so the auction alone decides what it pays
- [ ] **TODO 6.MONEY-MARKET.B3C** — `Money Market B3.c` PARTIAL — packages/kernel-rs/src/register.rs `pledge` encumbers named units and refuses to move them, so collateral cannot be pledged twice. Running out of it stops nobody borrowing, because the only borrowing is the central-bank draw and no interbank market competes for the same assets
- [ ] **TODO 6.MONEY-MARKET.C4A** — `Money Market C4.a` PARTIAL — packages/kernel-rs/src/mechanisms/bank_funding.rs journals a facility draw as an event any reader can see, so drawing is on the record. It is not information anybody acts on, and no bank prefers a market because there is no market to prefer
- [ ] **TODO 6.MONEY-MARKET.D5A** — `Money Market D5.a` PARTIAL — a facility draw and a published ratio are journalled, so two of the observable things exist. No rate is paid in a money market and no run of short closes is recorded, because neither the market nor the close exists

### 6. Banks Funding — 38 missing, 3 partial

> **Required review before this block:** read the **Banks Funding** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2455), then inspect `packages/kernel-rs/src/mechanisms/bank_funding.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 6.BANKS-FUNDING.A1** — `Banks Funding A1` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.A1A** — `Banks Funding A1.a` MISSING — no deposit base is split by who the depositor is, so nothing is retail. packages/kernel-rs/src/parties.rs carries the integer weight that would make an insured amount exact for a cell, and no insurance limit is declared for it to multiply
- [ ] **TODO 6.BANKS-FUNDING.A1B** — `Banks Funding A1.b` MISSING — a firm holds an account at the bank it was admitted with and nothing marks that funding as operational, so no balance is stickier than another for a reason
- [ ] **TODO 6.BANKS-FUNDING.A1C** — `Banks Funding A1.c` MISSING — no depositor is rate-sensitive, because no bank posts a deposit rate for one to respond to. Every balance stays where it is whatever happens
- [ ] **TODO 6.BANKS-FUNDING.A1D** — `Banks Funding A1.d` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.A2** — `Banks Funding A2` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.A2A** — `Banks Funding A2.a` MISSING — no wholesale funding is struck, so none rolls and no squeeze can bite on a roll. packages/kernel-rs/src/mechanisms/bank_funding.rs reads a shortfall each week and goes to the central bank rather than to a market that could decline
- [ ] **TODO 6.BANKS-FUNDING.A3** — `Banks Funding A3` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.A4** — `Banks Funding A4` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.A5** — `Banks Funding A5` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.B1** — `Banks Funding B1` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.B1A** — `Banks Funding B1.a` MISSING — no bank sets a deposit rate and no depositor responds to one, so nothing is bounded by a wholesale cost or a money-fund yield and no share of a base is contested
- [ ] **TODO 6.BANKS-FUNDING.B1B** — `Banks Funding B1.b` MISSING — no market sets a wholesale rate, because no wholesale funding book is declared. packages/kernel-rs/src/mechanisms/benchmarks.rs publishes a weekly funding fixing from whatever cleared, and no bank borrows at it
- [ ] **TODO 6.BANKS-FUNDING.B2** — `Banks Funding B2` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.B3** — `Banks Funding B3` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.C1** — `Banks Funding C1` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.C2** — `Banks Funding C2` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.C2A** — `Banks Funding C2.a` MISSING — packages/kernel-rs/src/mechanisms/bank_funding.rs reads a declared buffer ratio from `params`, which is a stated number rather than a preference derived from how liquid that bank's own liabilities are
- [ ] **TODO 6.BANKS-FUNDING.C3** — `Banks Funding C3` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.C3A** — `Banks Funding C3.a` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.C4** — `Banks Funding C4` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.D1** — `Banks Funding D1` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.D2** — `Banks Funding D2` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.D3** — `Banks Funding D3` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.D4** — `Banks Funding D4` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.D4A** — `Banks Funding D4.a` MISSING — no credit decision exists for a funding problem to reach, so there is no crunch channel. packages/kernel-rs/src/mechanisms/bank_funding.rs knows a bank is short and nothing asks it before writing a loan
- [ ] **TODO 6.BANKS-FUNDING.D5** — `Banks Funding D5` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.D6** — `Banks Funding D6` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.E1** — `Banks Funding E1` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.E2** — `Banks Funding E2` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.E3** — `Banks Funding E3` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.E3A** — `Banks Funding E3.a` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.E4** — `Banks Funding E4` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.E4A** — `Banks Funding E4.a` MISSING — no depositor withdraws, insured or otherwise, so no run starts anywhere and none starts with wholesale money. Nothing declares an insurance limit that would make the two behave differently
- [ ] **TODO 6.BANKS-FUNDING.E5** — `Banks Funding E5` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.F1** — `Banks Funding F1` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.F3** — `Banks Funding F3` MISSING — the Accounts family has no contribution and is NOT BUILT every period
- [ ] **TODO 6.BANKS-FUNDING.F4** — `Banks Funding F4` MISSING — `BankFunding` now proposes bounded sales and a collateralised facility draw, but `Line`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin` and `transformation` still have no production caller. Deposit classes and run behaviour remain unwired
- [ ] **TODO 6.BANKS-FUNDING.B2A** — `Banks Funding B2.a` PARTIAL — packages/kernel-rs/src/mechanisms/bank_funding.rs computes a blended cost of funds per bank from that bank's own mix, so the figure exists and differs between banks. Nothing downstream reads it: no loan is priced, so no funding condition reaches a borrower
- [ ] **TODO 6.BANKS-FUNDING.C1A** — `Banks Funding C1.a` PARTIAL — packages/kernel-rs/src/mechanisms/bank_capital.rs `haircut` varies with grade and tenor, so how surely an asset converts is expressible. No depth is read for any of them, and the facility draw applies one declared advance ratio whatever the asset
- [ ] **TODO 6.BANKS-FUNDING.E2A** — `Banks Funding E2.a` PARTIAL — a capital ratio, a facility draw and a rating action are all journalled, so three of the observable things exist for somebody to watch. No rate is paid up and no run of short weeks is recorded, and no depositor reads any of them

### 6. Banks Capital — 24 missing, 5 partial

> **Required review before this block:** read the **Banks Capital** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2529), then inspect `packages/kernel-rs/src/mechanisms/bank_capital.rs`, `packages/kernel-rs/src/mechanisms/loss.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 6.BANKS-CAPITAL.A2** — `Banks Capital A2` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.A2B** — `Banks Capital A2.b` MISSING — no instrument states a rank, so no subordinated layer exists between equity and senior paper. packages/kernel-rs/src/mechanisms/estate.rs `Rank` has the ordering and nothing issues into it
- [ ] **TODO 6.BANKS-CAPITAL.A2C** — `Banks Capital A2.c` MISSING — no resolution reaches senior creditors or depositors, because nothing values the book a resolution would take. The ladder has no rung for them to be last on
- [ ] **TODO 6.BANKS-CAPITAL.A3** — `Banks Capital A3` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.A4** — `Banks Capital A4` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.B1C** — `Banks Capital B1.c` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.B2** — `Banks Capital B2` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.B3** — `Banks Capital B3` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.B3A** — `Banks Capital B3.a` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.C3** — `Banks Capital C3` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.C3B** — `Banks Capital C3.b` MISSING — a bank sent to resolution is ceased like any other party, so its deposit line stops with it. Nothing carries the accounts on, which is the distinction the clause draws between resolution and bankruptcy
- [ ] **TODO 6.BANKS-CAPITAL.D1** — `Banks Capital D1` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D1A** — `Banks Capital D1.a` MISSING — no loan can be worth less than its face, because no loan is carried at a value at all. With nothing to write down there is no hole for a resolution to measure
- [ ] **TODO 6.BANKS-CAPITAL.D2** — `Banks Capital D2` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D2A** — `Banks Capital D2.a` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D3** — `Banks Capital D3` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D3A** — `Banks Capital D3.a` MISSING — no bank is acquired, so nothing takes both sides of a sheet and settles the difference. packages/kernel-rs/src/mechanisms/estate.rs winds a failed bank up instead, which pays claims rather than transferring a business
- [ ] **TODO 6.BANKS-CAPITAL.D3B** — `Banks Capital D3.b` MISSING — no acquirer bids, and none can decline. packages/kernel-rs/src/mechanisms/control.rs opens a takeover process for a company and no failed bank reaches it, so there is no bid to fall through from
- [ ] **TODO 6.BANKS-CAPITAL.D4** — `Banks Capital D4` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D5** — `Banks Capital D5` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.D6** — `Banks Capital D6` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.E1** — `Banks Capital E1` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.E2** — `Banks Capital E2` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.E3** — `Banks Capital E3` MISSING — `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller. Nothing recapitalises, nothing is resolved, nothing is bailed in
- [ ] **TODO 6.BANKS-CAPITAL.A1** — `Banks Capital A1` PARTIAL — packages/kernel-rs/src/instruments.rs `equity` is a residual and is never a stored pot, and `booked_equity` reads every position under the treatment its holder declared. A bank that declared `Carrying::Cost` is not moved by a mark, which is the mechanism XI-6 describes rather than a gap; the loss reaches it on the day it sells, and nothing forces that sale yet
- [ ] **TODO 6.BANKS-CAPITAL.A2A** — `Banks Capital A2.a` PARTIAL — packages/kernel-rs/src/mechanisms/estate.rs `Rank::Equity` is last in the waterfall, so equity is what a loss reaches first. It absorbs nothing in practice, because no loss is booked against a bank's equity outside a wind-up
- [ ] **TODO 6.BANKS-CAPITAL.B1** — `Banks Capital B1` PARTIAL — `bank.min_weighted` = 0.08 and `bank.min_leverage` = 0.03 are declared POLICY primitives owned by the parliament, and `BankCapital` reads them. Nothing weights an asset, so B1.a`s risk weights do not exist — no value reaches a balance sheet to be weighted
- [ ] **TODO 6.BANKS-CAPITAL.B1A** — `Banks Capital B1.a` PARTIAL — packages/kernel-rs/src/mechanisms/bank_capital.rs `classified_weight` reads the grade of an issuer, so two assets can weigh differently. No bank holds anything it weighs, so no preference between assets ever follows from it
- [ ] **TODO 6.BANKS-CAPITAL.C3A** — `Banks Capital C3.a` PARTIAL — packages/kernel-rs/src/mechanisms/mortality.rs applies the trigger on booked equity and a journalled funding failure, both of them observable. Nobody applies it: the kernel reads the condition itself, so there is no authority that could have looked and not acted

### 6. Insurers — 30 missing, 1 partial

> **Required review before this block:** read the **Insurers** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2671), then inspect `packages/kernel-rs/src/mechanisms/insurers.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 6.INSURERS.A2** — `Insurers A2` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.A2A** — `Insurers A2.a` MISSING — no policy is written, so nothing is a liability of an insurer and no beneficiary is insulated from an investment result. packages/kernel-rs/src/mechanisms/insurers.rs `Policies` journals a count and proposes no instruction
- [ ] **TODO 6.INSURERS.A2B** — `Insurers A2.b` MISSING — no contract states whether the beneficiary takes the investment result, because no contract exists. packages/kernel-rs/src/stores.rs `AgreementTerms` is where such a term would be typed and no policy row is struck
- [ ] **TODO 6.INSURERS.A3** — `Insurers A3` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.A4** — `Insurers A4` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.A4A** — `Insurers A4.a` MISSING — no insurer quotes a price for cover and no policy goes to whoever quotes lower, so nothing is placed and nothing is left unplaced. packages/kernel-rs/src/mechanisms/insurers.rs `InsurerMatching` posts in cash books only
- [ ] **TODO 6.INSURERS.A4B** — `Insurers A4.b` MISSING — no premium is computed from expected claims and a required return on capital, because no premium is computed at all. packages/kernel-rs/src/mechanisms/bank_capital.rs holds the capital arithmetic and no insurer reads it
- [ ] **TODO 6.INSURERS.A4C** — `Insurers A4.c` MISSING — packages/kernel-rs/src/stores.rs `Outlooks` would key an insurer's own claims experience to that insurer and nothing forms one, so no insurer learns what its periods cost it
- [ ] **TODO 6.INSURERS.B1** — `Insurers B1` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.B2** — `Insurers B2` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.B2A** — `Insurers B2.a` MISSING — no liability is discounted, so no rate move changes what one is worth. There is no rate in force to move and no insurer liability to revalue, so the sector's distinctive solvency channel is absent on both sides
- [ ] **TODO 6.INSURERS.B2B** — `Insurers B2.b` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.B3** — `Insurers B3` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.B4** — `Insurers B4` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.C1** — `Insurers C1` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.C2** — `Insurers C2` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.C2A** — `Insurers C2.a` MISSING — packages/kernel-rs/src/mechanisms/insurers.rs `InsurerMatching` builds its bid from its own `price_outlook`, which is a preference rather than a duration need, because no liability exists to match. No one-way demand for long paper reaches any book
- [ ] **TODO 6.INSURERS.C3** — `Insurers C3` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.C4** — `Insurers C4` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.C5** — `Insurers C5` MISSING — was OUT OF SCOPE on the ground that an insurer buys credit through the pools it subscribes to and the pool holds the mandate. the pools and the mandate machinery cited are the deleted TypeScript; nothing in packages/kernel-rs forces a sale on a downgrade (Ratings C1.a)
- [ ] **TODO 6.INSURERS.D1** — `Insurers D1` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.D2** — `Insurers D2` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.D3** — `Insurers D3` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.D4** — `Insurers D4` MISSING — was OUT OF SCOPE on the ground that the gap is hedged in the swap and futures books, "which exist". `irs` runs the generic loan-servicing mechanism and there is no futures book anywhere, so the reason the exemption rested on is not true of this engine
- [ ] **TODO 6.INSURERS.D4A** — `Insurers D4.a` MISSING — no insurer hedges anything, so no hedge turns a solvency gain into a cash call. packages/kernel-rs/src/mechanisms/derivative_layer.rs schedules such a call for a bilateral mark and no insurer holds a contract that reaches it
- [ ] **TODO 6.INSURERS.D5** — `Insurers D5` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.E1** — `Insurers E1` MISSING — there is no liability at all, so there is none without beneficiaries — the FORBID has nothing to refuse
- [ ] **TODO 6.INSURERS.E2** — `Insurers E2` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.E3** — `Insurers E3` MISSING — nothing stores a liability value, because nothing holds a liability. `insurers.rs present_value` is the read and has no caller
- [ ] **TODO 6.INSURERS.E4** — `Insurers E4` MISSING — `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing. No policy is written, no premium collected, no claim paid and no liability discounted
- [ ] **TODO 6.INSURERS.A1** — `Insurers A1` PARTIAL — `kinds::INSURER` is a named party with an account and rows, and packages/kernel-rs/src/mechanisms/insurers.rs `InsurerMatching` puts it in long books. It holds no liability to anybody, and `insurers` is imported by nothing

### 7. Currency — 21 missing, 7 partial

> **Required review before this block:** read the **Currency** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 818), then inspect `packages/kernel-rs/src/registry.rs`, `packages/kernel-rs/src/ledger.rs`, `packages/kernel-rs/src/mechanisms/currency.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 7.CURRENCY.A4** — `Currency A4` MISSING — a leg carries a `ccy` that nothing checks against the instrument it names, and settlement infers where a payment lands from the payee`s bank without reading a currency at all
- [ ] **TODO 7.CURRENCY.A5** — `Currency A5` MISSING — there is no seed, so there is no named set of currencies. packages/kernel-rs/src/registry.rs can hold one; nothing states it
- [ ] **TODO 7.CURRENCY.B4** — `Currency B4` MISSING — `SpotFx` proposes no instruction, so nothing is ever acquired from anybody at a price. packages/kernel-rs/src/mechanisms/currency.rs `short_of`/`MustBuy` is the shape and is dead code
- [ ] **TODO 7.CURRENCY.B5** — `Currency B5` MISSING — no bank runs a foreign position, because nothing proposes an FX trade for a bank to be the other side of
- [ ] **TODO 7.CURRENCY.C1** — `Currency C1` MISSING — `SpotFx` computes a rate and journals it. No FX trade is ever proposed, so no rate is cleared from anything that settled
- [ ] **TODO 7.CURRENCY.C3A** — `Currency C3.a` MISSING — packages/kernel-rs/src/mechanisms/currency.rs `Rates` is the single object the clause asks for and no production code enters that module at all, so no rate is read through it and no pair is read from anywhere else either
- [ ] **TODO 7.CURRENCY.C3B** — `Currency C3.b` MISSING — no pair clears, because packages/kernel-rs/src/mechanisms/spot_fx.rs proposes no trade. Nothing triangulates through a chosen currency and nothing makes one cheapest, so the outcome the clause protects is not being produced at all
- [ ] **TODO 7.CURRENCY.C4** — `Currency C4` MISSING — there is no numéraire read anywhere in packages/kernel-rs
- [ ] **TODO 7.CURRENCY.C4A** — `Currency C4.a` MISSING — value does not live in a numéraire, but only because there is no numéraire; the currency position is destroyed by settlement converting at par instead
- [ ] **TODO 7.CURRENCY.C5** — `Currency C5` MISSING — there is no rate in force. Settlement converts at one and nothing values a foreign position at all
- [ ] **TODO 7.CURRENCY.D1** — `Currency D1` MISSING — no rate is in force for a period and nothing values against one
- [ ] **TODO 7.CURRENCY.D1A** — `Currency D1.a` MISSING — no rate is in force for a period, so nothing can be valued at one and paid at another. packages/kernel-rs/src/prices.rs `write` refuses a second print for one (market, instrument) in one week, which is where a single rate would live once one is cleared
- [ ] **TODO 7.CURRENCY.D2** — `Currency D2` MISSING — there is no revaluation anywhere in packages/kernel-rs
- [ ] **TODO 7.CURRENCY.D2A** — `Currency D2.a` MISSING — nothing revalues a foreign position, so no difference reaches anybody's equity. No revaluation account exists on a currency issuer either, and packages/kernel-rs/src/mechanisms/firms.rs computes equity as a residual with no place to put one
- [ ] **TODO 7.CURRENCY.D2B** — `Currency D2.b` MISSING — every foreign position is unrevalued, which is what this FORBID calls money created or destroyed silently
- [ ] **TODO 7.CURRENCY.D3** — `Currency D3` MISSING — nothing revalues at all, so nothing can revalue in the wrong order. packages/kernel-rs/src/mechanisms/currency.rs holds no revaluation phase and no slot in the week is reserved for one
- [ ] **TODO 7.CURRENCY.D4** — `Currency D4` MISSING — nothing revalues and no family checks it
- [ ] **TODO 7.CURRENCY.E1** — `Currency E1` MISSING — no rate moves because somebody traded at it; nobody trades
- [ ] **TODO 7.CURRENCY.E2** — `Currency E2` MISSING — packages/kernel-rs/src/mechanisms/spot_fx.rs clears a rate from posted orders rather than computing one, so no parity or differential formula produces a rate. What is absent is the market itself: `currency.rs` is wired into no system, and a rate nothing trades cannot be checked against a formula either
- [ ] **TODO 7.CURRENCY.E2A** — `Currency E2.a` MISSING — no rate differential exists to be a reason for holding one currency over another, because no currency carries a rate. packages/kernel-rs/src/mechanisms/fx_forwards.rs computes a parity point and a basis and proposes no trade, so hedging costs nobody anything
- [ ] **TODO 7.CURRENCY.E4** — `Currency E4` MISSING — there is no flow to move a rate
- [ ] **TODO 7.CURRENCY.A3** — `Currency A3` PARTIAL — packages/kernel-rs/src/num.rs `MoneyAmount` carries a `CurrencyCode`, refuses `checked_add` across currencies and converts only against a named rate. It is not what the engine moves: packages/kernel-rs/src/ledger.rs settles `Units`, a bare magnitude whose currency is read off the instrument beside it
- [ ] **TODO 7.CURRENCY.A3A** — `Currency A3.a` PARTIAL — `MoneyAmount::checked_add` takes the currency along with the amount and refuses a mismatch, which is the shape the clause asks for. Every other arithmetic in the engine takes an `f64` and reads the currency from an instrument the caller passed separately, so the domain is checked by convention rather than by the type
- [ ] **TODO 7.CURRENCY.B1** — `Currency B1` PARTIAL — packages/kernel-rs/src/registry.rs `currency_of(region)` reads through the country (one fact, one writer). A party`s reporting in it is not built, and the `currency` module that would is imported by nothing
- [ ] **TODO 7.CURRENCY.B2** — `Currency B2` PARTIAL — packages/kernel-rs/src/register.rs holds a row per (party, money), so a foreign holding is a real position. it is not acquired by a trade — settlement converts at par on arrival
- [ ] **TODO 7.CURRENCY.C2** — `Currency C2` PARTIAL — packages/kernel-rs/src/mechanisms/currency.rs `Rates::of` inverts one print rather than storing two (Law 4). The store is dead code
- [ ] **TODO 7.CURRENCY.C3** — `Currency C3` PARTIAL — packages/kernel-rs/src/mechanisms/currency.rs `gap` measures the triangle and `arbitrage` bounds who closes it — the right shape, and C3.b`s no-triangulating-read is honoured by `Rates::of` answering None. All of it is dead code
- [ ] **TODO 7.CURRENCY.E3** — `Currency E3` PARTIAL — packages/kernel-rs/src/mechanisms/currency.rs has no written path and `Rates::of` refuses to invent one, which is the FORBID holding by absence. It holds in dead code

### 7. Spot FX — 13 missing, 5 partial

> **Required review before this block:** read the **Spot FX** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1577), then inspect `packages/kernel-rs/src/mechanisms/spot_fx.rs`, `packages/kernel-rs/src/mechanisms/currency.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 7.SPOT-FX.B3** — `Spot FX B3` MISSING — no portfolio allocator posts a currency order
- [ ] **TODO 7.SPOT-FX.B5** — `Spot FX B5` MISSING — no dealer participant posts a two-sided FX schedule or owns currency inventory
- [ ] **TODO 7.SPOT-FX.B5A** — `Spot FX B5.a` MISSING — packages/kernel-rs/src/mechanisms/spot_fx.rs `will_quote` is where a dealer would decline the flow it does not want, and no production code names it. Nothing quotes, so nothing is obliged either
- [ ] **TODO 7.SPOT-FX.B6** — `Spot FX B6` MISSING — no central-bank policy decision posts a bounded FX order
- [ ] **TODO 7.SPOT-FX.C2A** — `Spot FX C2.a` MISSING — packages/kernel-rs/src/mechanisms/spot_fx.rs clears each offered currency on its own book, so consistency across pairs is neither a constraint the solver carries nor a correction applied afterwards. `inconsistency` measures the gap and no production code names it
- [ ] **TODO 7.SPOT-FX.C3** — `Spot FX C3` MISSING — no dealer posts the two-sided schedules from which a bid-offer could emerge
- [ ] **TODO 7.SPOT-FX.C6** — `Spot FX C6` MISSING — the production world has no pair-keyed history from which zero-flow drift and one-way-flow movement can be measured
- [ ] **TODO 7.SPOT-FX.D1** — `Spot FX D1` MISSING — the settled counterparty positions are real, but no FX dealer participant fills a client and owns the resulting inventory
- [ ] **TODO 7.SPOT-FX.D2** — `Spot FX D2` MISSING — packages/kernel-rs/src/mechanisms/spot_fx.rs `square` names a counterparty but no production dealer calls it
- [ ] **TODO 7.SPOT-FX.D3** — `Spot FX D3` MISSING — packages/kernel-rs/src/mechanisms/spot_fx.rs can calculate inventory revaluation, but no production dealer carries that inventory
- [ ] **TODO 7.SPOT-FX.D4** — `Spot FX D4` MISSING — packages/kernel-rs/src/mechanisms/spot_fx.rs expresses a dealer inventory limit, but no production dealer consults it
- [ ] **TODO 7.SPOT-FX.F1** — `Spot FX F1` MISSING — ordinary goods settlement still pays in the buyer`s account money rather than applying one seller-money convention
- [ ] **TODO 7.SPOT-FX.F1B** — `Spot FX F1.b` MISSING — packages/kernel-rs/src/session.rs still chooses the buyer`s account instrument, so the settlement money changes with the buyer
- [ ] **TODO 7.SPOT-FX.A3** — `Spot FX A3` PARTIAL — packages/kernel-rs/src/mechanisms/currency.rs measures direct-versus-cross inconsistency, but production clears each offered currency independently and no bounded arbitrage order consumes the gap
- [ ] **TODO 7.SPOT-FX.C2** — `Spot FX C2` PARTIAL — packages/kernel-rs/src/mechanisms/spot_fx.rs clears each offered currency, but cross-pair consistency is neither a clearing constraint nor consumed by bounded arbitrage
- [ ] **TODO 7.SPOT-FX.C5** — `Spot FX C5` PARTIAL — packages/kernel-rs/src/mechanisms/spot_fx.rs uses one clearing rate for both reciprocal settlement legs and journals it, but the rate is not yet a pair-keyed production price used by valuation
- [ ] **TODO 7.SPOT-FX.D5** — `Spot FX D5` PARTIAL — packages/kernel-rs/src/ledger.rs settles a spot exchange only as reciprocal `Receipt::Fx` legs in two currencies, so every trade has two sides and each currency conserves by construction. The VERIFY itself is not taken: `spot_fx.rs position_after` reads the identity and nothing calls it, so dealer and client positions are never summed
- [ ] **TODO 7.SPOT-FX.E3** — `Spot FX E3` PARTIAL — packages/kernel-rs/src/mechanisms/currency.rs `arbitrage` bounds the arbitrageur by its own capital, which is the clause. It is dead code

### 7. Indices — 14 missing, 2 partial

> **Required review before this block:** read the **Indices** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2301), then inspect `packages/kernel-rs/src/mechanisms/benchmarks.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 7.INDICES.A1** — `Indices A1` MISSING — `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.A1A** — `Indices A1.a` MISSING — packages/kernel-rs/src/registry.rs is the one store of index definitions, so a definition is stable and in one place. It is not public: packages/kernel-rs/src/mechanisms/observer.rs publishes no definition, so nobody outside the kernel can reproduce a level
- [ ] **TODO 7.INDICES.B1** — `Indices B1` MISSING — `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.B2** — `Indices B2` MISSING — packages/kernel-rs/src/registry.rs `index` takes a constituent set at declaration and nothing adds to or removes from one, so a bond that matures stays in its basket and a line brought this week can never enter one. The set is fixed for the life of the world rather than changing as firms enter and bonds mature
- [ ] **TODO 7.INDICES.B2A** — `Indices B2.a` MISSING — nothing has been chained across a rebalance because no constituent set can change (B2). packages/kernel-rs/src/mechanisms/benchmarks.rs `PublishedIndices` computes the level against its base week each period and holds no chaining factor for one
- [ ] **TODO 7.INDICES.B3** — `Indices B3` MISSING — `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.B4** — `Indices B4` MISSING — `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.C1** — `Indices C1` MISSING — `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.C2** — `Indices C2` MISSING — `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.C2A** — `Indices C2.a` MISSING — `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.C3** — `Indices C3` MISSING — `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.C4** — `Indices C4` MISSING — `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.D5A** — `Indices D5.a` MISSING — packages/kernel-rs/src/mechanisms/benchmarks.rs stores no level and recomputes one from the definition each period, so nothing invents a history. There is no history either: the opening state is arbitrary, so every covariance measured against it is measured against that
- [ ] **TODO 7.INDICES.E3** — `Indices E3` MISSING — `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `mechanisms/benchmarks.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them
- [ ] **TODO 7.INDICES.D3** — `Indices D3` PARTIAL — packages/kernel-rs/src/mechanisms/benchmarks.rs `Fixes` publishes a weekly funding fixing from a cleared print and refuses anything else. one book of 1,546 prints per period, so there is almost never a fixing to publish
- [ ] **TODO 7.INDICES.D3A** — `Indices D3.a` PARTIAL — packages/kernel-rs/src/mechanisms/benchmarks.rs `Fixes` publishes a weekly funding fixing from a cleared print and refuses to publish from anything else, so a fixing is a read of transactions when there is one. No floating coupon fixes on it, because no instrument carries one

### 7. Cross-Border — 24 missing, 1 partial

> **Required review before this block:** read the **Cross-Border** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3968), then inspect `packages/kernel-rs/src/mechanisms/cross_border.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 7.CROSS-BORDER.A2** — `Cross-Border A2` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.A2A** — `Cross-Border A2.a` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.A3** — `Cross-Border A3` MISSING — a payment does NOT reach an account in the currency it was made in — `across` lands it in the payee`s bank`s money whatever that money is
- [ ] **TODO 7.CROSS-BORDER.A4** — `Cross-Border A4` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.B1** — `Cross-Border B1` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.B2** — `Cross-Border B2` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.B3** — `Cross-Border B3` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.B3A** — `Cross-Border B3.a` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.B4** — `Cross-Border B4` MISSING — nothing reconciles one region`s exports against another`s imports; the Flows family is NOT BUILT
- [ ] **TODO 7.CROSS-BORDER.C1** — `Cross-Border C1` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.C2** — `Cross-Border C2` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.C2A** — `Cross-Border C2.a` MISSING — no party borrows in a money it does not earn, so no rate move threatens anybody's solvency that way. The world has one currency and packages/kernel-rs/src/mechanisms/cross_border.rs strikes no foreign liability for one to move against
- [ ] **TODO 7.CROSS-BORDER.C3** — `Cross-Border C3` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.C4** — `Cross-Border C4` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.C5** — `Cross-Border C5` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.D3A** — `Cross-Border D3.a` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.D4** — `Cross-Border D4` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.D4A** — `Cross-Border D4.a` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.D6** — `Cross-Border D6` MISSING — see B4 — nothing sums the regions
- [ ] **TODO 7.CROSS-BORDER.E1** — `Cross-Border E1` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.E2** — `Cross-Border E2` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.E3** — `Cross-Border E3` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.E4** — `Cross-Border E4` MISSING — the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached
- [ ] **TODO 7.CROSS-BORDER.F1** — `Cross-Border F1` MISSING — the assembled world has one currency, so nothing about a region being open can be exercised
- [ ] **TODO 7.CROSS-BORDER.D3** — `Cross-Border D3` PARTIAL — `CrossBorder` derives the imbalance and reports it with a size rather than asserting it away, which is what D3 asks. no family checks it

### 8. M&A — 24 missing, 1 partial

> **Required review before this block:** read the **M&A** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3339), then inspect `packages/kernel-rs/src/mechanisms/control.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 8.M-A.A2** — `M&A A2` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.A3** — `M&A A3` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.A3A** — `M&A A3.a` MISSING — no bid is paid for in cash, so nothing has to be funded and no acquirer's leverage moves. packages/kernel-rs/src/mechanisms/control.rs opens a takeover process and proposes no instruction
- [ ] **TODO 8.M-A.A3B** — `M&A A3.b` MISSING — no bid is paid for in shares, so no acquirer's owners are diluted. packages/kernel-rs/src/mechanisms/equity.rs `apply` raises a count on an issue and no takeover reaches it
- [ ] **TODO 8.M-A.A4** — `M&A A4` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.A5** — `M&A A5` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.B1** — `M&A B1` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.B2** — `M&A B2` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.B2A** — `M&A B2.a` MISSING — no target's owners are asked, so none refuses and no bid fails. packages/kernel-rs/src/register.rs `votes_of_record` reads who could decide and no takeover puts anything to them
- [ ] **TODO 8.M-A.B3** — `M&A B3` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.B4** — `M&A B4` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.B5** — `M&A B5` MISSING — `Control` opens a takeover process and journals it. Nothing bids, nothing is accepted and nothing is refused, so a deal happens because a rule fired — which is what this FORBID names
- [ ] **TODO 8.M-A.C1** — `M&A C1` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.C2** — `M&A C2` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.C3** — `M&A C3` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.C4** — `M&A C4` MISSING — with one book of 1,546 printing per period there is no cheap firm to attract a bid, and nothing bids
- [ ] **TODO 8.M-A.D1** — `M&A D1` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.D2** — `M&A D2` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.D2A** — `M&A D2.a` MISSING — no acquirer's shares or bonds move on a bid, because no bid is made and neither instrument trades. The two prices that would disagree are not produced
- [ ] **TODO 8.M-A.D3** — `M&A D3` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.D4** — `M&A D4` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.D5** — `M&A D5` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.E2** — `M&A E2` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.E3** — `M&A E3` MISSING — `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached
- [ ] **TODO 8.M-A.A1** — `M&A A1` PARTIAL — packages/kernel-rs/src/mechanisms/control.rs `Control` reads an acquirer`s own valuation against a target`s price and opens a process. No price per share is agreed and no owner accepts — a decision missing inside a system that runs

### 8. Ratings — 15 missing, 9 partial

> **Required review before this block:** read the **Ratings** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 4033), then inspect `packages/kernel-rs/src/mechanisms/ratings.rs`, `packages/kernel-rs/src/mechanisms/second_opinion.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 8.RATINGS.A3** — `Ratings A3` MISSING — `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.A4** — `Ratings A4` MISSING — `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.B1** — `Ratings B1` MISSING — the probability of failing is written as a literal 0.0
- [ ] **TODO 8.RATINGS.B2** — `Ratings B2` MISSING — the loss given failure is written as a literal 0.0
- [ ] **TODO 8.RATINGS.B2A** — `Ratings B2.a` MISSING — packages/kernel-rs/src/mechanisms/ratings.rs grades a party and nothing grades an instrument, so an issue cannot differ from its issuer. Only one of the two ratings the clause requires exists
- [ ] **TODO 8.RATINGS.B3** — `Ratings B3` MISSING — `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.C4** — `Ratings C4` MISSING — `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.C5** — `Ratings C5` MISSING — `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.D1** — `Ratings D1` MISSING — `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.D2** — `Ratings D2` MISSING — `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.D3** — `Ratings D3` MISSING — `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.D4** — `Ratings D4` MISSING — `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.D5** — `Ratings D5` MISSING — `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.E2** — `Ratings E2` MISSING — `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.E3** — `Ratings E3` MISSING — `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts
- [ ] **TODO 8.RATINGS.A1** — `Ratings A1` PARTIAL — packages/kernel-rs/src/mechanisms/ratings.rs `Grading` writes a `standing::GRADE` per (house, name) and says it publicly. It is ordinal and published; an ungraded name is given one anyway
- [ ] **TODO 8.RATINGS.A2** — `Ratings A2` PARTIAL — `Grading` reads published income and its trend, and leverage as what a name owes against what it holds. Age, size and sector are not read
- [ ] **TODO 8.RATINGS.A5A** — `Ratings A5.a` PARTIAL — packages/kernel-rs/src/mechanisms/ratings.rs `Grading` stands each grade behind a named house, so two houses could disagree. No holder runs an assessment of its own: `repayment_of` keys one to a lender and nothing forms it, so the published opinion is the only one
- [ ] **TODO 8.RATINGS.C1** — `Ratings C1` PARTIAL — `agreed::MANDATE` carries the lowest grade a pool may hold and packages/kernel-rs/src/mechanisms/forced_sale.rs `ForcedSeller` reads a breach of it. it journals the breach and sells nothing
- [ ] **TODO 8.RATINGS.C1A** — `Ratings C1.a` PARTIAL — `agreed::MANDATE` carries the lowest grade a pool may hold and packages/kernel-rs/src/mechanisms/forced_sale.rs `ForcedSeller` reads a breach of it, so the boundary and the reader both exist. Nothing sells: the breach is journalled and no order is posted, so a downgrade moves nothing
- [ ] **TODO 8.RATINGS.C2** — `Ratings C2` PARTIAL — packages/kernel-rs/src/mechanisms/bank_capital.rs `BankCapital` weights an asset by `ratings::haircut(grade, 1.0)`, so a grade does consume capital. Nothing follows from the number (Banks Capital)
- [ ] **TODO 8.RATINGS.C3** — `Ratings C3` PARTIAL — `bank_capital::haircut(grade, …)` is per issuer credit rather than one number per instrument type, which is what C3 asks for, and it rises with every notch. Nothing pledges anything, so no haircut is ever applied to a borrowing: the module that would post it is imported by nothing
- [ ] **TODO 8.RATINGS.E1** — `Ratings E1` PARTIAL — C2 and C3 give a grade two consumers inside `BankCapital`. Neither changes what anybody does
- [ ] **TODO 8.RATINGS.E4** — `Ratings E4` PARTIAL — the distribution is a read over the standings, and every ungraded name is part of it at a stated grade rather than left out of the count

### 8. Reporting — 20 missing, 6 partial

> **Required review before this block:** read the **Reporting** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 4105), then inspect `packages/kernel-rs/src/mechanisms/reporting.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 8.REPORTING.A4A** — `Reporting A4.a` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.A5** — `Reporting A5` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.B1** — `Reporting B1` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.B2** — `Reporting B2` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.B3** — `Reporting B3` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.B4** — `Reporting B4` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.C4** — `Reporting C4` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.D1** — `Reporting D1` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.D2** — `Reporting D2` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.E1** — `Reporting E1` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.F1** — `Reporting F1` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.F2** — `Reporting F2` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.F3** — `Reporting F3` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.F4** — `Reporting F4` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.G1** — `Reporting G1` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.G3** — `Reporting G3` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.H1** — `Reporting H1` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.H2** — `Reporting H2` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.H3** — `Reporting H3` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.H4** — `Reporting H4` MISSING — The wired `reporting` row runs `mechanisms/reporting.rs::Publishes`: it publishes register-derived company accounts and stores estimates for banks that hold the issuer's paper. The typed guidance, restatement, consensus and surprise helpers beside that path are not called, and estimates are not published as journal events
- [ ] **TODO 8.REPORTING.A2** — `Reporting A2` PARTIAL — `Publishes` reads the equity account and the journal, so G2`s "the equity account`s movement over the fiscal period" is read off the record the events moved. What it reads is the account as it stands today rather than as it stood at the fiscal close, because a date-week has no inverse on the one calendar
- [ ] **TODO 8.REPORTING.A4** — `Reporting A4` PARTIAL — `reporting.asymmetry` = 45 days is the lag. Nothing is known privately in between, because nothing is known at all until it is said
- [ ] **TODO 8.REPORTING.C1** — `Reporting C1` PARTIAL — packages/kernel-rs/src/systems.rs wires `Publishes`, whose production path forms a holder-bank estimate from that bank's prior standing estimate and the company's newly published book value. It does not publish the estimate and does not consume guidance or the company's cleared operating markets
- [ ] **TODO 8.REPORTING.C2** — `Reporting C2` PARTIAL — packages/kernel-rs/src/mechanisms/reporting.rs stores each production estimate as a standing keyed by the named bank and company, and the standing store dates changes. It is not emitted publicly, so visibility is missing
- [ ] **TODO 8.REPORTING.C3** — `Reporting C3` PARTIAL — packages/kernel-rs/src/mechanisms/reporting.rs uses each bank's own prior estimate and `Parties::outlook_memory`, so histories can disagree. The result does not reach a share-book participant, so disagreement is not yet load-bearing
- [ ] **TODO 8.REPORTING.G2** — `Reporting G2` PARTIAL — packages/kernel-rs/src/mechanisms/reporting.rs publishes income as the movement of the equity account since the last report, which is the account`s own movement and no longer the residual`s. It does not decompose that movement into capital raised, distributions and what was earned, although each movement carries the `Moved` that says which it was

### 9. Central Bank — 26 missing, 4 partial

> **Required review before this block:** read the **Central Bank** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2931), then inspect `packages/kernel-rs/src/mechanisms/money.rs`, `packages/kernel-rs/src/mechanisms/bank_funding.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 9.CENTRAL-BANK.A2** — `Central Bank A2` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.A2C** — `Central Bank A2.c` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.A3** — `Central Bank A3` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.A4** — `Central Bank A4` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.B1** — `Central Bank B1` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.B1A** — `Central Bank B1.a` MISSING — no central bank decides anything, so nothing is weighed against a mandate. packages/kernel-rs/src/mechanisms/benchmarks.rs publishes price indices and no party reads one against a target, and no measure of capacity exists at all
- [ ] **TODO 9.CENTRAL-BANK.B3** — `Central Bank B3` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.B4** — `Central Bank B4` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.C1** — `Central Bank C1` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.C1A** — `Central Bank C1.a` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `OpenMarketPurchase` carries the size of a policy purchase and nothing constructs one, so no size is set by policy and none is set by an auction either
- [ ] **TODO 9.CENTRAL-BANK.C2** — `Central Bank C2` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.C2A** — `Central Bank C2.a` MISSING — packages/kernel-rs/src/ledger.rs `Leg::Mint` is how an issuer pays with money it makes, and packages/kernel-rs/src/mechanisms/bank_funding.rs uses it only for an advance. No purchase is paid for that way, because no purchase happens
- [ ] **TODO 9.CENTRAL-BANK.C3** — `Central Bank C3` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.C4** — `Central Bank C4` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.D2** — `Central Bank D2` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.D3B** — `Central Bank D3.b` MISSING — packages/kernel-rs/src/ledger.rs refuses a payment a balance cannot cover with `ShortOfMoney`, so no reserve account goes negative and none stands as an overdraft waiting to be repaid. The advance is struck instead, and it is collateralised rather than an overdraft
- [ ] **TODO 9.CENTRAL-BANK.E1** — `Central Bank E1` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.E3** — `Central Bank E3` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.E3A** — `Central Bank E3.a` MISSING — packages/kernel-rs/src/mechanisms/sovereign.rs `central_bank_remittance` nets an accrued coupon against operating cost, which is income rather than revaluation, and no production code names it. Nothing revalues a foreign position either, so neither figure exists to be separated
- [ ] **TODO 9.CENTRAL-BANK.E4** — `Central Bank E4` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.E5** — `Central Bank E5` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.F1** — `Central Bank F1` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.F2** — `Central Bank F2` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.F2A** — `Central Bank F2.a` MISSING — no peg is defended and no reserve is held to defend one, so nothing runs out and no peg breaks. packages/kernel-rs/src/mechanisms/spot_fx.rs proposes no trade, which is where a defence would have to happen
- [ ] **TODO 9.CENTRAL-BANK.F3** — `Central Bank F3` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.F4** — `Central Bank F4` MISSING — §31 still has no dedicated `works("central_bank", …)` policy system, policy-rate decision, full corridor or open-market operation. The collateralised penalty facility now exists inside `BankFunding`, but does not implement this clause
- [ ] **TODO 9.CENTRAL-BANK.A1** — `Central Bank A1` PARTIAL — packages/kernel-rs/src/instruments.rs makes the named central bank the issuer of reserves, while packages/kernel-rs/src/mechanisms/bank_funding.rs can mint and lend those reserves against collateral at a declared penalty. The broader policy-rate and corridor system remains absent
- [ ] **TODO 9.CENTRAL-BANK.A2A** — `Central Bank A2.a` PARTIAL — reserves are an instrument the central bank issued and banks hold, so that liability is a register read like any other. There is no currency line, the treasury banks nowhere in particular, and no deposit window exists
- [ ] **TODO 9.CENTRAL-BANK.A2B** — `Central Bank A2.b` PARTIAL — packages/kernel-rs/src/stores.rs keeps each facility advance as a dated `CENTRAL_BANK_FACILITY` row, so loans to banks are a book of rows. No sovereign paper, foreign reserve, claim on another central bank or swap-line draw is held
- [ ] **TODO 9.CENTRAL-BANK.D1** — `Central Bank D1` PARTIAL — packages/kernel-rs/src/mechanisms/bank_funding.rs implements a standing facility bounded by the borrowing bank's free eligible claim holdings and atomically encumbers the pledged units. It runs after the money-market session rather than as a seat in that session

### 9. Polity — 26 missing, 1 partial

> **Required review before this block:** read the **Polity** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3007), then inspect `packages/kernel-rs/src/mechanisms/polity.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 9.POLITY.A2** — `Polity A2` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.A2A** — `Polity A2.a` MISSING — packages/kernel-rs/src/mechanisms/polity.rs `Elections` opens a dated election and no platform is declared for anybody to differ from. With nothing on offer there is no decision for an electorate to take
- [ ] **TODO 9.POLITY.A3** — `Polity A3` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.B1** — `Polity B1` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.B1A** — `Polity B1.a` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.B2** — `Polity B2` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.B2B** — `Polity B2.b` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.B3** — `Polity B3` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.B4** — `Polity B4` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.C1** — `Polity C1` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.C2** — `Polity C2` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.C2A** — `Polity C2.a` MISSING — no coalition is formed, because no platform exists to measure distance between. Nothing reports a hung parliament and nothing repairs one either, so the standing mandate continues by default rather than as an outcome
- [ ] **TODO 9.POLITY.C3** — `Polity C3` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.C3A** — `Polity C3.a` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.C3B** — `Polity C3.b` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.C4** — `Polity C4` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.D1** — `Polity D1` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.D2** — `Polity D2` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.D3** — `Polity D3` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.D4** — `Polity D4` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.E1** — `Polity E1` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.E2** — `Polity E2` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.E3** — `Polity E3` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.E4** — `Polity E4` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.F3** — `Polity F3` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.F5** — `Polity F5` MISSING — The wired `polity` row runs `mechanisms/polity.rs::Elections`, which opens and journals a dated election process. Its platform, cell vote, seat, coalition, mandate, tax, transfer, monetary-decision and facility helpers are test-only: `Elections::run` calls none of them, and no mandate reaches a kernel store
- [ ] **TODO 9.POLITY.A1** — `Polity A1` PARTIAL — `parliament.seats` = 100 is declared `Kind::Policy`, `Owner::Constitution` — the one number no mechanism produces, as A1 asks. The rest of A1 waits on `polity`, which is imported by nothing

### 10. Derivative — 16 missing, 7 partial

> **Required review before this block:** read the **Derivative** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 964), then inspect `packages/kernel-rs/src/stores.rs`, `packages/kernel-rs/src/mechanisms/derivative_layer.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.DERIVATIVE.D1B** — `Derivative D1.b` MISSING — packages/kernel-rs/src/audit.rs `BilateralDerivativesAreZeroSum` checks that each contract resolves to two different live owners, which is the pairing and not the sum. No contract carries a value, so there is no pair of numbers to add to zero
- [ ] **TODO 10.DERIVATIVE.D2A** — `Derivative D2.a` MISSING — packages/kernel-rs/src/mechanisms/derivative_layer.rs `gross_notional` sums notionals and no production code names it, and nothing computes an exposure at all. With one of the two figures absent they cannot be told apart, and nothing reports either
- [ ] **TODO 10.DERIVATIVE.D3** — `Derivative D3` MISSING — `AgreementTerms::PriceForward` types its underlying as an instrument, but no production mechanism strikes that contract, so no live derivative can yet be checked against a price this world clears
- [ ] **TODO 10.DERIVATIVE.D3A** — `Derivative D3.a` MISSING — there is no underlying field, so the FORBID has nothing to refuse
- [ ] **TODO 10.DERIVATIVE.D4** — `Derivative D4` MISSING — no payoff function is carried on a contract; `derivative_layer.rs` variants are dead code
- [ ] **TODO 10.DERIVATIVE.D5** — `Derivative D5` MISSING — derivative terms now carry a typed settlement currency, and FX forwards carry both currencies, but a general derivative still has no typed payment-leg schedule with a currency on each leg
- [ ] **TODO 10.DERIVATIVE.D7** — `Derivative D7` MISSING — nothing strikes a derivative at a cleared level; `Derivatives` proposes nothing and opens no book
- [ ] **TODO 10.DERIVATIVE.D7A** — `Derivative D7.a` MISSING — nothing strikes a derivative, so no level is cleared from two willing sides and none is solved for either. `Derivatives` opens no book and proposes no instruction
- [ ] **TODO 10.DERIVATIVE.D7B** — `Derivative D7.b` MISSING — no contract is struck at all, so no par strike is found by a mechanism. packages/kernel-rs/src/mechanisms/derivative_layer.rs holds no door through which a fixed rate or spread could be cleared
- [ ] **TODO 10.DERIVATIVE.D8** — `Derivative D8` MISSING — nothing marks a contract after inception
- [ ] **TODO 10.DERIVATIVE.D8A** — `Derivative D8.a` MISSING — nothing marks a contract after inception, so no gain lands on one party and no loss on the other. The zero-sum family checks that a contract has two live sides and cannot check a sum nobody computes
- [ ] **TODO 10.DERIVATIVE.D9** — `Derivative D9` MISSING — `derivatives.collateral` is one of the three nouns with NO KERNEL HOME that `npm run world:runs` prints every run. `derivative_layer.rs initial_margin` and `variation` are dead code
- [ ] **TODO 10.DERIVATIVE.D9A** — `Derivative D9.a` MISSING — packages/kernel-rs/src/register.rs `pledge` is exactly the mechanism the clause names, taking units out of `free` while leaving them owned by the poster. No derivative posts anything, so the door is never used for one
- [ ] **TODO 10.DERIVATIVE.D10** — `Derivative D10` MISSING — no claim is ever filed against an estate, so an in-the-money party has nothing
- [ ] **TODO 10.DERIVATIVE.D10A** — `Derivative D10.a` MISSING — no reservation anywhere carries a term for the counterparty it would face, and no derivative is quoted at all. A weak dealer therefore loses no flow, and who a party faces changes nothing about what it will pay
- [ ] **TODO 10.DERIVATIVE.X2** — `Derivative X2` MISSING — `Derivatives` proposes no instruction, so no margin, premium or periodic payment leaves any account
- [ ] **TODO 10.DERIVATIVE.D1** — `Derivative D1` PARTIAL — packages/kernel-rs/src/mechanisms/derivative_layer.rs `agreed::DERIVATIVE` is a two-party agreement, so the two sides are named. Nothing makes it an asset to one and a liability to the other, because nothing marks a contract after inception, so neither side`s equity account is ever moved by what it is worth
- [ ] **TODO 10.DERIVATIVE.D2** — `Derivative D2` PARTIAL — a notional can sit in an agreement`s terms. `derivative_layer.rs gross_notional` is dead code, so D2.a`s distinction between notional and exposure is drawn nowhere
- [ ] **TODO 10.DERIVATIVE.D6** — `Derivative D6` PARTIAL — packages/kernel-rs/src/stores.rs `Agreements` carries an `until`, so a term can end. Payment dates on a leg are not represented, and `systems.rs` wires `derivative_layer::Derivatives`, but its production path does not perform this transition
- [ ] **TODO 10.DERIVATIVE.D6A** — `Derivative D6.a` PARTIAL — packages/kernel-rs/src/stores.rs `Agreements` carries an `until`, so a term ends on a stated date. No derivative leg carries a periodicity or a day count, so a periodic payment on one would have no convention to accrue against
- [ ] **TODO 10.DERIVATIVE.D11** — `Derivative D11` PARTIAL — packages/kernel-rs/src/stores.rs `Agreements::ends` removes the relation from both parties at once. `derivative_layer.rs close_out` is dead code
- [ ] **TODO 10.DERIVATIVE.D11A** — `Derivative D11.a` PARTIAL — packages/kernel-rs/src/stores.rs `Agreements::ends` removes the relation from both parties at once, so a termination is one event rather than two. No close-out value is stated on the terms or computed at the end, so what the terminated contract was worth is not recorded anywhere
- [ ] **TODO 10.DERIVATIVE.D12** — `Derivative D12` PARTIAL — an agreement is its own row and each derivative class has typed identity fields, so an underlying/reference can no longer be confused with a strike or currency. The store does not yet enforce economic-key uniqueness

### 10. Securities Lending — 22 missing, 1 partial

> **Required review before this block:** read the **Securities Lending** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1726), then inspect `packages/kernel-rs/src/mechanisms/securities_lending.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.SECURITIES-LENDING.A1** — `Securities Lending A1` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.A2** — `Securities Lending A2` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.A3** — `Securities Lending A3` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.A4** — `Securities Lending A4` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.A5** — `Securities Lending A5` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.A5A** — `Securities Lending A5.a` MISSING — no borrow fee clears, because no security is lent. packages/kernel-rs/src/mechanisms/securities_lending.rs `margin_over` and `worth_now` are named by no production code, and no book in a borrow is declared
- [ ] **TODO 10.SECURITIES-LENDING.A5B** — `Securities Lending A5.b` MISSING — packages/kernel-rs/src/mechanisms/securities_lending.rs `rebate` is the cash-collateral form of the same price and no production code names it. With no fee cleared there is no number for the two sides to see differently
- [ ] **TODO 10.SECURITIES-LENDING.B1** — `Securities Lending B1` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.B2** — `Securities Lending B2` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.B2A** — `Securities Lending B2.a` MISSING — nothing lends a security, so no mandate, collateral test or limit governs one. packages/kernel-rs/src/mechanisms/funds.rs `FundMandates` holds the constraint a lender would act under and no lending decision reads it
- [ ] **TODO 10.SECURITIES-LENDING.B3** — `Securities Lending B3` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.B4** — `Securities Lending B4` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.C1** — `Securities Lending C1` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.C2** — `Securities Lending C2` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.C2A** — `Securities Lending C2.a` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.C3** — `Securities Lending C3` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.C5** — `Securities Lending C5` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.D1** — `Securities Lending D1` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.D2** — `Securities Lending D2` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.D2A** — `Securities Lending D2.a` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.D3** — `Securities Lending D3` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.E3** — `Securities Lending E3` MISSING — `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller
- [ ] **TODO 10.SECURITIES-LENDING.E2** — `Securities Lending E2` PARTIAL — the lender`s exposure and the borrower`s title would be two reads of one security; with no loan ever struck there is nothing to double-count. the sum-to-issued check that would catch it does not exist

### 10. Prime Brokerage — 25 missing, 1 partial

> **Required review before this block:** read the **Prime Brokerage** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1787), then inspect `packages/kernel-rs/src/mechanisms/prime_brokerage.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.PRIME-BROKERAGE.A2** — `Prime Brokerage A2` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.A3** — `Prime Brokerage A3` MISSING — packages/kernel-rs/src/mechanisms/prime_brokerage.rs `Broking` gives each client one broker by `brokers[n % brokers.len()]`, and says so — so A3`s blind spot, which E3 depends on, cannot arise
- [ ] **TODO 10.PRIME-BROKERAGE.A4** — `Prime Brokerage A4` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.B1** — `Prime Brokerage B1` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.B1A** — `Prime Brokerage B1.a` MISSING — no broker lends to a client, so leverage is not a loan from anybody. packages/kernel-rs/src/mechanisms/hedge_funds.rs `Levering` journals a ratio and proposes no instruction, which makes it a property of the client rather than a liability to a named lender
- [ ] **TODO 10.PRIME-BROKERAGE.B2** — `Prime Brokerage B2` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.B3** — `Prime Brokerage B3` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.B4** — `Prime Brokerage B4` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.B5** — `Prime Brokerage B5` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.C1** — `Prime Brokerage C1` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.C1A** — `Prime Brokerage C1.a` MISSING — no margin is computed for a client book, so nothing accounts for offsetting legs within one. packages/kernel-rs/src/mechanisms/prime_brokerage.rs `Broking` values a book and proposes no requirement against it
- [ ] **TODO 10.PRIME-BROKERAGE.C1B** — `Prime Brokerage C1.b` MISSING — no broker decides anything about a client's margin, because none is set. With no decision there is nothing for a client to rely on and nothing for a broker to change
- [ ] **TODO 10.PRIME-BROKERAGE.C2** — `Prime Brokerage C2` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.C3** — `Prime Brokerage C3` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.C3A** — `Prime Brokerage C3.a` MISSING — no margin call is made, so nothing forces a client to sell. packages/kernel-rs/src/mechanisms/forced_sale.rs `ForcedSeller` posts such a sale into a book that must clear, and no broker sends a client to it
- [ ] **TODO 10.PRIME-BROKERAGE.C4** — `Prime Brokerage C4` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.C4A** — `Prime Brokerage C4.a` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.C5** — `Prime Brokerage C5` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.D1** — `Prime Brokerage D1` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.D2** — `Prime Brokerage D2` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.D3** — `Prime Brokerage D3` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.D4** — `Prime Brokerage D4` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.E1** — `Prime Brokerage E1` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.E2** — `Prime Brokerage E2` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.E3** — `Prime Brokerage E3` MISSING — `Broking` values a client book (one of the three inline units-times-price copies), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated
- [ ] **TODO 10.PRIME-BROKERAGE.A1** — `Prime Brokerage A1` PARTIAL — `agreed::PRIME_BROKERAGE` is a named bank and a named client, and packages/kernel-rs/src/mechanisms/prime_brokerage.rs `Broking` reads it and is wired. Nothing in that production path ends it

### 10. Derivative Layer — 36 missing, 2 partial

> **Required review before this block:** read the **Derivative Layer** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1858), then inspect `packages/kernel-rs/src/mechanisms/derivative_layer.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.DERIVATIVE-LAYER.A1** — `Derivative Layer A1` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.A2** — `Derivative Layer A2` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.A4** — `Derivative Layer A4` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.B1** — `Derivative Layer B1` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.B3** — `Derivative Layer B3` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.B3A** — `Derivative Layer B3.a` MISSING — no contract is struck, so nothing offsets anything and no exposure is carried against two counterparties. packages/kernel-rs/src/stores.rs `Agreements` keys a relation to its two parties, which is where the second exposure would be visible
- [ ] **TODO 10.DERIVATIVE-LAYER.B4** — `Derivative Layer B4` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.C1** — `Derivative Layer C1` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.C1A** — `Derivative Layer C1.a` MISSING — packages/kernel-rs/src/mechanisms/derivative_layer.rs `net_against` nets a pair and no production code names it, and `gross_notional` is unreached beside it. Neither figure is produced, so the distance between them is not there to be seen
- [ ] **TODO 10.DERIVATIVE-LAYER.C2** — `Derivative Layer C2` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.C2A** — `Derivative Layer C2.a` MISSING — packages/kernel-rs/src/mechanisms/derivative_layer.rs `novate` and `through_the_house` move a contract to a clearing house and no production code names either. Nothing is concentrated in a house, so no house solvency matters to anybody
- [ ] **TODO 10.DERIVATIVE-LAYER.C3** — `Derivative Layer C3` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.C3A** — `Derivative Layer C3.a` MISSING — packages/kernel-rs/src/mechanisms/derivative_layer.rs `initial_margin` sizes a posting and no production code names it. Nothing is posted, so no margin sits on a member's sheet as an asset at the house and nothing enters a leverage denominator
- [ ] **TODO 10.DERIVATIVE-LAYER.C3B** — `Derivative Layer C3.b` MISSING — packages/kernel-rs/src/mechanisms/derivative_layer.rs `cover_one` sizes the fund against the largest member and `contribution_of` splits it pro rata, and no production code names either. No default fund is held, trued up or refunded
- [ ] **TODO 10.DERIVATIVE-LAYER.C4** — `Derivative Layer C4` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.C4A** — `Derivative Layer C4.a` MISSING — no member defaults at a house, so no survivor writes down a mutualised loss against its equity. packages/kernel-rs/src/mechanisms/derivative_layer.rs `cut_to` is the waterfall step that would take it and no production code names it
- [ ] **TODO 10.DERIVATIVE-LAYER.C4B** — `Derivative Layer C4.b` MISSING — packages/kernel-rs/src/mechanisms/derivative_layer.rs `close_out` is where the defaulter's net position at one house becomes the loss to absorb, and no production code names it. Nothing pays a survivor in full and no margin is returned
- [ ] **TODO 10.DERIVATIVE-LAYER.C4C** — `Derivative Layer C4.c` MISSING — packages/kernel-rs/src/mechanisms/derivative_layer.rs `claim_on_the_estate` files what the house could not cover as an unsecured claim, and no production code names it. No close-out reaches an estate at all, ranked or otherwise
- [ ] **TODO 10.DERIVATIVE-LAYER.C4D** — `Derivative Layer C4.d` MISSING — nothing records a waterfall round, because no waterfall runs. packages/kernel-rs/src/journal.rs is where who defaulted, what each line paid and what went unfunded would be reported
- [ ] **TODO 10.DERIVATIVE-LAYER.C5** — `Derivative Layer C5` MISSING — packages/kernel-rs/src/mechanisms/derivative_layer.rs `Waterfall` is finite and enumerable by construction, and nothing reaches it. Running past the end of a waterfall is unreachable because no waterfall runs at all
- [ ] **TODO 10.DERIVATIVE-LAYER.D1** — `Derivative Layer D1` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.D2B** — `Derivative Layer D2.b` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.D3** — `Derivative Layer D3` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.D4** — `Derivative Layer D4` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.D4A** — `Derivative Layer D4.a` MISSING — no margin call is met, so none forces a sale. packages/kernel-rs/src/mechanisms/forced_sale.rs `ForcedSeller` carries that channel for its other causes and no derivative call reaches it
- [ ] **TODO 10.DERIVATIVE-LAYER.D5** — `Derivative Layer D5` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.E1** — `Derivative Layer E1` MISSING — `derivative_layer.rs admitted`/`cut_to`/`struck_beyond_capacity` are the capacity limit and have no caller
- [ ] **TODO 10.DERIVATIVE-LAYER.E2** — `Derivative Layer E2` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.E3** — `Derivative Layer E3` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.E4** — `Derivative Layer E4` MISSING — see E1: what was struck beyond what members could margin is exactly `struck_beyond_capacity`, which nothing calls
- [ ] **TODO 10.DERIVATIVE-LAYER.F1** — `Derivative Layer F1` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.F2** — `Derivative Layer F2` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.F3** — `Derivative Layer F3` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.F4** — `Derivative Layer F4` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.G2** — `Derivative Layer G2` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.G4** — `Derivative Layer G4` MISSING — `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home
- [ ] **TODO 10.DERIVATIVE-LAYER.D2** — `Derivative Layer D2` PARTIAL — packages/kernel-rs/src/mechanisms/derivative_layer.rs compares consecutive bilateral marks and schedules the change as a dated call; the agreement still does not name the settlement currency
- [ ] **TODO 10.DERIVATIVE-LAYER.D2A** — `Derivative Layer D2.a` PARTIAL — packages/kernel-rs/src/mechanisms/derivative_layer.rs schedules the change in a bilateral mark as a dated call, so the amount and its date exist. No instruction is proposed for it and the agreement names no settlement currency, so no money leaves one account for another

### 10. CDS — 33 missing, 0 partial

> **Required review before this block:** read the **CDS** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 1966), then inspect `packages/kernel-rs/src/mechanisms/cds.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.CDS.A1** — `CDS A1` MISSING — `Protection` now strikes a typed two-party CDS agreement naming its reference, spread, tenor and settlement currency. It still selects terms without a real CDS order book and does not schedule premium or protection cash flows, so the specified contract is not yet operating
- [ ] **TODO 10.CDS.A1A** — `CDS A1.a` MISSING — packages/kernel-rs/src/mechanisms/cds.rs `Protection` can name a reference entity, and no default event is defined for it. packages/kernel-rs/src/mechanisms/sovereign.rs `Missed` is the only thing in the tree that decides a payment was not made, and no contract reads it
- [ ] **TODO 10.CDS.A1B** — `CDS A1.b` MISSING — no payoff is computed on an event, because no event triggers one. Par minus recovery needs a recovery, and nothing recovers anything: no estate has ever paid a claim
- [ ] **TODO 10.CDS.A1C** — `CDS A1.c` MISSING — no running spread clears. packages/kernel-rs/src/mechanisms/cds.rs journals a premium from a declared number rather than from a book, and no book in protection is declared anywhere
- [ ] **TODO 10.CDS.A1D** — `CDS A1.d` MISSING — no contract carries a tenor, so there is no curve of them and no term structure of credit anywhere in the tree. packages/kernel-rs/src/stores.rs `Agreements` carries an `until`, which is where a tenor would sit
- [ ] **TODO 10.CDS.A2** — `CDS A2` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.A3** — `CDS A3` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.A4** — `CDS A4` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.A5** — `CDS A5` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.A5A** — `CDS A5.a` MISSING — no index of reference names exists, so nothing is fixed at a roll and no weight settles out of a line on an event. packages/kernel-rs/src/registry.rs holds every index definition and none of them is a credit basket
- [ ] **TODO 10.CDS.A5B** — `CDS A5.b` MISSING — no index book in protection is declared and no single-name book either, so there are no two prints for a basis to sit between. Nothing measures one and nothing sets one
- [ ] **TODO 10.CDS.B1** — `CDS B1` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.B1A** — `CDS B1.a` MISSING — no bank hedges a loan with protection, because no protection is bought. packages/kernel-rs/src/mechanisms/bank_capital.rs weights a loan by the grade of its borrower and nothing offsets that weight with a hedge
- [ ] **TODO 10.CDS.B2** — `CDS B2` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.B2A** — `CDS B2.a` MISSING — nobody sells protection, so nobody is short a jump. packages/kernel-rs/src/mechanisms/bank_capital.rs is where such a position would consume capital and no contract reaches it, and no margin is posted against one either
- [ ] **TODO 10.CDS.B3** — `CDS B3` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.B4** — `CDS B4` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.B5** — `CDS B5` MISSING — packages/kernel-rs/src/mechanisms/cds.rs Protection crosses two OWN_VIEW standings and refuses to clear on one, but every holder of a view is a lender to the name, so both sides are hedging and the speculative participant is on neither. NOT GUARDED, and it cannot be: who is at a book is a property of the parties, not an absence in the text
- [ ] **TODO 10.CDS.C1** — `CDS C1` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.C3** — `CDS C3` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.C3A** — `CDS C3.a` MISSING — neither book prints, so there is no protection level and no cash spread for a basis to be the distance between. Nothing computes one at any tenor and nothing writes one either
- [ ] **TODO 10.CDS.C3B** — `CDS C3.b` MISSING — no basis is measured, so none can persist or be seen to. packages/kernel-rs/src/journal.rs is where such a reading would be published every period
- [ ] **TODO 10.CDS.C4** — `CDS C4` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.D1** — `CDS D1` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.D2** — `CDS D2` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.D2B** — `CDS D2.b` MISSING — no contract triggers, so none holds open through a workout. packages/kernel-rs/src/stores.rs `Processes` is where such a workout would run and packages/kernel-rs/src/mechanisms/estate.rs would settle the true-up, and neither is reached from a contract
- [ ] **TODO 10.CDS.D3** — `CDS D3` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.D4** — `CDS D4` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.D5** — `CDS D5` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.E1** — `CDS E1` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.E2** — `CDS E2` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.E3** — `CDS E3` MISSING — the `cds` system runs `mechanisms/cds.rs::Protection`, which is wired and can strike a typed CDS agreement from opposing views after an estate recovery exists; it does not schedule premiums, observe later reference events, or propose payouts, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears
- [ ] **TODO 10.CDS.E4** — `CDS E4` MISSING — `Protection` proposes no instruction, so a seller`s ability to pay is never part of anything

### 10. IRS — 26 missing, 0 partial

> **Required review before this block:** read the **IRS** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2052), then inspect `packages/kernel-rs/src/mechanisms/irs.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.IRS.A1** — `IRS A1` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.A1A** — `IRS A1.a` MISSING — packages/kernel-rs/src/mechanisms/benchmarks.rs `Fixes` publishes a weekly funding fixing only from a cleared print, so a transacted reference rate is possible. No swap names one, because no swap is struck
- [ ] **TODO 10.IRS.A1B** — `IRS A1.b` MISSING — no periodic exchange happens. packages/kernel-rs/src/mechanisms/lending.rs `Servicing` pays what a schedule says and no swap files a schedule, so nothing nets a fixed leg against a floating one
- [ ] **TODO 10.IRS.A1C** — `IRS A1.c` MISSING — no fixed rate is cleared. Nothing opens a book in a swap, so the level that would make one worth zero at inception is neither found by a market nor solved for
- [ ] **TODO 10.IRS.A1D** — `IRS A1.d` MISSING — no swap carries a notional at all, so nothing distinguishes one from a loan. packages/kernel-rs/src/stores.rs `AgreementTerms` is where a notional that never moves would be stated
- [ ] **TODO 10.IRS.A2** — `IRS A2` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.A3** — `IRS A3` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.B1** — `IRS B1` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.B2** — `IRS B2` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.B2A** — `IRS B2.a` MISSING — no party hedges a long-dated liability with a swap, so no one-way demand exists at any tenor. packages/kernel-rs/src/mechanisms/insurers.rs holds the liabilities that would create it and posts only in cash books
- [ ] **TODO 10.IRS.B3** — `IRS B3` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.B4** — `IRS B4` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.B5** — `IRS B5` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.C1** — `IRS C1` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.C1A** — `IRS C1.a` MISSING — there is no swap curve, because no swap clears. packages/kernel-rs/src/mechanisms/benchmarks.rs `curve_at` fits observed points and labels what it interpolated, and it has no swap prints to read
- [ ] **TODO 10.IRS.C2** — `IRS C2` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.C3** — `IRS C3` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.C3A** — `IRS C3.a` MISSING — no swap rate exists to sit beside a sovereign yield, so no spread between them is read. Nothing writes one either, which is the half of the clause that holds
- [ ] **TODO 10.IRS.C4** — `IRS C4` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.D1** — `IRS D1` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.D2** — `IRS D2` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.D3** — `IRS D3` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.D3A** — `IRS D3.a` MISSING — no swap is marked and no variation margin moves, so no hedger receives cash against an unrealised loss elsewhere. packages/kernel-rs/src/mechanisms/derivative_layer.rs schedules such a call for bilateral marks and no swap reaches it
- [ ] **TODO 10.IRS.D4** — `IRS D4` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark
- [ ] **TODO 10.IRS.E1** — `IRS E1` MISSING — the `irs` system runs `mechanisms/lending.rs Servicing`, the same mechanism `lending` runs — it pays what fell due on a schedule out of the payer`s account. That is principal and interest moving, which is what this FORBID calls a loan
- [ ] **TODO 10.IRS.E2** — `IRS E2` MISSING — `irs` runs `mechanisms/lending.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark

### 10. FX Forwards — 26 missing, 2 partial

> **Required review before this block:** read the **FX Forwards** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2113), then inspect `packages/kernel-rs/src/mechanisms/fx_forwards.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.FX-FORWARDS.A1B** — `FX Forwards A1.b` MISSING — no maturity leg is scheduled, so neither notional moves at the end. packages/kernel-rs/src/mechanisms/lending.rs `Servicing` pays what a schedule holds and no forward files one
- [ ] **TODO 10.FX-FORWARDS.A1C** — `FX Forwards A1.c` MISSING — no forward book is declared, so no forward rate clears. packages/kernel-rs/src/mechanisms/fx_forwards.rs computes a parity point instead, which is a formula rather than a price
- [ ] **TODO 10.FX-FORWARDS.A2** — `FX Forwards A2` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.A3** — `FX Forwards A3` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.A4** — `FX Forwards A4` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.B1** — `FX Forwards B1` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.B2** — `FX Forwards B2` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.B2A** — `FX Forwards B2.a` MISSING — packages/kernel-rs/src/mechanisms/fx_forwards.rs strikes at the parity point it computed, which is the identity applied to produce the rate rather than a consequence of somebody arbitraging it. `implied_funding` would read the differential back out and no production code names it
- [ ] **TODO 10.FX-FORWARDS.B2B** — `FX Forwards B2.b` MISSING — packages/kernel-rs/src/mechanisms/fx_forwards.rs `observed_basis` measures the gap and no production code names it, and nothing charges an arbitrageur for the balance sheet it would use. No constraint therefore lets a basis persist
- [ ] **TODO 10.FX-FORWARDS.B3** — `FX Forwards B3` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.B3A** — `FX Forwards B3.a` MISSING — no basis widens with anything, because packages/kernel-rs/src/mechanisms/fx_forwards.rs reads no funding condition in either currency. Scarcity in one of them changes no forward term
- [ ] **TODO 10.FX-FORWARDS.B3B** — `FX Forwards B3.b` MISSING — there are two bases and two parity points — `currency.rs covered`/`basis` and `fx_forwards.rs parity`/`basis`, the same expressions under two names, each documented as the only one
- [ ] **TODO 10.FX-FORWARDS.B4** — `FX Forwards B4` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.C1** — `FX Forwards C1` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.C1A** — `FX Forwards C1.a` MISSING — no cross-currency swap exists, so nothing inherits two curves. packages/kernel-rs/src/mechanisms/fx_forwards.rs strikes a single-dated forward and neither currency has a curve for it to inherit
- [ ] **TODO 10.FX-FORWARDS.C2** — `FX Forwards C2` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.C3** — `FX Forwards C3` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.C4** — `FX Forwards C4` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.D1** — `FX Forwards D1` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.D2** — `FX Forwards D2` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.D2A** — `FX Forwards D2.a` MISSING — packages/kernel-rs/src/mechanisms/fx_forwards.rs `roll_cost` is what rolling a hedge would cost and no production code names it. No hedge matures, so none is rolled and no recurring demand or cost follows
- [ ] **TODO 10.FX-FORWARDS.D3** — `FX Forwards D3` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.D4** — `FX Forwards D4` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.E2** — `FX Forwards E2` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.E3** — `FX Forwards E3` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.E4** — `FX Forwards E4` MISSING — `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged
- [ ] **TODO 10.FX-FORWARDS.A1** — `FX Forwards A1` PARTIAL — production now strikes only between opposite currency needs and records both typed currencies, rate, amount, tenor and counterparties. The terms still come from a parity reservation rather than a real forward book, and maturity legs are not scheduled
- [ ] **TODO 10.FX-FORWARDS.A1A** — `FX Forwards A1.a` PARTIAL — packages/kernel-rs/src/mechanisms/fx_forwards.rs strikes against a spot rate and a tenor, so the underlying is the rate at a future date. No spot rate ever clears, so what it references is a parity reservation rather than an observed market

### 10. Commodity Futures — 26 missing, 0 partial

> **Required review before this block:** read the **Commodity Futures** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2183), then inspect `packages/kernel-rs/src/mechanisms/commodities.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.COMMODITY-FUTURES.A1** — `Commodity Futures A1` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.A1A** — `Commodity Futures A1.a` MISSING — packages/kernel-rs/src/mechanisms/commodities.rs `Grade` names a quality and a place, so a deliverable is expressible. No future carries one, because packages/kernel-rs/src/mechanisms/commodities.rs `may_list` is named by no production code and nothing is listed
- [ ] **TODO 10.COMMODITY-FUTURES.A1B** — `Commodity Futures A1.b` MISSING — packages/kernel-rs/src/mechanisms/commodities.rs `at_expiry` decides between delivery and cash settlement and no production code names it. Nothing expires, so neither branch is ever taken
- [ ] **TODO 10.COMMODITY-FUTURES.A1C** — `Commodity Futures A1.c` MISSING — no book in a future is declared, so no futures price clears. The only commodity print is the physical one packages/kernel-rs/src/session.rs writes for the spot good
- [ ] **TODO 10.COMMODITY-FUTURES.A1D** — `Commodity Futures A1.d` MISSING — no contract exists to carry a fixed quantity, so size is never counted in contracts. packages/kernel-rs/src/instruments.rs `UnitId` is where that unit would be declared
- [ ] **TODO 10.COMMODITY-FUTURES.A2** — `Commodity Futures A2` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.A3** — `Commodity Futures A3` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.A4** — `Commodity Futures A4` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.B1** — `Commodity Futures B1` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.B2** — `Commodity Futures B2` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.B3** — `Commodity Futures B3` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.B3A** — `Commodity Futures B3.a` MISSING — nothing rolls, because nothing expires. packages/kernel-rs/src/mechanisms/commodities.rs `against_carry` is what the roll would cost or earn and no production code names it, so an investor's return has no roll in it
- [ ] **TODO 10.COMMODITY-FUTURES.B4** — `Commodity Futures B4` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.B5** — `Commodity Futures B5` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.C1** — `Commodity Futures C1` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.C1A** — `Commodity Futures C1.a` MISSING — packages/kernel-rs/src/mechanisms/commodities.rs `full_carry` is what buying, storing and financing costs and no production code names it. With no forward price there is nothing for a carry trade to bite on and no contango to arbitrage
- [ ] **TODO 10.COMMODITY-FUTURES.C1B** — `Commodity Futures C1.b` MISSING — no forward price exists, so it can be neither above nor below spot. The asymmetry the clause turns on needs two prices and there is one
- [ ] **TODO 10.COMMODITY-FUTURES.C2** — `Commodity Futures C2` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.C3** — `Commodity Futures C3` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.C4** — `Commodity Futures C4` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.C4A** — `Commodity Futures C4.a` MISSING — packages/kernel-rs/src/mechanisms/commodities.rs `can_converge` asks whether the deliverable can actually be delivered and no production code names it. Nothing converges and nothing is made to, so the clause holds over an empty set
- [ ] **TODO 10.COMMODITY-FUTURES.D1** — `Commodity Futures D1` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.D2** — `Commodity Futures D2` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.D3** — `Commodity Futures D3` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.D4** — `Commodity Futures D4` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered
- [ ] **TODO 10.COMMODITY-FUTURES.E2** — `Commodity Futures E2` MISSING — `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered

### 10. Hedge Funds — 23 missing, 3 partial

> **Required review before this block:** read the **Hedge Funds** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2744), then inspect `packages/kernel-rs/src/mechanisms/hedge_funds.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.HEDGE-FUNDS.A1** — `Hedge Funds A1` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.A3** — `Hedge Funds A3` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.A4** — `Hedge Funds A4` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.B1** — `Hedge Funds B1` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.B1A** — `Hedge Funds B1.a` MISSING — packages/kernel-rs/src/mechanisms/hedge_funds.rs `Levering` journals a ratio and proposes no instruction, so leverage is a figure about the fund rather than a loan from a named lender. No borrowing agreement is ever struck for one
- [ ] **TODO 10.HEDGE-FUNDS.B2** — `Hedge Funds B2` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.B3** — `Hedge Funds B3` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.B4** — `Hedge Funds B4` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.B5** — `Hedge Funds B5` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.C1** — `Hedge Funds C1` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.C2** — `Hedge Funds C2` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.C3** — `Hedge Funds C3` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.C4** — `Hedge Funds C4` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D1** — `Hedge Funds D1` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D2** — `Hedge Funds D2` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D3** — `Hedge Funds D3` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D4** — `Hedge Funds D4` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D4A** — `Hedge Funds D4.a` MISSING — no margin call, no forced sale and no mark feed each other, because none of the three happens to a fund. Nothing stands in for the loop either, so the absence is of the mechanism rather than of a parameter
- [ ] **TODO 10.HEDGE-FUNDS.D5** — `Hedge Funds D5` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D5A** — `Hedge Funds D5.a` MISSING — no fund carries a gate or a notice period, because no investor can redeem. packages/kernel-rs/src/stores.rs `AgreementTerms` is where such a term would be typed and no subscription agreement states one
- [ ] **TODO 10.HEDGE-FUNDS.D6** — `Hedge Funds D6` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.D7** — `Hedge Funds D7` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.E2** — `Hedge Funds E2` MISSING — `hedge_funds` runs `mechanisms/hedge_funds.rs Levering`, which journals and proposes nothing, and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable. Nothing borrows, nothing is margin-called and no loss chain runs
- [ ] **TODO 10.HEDGE-FUNDS.A2** — `Hedge Funds A2` PARTIAL — `agreed::SUBSCRIPTION` gives investors a share count they could redeem. Nothing redeems (Fund Shares C2), and `systems.rs` wires `hedge_funds::Levering` and its `Liquidity` participant, but the production path does not call the helper required by this clause
- [ ] **TODO 10.HEDGE-FUNDS.A5** — `Hedge Funds A5` PARTIAL — packages/kernel-rs/src/mechanisms/hedge_funds.rs `Liquidity` declares `Carrying::Market`, so what a fund buys is valued at the cleared print and its equity moves when the print does. Continuously is still a week, and a line whose book did not clear carries its last level
- [ ] **TODO 10.HEDGE-FUNDS.E3** — `Hedge Funds E3` PARTIAL — packages/kernel-rs/src/mechanisms/mortality.rs `Failing` ceases any party whose equity is below zero, a fund included. ceasing does nothing — it keeps what it held and its investors lose nothing

### 10. Private Equity — 25 missing, 1 partial

> **Required review before this block:** read the **Private Equity** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 2798), then inspect `packages/kernel-rs/src/mechanisms/private_equity.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 10.PRIVATE-EQUITY.A1** — `Private Equity A1` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.A2A** — `Private Equity A2.a` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.A3** — `Private Equity A3` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.A4** — `Private Equity A4` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.A5** — `Private Equity A5` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.B1** — `Private Equity B1` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.B2** — `Private Equity B2` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.B2A** — `Private Equity B2.a` MISSING — no buyout debt is struck, so nothing sits on a target rather than on the fund. packages/kernel-rs/src/mechanisms/private_equity.rs `sources_and_uses` is where the two sides of a deal would be placed and no production code names it
- [ ] **TODO 10.PRIVATE-EQUITY.B2B** — `Private Equity B2.b` MISSING — no lender decides whether a buyout happens, because no buyout is financed. packages/kernel-rs/src/mechanisms/private_equity.rs `Calling` schedules capital calls and never asks a credit market for anything
- [ ] **TODO 10.PRIVATE-EQUITY.B3** — `Private Equity B3` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.B4** — `Private Equity B4` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.B5** — `Private Equity B5` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.C1** — `Private Equity C1` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.C2** — `Private Equity C2` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.C3** — `Private Equity C3` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.C4** — `Private Equity C4` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.C5** — `Private Equity C5` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.D1** — `Private Equity D1` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.D2** — `Private Equity D2` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.D3** — `Private Equity D3` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.D4** — `Private Equity D4` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.D4A** — `Private Equity D4.a` MISSING — no distribution is ever made, so an investor cannot be owing a call and receiving nothing at the same time. packages/kernel-rs/src/mechanisms/private_equity.rs `route_calls` is the other half of that pairing and no production code names it
- [ ] **TODO 10.PRIVATE-EQUITY.D5** — `Private Equity D5` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.E1** — `Private Equity E1` MISSING — `private_equity` runs `mechanisms/private_equity.rs::Calling`, which is wired and schedules capital calls under typed commitments. Its buyout, marking, recapitalisation and exit helpers are not called by `Calling::run`; no buyout happens and nothing exits
- [ ] **TODO 10.PRIVATE-EQUITY.E3** — `Private Equity E3` MISSING — packages/kernel-rs/src/systems.rs wires `private_equity::Calling`, but that production path never calls `Exit` or `exit`, so nothing exits at any price. NOT GUARDED: an exit at a price nobody paid is a thing the code would DO rather than a word it would write, so what will hold it is wiring the module against a market that clears, not a check
- [ ] **TODO 10.PRIVATE-EQUITY.A2B** — `Private Equity A2.b` PARTIAL — the call falls due like any other payment and the wire refuses or queues it where the investor is short, so it is not bounded by spare cash. What is missing is the other half: an investor that does not pay does not DEFAULT on it — `mechanisms/loss.rs` crosses a standing on a LINE and a bilateral obligation has none, so the row runs out of periods and no fund learns of it

### 11. Firm Birth — 26 missing, 4 partial

> **Required review before this block:** read the **Firm Birth** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 3268), then inspect `packages/kernel-rs/src/mechanisms/firms.rs`, `packages/kernel-rs/src/parties.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 11.FIRM-BIRTH.A1** — `Firm Birth A1` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.A2** — `Firm Birth A2` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.A2A** — `Firm Birth A2.a` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.A3** — `Firm Birth A3` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.A4** — `Firm Birth A4` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.A4A** — `Firm Birth A4.a` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.A4B** — `Firm Birth A4.b` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.A5** — `Firm Birth A5` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.B1** — `Firm Birth B1` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.B2** — `Firm Birth B2` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.B3** — `Firm Birth B3` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.B4** — `Firm Birth B4` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.C1** — `Firm Birth C1` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.C2** — `Firm Birth C2` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.C3** — `Firm Birth C3` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.D1** — `Firm Birth D1` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.D2** — `Firm Birth D2` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.D2B** — `Firm Birth D2.b` MISSING — no trade creditor exists to rank, because no invoice is outstanding. packages/kernel-rs/src/mechanisms/estate.rs `Rank` has the unsecured rung they would sit on and nothing files a claim into it
- [ ] **TODO 11.FIRM-BIRTH.D2C** — `Firm Birth D2.c` MISSING — no close-out claim reaches an estate, because no derivative terminates. packages/kernel-rs/src/mechanisms/derivative_layer.rs `claim_on_the_estate` would file one and no production code names it
- [ ] **TODO 11.FIRM-BIRTH.D3** — `Firm Birth D3` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.D4** — `Firm Birth D4` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.D4A** — `Firm Birth D4.a` MISSING — a firm that dies ends no engagement, so no headcount moves and the labour market sees nothing. packages/kernel-rs/src/mechanisms/employment.rs `Wages` pays a standing engagement and has no separation path for one to travel
- [ ] **TODO 11.FIRM-BIRTH.D5** — `Firm Birth D5` MISSING — a ceased party keeps its cash and its holdings for ever, and no estate is opened
- [ ] **TODO 11.FIRM-BIRTH.D6** — `Firm Birth D6` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.E2** — `Firm Birth E2` MISSING — nothing has a destination — not an asset, not a liability, not an employee, not a contract
- [ ] **TODO 11.FIRM-BIRTH.E4** — `Firm Birth E4` MISSING — `mortality` runs `mechanisms/mortality.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable. Nothing is born, nothing is funded, nothing is distressed and nothing is resolved
- [ ] **TODO 11.FIRM-BIRTH.C4** — `Firm Birth C4` PARTIAL — `Failing` traces a cessation to the equity read that caused it and journals the size. That read now takes each position as its holder declared it, so a firm that holds its stock at cost is still not failed by a price move, and one holding marked paper is
- [ ] **TODO 11.FIRM-BIRTH.D2A** — `Firm Birth D2.a` PARTIAL — packages/kernel-rs/src/mechanisms/estate.rs `Ranked` divides what the estate actually holds among ranked claims, so a recovery is whatever the assets fetched and never a stated fraction. Nothing sells an estate's assets into a book, so what they fetch is a carrying figure rather than a price
- [ ] **TODO 11.FIRM-BIRTH.D6A** — `Firm Birth D6.a` PARTIAL — packages/kernel-rs/src/audit.rs reports a holding left on a ceased party with its owner and size, so a residual is found. It is found per instrument rather than per currency, and nothing pays one away: the audit never repairs and no mechanism clears a dead party's balances
- [ ] **TODO 11.FIRM-BIRTH.E1** — `Firm Birth E1` PARTIAL — packages/kernel-rs/src/mechanisms/mortality.rs `Failing` ceases any party whose equity is below zero, the central bank excepted by its profile (XI-3`s one exception, and for the reason XI-3 gives). ceasing sets a flag and nothing else

### 12. Audit — 2 missing, 3 partial

> **Required review before this block:** read the **Audit** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 706), then inspect `packages/kernel-rs/src/audit.rs`, `packages/kernel-rs/src/assembly.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 12.AUDIT.D3** — `Audit D3` MISSING — no seeded generator exists in packages/kernel-rs. `Draw` in the bins is a counter-based sequence with a hard-coded start and nothing takes a seed value
- [ ] **TODO 12.AUDIT.D4** — `Audit D4` MISSING — a Part XII measurement, and it needs families that are built
- [ ] **TODO 12.AUDIT.B5** — `Audit B5` PARTIAL — packages/kernel-rs/src/stores.rs `Equity` keeps each party's account as the movements that made it and packages/kernel-rs/src/audit.rs `AccountsBalance` reports the difference against the residual, with the dust of the two walks that produced them. What is short is on the account's side: settlement moves it for capital paid in, for income and cost receipts and for what a disposal realised, and assembly for what an exhausted estate did not pay, so units destroyed with no proceeds, transfers with no consideration and a mark that moves are changes to the residual that reach no account
- [ ] **TODO 12.AUDIT.B7** — `Audit B7` PARTIAL — packages/kernel-rs/src/audit.rs `FlowsAreComplete` compares every non-money holding`s change against the legs that said why it moved, over instructions that SETTLED. It does not cover money: a money leg does not state where its units land, and a family re-deriving settlement`s routing would be reading its answer (Audit C3) — that half is `MoneyIsConserved`, per currency. Per-account money flows are unchecked, and stay so until settlement`s routing is a read a family can take
- [ ] **TODO 12.AUDIT.B8** — `Audit B8` PARTIAL — all ten families are built and separately reported; the distinct-family logic test prevents two report identities collapsing into one. The long-run one-defect experiment remains measurement work.

### 13. Seed — 26 missing, 1 partial

> **Required review before this block:** read the **Seed** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 753), then inspect `packages/kernel-rs/src/opening.rs`, `packages/kernel-rs/src/assembly.rs`, `packages/kernel-rs/src/src/bin/world_runs.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 13.SEED.A1** — `Seed A1` MISSING — §5 is not built. The only opening state in the repository is packages/kernel-rs/src/bin/world_runs.rs, whose own header declares it arbitrary and not a seed
- [ ] **TODO 13.SEED.A2** — `Seed A2` MISSING — there is no period zero. The only opening state is packages/kernel-rs/src/bin/world_runs.rs, which declares itself arbitrary and is audited like any other week, so no violation can be attributed to a seed
- [ ] **TODO 13.SEED.A2A** — `Seed A2.a` MISSING — there is no period zero, so no violation can be attributed to a seed rather than to a mechanism. The opening state in packages/kernel-rs/src/bin/world_runs.rs declares itself arbitrary and is audited like any other week
- [ ] **TODO 13.SEED.A3** — `Seed A3` MISSING — there is no period zero, so there is no opening STOCK for a flow to act on. What the world starts from is packages/kernel-rs/src/bin/world_runs.rs's arbitrary construction, which declares itself arbitrary in its own header
- [ ] **TODO 13.SEED.A4** — `Seed A4` MISSING — nothing is seeded, so nothing is seeded free either: packages/kernel-rs/src/instruments.rs `issue` refuses a line with no issuer and packages/kernel-rs/src/register.rs keys every holding by a holder, so the FORBID is held by the stores rather than by an opening state anybody wrote
- [ ] **TODO 13.SEED.A5** — `Seed A5` MISSING — nothing in packages/kernel-rs takes a seed value. The one generator is `Draw`, redeclared in each of eight binaries with its own hard-coded constant
- [ ] **TODO 13.SEED.B1** — `Seed B1` MISSING — the populations in world_runs.rs are drawn from a counter and declared arbitrary
- [ ] **TODO 13.SEED.B1A** — `Seed B1.a` MISSING — nothing seeds a population. packages/kernel-rs/src/parties.rs represents a cell with an integer weight, so an ensemble standing for a population is representable, and no weight anywhere is drawn to make a sum come out
- [ ] **TODO 13.SEED.B4** — `Seed B4` MISSING — sizes in world_runs.rs come from `Draw::spread`, which is the arbitrary world, not a seed
- [ ] **TODO 13.SEED.B5** — `Seed B5` MISSING — there is no seed: `opening.rs` validates and builds a described opening state and nothing generates one, so no ratio is copied in because no ratio is drawn at all. The FORBID holds by the system being absent
- [ ] **TODO 13.SEED.C1** — `Seed C1` MISSING — there is no opening state to balance. The Accounts family that would check one exists and compares the residual with the equity account every week
- [ ] **TODO 13.SEED.C2** — `Seed C2` MISSING — no opening balance sheet names a counterparty party by party, because there is no opening balance sheet. Every holding packages/kernel-rs/src/bin/world_runs.rs writes does name one, but it is an arbitrary construction and not a seed
- [ ] **TODO 13.SEED.C3** — `Seed C3` MISSING — nothing is outstanding at period zero, so nothing carries terms and a remaining life from before the run. packages/kernel-rs/src/instruments.rs stamps both at issuance and every issue in this world happens inside a week it ran
- [ ] **TODO 13.SEED.C3A** — `Seed C3.a` MISSING — no seed issues debt, so there is no opening maturity profile to spread or to concentrate. packages/kernel-rs/src/mechanisms/treasury.rs spreads what it issues itself across tenors, and it starts from a stock that has none
- [ ] **TODO 13.SEED.C4** — `Seed C4` MISSING — nothing is seeded, so no opening price exists. packages/kernel-rs/src/prices.rs `Provenance::Seeded` is the slot such a level would carry and no production code writes one
- [ ] **TODO 13.SEED.C4A** — `Seed C4.a` MISSING — packages/kernel-rs/src/prices.rs `Provenance::Seeded` marks a level nobody cleared and nothing writes one, so there is no seeded price that could fail to re-clear next period
- [ ] **TODO 13.SEED.C4B** — `Seed C4.b` MISSING — nothing separates an opening condition from a term, because nothing is seeded at all. packages/kernel-rs/src/instruments.rs fixes a coupon once at `issue` and no table strikes one across the world
- [ ] **TODO 13.SEED.C5** — `Seed C5` MISSING — no sector balance sheet is read at period zero, because there is no period zero to read one at: the arbitrary scale world is built by a counter and declared arbitrary
- [ ] **TODO 13.SEED.D1** — `Seed D1` MISSING — there are no opening stocks to be consistent with the flows, because there are none at all. The debts packages/kernel-rs/src/bin/world_runs.rs writes carry coupons nobody was checked to be able to pay
- [ ] **TODO 13.SEED.D1A** — `Seed D1.a` MISSING — there is no period zero for period one to follow, so the first week is neither quiet nor a shock. Every number it produces comes out of the arbitrary opening state rather than out of a seed the flows agree with
- [ ] **TODO 13.SEED.D2** — `Seed D2` MISSING — `stores.rs` `Schedules` carries a covered interval per due, so an accrual position is representable; nothing states one at construction, and no opening instrument starts part-accrued
- [ ] **TODO 13.SEED.D3** — `Seed D3` MISSING — a quiet first period is a measurement, and it needs a seed the flows agree with. The arbitrary world's first week is loud by construction: it reports 16,750 ownership violations and 1,357 flow violations from the moment it opens
- [ ] **TODO 13.SEED.D4** — `Seed D4` MISSING — there is no seed, and the test the row cited is gone with the TypeScript engine
- [ ] **TODO 13.SEED.E1** — `Seed E1` MISSING — nothing is seeded, so no outcome is. `prices.rs` `Provenance::Seeded` is the slot an opening level would carry and the arbitrary world writes none. The FORBID holds by the system being absent
- [ ] **TODO 13.SEED.E2** — `Seed E2` MISSING — no seed sets a reason or an endowment. packages/kernel-rs/src/parties.rs carries a household's preferences and packages/kernel-rs/src/params.rs its declared numbers, and the arbitrary world fills both from a counter rather than from a described opening state
- [ ] **TODO 13.SEED.E3** — `Seed E3` MISSING — there is no seed parameter to change, and the read the row cited is gone with the TypeScript engine
- [ ] **TODO 13.SEED.B2** — `Seed B2` PARTIAL — packages/kernel-rs/src/parties.rs gives a party an identity that survives the run. What is missing is the seed that would create them

### 14. Observer — 19 missing, 4 partial

> **Required review before this block:** read the **Observer** section of `docs/spec/PROJECT_PHOENIX.md` (requirements begin at line 4212), then inspect `packages/kernel-rs/src/mechanisms/observer.rs` and the registration in `packages/kernel-rs/src/systems.rs`. Re-read the relevant coverage row before each point; its note
> identifies known dead code, missing production callers, and verification evidence. Do not implement
> from this summary alone.

- [ ] **TODO 14.OBSERVER.A2** — `Observer A2` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.A3** — `Observer A3` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.A5** — `Observer A5` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.A5A** — `Observer A5.a` MISSING — no statistic is published at all, so none is available instantly and exactly. packages/kernel-rs/src/mechanisms/observer.rs `Observing` journals a count and nothing carries a lag or a revision
- [ ] **TODO 14.OBSERVER.B1** — `Observer B1` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.B2** — `Observer B2` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.B3** — `Observer B3` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.B4** — `Observer B4` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.B5** — `Observer B5` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.C1** — `Observer C1` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.C2** — `Observer C2` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.C2A** — `Observer C2.a` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.C3** — `Observer C3` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.C4** — `Observer C4` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.D2** — `Observer D2` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.D3** — `Observer D3` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.F1** — `Observer F1` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.F2** — `Observer F2` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.F4** — `Observer F4` MISSING — `observer` runs `mechanisms/observer.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached. There is no surface
- [ ] **TODO 14.OBSERVER.A1** — `Observer A1` PARTIAL — packages/kernel-rs/src/prices.rs `Print` carries its instrument, period, unit and provenance, so a stale mark is visibly stale (A1.a). Nothing displays one: there is no observer surface
- [ ] **TODO 14.OBSERVER.A1A** — `Observer A1.a` PARTIAL — packages/kernel-rs/src/prices.rs `Print` carries the week it is OF and the week its level was struck in, so how stale a carried mark is travels with it as a subtraction rather than a walk. No surface displays one, so nothing shows a price at all, correctly dated or otherwise
- [ ] **TODO 14.OBSERVER.A4** — `Observer A4` PARTIAL — packages/kernel-rs/src/journal.rs `is_public`/`subjects_of` is the rule, and packages/kernel-rs/src/module.rs `ParticipantView` holds one party. `public_event`, the check that applies it, has no caller outside its own test. NOT GUARDED: `public_event` is the rule and nothing in the surface calls it, so the boundary holds only as long as the surface keeps reading through `ParticipantView`
- [ ] **TODO 14.OBSERVER.E1** — `Observer E1` PARTIAL — nothing is displayed at all, so there is no display-only number. The observer module that would display is unreached, and there is no surface

<!-- /plan:gaps -->
