# Requirement coverage

One row per REASON, VERIFY and FORBID in the specification. `MET at <path>` means the cited module
implements the clause; `PARTIAL` says what is still missing; `MISSING` is work; `OUT OF SCOPE` states a
reason and keeps the clause (Appendix C). Re-mark in the same change that meets a requirement, and
recount with `npm run coverage:spec` rather than adjusting a tally.

**EVERY ROW WAS RE-READ AGAINST THE SOURCE IN THE VERIFICATION PASS, AND MOST OF THEM CHANGED.**
The file previously held 1,090 `MET`, 130 `PARTIAL`, 176 `MISSING` and 2 `OUT OF SCOPE`. Of the
1,151 rows citing a path, **1,121 cited only paths inside `packages/engine`, which is not in the
repository** — the engine is Rust (`packages/kernel-rs`) and the TypeScript workspace is gone. A
`MET` whose only evidence is a citation that does not resolve asserts nothing, so every row was
opened against the Rust and re-marked from what is there.

What the pass found, in one line each, with the detail in `docs/VERIFICATION.md`:

- 539 of the 625 public items in `src/mechanisms/` are reached only by their own tests; 22 modules
  have not one item reached.
- 33 of the 41 mechanisms propose no instruction; the whole world has ten `ctx.propose` sites.
- 7 of the 10 audit families print `not-built` every period, and one of the three that reports a
  number compares a row with itself.
- There is no seed, no central bank, no policy rate, no money market mechanism, no revaluation and
  no successor.
- Every participant's reservation is a stated multiple of the price the book last printed.

`MET` still means what it said: **a claim about the SOURCE, not about the world.** Where a row is
now `MET` it means the Rust implements the clause and is reached; where it is `PARTIAL` the `why`
says which half is there. The `UNMEASURED` marks and the `coverage:reached` measurement the old
header described are gone with the TypeScript, and there is no such script in `package.json`.

## Money

| requirement | status | where / why |
|---|---|---|
| `Money A1` | MET | packages/kernel-rs/src/instruments.rs (`issue` asserts an issuer at the site; `owed_by` reads what an issuer owes from what others hold of what it issued) |
| `Money A1.d` | PARTIAL | packages/kernel-rs/src/instruments.rs `issue` refuses an instrument with no issuer. VERIFICATION 5.1: the one leg that CREATES money, `ledger.rs Leg::Mint`, is unchecked — its `issuer` is never compared with the money`s own issuer and its amount may be negative |
| `Money A2` | MET | packages/kernel-rs/src/instruments.rs (`ccy` per instrument, `ccy_of`). VERIFICATION 5.4: the previous citation answered a different clause (indivisible pieces and price ticks), and none of that machinery exists in packages/kernel-rs |
| `Money A2.b` | MISSING | VERIFICATION 5.3: there is no currency-carrying amount type in packages/kernel-rs. Money is a bare f64 in every leg, store and mechanism, and nothing can refuse an addition across two currencies |
| `Money A3` | MET | packages/kernel-rs/src/ledger.rs `account_of` (which money a party holds — its bank`s, or its own where it banks nowhere), packages/kernel-rs/src/register.rs (a row is keyed by instrument, and an instrument is one issuer`s one money) |
| `Money A4` | MISSING | no read of the money stock exists, and the Money audit family that would check it is one contribution deep (VERIFICATION 1.4) |
| `Money B1` | MET | packages/kernel-rs/src/register.rs (a row is (holder, instrument)) with packages/kernel-rs/src/instruments.rs (an instrument is (issuer, ccy)), so the triple is determined and kept once. Re-marked from MISSING: the citation was absent, the mechanism is not |
| `Money B1.b` | MISSING | packages/kernel-rs/src/register.rs `held_total` is the read it needs, but nothing compares it with an issuer`s money liability, and there is no issued amount to compare it with (VERIFICATION 7.1) |
| `Money B2` | MET | packages/kernel-rs/src/register.rs (the row persists and `money_delta` is its only writer), packages/kernel-rs/src/ledger.rs (which is settlement`s alone — `MechanismContext` holds `&Register`, so no module can write). Re-marked from MISSING |
| `Money B3` | PARTIAL | packages/kernel-rs/src/register.rs `money_delta` can carry a negative total, but settlement refuses a payment the payer cannot make, so nothing reaches it through the wire. B3.a`s credit decision by the bank is in packages/kernel-rs/src/mechanisms/bank_funding.rs; what is missing is B3.b, a bank overdrawn at the central bank |
| `Money B3.c` | PARTIAL | the cited modules are gone. packages/kernel-rs/src/ledger.rs refuses rather than overdrawing (`ShortOfMoney`, `BankCouldNotSettle`), which is the other half of the clause. The refusal is recorded on the wire; the LENDING that would make an overdraft priced is not reached (VERIFICATION 5.1 area) |
| `Money C1` | PARTIAL | packages/kernel-rs/src/ledger.rs `Leg::Money` names payer, payee, amount, instrument and a `Receipt` (the reason, which cannot be omitted). VERIFICATION 5.2: its `ccy` is a second copy of the instrument`s currency, never checked against it, and read by one mechanism |
| `Money C2` | MET | packages/kernel-rs/src/ledger.rs `Settlement::attempt` (payer minus, payee plus, one rule), and C2.a`s interbank leg: where the payee banks elsewhere the deposit is extinguished, recreated, and reserves move between the two banks |
| `Money C2.c` | MISSING | nothing sums the legs of a pass. The Money family has one contribution and it checks that money accounts carry no lots (VERIFICATION 1.4); the issuance exception C4.c exists to measure is unmeasured and unguarded (5.1) |
| `Money C3` | MISSING |  |
| `Money C4` | PARTIAL | packages/kernel-rs/src/ledger.rs `Leg::Mint` is the one door that creates money — but VERIFICATION 5.1: it is never checked, so the creators are not enumerable and not few |
| `Money C4.c` | MISSING | VERIFICATION 5.1, 1.4: nothing counts issuance legs and nothing reads the money stock`s change |
| `Money D1` | MET | packages/kernel-rs/src/ledger.rs `Settlement` (every instruction ever applied, numbered, in order, with its legs — the wire IS the history, and a reader walks it rather than keeping a tally) |
| `Money D2` | MET | packages/kernel-rs/src/instruments.rs `hard_coded_price` (1.0 for Class::Money and nothing else), packages/kernel-rs/src/register.rs (a money row is a TOTAL and carries no lots) |
| `Money D3` | MISSING | VERIFICATION 1.4: the Flows family is NOT BUILT. packages/kernel-rs/src/mechanisms/capital_programme.rs `PlantMoves` does this identity for capital lines only, in the Units family |
| `Money D5` | MISSING | VERIFICATION 0.2: Money has no clause D5. The row marks something the specification does not contain; the payment queue it describes is packages/kernel-rs/src/ledger.rs `Queue` and belongs against E1 if anywhere |
| `Money D4` | MET | packages/kernel-rs/src/module.rs `MechanismContext` holds `register: &Register`, so a module cannot write the register at all; `propose` is the only door and it takes legs. The prohibition is structural |
| `Money E1` | PARTIAL | packages/kernel-rs/src/ledger.rs (a payer that cannot pay does not pay, nothing half-settles, and the outcome is recorded by name), packages/kernel-rs/src/ledger.rs `Queue::gave_up` (the arrear). What is missing is the downstream: no claim is ever filed against an estate (VERIFICATION 4.1), so `owed_to_the_state` has no caller |
| `Money E1.a` | MET | packages/kernel-rs/src/ledger.rs (the fail is a recorded `Outcome`, and the payer is never overdrawn — `ShortOfMoney` refuses) |
| `Money E1.b` | MISSING | the arrears register cited is gone with the TypeScript. packages/kernel-rs/src/ledger.rs `Queue` records that a payment was late; nothing writes the payee a receivable |
| `Money E2` | MET | packages/kernel-rs/src/ledger.rs (there is no reversal door; a correction is a new instruction) |
| `Money E3` | MET | packages/kernel-rs/src/ledger.rs `Settlement::attempt` (every leg pre-checked before any is applied, in order, so two instructions on one balance cannot both succeed) |
| `Money E4` | MISSING | VERIFICATION 7.3: settlement never reads `parties.alive`. A ceased party pays and is paid like any other, and VERIFICATION 7.2 leaves it holding the money to do it with |
| `Money F1` | MISSING |  |
| `Money F1.a` | MISSING |  |
| `Money F2` | MISSING |  |
| `Money F3` | MISSING | no clearing house residual is read; the ZeroSum family is NOT BUILT (VERIFICATION 1.4) |
| `Money F4` | MISSING | nothing counts money landing on a holder with no account; the Money family is one contribution deep (VERIFICATION 1.4) |
| `Money G1` | MET | packages/kernel-rs/src/calendar.rs `Calendar` (cycles within a period), packages/kernel-rs/src/assembly.rs `World::step` |
| `Money G2` | MET | packages/kernel-rs/src/calendar.rs `Cycle` and `cycles_per_period` |
| `Money G3` | MET | packages/kernel-rs/src/calendar.rs (one epoch, one period length, `start_of`/`period_on` the one mapping, `plus_months` for placing a periodicity by DATE, `year_fraction` from the dates) |
| `Money G3.b` | MISSING | nothing refuses a periodicity finer than a period. `Calendar` has no such check; the four citations of G3.b in packages/kernel-rs are comments in polity.rs and running.rs saying a term is placed by date |
| `Money G4` | MET | packages/kernel-rs/src/calendar.rs, packages/kernel-rs/src/ledger.rs (every instruction carries its period), packages/kernel-rs/src/journal.rs |
| `Money G4.a` | MET | packages/kernel-rs/src/calendar.rs `Period` derives no `Default`, so there is no period that means now and unset at once |

## Register

| requirement | status | where / why |
|---|---|---|
| `Register A1` | MET | packages/kernel-rs/src/register.rs (a row is holder + instrument + quantity, and the quantity is in the instrument`s own unit) |
| `Register A2` | MET | packages/kernel-rs/src/instruments.rs `issuer_of` (a holding is a claim on a named issuer, and A2.a`s "pays to whoever the register says holds it, then" is settlement reading the register at the moment it applies). Re-marked from MISSING |
| `Register A3` | MET | packages/kernel-rs/src/register.rs `open` (asserts the holder at the site) |
| `Register A4` | MET | packages/kernel-rs/src/register.rs `open`, packages/kernel-rs/src/instruments.rs `issue` (both assert) |
| `Register B1` | MISSING | VERIFICATION 7.1: there is no issued amount. `Instruments` has no such column and nothing records an issuance, re-opening, buyback, amortisation or maturity against one |
| `Register B2` | MISSING | VERIFICATION 1.3, 7.1: nothing sums the holders of an instrument against an issued amount, and there is no issued amount for it to be summed against. `audit.rs LotsAgainstQuantity` compares a row with itself (1.1) |
| `Register B3` | PARTIAL | packages/kernel-rs/src/instruments.rs `owed_by` reads the liability from the other side rather than storing it. It is used only by `equity()`, which VERIFICATION 6.1 shows values everything at cost |
| `Register B4` | MISSING | packages/kernel-rs/src/instruments.rs carries `matures` as a term, but nothing ceases an instrument and nothing resolves its holdings; default-into-recovery needs the estate claims nobody files (VERIFICATION 4.1) |
| `Register C1` | MET | packages/kernel-rs/src/ledger.rs `Leg::Asset` (the seller loses exactly what the buyer gains, both legs in one atomic instruction) |
| `Register C2` | MET | packages/kernel-rs/src/ledger.rs `Cause` (trade, payment, corporate action, production, settlement) and `Leg::Asset.price_per_unit` for C2.a |
| `Register C3` | MET | packages/kernel-rs/src/ledger.rs `Settlement::attempt` (every leg is pre-checked before any applies, so neither leg happens without the other), `Delivery` (and a delivery with no payment against it must be DECLARED free, which is what makes a dropped payment distinguishable from a legitimate one), `Outcome` (a fail is a named recorded state) |
| `Register C4` | MET | packages/kernel-rs/src/ledger.rs (`ShortOfUnits`: a holder cannot deliver what it does not hold) |
| `Register C5` | MISSING | nothing reads bought against sold over a window; the Flows and Ownership families that would are NOT BUILT or tautological (VERIFICATION 1.3, 1.4) |
| `Register D1` | MET | packages/kernel-rs/src/register.rs `of_holder` |
| `Register D2` | MET | packages/kernel-rs/src/register.rs `of_instrument` (both directions indexed, neither reconstructed) |
| `Register D3` | MISSING | VERIFICATION 6.1: the kernel`s balance-sheet read `instruments.rs equity` values every holding at its lot BASIS — a price stored on the holding, which is what this clause forbids. Where a mark is taken at all it is three inline copies with a silent fallback to cost (6.2) |
| `Register D4` | MET | packages/kernel-rs/src/register.rs (`Lot.basis_per_unit`, `debit` draws lots in order and returns what they cost, and packages/kernel-rs/src/ledger.rs turns that into the realised gain on the wire) |
| `Register D5` | MET | packages/kernel-rs/src/register.rs `pledge`/`free` (one lien per (holding, holder)), packages/kernel-rs/src/ledger.rs (`Encumbered`: only free units move) |
| `Register E1` | MISSING | the cited modules are gone. packages/kernel-rs/src/stores.rs `Schedules` holds what falls due, but nothing pays a coupon to the holders of record off the register |
| `Register E2` | PARTIAL | packages/kernel-rs/src/stores.rs `Owing` carries a maturity on the schedule; nothing extinguishes the holding when it is paid (VERIFICATION 7.1: there is no issued amount to reduce) |
| `Register E3` | MISSING | VERIFICATION 4.1: no claim is ever filed against an estate, so no loss lands on a holder |
| `Register E4` | MISSING | packages/kernel-rs/src/mechanisms/equity.rs `ShareEvent` names the four events; nothing issues, buys back, splits or cancels in the running world |
| `Register E5` | MET | packages/kernel-rs/src/ledger.rs `Delivery` and `Instruction::shape` (a register event either moves money or is DECLARED `Free`, and the declaration is checked against the legs — which is the clause, made unsayable rather than forbidden) |
| `Register F1` | MET | packages/kernel-rs/src/instruments.rs (the id IS the row and never moves; two instruments with the same terms are two rows) |
| `Register F2` | MISSING | VERIFICATION 7.2: `parties.rs cease` flips a flag and nothing else. There is no successor anywhere in packages/kernel-rs, and the dead party keeps its holdings |
| `Register F3` | MISSING | VERIFICATION 1.4: the Flows family is NOT BUILT, so nothing would notice unexplained drift across a period boundary |

## Clearing

| requirement | status | where / why |
|---|---|---|
| `Clearing A1` | MET | packages/kernel-rs/src/protocols.rs (**22c.1: ONE PROTOCOL PER VENUE, dispatched on the venue's own declaration.** There was one microstructure — a weekly uniform-price call auction — for bread, labour, loans, shares and freight alike, and a Walrasian auctioneer for bread is the one intermediary that never existed (Law 1). `Call` is a sealed cross at one level, which is what an auction and a fixing ARE; `Posted` is a seller standing behind an ask and a buyer taking the best it SAW, with what it can see a TECHNOLOGY because search is costly; `Book` is resting orders matched as they arrive, priced at the level the resting side was standing at. All three clear from real supply meeting real demand, which is what they have in common and the whole of it), packages/kernel-rs/src/session.rs `BookDecl.protocol` (Law 15: the kernel dispatches on the declaration and never on what is being traded) |
| `Clearing A2` | PARTIAL | packages/kernel-rs/src/clearing.rs (the solver takes schedules; every venue posts them) and packages/kernel-rs/src/protocols.rs. A2.a is still unmet on the participant side: a sell order may carry `price: None` and is then in the book at every level |
| `Clearing A3` | MET | packages/kernel-rs/src/module.rs `ParticipantView` (a schedule is written from that party`s own state and it can see nothing else), packages/kernel-rs/src/session.rs |
| `Clearing A4` | PARTIAL | packages/kernel-rs/src/clearing.rs asserts every BUY names a level. A sell with no level is admitted deliberately (XI-2`s forced seller) and is a price-taker of a level the mechanism has not produced |
| `Clearing B1` | MET | packages/kernel-rs/src/clearing.rs `Order.party`, packages/kernel-rs/src/session.rs (a fill becomes an instruction against the named party`s own account) |
| `Clearing B2` | MET | packages/kernel-rs/src/module.rs `ParticipantView`, packages/kernel-rs/src/session.rs (a party is asked and answers out of its own reason) |
| `Clearing B3` | MISSING | packages/kernel-rs/src/mechanisms/dealing.rs is a set of pure functions; no dealer posts into a book in the running world |
| `Clearing B4` | MET | packages/kernel-rs/src/clearing.rs (`clear` asserts that every BUY names a level, with the citation "Appendix B: an order to buy at any price is a buyer of last resort" — refused at the site, not checked afterwards) |
| `Clearing B5` | MET | packages/kernel-rs/src/clearing.rs (the solver sweeps the levels somebody posted and adds nothing) |
| `Clearing C1` | MET | packages/kernel-rs/src/clearing.rs `clear` (one sweep up the posted levels, two pointers, integer quantities) |
| `Clearing C2` | MET | packages/kernel-rs/src/clearing.rs (the candidate levels are the ones somebody NAMED; nothing is assigned) |
| `Clearing C3` | MET | packages/kernel-rs/src/clearing.rs `ration` (pro rata by LARGEST REMAINDER in integer pieces, summing to exactly the volume, so no piece is rounded away) |
| `Clearing C4` | MET | packages/kernel-rs/src/clearing.rs `Outcome` (Cleared, NoDemand, NoSupply, NoOverlap — all four are representable outcomes) |
| `Clearing C4.c` | MET | packages/kernel-rs/src/clearing.rs `bracket` (a failed cross returns `NoOverlap { best_bid, best_ask }`, which is not a Print and cannot be written as one) |
| `Clearing C5` | MET | packages/kernel-rs/src/clearing.rs (`clear` is a pure function of the posted orders and the venue`s rule) |
| `Clearing D1` | MET | packages/kernel-rs/src/prices.rs `Print` (instrument, market, period, price, currency, quoted-as and provenance) |
| `Clearing D2` | MET | packages/kernel-rs/src/clearing.rs `Fill`, packages/kernel-rs/src/session.rs `pair_up` |
| `Clearing D3` | PARTIAL | packages/kernel-rs/src/session.rs settles each trade as one atomic instruction. VERIFICATION 8.3: the print is written BEFORE any of them is attempted and stands even where every one failed |
| `Clearing D4` | MISSING | VERIFICATION 6.1, 6.3: the print does not become anybody`s mark. `instruments.rs equity` values holdings at cost and there is no revaluation anywhere in packages/kernel-rs |
| `Clearing D5` | MISSING | VERIFICATION 8.3: nothing reads bought against sold or cash paid against cash received per clearing, and a book can print with nothing settled |
| `Clearing E1` | MET | packages/kernel-rs/src/prices.rs (a print is in the store and any participant may read it) |
| `Clearing E2` | MISSING | nothing in packages/kernel-rs takes one book`s print as another book`s input. With one book of 1,546 printing per period (VERIFICATION 3.2) there is nothing to be an input |
| `Clearing E3` | MISSING | no dealer posts a schedule, so there is nothing to read a bid-offer off (Clearing B3) |
| `Clearing E4` | PARTIAL | packages/kernel-rs/src/prices.rs `Provenance::Carried` is the right shape — a book that ran and did not cross carries its last level and says so. VERIFICATION 8.3: a book whose trades all failed still writes `Provenance::Cleared` |
| `Clearing F1` | MET | packages/kernel-rs/src/world.rs `Phases` (the order is declared data and a phase reading a not-yet-produced print is refused), packages/kernel-rs/src/assembly.rs `World::step` |
| `Clearing F2` | PARTIAL | packages/kernel-rs/src/prices.rs keys a print by (instrument, period) so one period has one level. Nothing forbids a second print into the same period, and there is no rate in force that valuation and settlement are held to (VERIFICATION 8.1) |
| `Clearing F3` | MISSING | a Part XII measurement, and with one book clearing per period (VERIFICATION 3.2) there is nothing to move |

## Audit

| requirement | status | where / why |
|---|---|---|
| `Audit A1` | PARTIAL | packages/kernel-rs/src/audit.rs `Contribution` (a family is a read of the state, run where it is meant to be consistent). VERIFICATION 1.1: `LotsAgainstQuantity`, one of the four contributions that exist, reads one thing against itself, which A1.a forbids and which always passes |
| `Audit A2` | MET | packages/kernel-rs/src/audit.rs `Violation` (family, spec, owner, size, unit, period, message — a finding with no owner cannot be chased) |
| `Audit A3` | MET | packages/kernel-rs/src/audit.rs `Violation.size` and `Violation.unit` |
| `Audit A4` | MET | packages/kernel-rs/src/audit.rs and packages/kernel-rs/src/num.rs `dust` (each family derives its own tolerance from its own terms and magnitudes — terms × ε × Σ of the absolute magnitudes — and tools/phoenix-check refuses a tolerance nobody derived) |
| `Audit B1` | MISSING | VERIFICATION 1.4: nothing checks that money is conserved. The Money family has one contribution, `audit.rs ATotalCarriesNoLots`, and what it checks is that a money account carries no lots |
| `Audit B2` | MISSING | VERIFICATION 1.3: nothing sums the holders of an instrument against its issued amount. `audit.rs LotsAgainstQuantity` is the only Ownership contribution that claims to and it compares a row with itself (1.1) |
| `Audit B3` | MISSING | VERIFICATION 1.4: the Prices family has no contribution; `audit.rs Audit::over` declares it NOT BUILT in the assembled world |
| `Audit B4` | MISSING | VERIFICATION 1.4: the CrossMarket family has no contribution and is declared NOT BUILT. (It was PARTIAL naming worklist 12/13c; the mark is the same fact stated as a status) |
| `Audit B5` | MISSING | VERIFICATION 1.4: the Accounts family has no contribution and is declared NOT BUILT. No code reads assets minus liabilities against an equity account |
| `Audit B6` | MISSING | VERIFICATION 1.4: the Names family has no contribution and is declared NOT BUILT |
| `Audit B7` | MISSING | VERIFICATION 1.4: the Flows family has no contribution and is declared NOT BUILT |
| `Audit B8` | MISSING | independence cannot be measured while seven of the ten families are NOT BUILT (VERIFICATION 1.4) |
| `Audit C1` | MET | packages/kernel-rs/src/audit.rs `Audit::run` (every family, every period, off ONE walk of the register — a family's independence is about the SOURCE it reads, not how many times the register is visited), packages/kernel-rs/src/assembly.rs `World::step` (**22e: it is IN THE PERIOD LOOP.** `audit.rs` and its families were built and tested and `grep audit assembly.rs` returned one hit, in a comment — the assembled world had stepped in every run since the port with no family ever visiting it. It runs last, over what the period left behind, and never repairs) |
| `Audit C2` | MET | packages/kernel-rs/src/assembly.rs `World::step` (the audit runs every period, last, over what the period left behind), packages/kernel-rs/src/audit.rs `Audit::run` |
| `Audit C3` | PARTIAL | packages/kernel-rs/src/audit.rs `Audit::run` runs the same contributions every period. What is not true is the rest of C3: seven families are NOT BUILT, so the invariants are not the same set the spec names (VERIFICATION 1.4) |
| `Audit C4` | MET | packages/kernel-rs/src/audit.rs (`run` takes `&Register` and every contribution takes `&Visit`, so a family cannot write; the test asserts the register is unchanged after a violation) |
| `Audit D1` | MET | packages/kernel-rs/src/audit.rs `Report` (one per family, with its contributors and its violations) |
| `Audit D2` | MET | packages/kernel-rs/src/audit.rs `Violation` (owner and size on every one), packages/kernel-rs/src/bin/world_runs.rs |
| `Audit D3` | MISSING | VERIFICATION 1.5: no seeded generator exists in packages/kernel-rs. `Draw` in the bins is a counter-based sequence with a hard-coded start and nothing takes a seed value |
| `Audit D4` | MISSING | a Part XII measurement, and it needs families that are built (VERIFICATION 1.4) |
| `Audit E1` | MET | packages/kernel-rs/src/audit.rs (an absence has nothing to be inconsistent with — the audit takes only the register and the wire and can report nothing about a mechanism that does not exist) |
| `Audit E2` | MET | packages/kernel-rs/src/audit.rs `Audit::over` and `Family::ALL` (**22e.2: an unbuilt family cannot be ABSENT from the report.** `over` is the only way to build an audit of a world and it declares `NotBuilt` for every family nobody contributed to, so a reader counting violations over the families that happened to be assembled cannot read a world with one family built and nine missing as a world with no violations. The lie was measured: four of nine families were tautologies until item 4 and were reported green for thirteen `done` rows, 21.134.D15), packages/kernel-rs/src/bin/world_runs.rs (it prints `not-built` by name, every period) |
| `Audit E3` | MET | packages/kernel-rs/src/audit.rs `NotBuilt` alongside `docs/COVERAGE.md` itself: a family that is green and a clause that is unmet coexist by construction |

## Seed

| requirement | status | where / why |
|---|---|---|
| `Seed A1` | MISSING | VERIFICATION 2.1: §5 is not built. The only opening state in the repository is packages/kernel-rs/src/bin/world_runs.rs, whose own header declares it arbitrary and not a seed |
| `Seed A2` | MISSING | VERIFICATION 2.1: there is no period zero, and the Accounts family that would audit it is NOT BUILT (1.4) |
| `Seed A3` | MISSING | VERIFICATION 2.1 |
| `Seed A4` | MISSING | VERIFICATION 2.1 |
| `Seed A5` | MISSING | VERIFICATION 1.5: nothing in packages/kernel-rs takes a seed value. The one generator is `Draw`, redeclared in each of eight binaries with its own hard-coded constant |
| `Seed B1` | MISSING | VERIFICATION 2.1: the populations in world_runs.rs are drawn from a counter and declared arbitrary |
| `Seed B2` | PARTIAL | packages/kernel-rs/src/parties.rs gives a party an identity that survives the run. What is missing is the seed that would create them (VERIFICATION 2.1) |
| `Seed B3` | MET | packages/kernel-rs/src/registry.rs (21e: each party is placed in a region, and `currency_of(region)` reads THROUGH the country that holds the money — so a region determines its money uniquely and keeps no copy of the fact, Law 4) |
| `Seed B4` | MISSING | VERIFICATION 2.1: sizes in world_runs.rs come from `Draw::spread`, which is the arbitrary world, not a seed |
| `Seed B5` | MISSING |  |
| `Seed C1` | MISSING | VERIFICATION 2.1, 1.4: no opening state, and no Accounts family to check that it balances |
| `Seed C2` | MISSING | VERIFICATION 2.1 |
| `Seed C3` | MISSING | VERIFICATION 2.1 |
| `Seed C4` | MISSING | VERIFICATION 2.1 |
| `Seed C5` | MISSING |  |
| `Seed D1` | MISSING | VERIFICATION 2.1 |
| `Seed D2` | MISSING |  |
| `Seed D3` | MISSING |  |
| `Seed D4` | MISSING | VERIFICATION 2.1: there is no seed, and the test the row cited is gone with the TypeScript engine |
| `Seed E1` | MISSING |  |
| `Seed E2` | MISSING | VERIFICATION 2.1 |
| `Seed E3` | MISSING | VERIFICATION 2.1: there is no seed parameter to change, and the read the row cited is gone with the TypeScript engine |

## Currency

| requirement | status | where / why |
|---|---|---|
| `Currency A1` | MET | packages/kernel-rs/src/registry.rs (a currency is a named unit of account with an issuer), packages/kernel-rs/src/instruments.rs |
| `Currency A2` | MET | packages/kernel-rs/src/registry.rs (21e: `Registry::currency(issuer)` is the only way to make one and there is no constructor without an issuer, so a money nobody owes cannot be written — Money A2 one level up from 5 A4's free money) |
| `Currency A3` | MISSING | VERIFICATION 5.3: there is no amount type in packages/kernel-rs that carries a currency. An amount is a bare f64 in every leg, store and mechanism, so A3.a`s "a function that takes an amount takes its currency with it" does not hold anywhere |
| `Currency A4` | MISSING | VERIFICATION 5.2, 8.1: a leg carries a `ccy` that nothing checks against the instrument it names, and settlement infers where a payment lands from the payee`s bank without reading a currency at all |
| `Currency A5` | MISSING | VERIFICATION 2.1: there is no seed, so there is no named set of currencies. packages/kernel-rs/src/registry.rs can hold one; nothing states it |
| `Currency B1` | PARTIAL | packages/kernel-rs/src/registry.rs `currency_of(region)` reads through the country (one fact, one writer). A party`s reporting in it is not built |
| `Currency B2` | PARTIAL | packages/kernel-rs/src/register.rs holds a row per (party, money), so a foreign holding is a real position. VERIFICATION 8.1: it is not acquired by a trade — settlement converts at par on arrival |
| `Currency B3` | MISSING | VERIFICATION 8.1: `ledger.rs across()` never compares currencies. A payment whose payee banks in another money lands as that money, same number, no rate, no counterparty — which is the conversion at the ledger boundary this FORBID names |
| `Currency B4` | MISSING | VERIFICATION 9.1: `SpotFx` proposes no instruction, so nothing is ever acquired from anybody at a price. packages/kernel-rs/src/mechanisms/currency.rs `short_of`/`MustBuy` is the shape and is dead code (10.1) |
| `Currency B5` | MISSING | no bank runs a foreign position; VERIFICATION 9.1 |
| `Currency C1` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it. No FX trade is ever proposed, so no rate is cleared from anything that settled |
| `Currency C2` | PARTIAL | packages/kernel-rs/src/mechanisms/currency.rs `Rates::of` inverts one print rather than storing two (Law 4). The store is dead code (VERIFICATION 10.1) |
| `Currency C3` | PARTIAL | packages/kernel-rs/src/mechanisms/currency.rs `gap` measures the triangle and `arbitrage` bounds who closes it — the right shape, and C3.b`s no-triangulating-read is honoured by `Rates::of` answering None. All of it is dead code (VERIFICATION 10.1) |
| `Currency C4` | MISSING | there is no numéraire read anywhere in packages/kernel-rs |
| `Currency C4.a` | MISSING | VERIFICATION 8.1: value does not live in a numéraire, but only because there is no numéraire; the currency position is destroyed by settlement converting at par instead |
| `Currency C5` | MISSING | VERIFICATION 8.1: there is no rate in force. Settlement converts at one and nothing values a foreign position at all (6.3) |
| `Currency D1` | MISSING | VERIFICATION 6.3: no rate is in force for a period and nothing values against one |
| `Currency D2` | MISSING | VERIFICATION 6.3: there is no revaluation anywhere in packages/kernel-rs |
| `Currency D2.b` | MISSING | VERIFICATION 6.3, 8.1: every foreign position is unrevalued, which is what this FORBID calls money created or destroyed silently |
| `Currency D3` | MISSING | VERIFICATION 6.3 |
| `Currency D4` | MISSING | VERIFICATION 6.3, 1.4: nothing revalues and no family checks it |
| `Currency E1` | MISSING | VERIFICATION 9.1: no rate moves because somebody traded at it; nobody trades |
| `Currency E2` | MISSING |  |
| `Currency E3` | PARTIAL | packages/kernel-rs/src/mechanisms/currency.rs has no written path and `Rates::of` refuses to invent one, which is the FORBID holding by absence. It holds in dead code (VERIFICATION 10.1) |
| `Currency E4` | MISSING | VERIFICATION 9.1: there is no flow to move a rate |

## Bond

| requirement | status | where / why |
|---|---|---|
| `Bond N1` | MET | packages/kernel-rs/src/instruments.rs (`issue` asserts a named issuer; `issuer_of` reads it) |
| `Bond N2` | PARTIAL | packages/kernel-rs/src/instruments.rs carries a `unit`, and the register counts units of it. VERIFICATION 7.1: there is no issued PRINCIPAL — no column records the amount owed |
| `Bond N3` | MET | packages/kernel-rs/src/instruments.rs `ccy_of` (one currency per line). VERIFICATION 5.3: what is not true is that every figure about it is in that money |
| `Bond N4` | MET | packages/kernel-rs/src/instruments.rs `matures_on` (a Day on the one calendar) |
| `Bond N5` | PARTIAL | packages/kernel-rs/src/instruments.rs `coupon_of` is a fixed rate or `None` (N5.a and N5.c). N5.b has no representation: there is no margin, no reference-rate field, and nothing fixes a floating coupon |
| `Bond N5.b` | MISSING | there is no floating coupon. `instruments.rs` has one `coupon: Option<f64>` and no reference rate; packages/kernel-rs/src/running.rs `Floating` brings a line and journals, and never fixes a coupon on one |
| `Bond N6` | MISSING | no instrument carries a periodicity or an accrual convention. The only day count in packages/kernel-rs is `calendar.rs year_fraction`, which is ACT/365F for the whole world |
| `Bond N7` | MET | packages/kernel-rs/src/prices.rs `Print` (per unit, in the instrument`s currency), packages/kernel-rs/src/session.rs (written where a book cleared) |
| `Bond N7.b` | MET | packages/kernel-rs/src/prices.rs (there is no door that writes a price from a yield, a spread or an OAS; the FORBID holds by absence) |
| `Bond N8` | MET | packages/kernel-rs/src/register.rs `of_instrument` (who holds how many units). Re-marked from MISSING: the citation was absent, the mechanism is not |
| `Bond N8.a` | MISSING | VERIFICATION 1.3, 7.1: nothing sums units held against units issued, and there is no issued amount |
| `Bond N9` | PARTIAL | packages/kernel-rs/src/ledger.rs `Leg::Asset` with `Delivery::AgainstPayment` gives N9.a — the paper one way and the cash the other, in one atomic instruction. N9.b is not met: packages/kernel-rs/src/mechanisms/corporate_credit.rs `accrued` computes accrued interest and has no caller (VERIFICATION 11.1), so nothing is added to what a trade settles and a coupon is a windfall to whoever holds it on the date |
| `Bond N10` | MISSING | nothing redeems an instrument. `instruments.rs` and `register.rs` have no door that ceases a line or empties its holdings |
| `Bond N11` | MISSING | no instrument carries an early-termination regime, and there is no field in which "none" could be stated |
| `Bond N12` | MISSING | no instrument carries a definition of default. packages/kernel-rs/src/mechanisms/sovereign.rs `Missed` is the shape and is dead code (VERIFICATION 11.1) |
| `Bond N13` | MISSING | packages/kernel-rs/src/mechanisms/estate.rs `Rank` is the ordering, but no instrument states what its holder is entitled to on failure, and no claim is ever filed (VERIFICATION 4.1) |
| `Bond N14` | PARTIAL | packages/kernel-rs/src/instruments.rs `display` builds issuer + coupon + maturity and the id is never the name. It is called by packages/kernel-rs/src/mechanisms/observer.rs `display_name`, which is itself dead code (VERIFICATION 11.1) |

## Derivative

| requirement | status | where / why |
|---|---|---|
| `Derivative D1` | PARTIAL | packages/kernel-rs/src/running.rs `agreed::DERIVATIVE` is a two-party agreement, so the two sides are named. Nothing makes it an asset to one and a liability to the other: no mark exists (VERIFICATION 6.3) and the Accounts family is NOT BUILT (1.4) |
| `Derivative D1.a` | MET | packages/kernel-rs/src/stores.rs `Agreements` (a relation names two parties and there is no constructor with one) |
| `Derivative D1.b` | MISSING | VERIFICATION 1.4: the ZeroSum family has no contribution and is declared NOT BUILT every period |
| `Derivative D2` | PARTIAL | a notional can sit in an agreement`s terms. `derivative_layer.rs gross_notional` is dead code (VERIFICATION 11.1), so D2.a`s distinction between notional and exposure is drawn nowhere |
| `Derivative D3` | MISSING | nothing states a derivative`s underlying, so nothing can be checked against a price this world clears |
| `Derivative D3.a` | MISSING | there is no underlying field, so the FORBID has nothing to refuse |
| `Derivative D4` | MISSING | no payoff function is carried on a contract; `derivative_layer.rs` variants are dead code |
| `Derivative D5` | MISSING | an agreement carries no per-leg currency |
| `Derivative D6` | PARTIAL | packages/kernel-rs/src/stores.rs `Agreements` carries an `until`, so a term can end. Payment dates on a leg are not represented |
| `Derivative D7` | MISSING | nothing strikes a derivative at a cleared level; `Derivatives` proposes nothing and opens no book (VERIFICATION 9.1) |
| `Derivative D8` | MISSING | VERIFICATION 6.3: nothing marks a contract after inception |
| `Derivative D9` | MISSING | `derivatives.collateral` is one of the three nouns with NO KERNEL HOME that `npm run world:runs` prints every run. `derivative_layer.rs initial_margin` and `variation` are dead code (VERIFICATION 11.1) |
| `Derivative D10` | MISSING | VERIFICATION 4.1: no claim is ever filed against an estate, so an in-the-money party has nothing |
| `Derivative D11` | PARTIAL | packages/kernel-rs/src/stores.rs `Agreements::ends` removes the relation from both parties at once. `derivative_layer.rs close_out` is dead code |
| `Derivative D12` | PARTIAL | an agreement is its own row, so two contracts are two rows. Counterparties + underlying + term + strike is not the key: there is no underlying and no strike |
| `Derivative X1` | MET | packages/kernel-rs/src/stores.rs `Agreements` is not the register, so a contract has no issuer and no issued amount and does not enter the ownership walk |
| `Derivative X2` | MISSING | VERIFICATION 9.1: `Derivatives` proposes no instruction, so no margin, premium or periodic payment leaves any account |
| `Derivative X3` | MET | nothing prices an underlying off a contract, because nothing prices a contract at all (Derivative D8) |

## Corporate Credit

| requirement | status | where / why |
|---|---|---|
| `Corporate Credit A1` | PARTIAL | packages/kernel-rs/src/parties.rs and packages/kernel-rs/src/instruments.rs give a named issuer with a balance sheet. VERIFICATION 12.2: the `corporate_credit` system runs `running.rs Funding`, which is also `short_term_debt`'s mechanism; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable (11.1) |
| `Corporate Credit A2` | MISSING | VERIFICATION 12.2: the `corporate_credit` system runs `running.rs Funding`, which is also `short_term_debt`'s mechanism; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable (11.1) — `capital.debt_share` = 0.6 is the capital structure, stated (VERIFICATION 13.4) |
| `Corporate Credit A2.b` | MISSING | nothing carries a leverage target or a covenant line. `corporate_credit.rs Covenant` is unreachable. VERIFICATION 12.2: the `corporate_credit` system runs `running.rs Funding`, which is also `short_term_debt`'s mechanism; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable (11.1) |
| `Corporate Credit A2.c` | MISSING | VERIFICATION 13.4: `capital.debt_share` assigns the structure as a Preference primitive, which is what this VERIFY says it must never be |
| `Corporate Credit A3` | PARTIAL | packages/kernel-rs/src/running.rs `Funding` reads what falls due against what the party holds. `corporate_credit.rs coverage` is unreachable, so A3.b's read does not exist |
| `Corporate Credit A3.a` | PARTIAL | packages/kernel-rs/src/stores.rs `Owing` distinguishes interest from principal on the schedule; nothing reads the two together as service |
| `Corporate Credit A3.b` | MISSING | `corporate_credit.rs coverage` computes it and is unreachable (VERIFICATION 11.1) |
| `Corporate Credit A4` | MISSING | no assessment is held by anybody. packages/kernel-rs/src/mechanisms/second_opinion.rs is not imported (12.2) and packages/kernel-rs/src/mechanisms/ratings.rs `Rating` is dead code (11.1) |
| `Corporate Credit A4.a` | MISSING | see A4 |
| `Corporate Credit A4.b` | MISSING | see A4. VERIFICATION 13.2: every participant of a kind posts the same reservation, so nothing anywhere disagrees about a credit |
| `Corporate Credit B1` | MISSING | no instrument carries an early-termination regime (Bond N11) |
| `Corporate Credit B2` | MISSING | `corporate_credit.rs Covenant`/`standing`/`waive` implement B2, B2.a and B2.b and are unreachable. VERIFICATION 12.2: the `corporate_credit` system runs `running.rs Funding`, which is also `short_term_debt`'s mechanism; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable (11.1) |
| `Corporate Credit B3` | MISSING | no instrument carries a seniority, and no claim is ever filed against an estate (VERIFICATION 4.1) |
| `Corporate Credit B4` | MISSING | there is no floating coupon anywhere (Bond N5.b) |
| `Corporate Credit C1` | MISSING | `Funding` brings paper directly. No underwriter is appointed and none is paid. `corporate_credit.rs bring`/`Brought` is unreachable |
| `Corporate Credit C2` | MISSING | no book is built. `corporate_credit.rs Indication` is unreachable |
| `Corporate Credit C3` | MISSING | VERIFICATION 13.4: the coupon is `funding.coupon` = 0.04 for every issuer in the world, read from the parameter register. Nothing prices an issue |
| `Corporate Credit C4` | MISSING | there is no walk-away: `Funding` brings the paper unconditionally |
| `Corporate Credit C5` | MISSING | nothing allocates a book |
| `Corporate Credit C6` | MISSING | no proceeds reach an issuer and no fee reaches an underwriter; `Funding` proposes no instruction (VERIFICATION 9.1) |
| `Corporate Credit C7` | MISSING | `corporate_credit.rs` has the whole shape — `Brought`, `Basis`, `Member`, `syndicate`, `fee_gap` — and it is unreachable. VERIFICATION 12.2: the `corporate_credit` system runs `running.rs Funding`, which is also `short_term_debt`'s mechanism; `mechanisms::corporate_credit` is imported by nothing, so every function in it is unreachable (11.1) |
| `Corporate Credit C7.a` | MISSING | see C7 |
| `Corporate Credit C7.b` | MISSING | see C7; `fee_gap` is the read and is unreachable |
| `Corporate Credit C8` | MISSING | `corporate_credit.rs tap` is unreachable; `Funding` brings a fresh line every time it is short |
| `Corporate Credit C9` | MISSING | `agreed::COMMITMENT` is the shape and `corporate_credit.rs draw` decides it; the module is unreachable and nothing opens a line |
| `Corporate Credit C10` | MISSING | `corporate_credit.rs syndicate` is unreachable |
| `Corporate Credit C10.a` | MISSING | see C10 |
| `Corporate Credit C10.b` | MISSING | see C10 — the FORBID is guarded inside `syndicate`, which nothing calls |
| `Corporate Credit C10.c` | MISSING | see C10 |
| `Corporate Credit C11` | MISSING | `corporate_credit.rs Basis` states the two bases and is unreachable |
| `Corporate Credit C11.a` | MISSING | see C11 |
| `Corporate Credit C11.b` | MISSING | see C11 |
| `Corporate Credit C11.c` | MISSING | see C11 |
| `Corporate Credit C11.d` | MISSING | see C11 — a FORBID whose guard is in unreachable code |
| `Corporate Credit C11.e` | MISSING | see C11 |
| `Corporate Credit D1` | MISSING | `corporate_credit.rs secondary` is the schedule-posting side and is unreachable; no participant posts in a corporate bond book |
| `Corporate Credit D2` | MISSING | see D1 — nothing clears |
| `Corporate Credit D3` | MISSING | VERIFICATION 12.1: `dealing` is neither wired as a system nor imported by anything |
| `Corporate Credit D4` | MISSING | see D3 |
| `Corporate Credit D5` | MISSING | see D1 |
| `Corporate Credit D6` | MET | packages/kernel-rs/src/session.rs settles a trade as one instruction, units one way and money the other (the clause holds wherever a book does clear) |
| `Corporate Credit D7` | MISSING | `corporate_credit.rs accrued` is unreachable, so nothing adds accrued to what a trade settles (Bond N9.b) |
| `Corporate Credit D8` | MET | packages/kernel-rs/src/prices.rs — there is no door that writes a price from a derived measure, so the FORBID holds by absence. The static guard this row used to cite was deleted with the TypeScript and nothing replaced it (VERIFICATION 4.3, item 0j.6) |
| `Corporate Credit E1` | MET | packages/kernel-rs/src/register.rs `of_instrument` |
| `Corporate Credit E2` | MISSING | VERIFICATION 1.3: nothing sums held against issued |
| `Corporate Credit E3` | MISSING | VERIFICATION 6.1: `instruments.rs equity` values holdings at cost, not at the cleared price |
| `Corporate Credit E4` | MISSING | VERIFICATION 6.3: there is no revaluation, so no change in a mark reaches anybody's income. `corporate_credit.rs mark` is unreachable |
| `Corporate Credit E5` | MISSING | VERIFICATION 13.2: a holder's reservation is a stated multiple of the last print, not its cost of funds, its expected loss and its capital |
| `Corporate Credit E5.d` | MISSING | see E5 |
| `Corporate Credit E6` | MISSING | `corporate_credit.rs funding_withdrawn` is unreachable |
| `Corporate Credit E7` | MISSING | nothing charges regulatory capital against a position |
| `Corporate Credit E8` | PARTIAL | packages/kernel-rs/src/register.rs `pledge` encumbers units. Nothing pledges a corporate bond, and no haircut is read |
| `Corporate Credit E9` | MISSING | VERIFICATION 12.1: `observer` is not imported and packages/kernel-rs/src/mechanisms/observer.rs is dead code |
| `Corporate Credit F1` | MISSING | nothing accrues interest to a holder of record |
| `Corporate Credit F2` | PARTIAL | packages/kernel-rs/src/running.rs `Servicing` pays what fell due on the schedule out of the issuer's account. It pays the SCHEDULE's payee, not whoever the register says holds the paper then (Register A2.a) |
| `Corporate Credit F3` | PARTIAL | see F2 — principal is an `Owing` on the schedule; nothing reduces the holding when it is paid (Register E2) |
| `Corporate Credit F4` | MISSING | there is no early-termination regime to exercise (B1) |
| `Corporate Credit F5` | MISSING | `corporate_credit.rs refinanced_at` is unreachable; nothing retires an old issue with a new one |
| `Corporate Credit F6` | MISSING | nothing matures an instrument out of existence (Bond N10) |
| `Corporate Credit G1` | MISSING | no covenant exists and no missed payment is published as an event holders can observe |
| `Corporate Credit G2` | MISSING | nothing accelerates |
| `Corporate Credit G3` | MISSING | VERIFICATION 4.1: no claim is ever filed against an estate |
| `Corporate Credit G4` | MISSING | VERIFICATION 4.1, 7.2: nothing realises an estate; a dead party keeps what it held |
| `Corporate Credit G5` | MISSING | packages/kernel-rs/src/mechanisms/estate.rs `waterfall` is correct and is never reached (4.1) |
| `Corporate Credit G6` | MISSING | see G5 |
| `Corporate Credit G7` | MISSING | nothing restructures; there are no holders to decide |
| `Corporate Credit G8` | MISSING | there is no assessment for a default to move (A4) |
| `Corporate Credit H1` | MISSING | VERIFICATION 11.1: packages/kernel-rs/src/mechanisms/benchmarks.rs `Index` and `Constituent` are dead code |
| `Corporate Credit H2` | MISSING | a VERIFY, and it needs both sides at once: enough graded names for an assessment to differ across, and enough traded paper for the difference to show in a spread. Every grade this world publishes is the worst one (21.69) and the corporate books are thin (21.58). It is a MEASUREMENT and never a rule; positioned at 23.3 with Part XII's |
| `Corporate Credit H3` | MISSING | a VERIFY over two claims on ONE issuer that rank differently, and this world's issuers have one line each. It is a measurement and never a rule; positioned at 23.3, after 17b gives an issuer a junior tranche to be junior to |
| `Corporate Credit H4` | MISSING | the cash market clears (D2) and the synthetic is §19's CDS book, which exists and has never traded in a scale model (BUILT AND DEAD, `check:existence`). The BASIS between them is one derivation and it was 18.5's and item 18 closed without it; positioned at 21.137 |
| `Corporate Credit H4.a` | MISSING | the same basis, read the other way: neither leg sets the other. It is refused by construction — a derivative on an uncleared price is forbidden and both books clear on their own — and what is not built is the MEASUREMENT of the gap. Positioned at 21.137 with H4 |

## Sovereign

| requirement | status | where / why |
|---|---|---|
| `Sovereign A1` | PARTIAL | packages/kernel-rs/src/running.rs `Sovereign` reads what falls due on the treasury`s own schedule. Its outlays are hard-coded 0.0 (`Programme { redemptions, outlays: 0.0, buffer }`), so a fiscal authority with revenue and outlays does not exist |
| `Sovereign A1.c` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign A2` | PARTIAL | packages/kernel-rs/src/mechanisms/sovereign.rs `Programme::to_raise` sizes the need against the buffer, and packages/kernel-rs/src/running.rs `Funding` brings paper for it. There is no tenor mix and no plan made ahead of the outlay |
| `Sovereign A3` | MET | packages/kernel-rs/src/ledger.rs `account_of` (the treasury pays out of one balance, and it can be empty — a payment it cannot make is refused or queued) |
| `Sovereign A3.b` | MET | VERIFICATION 14.1, 11.2: there is no overdraft path anywhere. The guard written for it, `mechanisms/money.rs NoOverdraftForTheTreasury`, is dead code |
| `Sovereign A4` | MISSING | packages/kernel-rs/src/mechanisms/sovereign.rs `Missed` is the definition and is dead code (VERIFICATION 11.1). Nothing makes default a choice |
| `Sovereign B1` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign B2` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign B3` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign B4` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign B5` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign B6` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign B7` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign C1` | PARTIAL | packages/kernel-rs/src/systems.rs `TreasuryIssues` posts a sell sized from its own position. Nothing announces it ahead of the session, so C1.a`s calendar does not exist |
| `Sovereign C2` | MET | packages/kernel-rs/src/protocols.rs `Protocol::Call` with packages/kernel-rs/src/clearing.rs `clear` IS a uniform-price single-round sealed cross: one level, every winner at the stop-out. `mechanisms/sovereign.rs Auction` is a second implementation of it with no caller |
| `Sovereign C3` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign C4` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign C5` | PARTIAL | packages/kernel-rs/src/mechanisms/sovereign.rs `handle` returns `ComeBackToTheMarket { still_short }`. VERIFICATION 9.1: `Sovereign` journals the answer and proposes nothing, so the treasury never comes back |
| `Sovereign C6` | MET | packages/kernel-rs/src/session.rs (a fill settles as an instruction, so proceeds reach the treasury`s account) |
| `Sovereign C7` | PARTIAL | see C5 — the three ways a shortfall is handled are computed and said. None of them is done |
| `Sovereign D1` | MET | packages/kernel-rs/src/session.rs and packages/kernel-rs/src/prices.rs (where the book crosses, a price per unit prints) |
| `Sovereign D2` | MET | packages/kernel-rs/src/prices.rs (a yield is nowhere written into a price; the print is what the book crossed at) |
| `Sovereign D3` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign D4` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign D5` | PARTIAL | packages/kernel-rs/src/register.rs `pledge` can encumber a holding. Nothing sets a haircut and nothing pledges sovereign paper |
| `Sovereign D6` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign E1` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign E1.a` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign E2` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign E3` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign E4` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign E5` | MISSING | no risk weight is read anywhere against a sovereign holding |
| `Sovereign F1` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign F2` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign F3` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign F4` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign F5` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign G1` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign G2` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign G3` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign G4` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign G5` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign H1` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign H2` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign H3` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign H4` | MET | VERIFICATION 14.1: the boundary is drawn where A3.b draws it, because there is no monetary financing path at all |
| `Sovereign H5` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign I1` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign I1.a` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign I2` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign I3` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |
| `Sovereign I3.a` | MISSING | VERIFICATION 12.2, 9.1: the `sovereign` system runs `running.rs Sovereign`, which reads the treasury`s schedule, computes how a shortfall would be handled and journals it — it proposes no instruction. `mechanisms/sovereign.rs Auction` and `Missed` have no caller, and there is no central bank (14.1) for the monetary boundary |

## Short-Term Debt

| requirement | status | where / why |
|---|---|---|
| `Short-Term Debt A1` | PARTIAL | packages/kernel-rs/src/running.rs `Funding` brings paper with a tenor and a coupon read from `params`. A1.a`s discount instrument does not exist: every line it brings carries a coupon |
| `Short-Term Debt A2` | MISSING | VERIFICATION 13.4: the price is `paper.coupon` = 0.03 as a term for every issuer; `short_term_debt.rs yield_on` derives a yield from a price and is unreachable (11.1) |
| `Short-Term Debt A3` | PARTIAL | the issuer is whoever was short, so a type is an issuer. Nothing prices the credit differently for one (Corporate Credit A4) |
| `Short-Term Debt B1` | MET | packages/kernel-rs/src/running.rs `Funding` reads what falls due inside its own window and brings paper for exactly that shortfall |
| `Short-Term Debt B2` | MISSING | VERIFICATION 12.1, 12.2: `short_term_debt` runs `running.rs Funding` — the same mechanism as `corporate_credit` — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book |
| `Short-Term Debt B3` | MISSING | VERIFICATION 12.1, 12.2: `short_term_debt` runs `running.rs Funding` — the same mechanism as `corporate_credit` — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book |
| `Short-Term Debt B4` | MISSING | VERIFICATION 12.1, 12.2: `short_term_debt` runs `running.rs Funding` — the same mechanism as `corporate_credit` — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book |
| `Short-Term Debt B5` | MISSING | VERIFICATION 12.1, 12.2: `short_term_debt` runs `running.rs Funding` — the same mechanism as `corporate_credit` — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book |
| `Short-Term Debt C1` | MISSING | VERIFICATION 12.1, 12.2: `short_term_debt` runs `running.rs Funding` — the same mechanism as `corporate_credit` — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book |
| `Short-Term Debt C2` | MISSING | VERIFICATION 12.1, 12.2: `short_term_debt` runs `running.rs Funding` — the same mechanism as `corporate_credit` — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book |
| `Short-Term Debt C3` | MISSING | VERIFICATION 12.1, 12.2: `short_term_debt` runs `running.rs Funding` — the same mechanism as `corporate_credit` — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book |
| `Short-Term Debt C4` | MISSING | VERIFICATION 12.1, 12.2: `short_term_debt` runs `running.rs Funding` — the same mechanism as `corporate_credit` — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book |
| `Short-Term Debt D1` | MISSING | VERIFICATION 12.1, 12.2: `short_term_debt` runs `running.rs Funding` — the same mechanism as `corporate_credit` — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book |
| `Short-Term Debt D2` | MISSING | VERIFICATION 12.1, 12.2: `short_term_debt` runs `running.rs Funding` — the same mechanism as `corporate_credit` — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book |
| `Short-Term Debt D3` | MISSING | VERIFICATION 12.1, 12.2: `short_term_debt` runs `running.rs Funding` — the same mechanism as `corporate_credit` — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book |
| `Short-Term Debt D4` | MISSING | VERIFICATION 12.1, 12.2: `short_term_debt` runs `running.rs Funding` — the same mechanism as `corporate_credit` — and `mechanisms/short_term_debt.rs Limit`, `Rolled`, `roll`, `wall`, `prefers_paper`, `spread_over_bill` and `redeem` have no caller (11.1). No participant posts in a paper book |
| `Short-Term Debt E1` | MET | packages/kernel-rs/src/running.rs `Funding` brings a fresh line each period against the shortfall rather than rolling the old one at a written rate; nothing rolls automatically |
| `Short-Term Debt E2` | MET | packages/kernel-rs/src/prices.rs (no discount is ever computed from a curve, because nothing fits one) |
| `Short-Term Debt E3` | PARTIAL | packages/kernel-rs/src/ledger.rs refuses a delivery beyond what is held, so an outstanding cannot go negative. Nothing matures, so a maturity passing without cash moving is what every line does (Bond N10) |

## Equity

| requirement | status | where / why |
|---|---|---|
| `Equity A1` | MET | packages/kernel-rs/src/mechanisms/equity.rs (a share is the residual, ranking below all debt — packages/kernel-rs/src/mechanisms/estate.rs `Rank::Equity` is last) |
| `Equity A2` | MET | packages/kernel-rs/src/instruments.rs `Class::Share` counted in its own unit, and packages/kernel-rs/src/mechanisms/equity.rs `Line.outstanding` changed only by `apply` |
| `Equity A3` | MET | packages/kernel-rs/src/instruments.rs `ccy_of` (a share is quoted in its issuer`s money) |
| `Equity A4` | MET | packages/kernel-rs/src/instruments.rs (a share is brought with `matures: None`, and packages/kernel-rs/src/mechanisms/equity.rs says why there is no field to leave unset) |
| `Equity A5` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity A5.b` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity A6` | PARTIAL | packages/kernel-rs/src/instruments.rs `display` names a share by its issuer. Its one caller, `observer.rs display_name`, is dead code (VERIFICATION 11.1) |
| `Equity B1` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity B2` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity B3` | MET | packages/kernel-rs/src/prices.rs (nothing writes a price from a multiple, a book value or a discounted cash flow — the FORBID holds by absence) |
| `Equity B4` | MET | packages/kernel-rs/src/register.rs and packages/kernel-rs/src/prices.rs (shares times price is computed where it is asked and stored nowhere) |
| `Equity B4.a` | MET | nothing compares a stored capitalisation with shares times price, because nothing stores one |
| `Equity B5` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity B6` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity C1` | MET | packages/kernel-rs/src/register.rs `of_instrument` (who holds how many shares) |
| `Equity C1.a` | MISSING | VERIFICATION 1.3: nothing sums held against shares outstanding |
| `Equity C2` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity C3` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity C4` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity C5` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity C6` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity C7` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity D1` | PARTIAL | packages/kernel-rs/src/running.rs `Floating` brings a share line for a party short of capital or left holding unsold paper. `equity.shares` = 1000 is a stated count (VERIFICATION 13.4), nothing sells the shares, and D1.a`s dilution never happens |
| `Equity D2` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity D3` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity D4` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity E1` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity E2` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity E3` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity E4` | MISSING | VERIFICATION 4.1: nothing wipes a register on insolvency, because no estate is ever opened |
| `Equity F1` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity F2` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity F3` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity F4` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity G1` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity G2` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |
| `Equity G3` | MISSING | VERIFICATION 12.1: the `equity` system runs `running.rs Floating`, which brings a share line and journals; `mechanisms/equity.rs` is imported by nothing. No participant posts in an equity book, so no share price ever clears and nothing in §10 B–G happens |

## Money Market

| requirement | status | where / why |
|---|---|---|
| `Money Market A1` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market A2` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market A2.b` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market A3` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market B1` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market B2` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market B2.b` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market B3` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market B4` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market B5` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market B6` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market B6.a` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market B7` | MET | VERIFICATION 14.1: a name cannot fund because there is no market to fund in. The clause holds by absence, which is not the same as being representable |
| `Money Market C1` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market C2` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market C3` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market C4` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market C5` | MET | VERIFICATION 14.1: there is no central-bank credit at all, so there is none that is uncollateralised, unpriced and unlimited |
| `Money Market D1` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market D2` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market D3` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market D4` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market D5` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market D5.b` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market D6` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market E1` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market E2` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |
| `Money Market E3` | MISSING | VERIFICATION 12.1, 14.1: §11 has a participant and no mechanism. `posts("money_market", …, MoneyMarketBanks)` quotes `money_market.lends_at` and `money_market.borrows_at` — both declared 1.0 per annum — into an overnight book; `mechanisms/money_market.rs`, which holds the position, the corridor, the facility and the run, is imported by nothing |

## Spot FX

| requirement | status | where / why |
|---|---|---|
| `Spot FX A1` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX A2` | PARTIAL | packages/kernel-rs/src/mechanisms/currency.rs `Pair`/`Rates::of` hold one print and invert it rather than storing two. All of it is dead code (VERIFICATION 10.1) |
| `Spot FX A3` | PARTIAL | see A2 — `gap` measures the triangle and nothing repairs a print from it; dead code |
| `Spot FX B1` | PARTIAL | packages/kernel-rs/src/running.rs `SpotFx` reads who owes a money it has not got off its own schedule, which is the reason. Nothing is done about it (VERIFICATION 9.1) |
| `Spot FX B2` | PARTIAL | see B1 — it reads who holds a money it does not bank in |
| `Spot FX B3` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX B4` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX B5` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX B6` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX C1` | PARTIAL | packages/kernel-rs/src/mechanisms/spot_fx.rs `Posted`/`clearing` take schedules in rate space and are called by `SpotFx`. VERIFICATION 9.2: the rate each side posts is read from a print keyed by a currency code cast to an instrument id |
| `Spot FX C2` | PARTIAL | see C1. C2.a`s cross-consistency is not a constraint on the clearing: each currency clears alone |
| `Spot FX C3` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX C4` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX C5` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX C6` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX D1` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX D2` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX D3` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX D4` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX D5` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX E1` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX E2` | MET | packages/kernel-rs/src/mechanisms/spot_fx.rs (no parity formula, no differential applied to a level, no written path — the FORBID holds by absence) |
| `Spot FX E3` | PARTIAL | packages/kernel-rs/src/mechanisms/currency.rs `arbitrage` bounds the arbitrageur by its own capital, which is the clause. It is dead code (VERIFICATION 10.1) |
| `Spot FX E4` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX F1` | MISSING | VERIFICATION 9.1: `SpotFx` computes a rate and journals it (`let _ = ccy;`). No FX trade is ever proposed, so nothing settles, no position moves and no dealer carries anything |
| `Spot FX F1.a` | MISSING | VERIFICATION 8.1, 8.2: settlement converts at par on arrival with no counterparty, which is exactly the conversion inside a trade this FORBID names |
| `Spot FX F1.b` | MISSING | VERIFICATION 8.2: what a purchase settles in depends on the BUYER — `session.rs` pays out of the buyer`s own account — which is the convention this FORBID refuses. `settles_in` is written twice and called by neither (9.3) |

## Fund Shares

| requirement | status | where / why |
|---|---|---|
| `Fund Shares A1` | MET | packages/kernel-rs/src/parties.rs and packages/kernel-rs/src/register.rs (a fund is a named party with an account and rows) |
| `Fund Shares A2` | PARTIAL | packages/kernel-rs/src/running.rs `Subscribing` counts shares off `agreed::SUBSCRIPTION` terms. They are agreement terms rather than an instrument holders hold, so A2`s "counted in shares" is a number on a relation |
| `Fund Shares A3` | PARTIAL | packages/kernel-rs/src/mechanisms/redeemable.rs `Book::nav` computes assets minus liabilities over shares. Nothing checks that a fund`s equity is zero; the Accounts family is NOT BUILT (VERIFICATION 1.4) |
| `Fund Shares A4` | PARTIAL | `agreed::MANDATE` carries the lowest grade a pool may hold, and packages/kernel-rs/src/systems.rs `FundMandates` reads it. Nothing else constrains what a fund buys |
| `Fund Shares B1` | PARTIAL | packages/kernel-rs/src/mechanisms/redeemable.rs `Book::nav` is a read every time and is stored nowhere. VERIFICATION 6.2: its asset side is one of the three inline copies of units-times-price with a silent fallback to cost |
| `Fund Shares B2` | PARTIAL | see B1 — where a line printed the mark is the print; where it did not, the fallback is cost and nothing says so (B2.a) |
| `Fund Shares B3` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares B4` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares C1` | MET | packages/kernel-rs/src/running.rs `Subscribing` proposes the instruction: cash to the pool, shares to the holder at NAV (one of the ten propose sites, VERIFICATION 9.1) |
| `Fund Shares C2` | MISSING | `funds.rs Redeems`/`Redeemed` and `redeemable.rs holders_against_the_book` are the redemption and are unreachable (VERIFICATION 11.1). Nothing redeems, so C2.b`s forced-seller channel — "the point" — does not exist |
| `Fund Shares C3` | PARTIAL | shares rise on a subscription. Nothing takes them back (C2) |
| `Fund Shares C4` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares C5` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares D1` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares D2` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares D3` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares D4` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares D5` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares E1` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares E2` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares E3` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares E4` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares F1` | MET | packages/kernel-rs/src/ledger.rs (a fund holds only what an instruction delivered to it) |
| `Fund Shares F2` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares F3` | MISSING | VERIFICATION 9.1: the only fund mechanism that moves anything is `running.rs Subscribing` (cash in, shares out) and `Winding` (a pool paying its holders). `mechanisms/funds.rs Redeems`, `Redeemed`, `broke_the_buck`, `beats_the_deposit`, `cost_to_those_who_stay`, `shares_reconcile`, `winding_sale`, `mislaid` and `is_wound_up` have no caller (11.1) |
| `Fund Shares G1` | PARTIAL | packages/kernel-rs/src/mechanisms/redeemable.rs is the shape of a redeemable claim. `Meeting` and `holders_against_the_book` have no caller, so no investor can ask for its money back |

## Securities Lending

| requirement | status | where / why |
|---|---|---|
| `Securities Lending A1` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending A2` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending A3` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending A4` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending A5` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending B1` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending B2` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending B3` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending B4` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending C1` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending C2` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending C2.a` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending C3` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending C4` | MET | packages/kernel-rs/src/register.rs `pledge`/`free` and packages/kernel-rs/src/ledger.rs `Encumbered` — posted collateral leaves the poster`s free balance and cannot move |
| `Securities Lending C5` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending D1` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending D2` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending D2.a` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending D3` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |
| `Securities Lending E1` | MET | packages/kernel-rs/src/ledger.rs `ShortOfUnits` refuses a delivery beyond what is held, so a short that nobody lent cannot be written |
| `Securities Lending E2` | PARTIAL | the lender`s exposure and the borrower`s title would be two reads of one security; with no loan ever struck there is nothing to double-count. VERIFICATION 1.3: the sum-to-issued check that would catch it does not exist |
| `Securities Lending E3` | MISSING | VERIFICATION 9.1: `StockLending` proposes no instruction, so no security is delivered, no collateral posted, no fee paid and no manufactured payment made. `mechanisms/securities_lending.rs manufactured`, `recall`, `lendable`, `chain_from`, `can_short`, `Reinvested`, `FailedReturn` and `OnRecall` have no caller (11.1) |

## Prime Brokerage

| requirement | status | where / why |
|---|---|---|
| `Prime Brokerage A1` | PARTIAL | `agreed::PRIME_BROKERAGE` is a named bank and a named client, and packages/kernel-rs/src/running.rs `Broking` reads it. Nothing ends it |
| `Prime Brokerage A2` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage A3` | MISSING | packages/kernel-rs/src/running.rs `Broking` gives each client one broker by `brokers[n % brokers.len()]`, and says so — so A3`s blind spot, which E3 depends on, cannot arise |
| `Prime Brokerage A4` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage B1` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage B2` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage B3` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage B4` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage B5` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage C1` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage C2` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage C3` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage C3.b` | MET | packages/kernel-rs/src/systems.rs and packages/kernel-rs/src/running.rs contain no floor on an available line, and tools/phoenix-check refuses a bound anywhere in the engine. The narrower guard this row used to cite was deleted with the TypeScript (VERIFICATION 4.3, item 0j.6) |
| `Prime Brokerage C4` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage C4.a` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage C5` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage D1` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage D2` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage D3` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage D4` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage E1` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage E2` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage E3` | MISSING | VERIFICATION 9.1: `Broking` values a client book (one of the three inline units-times-price copies, 6.2), reads what the broker has lent and journals. It proposes no instruction: no loan is made, no margin is called, nothing is liquidated |
| `Prime Brokerage E4` | MET | `broker.limit` = 100000 money is read per client, so an exposure is finite |

## Derivative Layer

| requirement | status | where / why |
|---|---|---|
| `Derivative Layer A1` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer A2` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer A3` | PARTIAL | a derivative is one `agreed::DERIVATIVE` row naming two parties, so it is one contract and not two. Nothing values it, so the two sides are not a number read twice (VERIFICATION 6.3) |
| `Derivative Layer A4` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer B1` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer B2` | MET | packages/kernel-rs/src/stores.rs `Agreements` (one row, both parties indexed — not a copy on each book) |
| `Derivative Layer B3` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer B4` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer C1` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer C2` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer C3` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer C4` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer C5` | MET | packages/kernel-rs/src/mechanisms/derivative_layer.rs `Waterfall` is finite and enumerable by construction. It is dead code (VERIFICATION 11.1), so running past its end is unreachable rather than representable |
| `Derivative Layer D1` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer D2` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer D2.b` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer D2.c` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer D3` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer D4` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer D5` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer E1` | MISSING | `derivative_layer.rs admitted`/`cut_to`/`struck_beyond_capacity` are the capacity limit and have no caller (VERIFICATION 11.1) |
| `Derivative Layer E2` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer E3` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer E4` | MISSING | see E1: what was struck beyond what members could margin is exactly `struck_beyond_capacity`, which nothing calls |
| `Derivative Layer F1` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer F2` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer F3` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer F4` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer G1` | MET | packages/kernel-rs/src/stores.rs `Agreements` names two parties and there is no constructor with one |
| `Derivative Layer G2` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |
| `Derivative Layer G3` | MET | packages/kernel-rs/src/ledger.rs and stores.rs contain no netting across counterparties; `Settlement::unwind` states that every leg of a gridlock ring settles at full value and nothing is cancelled against anything |
| `Derivative Layer G4` | MISSING | VERIFICATION 9.1, 11.1: `Derivatives` proposes no instruction, and 15 of the 17 public items in `mechanisms/derivative_layer.rs` — `House`, `Waterfall`, `Variation`, `initial_margin`, `variation`, `close_out`, `novate`, `offset`, `net_against`, `through_the_house`, `claim_on_the_estate`, `gross_notional`, `admitted`, `cut_to`, `struck_beyond_capacity` — have no caller. `derivatives.collateral` is one of the three nouns with no kernel home |

## CDS

| requirement | status | where / why |
|---|---|---|
| `CDS A1` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS A2` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS A3` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS A4` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS A4.a` | MET | a reference entity would have to be a party in this world; nothing writes protection on anything else, because nothing writes protection |
| `CDS A5` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS B1` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS B2` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS B3` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS B4` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS B5` | MISSING | VERIFICATION 13.2: every participant of a kind posts the same reservation, so no speculative view exists on either side of any book. The static guard this row used to cite was deleted with the TypeScript and nothing replaced it (VERIFICATION 4.3, item 0j.6) |
| `CDS C1` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS C2` | MET | packages/kernel-rs/src/mechanisms/cds.rs derives the implied probability from a spread and never the other way. It is the right direction in code nothing reaches (VERIFICATION 12.1) |
| `CDS C3` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS C4` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS D1` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS D2` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS D2.a` | MET | packages/kernel-rs/src/mechanisms/cds.rs has no fixed recovery: a recovery is an argument, not a constant. Nothing calls it |
| `CDS D3` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS D4` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS D5` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS E1` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS E2` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS E3` | MISSING | VERIFICATION 12.1, 9.1: the `cds` system runs `running.rs Protection`, which journals; `mechanisms::cds` is imported by nothing, so `Contract`, `Series`, `Point`, `net_notional`, `owed_on_event`, `pays_out` and `unfunded_exposure` are unreachable. No CDS is ever struck and no spread ever clears |
| `CDS E4` | MISSING | VERIFICATION 9.1: `Protection` proposes no instruction, so a seller`s ability to pay is never part of anything |

## IRS

| requirement | status | where / why |
|---|---|---|
| `IRS A1` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS A2` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS A3` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS A4` | MET | a schedule row is in one money, so nothing this system does spans two currencies |
| `IRS B1` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS B2` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS B3` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS B4` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS B5` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS C1` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS C2` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS C3` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS C4` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS D1` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS D2` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS D3` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS D4` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS E1` | MISSING | VERIFICATION 12.2: the `irs` system runs `running.rs Servicing`, the same mechanism `lending` runs — it pays what fell due on a schedule out of the payer`s account. That is principal and interest moving, which is what this FORBID calls a loan |
| `IRS E2` | MISSING | VERIFICATION 12.2: `irs` runs `running.rs Servicing`, the generic pay-what-fell-due mechanism shared with `lending`. `mechanisms::irs` is imported by nothing, so `Swap`, `Net`, `fixes_at` and `pairs_up` are unreachable. There is no fixed leg, no floating leg, no fixing, no netting and no mark |
| `IRS E3` | MET | there is no floating leg at all, so none fixes on a rate this world does not produce (Bond N5.b) |

## FX Forwards

| requirement | status | where / why |
|---|---|---|
| `FX Forwards A1` | PARTIAL | packages/kernel-rs/src/mechanisms/fx_forwards.rs `Forward::struck` refuses one money on both legs, refuses a party facing itself and refuses zero time. It is dead code (VERIFICATION 11.1) |
| `FX Forwards A2` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards A3` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards A4` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards B1` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards B2` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards B3` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards B3.b` | MISSING | VERIFICATION 10.2: there are two bases and two parity points — `currency.rs covered`/`basis` and `fx_forwards.rs parity`/`basis`, the same expressions under two names, each documented as the only one |
| `FX Forwards B4` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards C1` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards C2` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards C3` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards C4` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards D1` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards D2` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards D3` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards D4` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards E1` | MET | nothing writes a forward rate from a parity formula, because nothing writes a forward rate |
| `FX Forwards E2` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards E3` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |
| `FX Forwards E4` | MISSING | VERIFICATION 9.1: `FxForwards` computes a parity point and a basis and journals them. It proposes no instruction, so no forward is struck, nothing settles and no notional is exchanged |

## Commodity Futures

| requirement | status | where / why |
|---|---|---|
| `Commodity Futures A1` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures A2` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures A3` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures A4` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures B1` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures B2` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures B3` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures B4` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures B5` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures C1` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures C2` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures C3` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures C4` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures D1` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures D2` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures D3` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures D4` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures E1` | MET | no futures price exists at all |
| `Commodity Futures E2` | MISSING | VERIFICATION 11.1: `mechanisms/commodities.rs Future`, `AtExpiry`, `at_expiry`, `against_carry`, `full_carry`, `can_converge`, `may_list`, `open_interest_against_supply` and `supply_at` have no caller. No future is ever listed, margined, rolled or delivered |
| `Commodity Futures E3` | MET | `commodities.rs against_carry` prices a roll off the curve and has no caller; nothing rolls, so no roll is free |

## Commodities Spot

| requirement | status | where / why |
|---|---|---|
| `Commodities Spot A1` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot A2` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot A3` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot A4` | PARTIAL | packages/kernel-rs/src/register.rs holds units of a good by named holder at a named place. `commodities.rs supply_at` is the read and has no caller |
| `Commodities Spot B1` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot B2` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot B3` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot B4` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot C1` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot C2` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot C3` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot C4` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot D1` | PARTIAL | packages/kernel-rs/src/session.rs clears a goods book per line. A grade-and-location identity is not the key: a line is one instrument, and packages/kernel-rs/src/places.rs holds where things are separately |
| `Commodities Spot D2` | PARTIAL | inventory is the register`s own rows and carries across periods. It is not an input to any price: every bid is `last print x k` (VERIFICATION 13.2) |
| `Commodities Spot D3` | MET | packages/kernel-rs/src/running.rs `Storing` proposes the payment — storage costs money and it is paid to whoever owns the storage (one of the ten propose sites, VERIFICATION 9.1) |
| `Commodities Spot D4` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot D5` | MISSING | VERIFICATION 1.4: the Units family has one contribution, `capital_programme.rs PlantMoves`, and it covers capital lines only. `commodities.rs units_balance` is the identity for goods and has no caller |
| `Commodities Spot E1` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot E2` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot E3` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot E4` | MISSING | VERIFICATION 9.1: the `commodities` system runs `running.rs Storing`, which charges storage and reads what was consumed off the wire. Nothing produces a commodity, nothing prices one against inventory, and `mechanisms/commodities.rs Producer`, `supply_at`, `units_balance` and `open_interest_against_supply` have no caller (11.1) |
| `Commodities Spot F1` | MET | packages/kernel-rs/src/ledger.rs (`Leg::Destroy` is pre-checked against what is free, so nothing is consumed that is not there) |
| `Commodities Spot F2` | MET | packages/kernel-rs/src/ledger.rs (a `Destroy` beyond what is held is refused as `ShortOfUnits`; a holding cannot go negative through the wire) |
| `Commodities Spot F3` | MET | packages/kernel-rs/src/prices.rs (no commodity price series is applied anywhere) |

## Indices

| requirement | status | where / why |
|---|---|---|
| `Indices A1` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices A2` | MET | packages/kernel-rs/src/mechanisms/benchmarks.rs `fix` takes only a `Provenance::Cleared` print — a carried or seeded one is refused, which is what "reads cleared prices and nothing else" means |
| `Indices A3` | MET | packages/kernel-rs/src/mechanisms/benchmarks.rs (nothing feeds an index level back to a constituent, because no index level is computed at all) |
| `Indices A4` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices B1` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices B2` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices B3` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices B4` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices C1` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices C2` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices C2.a` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices C3` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices C4` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices D1` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices D2` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices D3` | PARTIAL | packages/kernel-rs/src/running.rs `Fixes` publishes an overnight fixing from a cleared print and refuses anything else. VERIFICATION 3.2: one book of 1,546 prints per period, so there is almost never a fixing to publish |
| `Indices D3.b` | MET | packages/kernel-rs/src/mechanisms/benchmarks.rs `fix` returns `None` for `Provenance::Carried` and `Provenance::Seeded`, so a posted rate can never be a benchmark |
| `Indices D4` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices D5` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices E1` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |
| `Indices E2` | MET | no index level is stored anywhere, because none is computed |
| `Indices E3` | MISSING | VERIFICATION 11.1: `mechanisms/benchmarks.rs Index`, `Constituent`, `Base`, `Weighing`, `Fixing`, `divergence`, `on_split`, `squeeze` and `trackers_must_trade` have no caller. Only `fix` is reached, by `running.rs Fixes`. No index is built, so §22 A, B, C and D1, D2, D4, D5 have nothing behind them |

## Banks Lending

| requirement | status | where / why |
|---|---|---|
| `Banks Lending A1` | PARTIAL | `agreed::COMMITMENT` is a named lender to a named borrower and packages/kernel-rs/src/mechanisms/lending.rs `Rows` is a book of loan rows. Nothing originates one |
| `Banks Lending A2` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending A3` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending A3.a` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending A3.b` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending A4` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending A5` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending B1` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending B1.c` | MISSING | VERIFICATION 13.4: `lending.will_lend` = 0.3 "per unit held" is a bank lending a fraction of what it holds, which is what this FORBID names |
| `Banks Lending B2` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending B2.d` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending C1` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending C1.d` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending C2` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending C3` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending C3.a` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending C4` | MET | there is no default-probability model anywhere, so there are not two (Corporate Credit A4) |
| `Banks Lending D1` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending D2` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending D2.b` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending D3` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending D4` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending D5` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending E1` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending E2` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending E3` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending E4` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending E5` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending E5.a` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending E6` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending F1` | PARTIAL | packages/kernel-rs/src/mechanisms/lending.rs `Rows` is a sum of named rows and never a scalar. It has one caller and nothing writes a row into it |
| `Banks Lending F1.a` | MET | packages/kernel-rs/src/mechanisms/lending.rs holds loans as rows; there is no loan-book number anywhere that is not a sum of rows |
| `Banks Lending F2` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |
| `Banks Lending F3` | MISSING | VERIFICATION 12.1: `lending` runs `running.rs Servicing` — the generic pay-what-fell-due mechanism, shared with `irs` — and `mechanisms::lending` is imported by nothing beyond its own `Rows`. No loan is written, no rate quoted, no borrower declined, no provision booked and no loss written off |

## Banks Funding

| requirement | status | where / why |
|---|---|---|
| `Banks Funding A1` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding A1.d` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding A2` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding A3` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding A4` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding A5` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding B1` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding B2` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding B2.b` | MET | packages/kernel-rs/src/stores.rs `Schedules` is the one writer of what a liability pays, so a margin statistic and the cash that leaves read the same row |
| `Banks Funding B3` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding C1` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding C2` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding C3` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding C3.a` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding C4` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding D1` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding D2` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding D3` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding D4` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding D5` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding D6` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding D6.a` | MET | VERIFICATION 14.1: there is no central-bank credit line at all, so there is none that is unbounded and unpriced |
| `Banks Funding E1` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding E2` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding E3` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding E3.a` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding E4` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding E5` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding F1` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |
| `Banks Funding F2` | MET | packages/kernel-rs/src/register.rs (one row at the central bank, moved only by settlement legs — `ledger.rs across` is its only writer, and there is no mirrored copy) |
| `Banks Funding F3` | MISSING | VERIFICATION 1.4: the Accounts family has no contribution and is NOT BUILT every period |
| `Banks Funding F4` | MISSING | VERIFICATION 9.1: `BankFunding` reads and journals, and proposes no instruction. `mechanisms/bank_funding.rs Liquid`, `Short`, `after_outflow`, `buffer_wanted`, `balances`, `net_interest_margin`, `transformation` and `when_short` have no caller (11.1). No deposit class exists, nothing runs, nothing is bid up for |

## Banks Capital

| requirement | status | where / why |
|---|---|---|
| `Banks Capital A1` | PARTIAL | packages/kernel-rs/src/instruments.rs `equity` is a residual and is never a stored pot. VERIFICATION 6.1: it is computed at cost, so a loss on a mark never reaches it |
| `Banks Capital A1.a` | MET | packages/kernel-rs/src/instruments.rs `equity` is computed at every read and nothing withdraws from it |
| `Banks Capital A2` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital A3` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital A4` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital B1` | PARTIAL | `bank.min_weighted` = 0.08 and `bank.min_leverage` = 0.03 are declared POLICY primitives owned by the parliament, and `BankCapital` reads them. Nothing weights an asset, so B1.a`s risk weights do not exist |
| `Banks Capital B1.c` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital B2` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital B3` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital B3.a` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital C1` | PARTIAL | packages/kernel-rs/src/running.rs `Failing` ceases a party whose `equity` is below zero, which is the solvency trigger. C1.a`s second trigger does not exist: nothing fails for liquidity, so the resolution can never say which one fired |
| `Banks Capital C2` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital C3` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital D1` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital D2` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital D2.a` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital D3` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital D4` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital D5` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital D6` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital E1` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital E2` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |
| `Banks Capital E3` | MISSING | VERIFICATION 9.1: `BankCapital` reads a bank position against two declared ratios and journals. It proposes nothing, and `mechanisms/bank_capital.rs Absorbed`, `Binding`, `Conservation`, `absorb`, `hole`, `trigger` and `no_creditor_worse_off` have no caller (11.1). Nothing recapitalises, nothing is resolved, nothing is bailed in |

## Dealer Desks

| requirement | status | where / why |
|---|---|---|
| `Dealer Desks A1` | PARTIAL | packages/kernel-rs/src/systems.rs `Dealers` is a named party of `kinds::DEALER` with its own register rows. It has no balance sheet inside a bank (A1) and no funding cost |
| `Dealer Desks A2` | PARTIAL | packages/kernel-rs/src/systems.rs `Dealers::orders` posts a bid and an ask. VERIFICATION 12.1: no dealer kind is wired as a system, and 13.1: both sides come off a stated width applied to a constant |
| `Dealer Desks A3` | MET | packages/kernel-rs/src/register.rs (what it bought and has not sold is its own row, read by `Dealers::orders` as `view.quantity(line)`) |
| `Dealer Desks A4` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks B1` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks B2` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks B3` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks B4` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks C1` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks C2` | PARTIAL | packages/kernel-rs/src/systems.rs `Dealers::orders` skews on inventory against its limit, which is the one part of §26 C that is a consequence. The level it skews around is a constant (VERIFICATION 13.1) |
| `Dealer Desks C3` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks C4` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks C5` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks C5.a` | MISSING | VERIFICATION 13.1: `bid = around - width - skew; ask = around + width - skew` is a stated width applied to a mid, which is what this FORBID names |
| `Dealer Desks C5.b` | MISSING | VERIFICATION 13.1: `dealer.width` = 0.02, declared "share of the mid", is the stated spread table this FORBID names |
| `Dealer Desks D1` | PARTIAL | packages/kernel-rs/src/systems.rs `Dealers::orders` stops quoting at `held.abs() >= limit`. VERIFICATION 13.1: the comparison is units against a money amount |
| `Dealer Desks D2` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks D3` | MISSING | nothing charges a desk rent for its inventory. XI-4 joint three: a desk that carries inventory for free has no reason to shed it |
| `Dealer Desks D4` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks D5` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks E1` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks E2` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks E3` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks E4` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks F1` | MET | packages/kernel-rs/src/systems.rs `Dealers::orders` returns no orders once the limit binds, so the balance sheet is finite |
| `Dealer Desks F2` | MISSING | VERIFICATION 12.1: §26 runs `systems.rs Dealers`, a participant posting a stated width around a constant (13.1). `mechanisms/dealing.rs` is imported by nothing |
| `Dealer Desks F3` | MISSING | a desk has no P&L at all: VERIFICATION 6.1 values its book at cost and 6.3 has nothing revalue it, so what the inventory did is never booked |

## Insurers

| requirement | status | where / why |
|---|---|---|
| `Insurers A1` | PARTIAL | `kinds::INSURER` is a named party with an account and rows, and packages/kernel-rs/src/systems.rs `InsurerMatching` puts it in long books. It holds no liability to anybody |
| `Insurers A2` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers A3` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers A4` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers B1` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers B2` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers B2.b` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers B3` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers B4` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers C1` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers C2` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers C3` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers C4` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers C5` | MISSING | was OUT OF SCOPE on the ground that an insurer buys credit through the pools it subscribes to and the pool holds the mandate. VERIFICATION 12.1: the pools and the mandate machinery cited are the deleted TypeScript; nothing in packages/kernel-rs forces a sale on a downgrade (Ratings C1.a) |
| `Insurers D1` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers D2` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers D3` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers D4` | MISSING | was OUT OF SCOPE on the ground that the gap is hedged in the swap and futures books, "which exist". VERIFICATION 12.2: `irs` runs the generic loan-servicing mechanism and there is no futures book anywhere, so the reason the exemption rested on is not true of this engine |
| `Insurers D5` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers E1` | MISSING | VERIFICATION 12.1: there is no liability at all, so there is none without beneficiaries — the FORBID has nothing to refuse |
| `Insurers E2` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |
| `Insurers E3` | MISSING | nothing stores a liability value, because nothing holds a liability. `insurers.rs present_value` is the read and has no caller |
| `Insurers E4` | MISSING | VERIFICATION 12.1: `insurers` is wired as a participant only — `posts("insurers", …, InsurerMatching)` — and `mechanisms/insurers.rs Institution`, `Bears`, `Catastrophe`, `Hedged`, `OnShortfall`, `Placed`, `present_value`, `duration_gap`, `experience` and `illiquidity_premium` are imported by nothing (11.1, 12.1). No policy is written, no premium collected, no claim paid and no liability discounted |

## Hedge Funds

| requirement | status | where / why |
|---|---|---|
| `Hedge Funds A1` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds A2` | PARTIAL | `agreed::SUBSCRIPTION` gives investors a share count they could redeem. Nothing redeems (Fund Shares C2) |
| `Hedge Funds A3` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds A4` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds A5` | MISSING | VERIFICATION 6.1: its book is valued at cost by `instruments.rs equity`, so its equity does not move when a price does |
| `Hedge Funds B1` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds B2` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds B3` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds B4` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds B5` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds C1` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds C2` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds C3` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds C4` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds D1` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds D2` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds D3` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds D4` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds D5` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds D6` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds D7` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds E1` | MET | packages/kernel-rs/src/stores.rs (borrowing exists only as an agreement naming a lender; there is no leverage field on a party) |
| `Hedge Funds E2` | MISSING | VERIFICATION 12.1, 9.1: `hedge_funds` runs `running.rs Levered`, which journals and proposes nothing, and `mechanisms::hedge_funds` is imported by nothing — `Borrowing`, `Round`, `after_a_loss`, `spiral` and `will_buy` are unreachable (11.1). Nothing borrows, nothing is margin-called and no loss chain runs |
| `Hedge Funds E3` | PARTIAL | packages/kernel-rs/src/running.rs `Failing` ceases any party whose equity is below zero, a fund included. VERIFICATION 7.2: ceasing does nothing — it keeps what it held and its investors lose nothing |

## Private Equity

| requirement | status | where / why |
|---|---|---|
| `Private Equity A1` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity A2` | MET | packages/kernel-rs/src/running.rs `Calling` proposes the capital call — real money out of a named investor`s balance (one of the ten propose sites, VERIFICATION 9.1) |
| `Private Equity A2.a` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity A2.b` | PARTIAL | `Calling` proposes the call and the wire refuses or queues it where the investor is short, so the call is not bounded by spare cash. What is missing is the other half: an investor that does not pay does not default on it |
| `Private Equity A3` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity A4` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity A5` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity B1` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity B2` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity B3` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity B4` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity B5` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity C1` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity C2` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity C3` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity C4` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity C5` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity C5.a` | MET | VERIFICATION 6.1: an unlisted holding is carried at cost by `instruments.rs equity` and no mark is ever treated as cleared. It holds because nothing marks anything |
| `Private Equity D1` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity D2` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity D3` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity D4` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity D5` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity E1` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |
| `Private Equity E2` | MET | see A2 — a call is a real payment out of a real balance |
| `Private Equity E3` | MISSING | VERIFICATION 12.1: `private_equity` runs `running.rs Calling`, which proposes capital calls; `mechanisms::private_equity` is imported by nothing, so `Buyout`, `Called`, `Exit`, `Failure`, `Mark`, `exit`, `mark_against_exit`, `sources_and_uses` and `transformed` are unreachable (11.1). No buyout happens, nothing is held, nothing exits |

## Treasury

| requirement | status | where / why |
|---|---|---|
| `Treasury A1` | MET | packages/kernel-rs/src/parties.rs `kinds::TREASURY` with an account it pays out of, and packages/kernel-rs/src/ledger.rs refuses a payment it cannot make, so A1.a`s balance can run low |
| `Treasury A2` | MET | packages/kernel-rs/src/registry.rs `currency_of(region)` read through the country |
| `Treasury A3` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury B1` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury B2` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury B3` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury B4` | PARTIAL | packages/kernel-rs/src/running.rs `Servicing` pays what falls due, maturing debt included. VERIFICATION 15.1: it pays one holder rather than the holders |
| `Treasury C1` | MISSING | nothing levies a tax. `treasury.rs Collected` is the read of what named payers remitted and has no caller (VERIFICATION 11.1) |
| `Treasury C2` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury C3` | MISSING | see C1 — there are no receipts, so there is nothing to be a sum of |
| `Treasury D1` | PARTIAL | packages/kernel-rs/src/systems.rs `TreasuryIssues` sizes its order with `treasury::must_raise(outlays, receipts, own_cash, buffer)` and posts a sell. Receipts are always zero (C1), and `running.rs Funding` calls the same function with `0.0` hard-coded for receipts |
| `Treasury D2` | MET | packages/kernel-rs/src/systems.rs `TreasuryIssues` posts a sell into a book that must clear, and packages/kernel-rs/src/session.rs settles what crosses |
| `Treasury D3` | MET | VERIFICATION 14.1, 11.2: there is no overdraft path and no facility that amounts to one. The guard written for it is dead code |
| `Treasury D4` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury D5` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury D5.a` | MET | packages/kernel-rs/src/clearing.rs asserts every buy names a level, so nobody absorbs a remainder by construction |
| `Treasury D6` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury E1` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury E2` | PARTIAL | packages/kernel-rs/src/systems.rs `TreasuryIssues` chooses a size against its own cash position each period. It chooses no timing and no maturity (E1) |
| `Treasury E3` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury E4` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury F1` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury F2` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury F3` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |
| `Treasury F4` | MISSING | VERIFICATION 12.1: `treasury` is wired as a participant (`posts("treasury", …, TreasuryIssues)`) and `mechanisms::treasury` is imported only for `must_raise`. `Treasury`, `Outlay`, `Collected`, `Bond`, `BoughtInTheMarket`, `central_bank_buys`, `cost_of_issuing`, `debt_reconciles`, `interest_reaches` and `rollover_exposure` have no caller (11.1). Nothing spends on a named thing, nothing taxes a named payer |

## Central Bank

| requirement | status | where / why |
|---|---|---|
| `Central Bank A1` | PARTIAL | packages/kernel-rs/src/instruments.rs (an issuer issues one money and `account_of` reads it, so a central bank is the issuer of its reserves). Nothing else of §31 exists |
| `Central Bank A2` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank A2.c` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank A3` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank A4` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank B1` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank B2` | MET | there is no policy rate anywhere in packages/kernel-rs, so it is not traded. The clause holds by the whole of §31 being absent |
| `Central Bank B3` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank B3.a` | MET | VERIFICATION 14.1: the FORBID holds because there is no policy rate and no money market — not because a corridor separates them |
| `Central Bank B4` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank C1` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank C1.b` | MET | VERIFICATION 14.1: nothing buys in a primary market at all |
| `Central Bank C2` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank C3` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank C4` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank D1` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank D2` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank D3` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank D3.a` | MET | VERIFICATION 14.1: there is no lending facility, so it cannot lend to an insolvent bank |
| `Central Bank D4` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank E1` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank E2` | MET | VERIFICATION 14.1, 11.2: the treasury has no overdraft path. The guard written for it, `mechanisms/money.rs NoOverdraftForTheTreasury`, is dead code |
| `Central Bank E3` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank E4` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank E5` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank F1` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank F2` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank F3` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |
| `Central Bank F4` | MISSING | VERIFICATION 14.1: §31 has no system. There is no `works("central_bank", …)` anywhere in packages/kernel-rs/src/systems.rs, no policy rate, no corridor, no facility and no open-market operation in the engine |

## Polity

| requirement | status | where / why |
|---|---|---|
| `Polity A1` | PARTIAL | `parliament.seats` = 100 is declared `Kind::Policy`, `Owner::Constitution` — the one number no mechanism produces, as A1 asks |
| `Polity A2` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity A3` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity A4` | MET | packages/kernel-rs/src/running.rs `Elections` places the term by DATE (`today.0 - last >= term`, read off the journal), never by a count of periods |
| `Polity B1` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity B1.a` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity B2` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity B2.a` | MET | there is no turnout, swing or vote-share parameter anywhere in the 61 declared numbers |
| `Polity B2.b` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity B3` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity B4` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity C1` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity C2` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity C3` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity C3.a` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity C3.b` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity C4` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity D1` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity D2` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity D3` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity D3.a` | MISSING | VERIFICATION 13.4: `treasury.will_accept` = 0.98 is a PRICE declared `Kind::Policy` with `Owner::Parliament`, which is exactly what this FORBID refuses |
| `Polity D4` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity D5` | MET | packages/kernel-rs/src/params.rs `ParamDecl.owner` prints the owner beside the value, and packages/kernel-rs/src/systems.rs names one for all 61 |
| `Polity E1` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity E2` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity E3` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity E4` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity F1` | MET | packages/kernel-rs/src/running.rs `Elections` opens an election off the calendar; no result is written anywhere |
| `Polity F2` | MET | no POLICY primitive follows a series: every one of the six is a single declared value |
| `Polity F3` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |
| `Polity F4` | MET | no approval rating exists |
| `Polity F5` | MISSING | VERIFICATION 12.1: `polity` runs `running.rs Elections`, which opens an election process on a term timer and journals it. `mechanisms::polity` is imported by nothing, so `Platform`, `votes_for`, `poll`, `government`, `Elected`, `Owns` and `turnout` are unreachable (11.1). No cell votes, no seats are allotted, no coalition forms and no mandate reaches the register — so §47 C3.b, "every such primitive`s value in the register equals the standing mandate`s, every period, exactly", has no mandate to equal |

## Firm

| requirement | status | where / why |
|---|---|---|
| `Firm A1` | MET | packages/kernel-rs/src/parties.rs and packages/kernel-rs/src/register.rs (a firm is a named party with an account and rows) |
| `Firm A2` | PARTIAL | packages/kernel-rs/src/parties.rs puts it in a region and packages/kernel-rs/src/registry.rs reads its money through the country. It has no sector: nothing says what it buys and sells beyond the lines it holds |
| `Firm A3` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm A4` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm B1` | PARTIAL | packages/kernel-rs/src/session.rs prints revenue where a goods book crosses, and `GoodsSellers` posts from the firm`s own cost. VERIFICATION 3.2: one book of 1,546 crosses per period |
| `Firm B2` | MET | packages/kernel-rs/src/running.rs `Making` draws the recipe`s inputs as `Leg::Destroy` legs at their own lot cost |
| `Firm B3` | MET | packages/kernel-rs/src/running.rs `Wages` pays named households, and `Making` reads the same engagements into the batch cost |
| `Firm B4` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm B5` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm B6` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm C1` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm C2` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm C3` | MET | packages/kernel-rs/src/instruments.rs `equity` is a read of assets minus liabilities and can be negative. VERIFICATION 6.1: it is a read at cost |
| `Firm C4` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm D1` | MET | packages/kernel-rs/src/ledger.rs refuses or queues a payment out of a balance that will not cover it |
| `Firm D2` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm D3` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm D4` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm D5` | PARTIAL | packages/kernel-rs/src/ledger.rs `Queue::gave_up` records the arrear when the days run out. Nothing follows: no event, no lender loss, no rating action |
| `Firm E1` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm E2` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm E3` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm E4` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm E5` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm E6` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |
| `Firm E7` | MET | packages/kernel-rs/src/running.rs `Forming` forms the firm`s own adaptive expectation of what it sells and how much, and `Publishes` publishes a result against it |
| `Firm F1` | MET | packages/kernel-rs/src/ledger.rs (every leg names both sides; there is no revenue without a buyer and no cost without a payee) |
| `Firm F2` | MET | no earnings path exists: a firm`s result is whatever its own legs and marks produced |
| `Firm F3` | MET | packages/kernel-rs/src/ledger.rs (a firm`s balance is checked before every payment and a short one fails) |
| `Firm F4` | MISSING | VERIFICATION 12.1: `firms` runs `running.rs Reporting`, which journals a firm result; `mechanisms::firms` is imported by nothing, so `Firm`, `CostLine`, `Invoice`, `Distribution`, `Funds`, `LeverageTarget`, `revenue`, `costs_at`, `operating_profit`, `receivables` and `cash_from_operations` are unreachable (11.1). A firm decides nothing in §32 E |

## Capital Programme

| requirement | status | where / why |
|---|---|---|
| `Capital Programme A1` | MET | packages/kernel-rs/src/instruments.rs `Class::Plant` held by a named firm on the register |
| `Capital Programme A2` | MET | packages/kernel-rs/src/mechanisms/capital_programme.rs `capacity` over the vintages, read by packages/kernel-rs/src/running.rs `Making` — output is limited by it |
| `Capital Programme A3` | MISSING | packages/kernel-rs/src/mechanisms/capital_programme.rs `worn` is the one schedule A3 asks for and it is unreached (VERIFICATION 11.1). Nothing depreciates: no charge against profit and no reduction in the stock. (The row was also the one MALFORMED row in this file — it had no closing table cell, so no reader and no tool had ever counted it: VERIFICATION 0.5) |
| `Capital Programme A4` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme A5` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme A6` | MET | packages/kernel-rs/src/register.rs lots ARE the vintages (units, cost, service date), and `Making` reads capacity over them rather than storing a stock |
| `Capital Programme A6.b` | MET | packages/kernel-rs/src/mechanisms/capital_programme.rs `PlantMoves` is the one real audit family in the world: it walks the period`s legs, compares them with the register per capital line, and derives its own dust (VERIFICATION 1.4) |
| `Capital Programme B1` | PARTIAL | packages/kernel-rs/src/running.rs `Building` reads the firm`s own `HOW_MUCH_IT_SELLS` and `WHAT_IT_SELLS_FOR` outlooks and opens a capital programme against them. It proposes nothing, so nothing is bought and no capacity arrives |
| `Capital Programme B2` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme B3` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme B4` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme B5` | MET | no investment rate exists: `Building` compares an expected return against a hurdle and is not a fraction of anything |
| `Capital Programme C1` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme C2` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme C3` | PARTIAL | `invest.takes` = 3 periods is the build lag and `Building` opens a process that closes after it. Nothing is in service at the end of it |
| `Capital Programme C4` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme D1` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme D2` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme D3` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme D4` | MISSING | VERIFICATION 1.4: capacity, output and utilisation are reconciled nowhere; `recipe.rs utilisation` is unreached |
| `Capital Programme E1` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme E2` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme E3` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme E4` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |
| `Capital Programme F1` | MISSING | VERIFICATION 9.1: `Building` opens a capital programme and journals, and proposes no instruction — nothing is bought from a capital-goods producer, nothing is paid for and no plant arrives. Of 9 items in mechanisms/capital_programme.rs, `Plant`, `PlantMoves` and `capacity` are reached and `worn` is not (11.1) |

## Firm Birth

| requirement | status | where / why |
|---|---|---|
| `Firm Birth A1` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth A2` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth A2.a` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth A3` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth A4` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth A5` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth A4.a` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth A4.b` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth B1` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth B2` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth B3` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth B4` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth C1` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth C2` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth C2.a` | MET | no default is drawn from a hazard rate anywhere; packages/kernel-rs/src/running.rs `Failing` reads a party`s own equity |
| `Firm Birth C3` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth C4` | PARTIAL | `Failing` traces a cessation to the equity read that caused it and journals the size. VERIFICATION 6.1: that read is at cost, so no price move can cause one |
| `Firm Birth D1` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth D2` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth D3` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth D4` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth D5` | MISSING | VERIFICATION 7.2: a ceased party keeps its cash and its holdings for ever, and no estate is opened |
| `Firm Birth D6` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |
| `Firm Birth E1` | PARTIAL | packages/kernel-rs/src/running.rs `Failing` ceases any party whose equity is below zero, the central bank excepted by its profile (XI-3`s one exception, and for the reason XI-3 gives). VERIFICATION 7.2: ceasing sets a flag and nothing else |
| `Firm Birth E2` | MISSING | VERIFICATION 7.2, 4.1: nothing has a destination — not an asset, not a liability, not an employee, not a contract |
| `Firm Birth E3` | MET | nothing creates a firm, so births do not offset deaths by construction — the population only falls |
| `Firm Birth E4` | MISSING | VERIFICATION 12.1: `mortality` runs `running.rs Failing`, which ceases a party whose equity is negative; `mechanisms::mortality` is imported by nothing, so `Ceased`, `Destination`, `CentralBankLoss` and `can_cease` are unreachable (11.1). Nothing is born, nothing is funded, nothing is distressed and nothing is resolved |

## M&A

| requirement | status | where / why |
|---|---|---|
| `M&A A1` | PARTIAL | packages/kernel-rs/src/running.rs `Control` reads an acquirer`s own valuation against a target`s price and opens a process. No price per share is agreed and no owner accepts |
| `M&A A2` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A A3` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A A4` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A A5` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A B1` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A B2` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A B3` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A B4` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A B5` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals it. Nothing bids, nothing is accepted and nothing is refused, so a deal happens because a rule fired — which is what this FORBID names |
| `M&A C1` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A C2` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A C3` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A C4` | MISSING | VERIFICATION 3.2: with one book of 1,546 printing per period there is no cheap firm to attract a bid, and nothing bids |
| `M&A D1` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A D2` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A D3` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A D4` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A D5` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A E1` | MET | no acquisition happens at all, so none happens without payment |
| `M&A E2` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |
| `M&A E3` | MISSING | VERIFICATION 9.1: `Control` opens a takeover process and journals; it proposes no instruction, so no shares transfer, nobody is paid and no balance sheets combine. `mechanisms/control.rs Combined`, `Debt`, `Resistance`, `contested`, `leverage_after` and `payment_conserves` are unreached (11.1) |

## Trade Credit

| requirement | status | where / why |
|---|---|---|
| `Trade Credit A1` | PARTIAL | packages/kernel-rs/src/ledger.rs `Queue::given_time` holds the SAME debt open longer rather than writing a second one — receivable and payable are one row, which is what A1 asks. Nothing extends terms: `TradeCredit` journals |
| `Trade Credit A2` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit A3` | MISSING | `trade_credit.rs Terms`/`Discount`/`implied_rate` are unreached (VERIFICATION 11.1), so there is no discount for paying early and therefore no implicit rate |
| `Trade Credit A4` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit B1` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit B2` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit B3` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit B4` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit B5` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit C1` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit C2` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit C3` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit C4` | PARTIAL | the identity holds by construction — a queued payment is one obligation seen from two sides — and nothing checks it: the Flows family is NOT BUILT (VERIFICATION 1.4) |
| `Trade Credit D1` | PARTIAL | packages/kernel-rs/src/ledger.rs `Queue` makes lateness a real state with a day. Nothing reads it as a stress on the seller |
| `Trade Credit D2` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit D3` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit D3.a` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit D4` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit D5` | MISSING | VERIFICATION 9.1: `TradeCredit` journals and proposes nothing, and only 2 of the 11 items in mechanisms/trade_credit.rs are reached (11.1). No supplier decides to extend terms, none tightens them, and nothing runs along the supply network |
| `Trade Credit E1` | MET | packages/kernel-rs/src/ledger.rs `Queue` means a payment can be owed and not made, so nothing settles instantly by construction |
| `Trade Credit E2` | MET | packages/kernel-rs/src/ledger.rs `Queue::joins` asserts the payer is somebody |
| `Trade Credit E3` | MISSING | VERIFICATION 7.2: nothing resolves when a debtor ceases — a queued payment owed by a dead party stays queued |

## XI-11

| requirement | status | where / why |
|---|---|---|
| `XI-11` | MISSING | VERIFICATION 9.1: `Securitising` brings a note line and opens a process; it proposes no instruction, so no loan is transferred into a vehicle, no tranche is sold and no holder takes a loss. `securitisation.rs Took` and `capacity_freed` are unreached (11.1). Its two prerequisites are also absent: loans are not rows anybody writes (Banks Lending F1) and a loss is not an event (VERIFICATION 4.1) |

## Goods

| requirement | status | where / why |
|---|---|---|
| `Goods A1` | MET | packages/kernel-rs/src/instruments.rs `Class::Good` with its own `UnitId`, and packages/kernel-rs/src/registry.rs says what one of a unit is divided into |
| `Goods A2` | MET | packages/kernel-rs/src/mechanisms/recipe.rs `Recipe`/`Line` — fixed input quantities per unit of output, plus labour and capital services |
| `Goods A2.b` | MET | packages/kernel-rs/src/mechanisms/recipe.rs draws `per x units`, never money divided by a price, so a price doubling does not halve the draw |
| `Goods A3` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods A4` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods B1` | MET | packages/kernel-rs/src/running.rs `Making` decides from the firm`s own reasons — capacity off its plant vintages, its own outlook of what it sells, the hours its engagements give it and what they cost — and the quantity is what comes out |
| `Goods B1.d` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods B2` | MET | packages/kernel-rs/src/running.rs `Making` proposes the inputs the line drew as `Leg::Destroy` legs in the same pass (one of the ten propose sites) |
| `Goods B3` | MET | packages/kernel-rs/src/stores.rs `InProgress` holds work in progress with what it cost |
| `Goods B4` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods B5` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods C1` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods C2` | MET | packages/kernel-rs/src/session.rs and packages/kernel-rs/src/prices.rs (a goods book prints per (market, instrument, period) and the print is stored so the next period can re-mark against it) |
| `Goods C3` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods C4` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods C5` | MET | packages/kernel-rs/src/session.rs (what did not fill rests or lapses; unsold output stays on the seller`s row) |
| `Goods C6` | MISSING | VERIFICATION 8.2: the buyer pays out of its OWN account, not the seller`s money, and nothing is bought from anybody |
| `Goods D1` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods D2` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods D3` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods D4` | MISSING | packages/kernel-rs/src/mechanisms/goods.rs `Consignment::landed_cost` is ex-works plus freight plus duty and has no caller |
| `Goods D5` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods E1` | MET | packages/kernel-rs/src/register.rs (a holding is lots, each carrying what it cost) |
| `Goods E2` | MISSING | packages/kernel-rs/src/mechanisms/goods.rs `carry` implements lower of cost and net realisable value with the broker-dealer exception, exactly as E2/E2.b ask, and it has no caller (VERIFICATION 11.1): of 19 items in the module only `CostFlow`, `Lot`, `take` and `Perishing` are reached |
| `Goods E2.c` | MISSING | see E2 — the FORBID`s guard is `carry`, which nothing calls, and nothing marks inventory at all (VERIFICATION 6.1) |
| `Goods E3` | MISSING | see E2 — no holding loss is ever an event with a date and an income line |
| `Goods E4` | MET | packages/kernel-rs/src/mechanisms/goods.rs and packages/kernel-rs/src/running.rs (a `Leg::Destroy` with `Gone::Perished` removes units at the lot`s own cost and the wire records the disposal) |
| `Goods E5` | MET | packages/kernel-rs/src/mechanisms/goods.rs `CostFlow` offers first-in-first-out and weighted average and no third option, and `take` is what `running.rs` draws lots with |
| `Goods F1` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods F2` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods F3` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods F4` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods F5` | PARTIAL | packages/kernel-rs/src/mechanisms/goods.rs computes cost of goods sold over the lots that left. Nothing books it: no income statement is produced from it |
| `Goods F5.b` | MET | packages/kernel-rs/src/mechanisms/goods.rs (a batch`s cost is capitalised into the lot and is not also a period expense — one cost, one place) |
| `Goods G1` | MISSING | VERIFICATION 11.1: packages/kernel-rs/src/mechanisms/benchmarks.rs `Index`/`Weighing` are dead code; no producer or consumer price index is built |
| `Goods G1.c` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods G2` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods G3` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |
| `Goods G4` | MISSING | VERIFICATION 12.1: `goods` is wired as a participant (`GoodsSellers`) and the making is `running.rs Making` over `mechanisms::recipe`. What is missing is everything downstream of the line: no delivery, no carrier, no landed cost, no write-down to net realisable value, no income statement and no price index |

## Freight

| requirement | status | where / why |
|---|---|---|
| `Freight A1` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight A2` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight A3` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight A4` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight B1` | PARTIAL | packages/kernel-rs/src/systems.rs `LetsItsPlant` lets the USE of plant lines and reads `plant.upkeep`. It is not a route, a ship or a capacity |
| `Freight B2` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight B3` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight B4` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight C1` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight C2` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight C3` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight D1` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight D2` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight D3` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight D4` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight D5` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight D6` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight E1` | MISSING | VERIFICATION 12.1: nothing transports anything, so there is no transport to be instantaneous or costless — the FORBID holds by the system being absent |
| `Freight E2` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |
| `Freight E3` | MISSING | VERIFICATION 12.1, 11.1: `freight` is wired as a participant (`posts("freight", …, LetsItsPlant)`) that lets plant, and not one of the 12 items in mechanisms/freight.rs — `Route`, `Carrier`, `Shipment`, `Shipper`, `Booking`, `decides`, `demand_on`, `location_basis` — is reached. There are no routes, no capacity, no transit time and no location basis |

## Labour

| requirement | status | where / why |
|---|---|---|
| `Labour A1` | MET | `agreed::ENGAGEMENT` is hours of a named household`s time supplied to a named firm, with terms `[wage per person per period, hours per person per period, headcount]` |
| `Labour A2` | MET | see A1 — the wage is per unit of time, in the employer`s money |
| `Labour A3` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour A3.b` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour A4` | MET | `agreed::ENGAGEMENT` persists across periods and is a state rather than a per-period trade |
| `Labour B1` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour B2` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour B3` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour B4` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour B5` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour C1` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour C2` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour C3` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour C4` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour C5` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour D1` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour D2` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour D2.b` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour D3` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour D4` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour D5` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour E1` | MET | packages/kernel-rs/src/running.rs `Wages` proposes the payment: the week`s wages leave the employer`s account and reach the worker (one of the ten propose sites) |
| `Labour E2` | PARTIAL | packages/kernel-rs/src/running.rs `Making` reads the wage bill into the unit cost of a batch, so labour is a firm cost. Nothing turns it into a price |
| `Labour E3` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour E4` | MISSING | VERIFICATION 12.1: `employment` runs `running.rs Wages`, which pays a standing engagement; not one of the 11 items in mechanisms/employment.rs — `Posting`, `Match`, `matching`, `Separation`, `Ended`, `enters_at`, `moving`, `unemployment`, `Engagement`, `Engagements` — is reached (11.1). Nobody is hired, nobody is separated, no vacancy is posted, no wage clears and no person has a state |
| `Labour F1` | MET | packages/kernel-rs/src/running.rs `Wages` pays out of the named employer`s own account; there is no employment that is not an engagement between two named parties |
| `Labour F2` | MET | packages/kernel-rs/src/running.rs `Wages` asserts `heads > 0 && heads <= weight`, so a wage bill without headcount and a headcount above the workforce are both refused at the site |
| `Labour F3` | MET | no unemployment rate exists anywhere |

## Housing

| requirement | status | where / why |
|---|---|---|
| `Housing A1` | PARTIAL | packages/kernel-rs/src/instruments.rs `Class::Plant` with a unit, held by a named party, and packages/kernel-rs/src/places.rs puts it in a region. A dwelling is not counted in dwellings: it is a plant line |
| `Housing A2` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing A3` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing A4` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing A5` | MISSING | packages/kernel-rs/src/running.rs `Housing` reads `dwelling.upkeep` into `let _upkeep` and discards it. Nothing depreciates and nothing is maintained |
| `Housing B1` | PARTIAL | packages/kernel-rs/src/mechanisms/housing.rs `clearing` crosses bids against offers per place and `Housing` runs it. VERIFICATION 9.1: nothing settles — no dwelling moves in the register and no money is paid |
| `Housing B2` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing B3` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing B4` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing B4.a` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing B5` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing C1` | PARTIAL | packages/kernel-rs/src/running.rs `Housing` strikes an `agreed::MORTGAGE` naming a lender and a borrower. It carries `[price, deposit_share]`, no term, no rate, no amortisation and NO SCHEDULE — so nothing ever falls due on a mortgage |
| `Housing C2` | MISSING | see C1 — an `Agrees` carries terms and no `owing`, so no interest and no principal is ever payable |
| `Housing C3` | MISSING | `housing.rs loan_to_value` is the read and is unreached (VERIFICATION 11.1) |
| `Housing C4` | MISSING | `housing.rs foreclose`/`Foreclosed` are unreached. Nothing defaults, nothing is seized and no foreclosed supply returns to the market |
| `Housing C5` | PARTIAL | packages/kernel-rs/src/mechanisms/housing.rs `standard` is reached and `standing::LENDING_STANDARD` carries it, so the standard is a read of the lender`s own measurements rather than a constant. Nothing tightens it in response to anything |
| `Housing C6` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing D1` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing D2` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing D3` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing D4` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing D5` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing E1` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing E2` | MET | packages/kernel-rs/src/mechanisms/housing.rs `clearing` crosses real bids against real offers; no price path exists |
| `Housing E3` | MISSING | VERIFICATION 9.1: `Housing` clears a price per place and strikes mortgages and tenancies as agreements, and proposes no instruction — nothing is bought, nothing is paid and no dwelling moves. Of 16 items in mechanisms/housing.rs only `standard`, `can_bid`, `clearing`, `Bid`, `Offer` and `Standard` are reached (11.1) |
| `Housing E4` | MISSING | VERIFICATION 1.4: nothing reconciles mortgage debt against mortgage assets, and a mortgage is an agreement rather than a claim anybody holds |

## Households

| requirement | status | where / why |
|---|---|---|
| `Households A1` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households A2` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households A2.b` | MISSING | cohort is not a dimension of the cell key: packages/kernel-rs/src/parties.rs carries one `key` and packages/kernel-rs/src/registry.rs declares no cohort |
| `Households A2.d` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households A2.e` | MET | packages/kernel-rs/src/parties.rs `Representation::Cell` with an integer `weight`, a named party with its own account and register rows |
| `Households A2.f` | PARTIAL | packages/kernel-rs/src/systems.rs `HouseholdBuyers::orders` is evaluated per cell and never at a sector mean. What it evaluates is `last print x 1.2` for every cell alike (VERIFICATION 13.2) |
| `Households A2.g` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households A3` | MET | packages/kernel-rs/src/parties.rs and packages/kernel-rs/src/register.rs (a cell is a named party with an account) |
| `Households B1` | MET | packages/kernel-rs/src/running.rs `Wages` pays a named employer`s wages to a named household cell |
| `Households B2` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households B3` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households B3.a` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households B4` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households B5` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households C1` | PARTIAL | packages/kernel-rs/src/systems.rs `HouseholdBuyers` spends out of its own money and keeps `household.keeps` back. C1.a`s income, C1.b`s wealth and C1.c`s own expectation are not read (VERIFICATION 13.3) |
| `Households C2` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households C3` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households C4` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households C5` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households D1` | MET | packages/kernel-rs/src/register.rs (a cell holds a real book of rows like any party, and it is not a residual) |
| `Households D2` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households D3` | MET | packages/kernel-rs/src/instruments.rs `equity` is a read and nothing stores a net worth. VERIFICATION 6.1: it is a read at cost |
| `Households D4` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households D5` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households D6` | MET | packages/kernel-rs/src/systems.rs `HouseholdBuyers` posts its own bids; it is a participant and never a residual holder |
| `Households E1` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households E2` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households E3` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households E4` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households E5` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households F1` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households F1.a` | MISSING | packages/kernel-rs/src/mechanisms/households.rs `age_at_boundary` is the cohort split and is unreached (VERIFICATION 11.1). Nothing ages |
| `Households F1.b` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households F2` | MISSING | `households.rs Inherited` is unreached; nothing dissolves and nothing is inherited |
| `Households F2.a` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households F3` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |
| `Households F4` | MISSING | VERIFICATION 12.1: `households` is wired as a participant (`HouseholdBuyers`) that bids `last print x 1.2`, and not one of the 17 items in mechanisms/households.rs — `Spending`, `Received`, `Inherited`, `Where`, `saves`, `prefers`, `burden`, `age_at_boundary`, `sector_income`, `sector_consumption`, `weighted_mean` — is reached (11.1) |

## Small-Business Pools

| requirement | status | where / why |
|---|---|---|
| `Small-Business Pools A1` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools A2` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools A2.a` | PARTIAL | the sector is cells rather than one firm. VERIFICATION 13.2: every cell posts the same reservation, so a mean-preserving spread crosses nothing |
| `Small-Business Pools A3` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools A4` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools A5` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools A6` | MET | packages/kernel-rs/src/parties.rs `Representation::Cell` with an integer weight, and packages/kernel-rs/src/running.rs `SmallBusiness` reads them per cell |
| `Small-Business Pools A6.a` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools A6.b` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools A6.c` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools B1` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools B2` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools B3` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools B4` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools B4.a` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools C1` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools C2` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools C3` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools C4` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools C5` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools C6` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools D1` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools D2` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools D3` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools D4` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools D4.a` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools E1` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools E2` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools E3` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools E4` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |
| `Small-Business Pools E5` | MET | packages/kernel-rs/src/parties.rs `reweigh` takes a `WeightEvent` and there is no other writer of a weight, so it changes by a named event and by nothing else |
| `Small-Business Pools E6` | MISSING | VERIFICATION 9.1: `SmallBusiness` journals and proposes nothing, and of 14 items in mechanisms/small_business.rs only one is reached (11.1). No cell borrows from a named lender, none defaults, none is promoted and no pool is cut |

## Cross-Border

| requirement | status | where / why |
|---|---|---|
| `Cross-Border A1` | MET | packages/kernel-rs/src/parties.rs `region_of`, read by packages/kernel-rs/src/running.rs `CrossBorder` to decide a leg crossed |
| `Cross-Border A2` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border A2.a` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border A3` | MISSING | VERIFICATION 8.1: a payment does NOT reach an account in the currency it was made in — `across` lands it in the payee`s bank`s money whatever that money is |
| `Cross-Border A4` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border B1` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border B2` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border B3` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border B3.a` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border B4` | MISSING | VERIFICATION 1.4: nothing reconciles one region`s exports against another`s imports; the Flows family is NOT BUILT |
| `Cross-Border C1` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border C2` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border C3` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border C4` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border C5` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border D1` | PARTIAL | packages/kernel-rs/src/running.rs `CrossBorder` computes a region`s current account from the period`s own settled legs, never a stored field, and reads each leg`s `receipt` for which account it lands in. VERIFICATION 5.2: the currency it records is the leg`s unchecked `ccy` copy |
| `Cross-Border D2` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border D3` | PARTIAL | `CrossBorder` derives the imbalance and reports it with a size rather than asserting it away, which is what D3 asks. VERIFICATION 1.4: no family checks it |
| `Cross-Border D3.a` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border D4` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border D4.a` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border D5` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border D6` | MISSING | see B4 — nothing sums the regions |
| `Cross-Border E1` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border E2` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border E3` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border E4` | MISSING | VERIFICATION 8.4: the world has one currency and, per 12.1, `households`, `insurers`, `money_market`, `freight` and `dealing` are participants without their mechanisms — so there is no foreign asset, no foreign-currency borrower and no bank funding in one money and lending in another. `cross_border.rs Exposure`, `Finances`, `currency_gap`, `default_reaches`, `exports_to`, `financed_by` and `in_buyers_money` are unreached (11.1) |
| `Cross-Border F1` | MISSING | VERIFICATION 8.4: the assembled world has one currency, so nothing about a region being open can be exercised |
| `Cross-Border F2` | MET | `CrossBorder` reads leg by leg with both parties named; nothing is netted into a regional aggregate |
| `Cross-Border F3` | MET | no trade or capital-flow series exists anywhere |

## Ratings

| requirement | status | where / why |
|---|---|---|
| `Ratings A1` | PARTIAL | packages/kernel-rs/src/running.rs `Grading` writes a `standing::GRADE` per (house, name) and says it publicly. It is ordinal and published; VERIFICATION 15.3: an ungraded name is given one anyway |
| `Ratings A2` | PARTIAL | `Grading` reads published income and its trend, and leverage as what a name owes against what it holds. Age, size and sector are not read |
| `Ratings A2.a` | MET | `Grading` reads the accounts and never a print — no spread reaches it |
| `Ratings A3` | MISSING | VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts |
| `Ratings A4` | MISSING | VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts |
| `Ratings A5` | MET | packages/kernel-rs/src/running.rs `Grading` stands the grade behind a named house, so it is that house`s opinion and not a property of the firm |
| `Ratings B1` | MISSING | VERIFICATION 15.2: the probability of failing is written as a literal 0.0 |
| `Ratings B2` | MISSING | VERIFICATION 15.2: the loss given failure is written as a literal 0.0 |
| `Ratings B3` | MISSING | VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts |
| `Ratings C1` | PARTIAL | `agreed::MANDATE` carries the lowest grade a pool may hold and packages/kernel-rs/src/running.rs `ForcedSelling` reads a breach of it. VERIFICATION 9.1: it journals the breach and sells nothing |
| `Ratings C2` | PARTIAL | packages/kernel-rs/src/running.rs `BankCapital` weights an asset by `ratings::haircut(grade, 1.0)`, so a grade does consume capital. Nothing follows from the number (Banks Capital) |
| `Ratings C3` | PARTIAL | `ratings::haircut(grade, …)` is per issuer credit rather than one number per instrument type, which is what C3 asks for. Nothing pledges anything, so no haircut is ever applied to a borrowing |
| `Ratings C4` | MISSING | VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts |
| `Ratings C5` | MISSING | VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts |
| `Ratings D1` | MISSING | VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts |
| `Ratings D2` | MISSING | VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts |
| `Ratings D3` | MISSING | VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts |
| `Ratings D4` | MISSING | VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts |
| `Ratings D5` | MISSING | VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts |
| `Ratings E1` | PARTIAL | C2 and C3 give a grade two consumers inside `BankCapital`. Neither changes what anybody does |
| `Ratings E2` | MISSING | VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts |
| `Ratings E3` | MISSING | VERIFICATION 9.1: `Grading` writes grades and journals; nothing sells, nothing is downgraded into a funding loss, and the feedback loop §44 D asks for has no step that acts |
| `Ratings E4` | PARTIAL | the distribution is a read over the standings. VERIFICATION 15.3 makes every ungraded name part of it at a stated grade |

## Reporting

| requirement | status | where / why |
|---|---|---|
| `Reporting A1` | PARTIAL | packages/kernel-rs/src/running.rs `Publishes` publishes a result per firm on a calendar. A1.a`s "public is a state read from the register" is not read: nothing asks whether outsiders hold its shares |
| `Reporting A1.a` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting A2` | PARTIAL | `Publishes` reads the register and the journal. VERIFICATION 6.1: what it reads is valued at cost, and G2`s "the equity account`s movement over the fiscal period" has no equity account (Audit B5) |
| `Reporting A2.a` | MET | `Publishes` composes nothing: every figure is a read of the register and the journal |
| `Reporting A3` | MET | packages/kernel-rs/src/calendar.rs `plus_months` places a quarter by date, and its own test asserts a quarter is three months and not ninety-one days |
| `Reporting A4` | PARTIAL | `reporting.asymmetry` = 45 days is the lag. Nothing is known privately in between, because nothing is known at all until it is said |
| `Reporting A4.a` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting A5` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting B1` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting B2` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting B3` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting B4` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting C1` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting C2` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting C3` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting C4` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting C5` | MET | nothing is handed a forecast: `mechanisms/reporting.rs estimate` is unreached, so no estimate exists at all |
| `Reporting C6` | MET | no estimate reads a share price, because no estimate exists |
| `Reporting D1` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting D2` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting D3` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting D3.a` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting E1` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting E2` | MET | no consensus exists for a decision to consult |
| `Reporting E3` | MET | no consensus is stored, because none is computed |
| `Reporting F1` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting F2` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting F2.a` | MET | no price-reaction rule exists anywhere: nothing moves a price on a surprise, because no surprise is recorded (Expectations B2) |
| `Reporting F3` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting F4` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting G1` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting G2` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting G3` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting G4` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting G5` | MET | no per-share figure is declared anywhere among the 61 parameters |
| `Reporting G6` | MET | packages/kernel-rs/src/calendar.rs places the fiscal period by date, and a period is the finest structure |
| `Reporting H1` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting H2` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting H3` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |
| `Reporting H4` | MISSING | VERIFICATION 12.1: `reporting` runs `running.rs Publishes`, which publishes a result and stands it behind the firm. Only 2 of the 18 items in mechanisms/reporting.rs are reached (11.1): `Estimate`, `Guidance`, `estimate`, `covering` and `is_the_answer_with_an_offset` are not. No bank publishes an estimate, no guidance is given, no consensus is read and no surprise is settled |

## Observer

| requirement | status | where / why |
|---|---|---|
| `Observer A1` | PARTIAL | packages/kernel-rs/src/prices.rs `Print` carries its instrument, period, unit and provenance, so a stale mark is visibly stale (A1.a). Nothing displays one |
| `Observer A2` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer A3` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer A4` | PARTIAL | packages/kernel-rs/src/journal.rs `is_public`/`subjects_of` is the rule, and packages/kernel-rs/src/module.rs `ParticipantView` holds one party. VERIFICATION 4.2: `public_event`, the check that applies it, has no caller |
| `Observer A5` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer B1` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer B2` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer B2.a` | MET | no event moves a price anywhere: `ctx.say` writes to the journal and nothing reads a journal row into a price |
| `Observer B3` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer B4` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer B5` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer C1` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer C2` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer C2.a` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer C3` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer C4` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer D1` | MET | packages/kernel-rs/src/journal.rs is written by the mechanisms as they act and is never a second history |
| `Observer D2` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer D3` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer E1` | PARTIAL | nothing is displayed at all, so there is no display-only number. The observer module that would display is unreached |
| `Observer E2` | MET | no scripted sequence exists: every journal row is written by a mechanism from the state |
| `Observer E3` | MET | packages/kernel-rs/src/module.rs `ParticipantView` and `MechanismContext` hold `&`-references to every store, so reading cannot write |
| `Observer F1` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer F2` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |
| `Observer F3` | MET | packages/kernel-rs/src/calendar.rs is the one calendar and there is no second one |
| `Observer F4` | MISSING | VERIFICATION 12.1: `observer` runs `running.rs Observing`, which journals a count. Not one of the 16 items in mechanisms/observer.rs — `Happened`, `Statistic`, `Acts`, `DerivedFrom`, `visible_to`, `display_name`, `history_of`, `dated_by`, `is_true_of`, `performance` — is reached (11.1). There is no surface |

## Expectations

| requirement | status | where / why |
|---|---|---|
| `Expectations A1` | PARTIAL | packages/kernel-rs/src/stores.rs `Outlooks` holds a party`s own forecast. VERIFICATION 13.3: of the six `about::` kinds only two are ever formed, and `WHAT_IT_KEEPS_EARNING` is read by Housing and written by nobody |
| `Expectations A2` | MET | packages/kernel-rs/src/running.rs `Forming` forms each party`s outlook from the prints on the lines IT holds and what IT delivered, and from nothing else |
| `Expectations A2.b` | MET | packages/kernel-rs/src/stores.rs `Outlooks` is keyed by party; there is no global expectation anywhere |
| `Expectations A3` | MISSING | VERIFICATION 13.3: outlooks are formed correctly by `running.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing (12.1) |
| `Expectations A4` | MET | `Forming` corrects last period`s outlook toward what was observed. Nothing runs the world forward |
| `Expectations A5` | MISSING | packages/kernel-rs/src/stores.rs `Outlooks` carries a level and no unit or horizon; `about::` names the subject and not the periodicity |
| `Expectations B1` | PARTIAL | `Forming` is adaptive at the party`s own speed. VERIFICATION 13.3: `outlook.memory` = 0.3 is one number for every party, which B1.a says it must not be — it is never drawn and never dispersed |
| `Expectations B1.b` | MISSING | VERIFICATION 13.3: outlooks are formed correctly by `running.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing (12.1) |
| `Expectations B2` | PARTIAL | packages/kernel-rs/src/running.rs `Forming` corrects the outlook by the difference between observed and expected. The surprise is not recorded as an event: nothing writes it, so B2.a and D3 have nothing to read |
| `Expectations B2.a` | MISSING | VERIFICATION 13.3: outlooks are formed correctly by `running.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing (12.1) |
| `Expectations B3` | MISSING | confidence is nowhere: nothing reads the width of a party`s own recent surprises, because no surprise is recorded (B2) |
| `Expectations B4` | MET | `Forming` reads the last outlook and this period`s prints, never the period`s own result of the decision it feeds |
| `Expectations B5` | MISSING | VERIFICATION 13.3: outlooks are formed correctly by `running.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing (12.1) |
| `Expectations C1` | MISSING | VERIFICATION 13.3: `HouseholdBuyers` bids `last print x 1.2` and reads no outlook |
| `Expectations C2` | MET | packages/kernel-rs/src/running.rs `Making` reads the firm`s own `HOW_MUCH_IT_SELLS` for the production decision and `Building` reads it and `WHAT_IT_SELLS_FOR` for the investment decision — a firm with no view does not act on somebody else`s |
| `Expectations C3` | MISSING | see C1 — XI-13`s reservation formed from something other than the price is exactly what 13.2 shows is absent |
| `Expectations C4` | MISSING | see C1 |
| `Expectations C5` | MISSING | see C1 |
| `Expectations C6` | MISSING | see C1, and Polity: no cell votes |
| `Expectations D1` | MET | `Forming` reads only the party`s own rows and the public prints on them |
| `Expectations D2` | MET | `Forming` forms per party, cells included, and nothing averages across them |
| `Expectations D3` | MISSING | VERIFICATION 13.3: outlooks are formed correctly by `running.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing (12.1) |
| `Expectations D4` | MET | no sentiment index exists |
| `Expectations E1` | MISSING | a Part XII measurement; there is no dispersion to read while every reservation is the same constant (VERIFICATION 13.2) |
| `Expectations E2` | MISSING | VERIFICATION 13.3: outlooks are formed correctly by `running.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing (12.1) |
| `Expectations E3` | MISSING | VERIFICATION 13.3: outlooks are formed correctly by `running.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing (12.1) |
| `Expectations E4` | MISSING | VERIFICATION 13.3: outlooks are formed correctly by `running.rs Forming` and NOTHING READS THEM — `grep outlooks() systems.rs` is empty. `mechanisms::expectations` is imported by nothing (12.1) |
