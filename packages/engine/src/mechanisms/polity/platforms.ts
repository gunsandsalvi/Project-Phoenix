/**
 * The parties of this world — DATA, and it lives in the registry.
 *
 * @spec Polity A2 Law 15
 *
 * A table of stated numbers is registry data, not mechanism: the rule that a mechanism carries no
 * numeric literal is exactly the rule that puts this table where it is, and the check said so the
 * moment the rows were written here (19.3). What is left in the module is the name.
 */
export { PLATFORMS } from '../../registry/platforms.js';
